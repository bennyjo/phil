# DEEP RETRO — 2026-09-21

Window: DEEP-2026-09-20 (1ea482e, 2026-09-20 04:47Z) → fb326b5
(2026-09-21 04:18Z). Sync clean: clone was shallow with a detached HEAD;
`git fetch --unshallow` completed, the "forced update" fetch line was the
usual shallow-boundary artifact, local main was strictly behind
origin/main (f772af1 → fb326b5, ancestry verified with
`merge-base --is-ancestor` before touching anything) and was
fast-forwarded. No manufactured divergence. `core/resolve.py` this run:
settled 0 of 39 ledger bets (8 still open) and **6 forecasts** — this
retro is those six rows' settlement retro, so the veto-table same-commit
duty (DEEP-2026-08-23) falls to this commit and is discharged in it.

The headline the day was supposed to produce did not arrive: the four
pre-registered election ledger rows (German set `23e40bbbccc5` CDU 11–14%
+ `7ec71e307f12` Berlin Linke; Russia set `9074e3f2fd49` UR-sweep +
`475edf2e2654` turnout ≥62%) are ALL still open — the elections happened
Sep 20 but the Polymarket markets have not officially resolved (resolve.py
settles on official resolutions only). Every pre-registered decision gated
on those settlements (joint event-night gradings, empirical-sd fit,
real-twin allowed-classes decision, fold_brier_delta conversion) therefore
**carries, undischarged, to the first deep retro after they settle**. What
DID settle is graded in full below, and one of the six rows is the most
informative veto counterfactual of the month.

## (a) Calibration and P&L

**Settled-bet book: unchanged.** No ledger settlements this window:
lifetime n=39, 13W/26L, −$7.02, brier_delta +0.1078, luck-adjusted z
−4.38 (the documented early-book long-shot bias; static). Cell verdicts
carry forward: politics-general bets n=4 +$57.62 delta −0.0502 (thin,
deciding rows still open), unclassified n=18 −$46.89 delta +0.126 (the
early book), news bets 0/6 −$30.00 delta +0.4179 (category-barred since).

**Six forecasts settled this run** (own-vs-market per-row Brier, negative
= beat the mid):

| row | market | est / mkt | result | dB |
|---|---|---|---|---|
| `506bc4c8087e` | AfD most seats MV (**outside-view-veto**) | 0.90 / 0.77 | WON | **−0.0429** |
| `b11a0fb518df` | AfD most seats MV (no-edge, later snapshot) | 0.83 / 0.795 | WON | −0.0131 |
| `b00b3a7aa43f` | AfD most seats Berlin (market-agrees) | 0.10 / 0.124 | lost (No) | −0.0054 |
| `9b8b7c8f2b87` | SPD most seats MV (market-agrees) | 0.18 / 0.165 | lost (No) | +0.0052 |
| `5cec1c574dbf` | SPD most seats MV (no-edge, later snapshot) | 0.24 / 0.22 | lost (No) | +0.0092 |
| `be7a36b2fa55` | Colts vs. Chiefs, Chiefs ML (no-edge) | 0.739 / 0.735 | WON | −0.0021 |

Sum dB −0.0491, and −0.0062 excluding the veto row: the skip machinery
tracked the market inside noise again, and the one row where the model
truly disagreed with the market — AfD-MV at a claimed 0.13 edge — the
model was RIGHT. Grading and its (non-)implications in (c) and the
playbook edits below.

**Stream-level** (score.py filter): settled=726, delta +0.0072, z −0.25 —
flat, as designed. Negative cells at n≥8 unchanged in identity:
mlb-moneyline −0.0499 (n=15), commodities-touch −0.0498 (n=10),
crypto-touch −0.0250 (n=9), commodities −0.0178 (n=10), box-office
−0.0151 (n=12), mlb-totals −0.0110 (n=32), soccer −0.0051 (n=97).
Worst: ai-model-release +0.0806 (n=31), market-microstructure +0.0750
(n=9), weather +0.0529 (n=33), news +0.0364 (n=20 in-filter). No cell
moved enough this window to change any allocation; small-n caveats all
stand. politics-general forecasts now +0.0072 (n=22) — the six new rows
pulled it toward zero from +0.0134.

## (b) Audit of hourly-agent strategy edits since 1ea482e

20 commits, all cycle/retro traffic. Files touched: `schedule.json`
(pacing + watch hygiene), `funnel.jsonl` (append-only telemetry),
`watchlist.json` (one substantive edit). Zero edits to playbook.md,
risk.json, discovery.py, tools/ — correct restraint for a window where
nothing bet-sized settled. **Zero reverts needed.** Verdicts:

1. **schedule.json maintenance — KEEP.** Watch items carried intact
   (all four election rows, WTI ladder ~Oct 1, RBA, PCE, Canada GDP);
   reasons dated, quoting resolve.py, following the DEEP-2026-09-15
   current-SET-only hygiene.
2. **Pacing — KEEP.** Trailing 24h: 8 FULL, 6 TRIGGERED, rest LIGHT,
   vs. the 4-FULL floor. Double the floor on election weekend is the
   right direction to err, and the FULL cycles did real work (election
   watch checks, Xi-visit re-quotes, Opus-release re-verification).
   Deferral was not used to dodge research this window — the opposite:
   the empty-calendar days are behind us and the agent throttled UP.
3. **watchlist.json PCE move_threshold 0.05 → 0.10 (ec53056, 02:42Z) —
   KEEP.** Exemplary edit: three dated fires on the same key in three
   days (Sep18 +0.070, Sep20 −0.050, Sep21 −0.065), each researched to
   no-catalyst, spread on the token measured at 0.01–0.10 on ~$660
   liquidity — the threshold sat inside bid/ask wobble. One caveat
   recorded here so the next reader has it: the three fires were all
   *drift toward Yes* on a book where we hold No (`8894592b953a`
   @0.68, mid now ~0.73). Three researched no-catalyst fires is
   evidence of noise, but if the drift continues without news, the
   Sep 30 BEA calendar entry (still armed) is the backstop, not the
   price trigger. Nothing to change today.
4. **Both non-election TRIGGERED fires legitimate** (pricemove on held
   positions; newmarket on the Saint-Tropez tennis qualifier, logged
   forecast-only under `unvalidated-method` — no odds feed, correctly
   not bet).

**Sensing audit:** discovery.py untouched; pool_by_query stable
(pool_total 755–810 across the window's funnel rows; liquid-multiday
101–116, by-liquidity 203–242, active-today 312–392, econ-tag 87–89).
No new query-artifact shaping. The >336h econ-horizon blind spot remains
the known operator-owned gap (lane-coverage memo; calendar-tier bets
since: still 0). The 04:18Z cycle collected 296/300 with 4 malformed
screener returns and one slow batch (~93s vs 30–60s) — noted, not yet a
pattern (first malformed-batch instance since 09-16; watch, don't act).

**Funnel-weld miss #2 (concrete, dated):** the 2026-09-21 04:18Z FULL
cycle (commit fb326b5) ran the full funnel per its own cycles.log line
(scan 810, screened 300/15 batches, escalated 15) and wrote **no
`strategy/funnel.jsonl` row** — the file's last row is the 02:12Z cycle;
fb326b5 touches only schedule.json. Identical shape to the first miss
(2026-09-19 14:19Z, commit 690f339, flagged DEEP-2026-09-20). The
funnel-completeness series is now 7-of-8 this window, and the CI
funnel-weld proposal has its second instance in three days. Escalated
again in (e); this is now a trend, not an anecdote.

## (c) Biggest estimation errors of the window

1. **AfD-MV outside-view-veto row (`506bc4c8087e`) — not an estimation
   error; the estimate WON. Graded fully because it is the strongest
   single test of the veto this month.** Est 0.90 vs market 0.77 eleven
   days pre-election, claimed edge 0.13, vetoed as a >0.10 judgment
   disagreement. AfD won MV; the counterfactual $5 Yes @0.78 pays
   +$1.41 (+0.28u), and the row beat the market by dB −0.0429. The
   inside view (state-level AfD polling lead ~10pts, structural east
   German pattern) was right and the market drifted to it (0.77 → 0.80
   by Sep 16). **Ruling: the veto stays, and not by inertia — by the
   pre-registered fork arithmetic**, recomputed today with this row
   included (playbook §relaxation-fork, Status 2026-09-21): the slice's
   two most recent walk-forward folds have dBrier +0.0323 / +0.0798
   (need both negative) and fold-4 CF pnl is **−$69.40** (need
   positive) — the first reading where BOTH gates fail. The newest
   fifth of the veto slice is losing counterfactual money with
   worse-than-market beliefs; one correct 0.13-edge election call does
   not outweigh that, and the fork exists precisely so this row's
   emotional pull ("we knew!") cannot loosen the rule post hoc.
2. **SPD-MV pair (`9b8b7c8f2b87` 0.18, `5cec1c574dbf` 0.24 vs mkt
   0.165/0.22): +0.005/+0.009 dB — variance.** Both snapshots sat
   within 0.02 of the mid; SPD losing was the ~78–82% case both were
   pricing. No lesson at this size.
3. **No benchmark errors.** The Chiefs ML row's 5-book power-devig fair
   (0.739) landed 0.004 off the close; the German most-seats rows had
   no odds-feed benchmark and correctly leaned on the market plus
   polls. Nothing settled this window where the benchmark itself was
   the mistake.

## (d) Discipline

Zero bets placed → min_edge, spread, in-play, sizing, correlation rules
not exercisable; none violated. Cash $952.98 stable across all window
cycles; 8 open positions, Russia pair at exactly the $10 event cap.
Ledgers written only by core. The veto-table same-commit duty triggered
by this run's own settlements is discharged in this commit (playbook
table row + mechanical line). Standing infrastructure notes, each with
one more dated instance this window: every cloud cycle ran
lease-acquired-but-unwritable ("push refused, proceeded unprotected") —
no collision occurred; the funnel-weld miss above.

**Real book:** `journal/real-ledger.jsonl` unchanged at 56 rows, all
settle sweeps, zero fills ever. The allowed-classes decision that was
dated post-Sep-20 needed tonight's politics settlements as its input;
those markets are unresolved, so the decision **carries with its
trigger intact** (first deep retro after the four election rows
settle), not silently dropped. For the record the starvation mechanism
is unchanged: `real.allowed_edge_classes=["cross-market"]` while the
settled book is 21 other / 18 unclassified / 5 info-race / 2 book-devig
/ 1 cross-market — the one allowed class is 2.6% of paper flow.

**Mech:** zero requests this window (last row 2026-09-18; all window
cycles cloud, no signer). Window counters (reset 2026-09-14) unchanged:
requests 14, deliveries 13, market-aware with context 9 (seen == sent
on all 9), paired ma/v4 sets 4, settled pairs 0.

## (e) Proposals — status pass

Written to journal/proposals.md as the 2026-09-21 pass; summary:

- **CI funnel-weld check (2026-09-14): ENDORSED, RE-URGED — second
  concrete miss** (fb326b5, 04:18Z today; first was 690f339, Sep 19).
  Two silent funnel drops in three days is a failure rate the
  completeness series can measure: 2 of the last 13 FULL cycles. The
  check is one line of CI: cycles.log gains a "FULL cycle" line ⇒ the
  same commit touches funnel.jsonl.
- **fold_brier_delta in counterfactual.py: final notice CARRIES
  (trigger not yet fired).** The German-bracket veto cluster it is
  dated against has not settled. Today's fork recompute did the
  by-hand fold arithmetic regardless (Status 2026-09-21 in the
  playbook) — the hand recipe works, which is exactly why the ask
  converts to informational once the cluster settles, per the standing
  commitment. Not converted early.
- **Real-twin starvation decision: CARRIES with trigger intact** (see
  (d); input = the four open election rows' official settlements).
- **watch.py banned-shape regexes: ENDORSED option (a), open.** Zero
  banned-shape fires this window; six legitimate fires; the PCE
  threshold raise (b)(3) reduces the noisiest key's fire rate the
  right way — by evidence, not by regex.
- **Mech nonce race (2026-09-17): ENDORSED, unchanged, with operator**
  (no mech traffic this window).
- **Screener quota refund (2026-09-15): ENDORSED, unchanged, with
  operator** (no failed-tier instance; one slow-batch + 4-malformed
  instance today, logged in (b), below the proposal's bar).
- **ODDS_API_KEY on operator runner: open, unchanged** (mlb-moneyline
  still best negative forecast cell, −0.0499 n=15).
- **Lease writability: open**, every cloud cycle this window
  unprotected as usual; no collision.
- **Sep-20 election pre-registrations (joint gradings, empirical-sd
  fit): STANDING, waiting on official resolution** — reaffirmed
  verbatim so no later cycle re-litigates them: the four rows grade as
  TWO event-night decisions; the sd fit runs on all settled vote-share
  rows in the retro that settles the German brackets.

## Strategy edits this retro (step 4)

1. **playbook.md — counterfactual ledger append** (same-commit duty for
   `506bc4c8087e`): table row (+0.28u), current mechanical line (145
   rows / 137 CF trades / 58W-79L / +$66.07 / dBrier +0.0361 / held-out
   +$70.08), three-sentence ruling. Evidence: this run's resolve.py
   settlement.
2. **playbook.md — relaxation fork Status 2026-09-21**: first
   double-gate failure (fold dBrier f3 +0.0323 / f4 +0.0798 both
   positive; fold-4 CF pnl −$69.40 negative; evts 101 ≥ 40 after a
   fresh `screen_replay.py events` run). Fork stays shut with its
   strongest reading yet; recorded so the AfD-MV win cannot be cited
   later against a stale status. Evidence: mechanical ledger +
   fold recipe over the 145-row slice.
3. **No risk.json edit** (nothing bet-sized settled), **no
   discovery.py edit** (pool stable, no query artifact), **no
   schedule.json override** (hourly pacing was correct; all
   pre-registrations already carried by watch items).

## Verdict

The window's one real disagreement with the market — AfD-MV at a claimed
0.13 edge — settled in the model's favor, and the pre-registered fork
arithmetic, recomputed honestly with that win included, still says the
veto stays: the newest fold of the veto slice is now losing money AND
miscalibrated, the first double-gate failure on record. Everything the
day was supposed to decide (joint election gradings, sd fit, real-twin
classes, fold-delta conversion) waits on official resolutions that
haven't landed; all carried with triggers intact rather than
half-graded early. The hourly agent needed zero reverts for the second
day running; the one systemic defect is the funnel weld, now missing
2 of its last 13 FULL-cycle rows.
