# RETRO-20260817-1813

LIGHT tick same-tick retro per schedule.json carrier rule (a settlement on a
light tick is graded immediately, never deferred).

## Settled: 2 of 3 legs of the Musk Aug15-17 2-day set (watch item)

- `2116100c3194` (40-64 tweets, category-bar skip): est_prob(Yes)=0.749 vs
  market ask 0.66 (mkt_prob 0.65), Yes-side edge 0.089, sub-0.10 so
  category-bar per DEEP-2026-08-15 taxonomy — no bet. Outcome: **No**.
- `4a6af22c4e9f` (65-89 tweets, category-bar skip, "book's favored
  bracket"): est_prob(Yes)=0.237 vs market ask 0.15 (mkt_prob 0.145),
  Yes-side edge 0.087, also sub-threshold — no bet. Outcome: **No**.

Both declines were correct (neither bracket hit). score.py's
`[category-bar]` skip-reason bucket now reads n=2 brier_delta=+0.0868 —
positive means the *market* beat the model on both: model's Yes-prob sat
meaningfully above the market's on both mid brackets (0.749 vs 0.65, 0.237
vs 0.15) while the true outcome was No on both, so the market's lower
prices were closer to truth. The bootstrap self-model is overweighting the
middle of its own distribution relative to what the book already priced in
for this off-fork 2-day instance — consistent direction on both legs, not
just noise on one.

**Not added to the outside-view-veto counterfactual ledger** (playbook
§"Outside-view veto: settled counterfactual ledger") — per the watch item's
own instruction, category-bar rows are excluded from that ledger; it tracks
only `outside-view-veto` skips.

## Still open: the veto leg (`8f663df4a762`, <40 tweets)

`resolve.py` settled only these two rows this tick; the third leg of the
set — <40 tweets, `outside-view-veto`, est_prob(Yes)=0.0146 vs market ask
0.20 (No-side edge 0.175) — has not resolved yet (resolution lag, same
pattern as several other watch items this window). Since both the 40-64
and 65-89 brackets lost, the actual tweet count landed either below 40 or
at/above 90. If it lands below 40, that's the outside-view-veto model
being very wrong on its own <40 estimate (1.46% vs an actual Yes) on the
row that actually matters for the veto record — worth flagging when it
settles. Updated the watch item in `schedule.json` to track only this
remaining leg.

**Verdict**: no playbook or risk.json edit — the category-bar decline
mechanism worked as intended (both declined, both correctly), and the two
settled rows are explicitly out of scope for the veto ledger by the
watch item's own rule. The mid-bracket overconfidence pattern (n=2) is too
small to act on alone; if it recurs on the still-open Aug18 weekly Musk
set (the veto's actual pre-registered fork), that's the point to revisit
the bootstrap model's variance.

No open ledger positions this tick (0/21 open) — nothing to monitor.
