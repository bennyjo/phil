# DEEP RETRO — 2026-08-26 (run ~04:4xZ)

Window since DEEP-2026-08-25 (~05:2xZ): **2 bets placed (first since Aug 24),
0 ledger settlements, 12 forecasts settled** (3 by this session's own resolve
run). The window's story: the event-trigger watcher fired four times on the
same defective shape (in-game MLB totals listed mid-game, unfillable books)
and the hourly agent declined all of them correctly while escalating the
pattern with model evidence; the screener's collect-failure class looks
closed after the operator's unwrap fix (300/300, 299/300, 300/300, 300/300 on
the window's four full runs); and the 04:17Z cycle converted a screener flag
into the cleanest structural bet this book has ever placed (a game already
Final, still priced 86.6% the wrong way). One repeated process slip
(pacing pointer, now welded into reconcile.py as check 4).

Sync note: session started shallow with the usual manufactured "forced
update" fetch line; unshallowed per the trigger guard, merge-base clean at
949df4d, local main strictly behind (0/107), plain fast-forward — no genuine
divergence. resolve.py this session: 0/21 ledger settled (3 open),
3 forecasts settled (WNBA Mystics/Mercury complementary pair — 1W/1L by
construction, est 0.57 at mkt 0.57, clean market-agrees rows; PIT@SD O/U 4.5
pace-model row LOST, see (c)).

## (a) Calibration and P&L

**Bets: n=21 settled, unchanged — 7W, −$45.48, ROI −0.433, z −3.08.**
Nothing settled this window; the settled-bet tables carry no new
information. By edge class still: book-devig the only settled class with
negative (good) brier_delta at n=1 — insufficient data, no floor changes.
Forecast-side threshold sweeps remain non-positive in every bucket
(edge≥0.02 n=27 ROI −0.025 → edge≥0.07 n=14 ROI −0.356, monotonically
worse with threshold) — the standing read holds: no evidence any min_edge
loosening would have printed money, plenty that tightening selection
matters more than tuning thresholds.

**Two new open positions, both audited bottom-up this session:**

1. **5fbf676cfd7f — $5 Arizona Diamondbacks ML @ 0.138 (04:27Z).**
   Independently re-verified here: MLB Stats API gamePk 825042 shows
   `Final, Chicago Cubs 4 @ Arizona Diamondbacks 5`; gamma market 3720883
   slug `mlb-chc-ari-2026-08-25`, gameStartTime 2026-08-26T01:40Z — exact
   same game, no date/slot ambiguity — and the book STILL priced Cubs
   0.866 at bet time and mid 0.134 twenty minutes later. This is a pure
   fact-final stale-book capture: the resolving fact is immutable, the
   only residual risk is resolver deviation from an unambiguous official
   result. Expected +$31.23 on resolution (market endDate Sep 2 at the
   latest). The placing cycle's verification discipline (cross-checked the
   sibling game, explained the conflicting Fox Sports score as a stale
   pre-9th cache) was exactly right. **KEEP, commendation.**
2. **c6f16acc55d9 — $5 GTA VI trailer-by-Aug-31 Yes @ 0.36 (21:26Z).**
   Multi-outlet (Variety/GameSpot/VideoCardz/Vice) reports of an Aug 27
   premiere, est 0.90, edge 0.54. Legitimate under the veto's own
   carve-out and the min_edge floors, and the research was real. But this
   session's re-read of the market surfaces two things the funnel note
   didn't weigh: the book is **liquid ($23.6k), not thin**, and it has NOT
   converged a day after the reports — an attentive market at 0.365
   against a "publicly scheduled" fact is pricing something; and the
   resolution terms require a video "explicitly marketed as a trailer"
   (teasers/gameplay showcases excluded), which is a real labeling risk
   for an event-premiere clip. est 0.90 may underweight both. Not a
   violation — grading pre-registered in schedule.json: if this loses,
   it is the liquid-book analog of b21e42c123a1 (market pricing
   resolution-criteria risk, not inattention). **KEEP the bet, flag the
   estimate.**

**Open-position MTM (pre-registered duty):** AfD Sachsen-Anhalt No
(de95e5168de3, entry 0.66): mid 0.675, unrealized **+$0.11** — first
positive checkpoint. Trend −0.04 → −0.23 → −0.30 → +0.11; Yes retreated
0.38 → 0.325, back toward the politpro 0.29 estimate. Inside noise, no
action; watchlist trigger continues to cover the reaction path.

**Forecasts: n=275 settled, delta +0.0028, z +0.06 — flat, as all month.**
Calibration buckets are clean at both tails (0.7–0.8: n=14 realized 0.93;
0.9–1.0: n=7 realized 1.00; 0.0–0.1: n=25 realized 0.08). The 12
settlements this window: 4 architecture-mismatch in-game rows (2W on the
model side, TEX@CWS 20.5 and PIT@SD 4.5 LOST — see (c)), the Valencia/
Betis and Mystics/Mercury market-agrees sets (all landed at-market), the
Oklahoma Superintendent category-bar row (market 0.86 beat est 0.62 —
politics-primary bar reconfirmed, class delta +0.1232), and the Kazakh
Auyl leg (WON at 0.82 — the foreign-general-election exception is now
graded n=1 clean, playbook already updated by the 06:37Z retro).
Category table: nothing crossed a decision threshold this window;
social-media-postcount (n=18, +0.0407) and politics-primary (n=6,
+0.1232) stay under their bars; no bar has contrary evidence.

## (b) Audit of the hourly agent's edits

Strategy diffs since f601ac9, verdicts:

1. **playbook.md Kazakh settlement update (06:37Z retro) — KEEP.**
   Accurate account; the exception clause's n=1 validation is recorded
   against the still-standing ruled-out gate, correctly not loosening it.
2. **playbook.md weather-bracket exploration entry (22:26Z) — KEEP,
   model discipline commendation.** New category surfaced by the
   screener, benchmark reachability established (open-meteo), three
   sibling-validated bucket sets tested, all three DECLINED because the
   self-model (assumed sd=1.2C) is unvalidated and two of three showed
   exactly the too-good-first-contact shape that burned Musk/UMich/
   box-office. Settles within ~36h on NOAA obs; the "grade before
   touching the category again" gate is pre-registered. This is the
   Musk-brackets lesson being applied unprompted, which is what the
   playbook is for.
3. **watchlist.json ISM calendar entry (01:22Z) — KEEP.** Release time
   verified correct (Sep 1 14:00Z = 10am ET); forecast 700dfea982ec
   linked.
4. **schedule.json watch-item maintenance (multiple cycles) — KEEP.**
   BoI structural note (Aug31 pair may share the Sep 1 decision with the
   September pair — count the 4-market cluster as ONE outcome) is a
   genuinely good correlation catch; NFP fit-score-5 entry properly
   pre-registers the std-70-vs-90 grading question.
5. **TRIGGERED-cycle judgment (01:29Z, 01:57Z, 02:24Z, 02:41Z) — KEEP,
   commendation.** Four new_market fires, all in-game MLB totals with
   one-sided near-1.0-ask books; all four correctly declined
   (architecture-mismatch), calibration forecasts recorded, and the
   pattern escalated to proposals.md with per-instance evidence
   (liquidityNum underflow vs one-sided book — correctly distinguished)
   rather than four disconnected symptom reports. The 02:41Z entry
   honestly marking itself "running tally, changes no conclusion" is the
   right instinct against proposal inflation.
6. **Funnel/screener instrumentation — CLEAN.** All FULL lines carry
   pool fields and screener fields; collected/expected reported every
   run (300/300, 299/300 with the malformed one noted, 300/300,
   300/300); reconcile passes over the window with zero gaps. The
   census-consistent taxonomy from DEEP-2026-08-25 was used correctly
   on the 03:14Z Fenerbahce census row.
7. **Pacing pointer — the window's one repeated slip, now WELDED.**
   `next_full_cycle_after` was last advanced by the 17:21Z FULL (to
   20:15Z); the six subsequent FULLs (22:16Z → 04:17Z) all ran past a
   stale pointer and none advanced it or refreshed the reason field.
   DEEP-2026-08-25 flagged exactly this as first-instance/prose-only and
   pre-committed to a weld on repeat; it repeated six times within 12
   hours. Fail-safe direction again (extra FULLs, not skipped work — and
   the 04:17Z FULL that the staleness produced is the one that found the
   Cubs/ARI bet, so no outcome harm), but the contract is now mechanical:
   **reconcile.py check 4** fails when the newest FULL funnel line is
   >65 minutes past `next_full_cycle_after` — running FULL every tick
   stays legal, but only by consciously touching the pointer each time.
   Pointer repaired to 07:45Z (3h cap; no event boundary before it — PCE
   12:30Z arrives via the calendar trigger regardless).

**Sensing (discovery.py unedited; pools 908–1051, 4/4 queries every FULL).**
The stratified screener + random tail is now the standing answer to "is
the pool an artifact of the queries" — and this window produced the first
hard proof the audit lane pays: the Cubs/ARI bet entered through a
screener divergence flag (0.166) on a market the four queries had no
special claim on, and the weather category entered the same way. Two new
sensing observations worth keeping: (i) the screener also surfaced two
markets the banned-patterns config should have excluded (daily crypto
Up/Down — proposal open, endorsed below); (ii) the new_market trigger's
in-game-listing defect is a sensing-quality issue at the operator layer,
fully escalated. No category went structurally dark this window.

**Pacing (schedule.json).** 10 FULLs + 4 TRIGGERED in the trailing 24h vs
min 4 — if anything the agent over-ran rather than deferred (the
overnight hourly-FULL stretch was pointer-staleness fallout, see item 7,
though the 04:17Z FULL retroactively justified it). No deferral-to-avoid-
research pattern; LIGHT ticks all carried machine-counted justifications.

## (c) Biggest estimation errors of the day

1. **PIT@SD O/U 4.5 pace-model row (ff58cfd7bef3): est 0.59 Over, settled
   Under — reasoning wrong in a knowable way, and cheap.** With the
   TEX@CWS 20.5 row (est 0.60 Over, LOST) the quick in-game Poisson
   pace-extrapolations are now 2W/2L on the model side across the four
   triggered instances — i.e. exactly coin-flip, which is what a
   minutes-stale extrapolation against a live book should produce. The
   architecture-mismatch veto didn't just dodge unfillable books; the
   model it dodged with has no demonstrated skill either. Standing
   conclusion sharpened: on in-game fires, record the calibration row if
   cheap, but stop treating the pace model's est as information — it is
   a benchmark for the veto's counterfactual, nothing more.
2. **GTA VI est 0.90 (open, pre-registered rather than settled):** the
   window's likeliest candidate for a real estimation error — a liquid
   attentive book at 0.365 was treated with a thin-book "market hasn't
   noticed" prior, and the "explicitly marketed as a trailer" resolution
   clause was not priced. Graded at settlement per the schedule.json
   pre-registration; no conclusion until then.
3. Oklahoma Superintendent (est 0.62 vs mkt 0.86, market right): not an
   error to fix — the category-bar already excludes betting this shape,
   and the row is one more datum for why (no runoff polling existed to
   anchor on; the market knew the first-round-lead transfer pattern).

## (d) Discipline violations

Betting: **none.** Both bets above min_edge with the correct floors
(0.04 structural; the 0.30 wide-book floor not needed — Cubs/ARI spread
0.008, GTA book tight at entry); no in-play bets (four in-play candidates
all declined); no correlated exposure (three open positions, unrelated
events); no spread breaches; odds/screener quotas respected; the ledger
carries required edge_class on both rows.

Process: **one, repeated — the pacing pointer (item (b)7), now welded as
reconcile check 4.** Everything else reconciles clean over the window,
including the funnel↔forecast coverage on all 15 lines.

## (e) Proposals (journal/proposals.md updated this commit)

- **Screener output-format (2026-08-25, endorsed):** → **actioned** —
  operator shipped the tolerant unwrap (operator-notes 2026-08-25
  ~06:35Z); four subsequent runs collected 1199/1200. Closed.
- **banned_question_patterns daily crypto Up/Down (2026-08-25):** →
  **endorsed.** Verified: both regexes in config target the hourly-candle
  phrasing only; 3809906/3809907 ("Bitcoin/Ethereum Up or Down on August
  25?") pass `keep()` and reached the screener. Recommend the
  ticker-scoped variant (`"Up or Down on [A-Z][a-z]+ [0-9]"` unanchored
  is safer than the broad one; either works) — same single-token-
  resolution risk class the existing patterns exclude.
- **new_market in-game fires (four 2026-08-26 entries):** → **endorsed as
  one consolidated pattern** (statuses set on all four). Four fires, two
  distinct symptoms (liquidityNum underflow n=1; post-listing one-sided
  unfillable books n=4), one shared root cause: gamma listing time ≠
  research-opportunity time for in-game sports totals. The evidence is
  complete — 3894923 was mechanically decided BEFORE the fire. Endorsed
  ask, cheapest-first: (1) suppress or tag `new_market` fires whose gamma
  record carries a `gameStartTime` in the past (one field the watcher
  already fetches — no extra call); (2) optionally the book-depth
  liquidity source from the first entry. Until actioned, the agent-side
  cost is bounded: each fire burns one narrow gamma fetch + a decline,
  and the 6-fires/day cap limits the bleed.
- **No new operator asks from this retro.** The pacing weld is
  agent-side; the edge-class split below is agent-side taxonomy.

## (f) Actions applied today

1. **strategy/playbook.md** — ledger edge-class split: `fact-final`
   (resolving fact already immutable at bet time) separated from
   `info-race` (pre-final: scheduled events, running counts). Evidence:
   the class's own 2W/4L record splits exactly on fact-finality; the
   operator's 2026-08-05 architectural ruling names fact-final reads as
   the architecture's fit while excluding speed races; 5fbf676cfd7f is
   the clean prototype. The two open rows are ledger-frozen as
   `info-race` (only core writes the ledger) — retros grade 5fbf676cfd7f
   under fact-final manually; future bets use the split at placement.
   If fact-final reaches n≥5 settled with the predicted record, a
   real-eligibility proposal is warranted; not before.
2. **strategy/tools/reconcile.py** — check 4 added (pacing-pointer
   freshness, per (b)7). Verified failing on the pre-repair state and
   passing post-repair.
3. **strategy/schedule.json** — pointer repaired 20:15Z→07:45Z with the
   repeat documented in `reason`; AfD MTM checkpoint appended (+$0.11,
   first positive, no action); new watch item pre-registering the
   Cubs/ARI + GTA settlements and the GTA liquid-book grading fork.
4. **strategy/watchlist.json** — price_moves entry added for GTA VI Yes
   (threshold 0.10/60min, expires Sep 1): both the failure catalyst (a
   drift down = the market learning the premiere isn't a "trailer") and
   the success catalyst (jump toward 1.0 on release, potentially with
   sibling GTA markets lagging) are worth a triggered wake.
5. **journal/proposals.md** — status pass per (e).
6. **strategy/risk.json — untouched:** zero settled bets this window; the
   sweeps still say the floors stay.
7. **No reverts.** Commendations: the four-for-four triggered-cycle
   declines with consolidated escalation, the weather-category first
   contact handled under full Musk-lesson discipline, and the Cubs/ARI
   bet — the first realized instance of the exact edge shape the
   operator's architecture note predicted this system should own.

## Watch items (carried in schedule.json)

**PCE today 12:30Z — first calendar-triggered econ cycle; 9 open rows.**
Then BoK ~tonight 01:00Z Aug 27 (largest open disagreement, est 0.38 vs
mkt 0.69); GTA VI premiere Aug 27 (bet + price trigger); Canada GDP +
UMich Aug 28 (fe954ed9f325 exclusion mandatory); weather batch grades on
Aug 26 NOAA obs (~tomorrow's cycles); Cubs/ARI resolution by ~Sep 2; MLB
weekly sweep ≥Aug 27; commodity/gas/market-cap sets ~Aug 31; JOLTS/ISM
Sep 1–3; NFP Sep 5; AfD MTM each deep retro through Sep 6.
