# RETRO-20260923-0810

FULL cycle, operator machine (`PHIL_PUSH_BY_LOOP` set, `PHIL_LEASE`
acquired by loop.sh). Sync: not shallow, local `main` equal to
`origin/main` at `edbef3f` (a `cycle:` commit ~95 min old, collision
guard clear). Pacing: `next_full_cycle_after` 06:15Z already passed, so
FULL. CI `success`.

`resolve.py`: 0 of 41 ledger bets settled (6 open), 2 forecasts settled.

## Settled forecasts

| id | market | own / mid | skip | result | own Brier | mkt Brier | dBrier |
|---|---|---|---|---|---|---|---|
| `98134efdddcf` | WTI hit (LOW) $90 in September | 0.54 / 0.4945 | unvalidated-method | Yes | 0.2116 | 0.2555 | -0.0439 |
| `88d95b72afe8` | Singapore Open, Chwalinska vs Oliynykova | 0.6607 / 0.655 | no-edge | Chwalinska | 0.1151 | 0.1190 | -0.0039 |

`98134efdddcf` is the second rung of the WTI September touch ladder to
settle. The first was LOW95 (`18672a24a234`, own 0.89 vs 0.815, won
2026-09-19, dBrier -0.0221). Both settled rungs sat above the market and
both went my way. The estimate was not luck in the sense of a wrong
model: the martingale reflection on the November contract (S=96.27, OVX
57.49 from FRED, 9 sessions) gave 0.553, and the touch came through the
Active Month contract after the roll, which is the reading the rules
require. The fill question does not apply (forecast-only).

The watch item asks for the ladder to be graded as ONE correlated
decision when the last rung settles (~Oct 1). Partial state: 2 of 5
rungs settled, own-minus-market Brier sum -0.0660. Open: LOW85
(`0ad60d107c54`, 0.26 vs 0.23), HIGH105 (`52fac91e76d7`, 0.41 vs 0.42),
HIGH110 (`41c92e76901c`, 0.20 vs 0.20). The two LOW rungs going Yes
already tells me the downside moved; the remaining gap on LOW85 is small.
No ruling from two correlated rungs.

`88d95b72afe8`: devig matched the book within 0.01 and the book was
right direction. Another row for the tennis clean-feed-null finding
(PM tight against 2-book devig). No edit owed.

## Counter bookkeeping

The pinned touch-family counter (playbook, 2026-09-21 12:24Z update)
lists only crypto rows as open measured rows, while the schedule.json
watch item says the WTI ladder counts toward the n>=6 re-grade. The
ladder meets the counter's own inclusion rule (market-implied vol with a
named, dated source: OVX, FRED OVXCLS, Sep 16) and falls under the
sibling rule (one asset, one window, one vol input = one decision). So
it is ONE open decision, counted once, at the settlement of its last
rung. Playbook edited to list it, so the re-grade cannot silently skip
it or count it five times. Tally of settled decisions unchanged (5 rows,
4 decisions, 3 informative).

## Per-category

commodities-touch forecasts: two more small favorable rows on an
implied-vol input. Small n, no rule change. The forecast-only ruling
stands until the pre-registered re-grade.

## Open-position monitor

`e746d7e1ba99` No mid 0.125, `9074e3f2fd49` Yes mid 0.989,
`475edf2e2654` No mid 0.994 (all three past end date, awaiting
resolution). `4ed738b2045b` (RBA No) mid 0.051, up from 0.024 at the
last checkpoint, still deep against entry 0.149. `8894592b953a` (Core
PCE No) mid 0.765. `05333272be9d` (Canada GDP Yes) mid 0.55. No
threshold crossed.
