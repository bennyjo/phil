# DEEP RETRO — 2026-08-13

Window since DEEP-2026-08-12 (~04:45Z): **0 bets placed, 1 ledger
settlement** — `d2dd24206542` (Iran ceasefire No @ 0.92) finally settled
LOST -$5 on 2026-08-12, completing the pre-registered Iran pair and making
info-race **2W/4L final**. The hourly agent enacted the pre-registered
non-party-primary-source rule the same tick, exactly as specified. The
other story: **the forecast stream crossed n=100 settled** (31 open), and
the July CPI print resolved 16 rows in one batch with zero bad forecasts
and zero bets — the cleanest full-event demonstration yet that the
skip/veto machinery is calibrated. The uncomfortable mirror of that: the
threshold sweep now has settled disagreements in every bucket and **all of
them are negative** — 100 forecasts in, there is still zero settled
evidence of being right when we disagree with the market.

Session note (sync): third consecutive deep-retro session on a shallow
clone; `git fetch` again printed a spurious "forced update"
(6bb2664→1a651a5). No recovery time burned this time (HEAD happened to be
at tip), but the artifact is live — the 2026-08-11 unshallow proposal
stays endorsed, third dated instance recorded in proposals.md. CI: green
at 1a651a5 (`core/ci.py` status success) after the hourly agent's ae7b34e
fix — step 0c's first live test passed.

## (a) Calibration and P&L

**Bets: n=20, wins 6, P&L -$46.89, ROI -0.469, brier_delta +0.1232,
z -3.21** (was -2.73 at n=19 — the est-0.98 Iran loss is exactly the
overconfidence signature the z line has been tracking; it lives in the
large-claimed-edge interpretive rows, per the DEEP-2026-08-07 cohort
split). By edge class: book-devig -0.0732 (n=1) and the unclassified-era
mlb-spreads -0.0568 (n=2) remain the only negative slices, both far under
any honest n floor. news is now n=2, both Iran legs, brier_delta +0.4507 —
a mechanism verdict (state-media corroboration), not a category-breadth
one, and the rule for it is enacted. Open: `b35963f465b4` (PPI ≤5.1% Yes
@ 0.78), mid 0.80, MTM +$0.13, past end date awaiting the print's flow
into resolution.

**Forecasts: n=100 settled, brier_delta +0.0045, z -0.98.** The slices,
machine-computed (score.py):

- `no-edge` n=81, delta +0.0006 — at-market by construction, behaving as
  designed.
- `market-agrees` n=13, delta -0.0123 — the skips hold up; nothing
  skipped as "market agrees" has embarrassed the skip.
- `outside-view-veto` n=3 settled, delta +0.1109 — every officially
  settled veto row went to the market. The veto keeps being right.
- `architecture-mismatch` n=2, delta +0.1093 — same story.
- CPI batch (16 rows, RETRO-20260812-1535): all four winning buckets were
  the modal buckets both we and the market favored; all 12 non-hits
  correctly low-probability. econ forecast n=13 delta -0.0163, econ-cpi
  n=4 -0.0017 — the only mildly negative (good) forecast slices with any
  n, still under the ~15 floor as single categories.
- mlb-spreads forecast n=21, delta -0.0003, stable across three slates
  (−0.0004 → −0.0003) — the clean-feed null is **solid**. Acted on, see
  (f).

**Threshold sweep (first window with real n): every bucket negative.**
edge≥0.02: n=6, ROI -0.207, delta +0.0268; edge≥0.03: n=5, ROI -0.574;
edge≥0.04 through ≥0.15: n=2, ROI -1.0, delta +0.0917. Counterfactually
flat-betting every settled forecast that cleared any floor would have
lost money at every floor. n is small and same-family-correlated (the
primary rows dominate the high buckets), but the direction is uniform and
matches the bet-side z: **the disagreement stream is the problem, the
agreement stream is fine.** Nothing here argues for loosening any floor;
everything argues the floors are the only thing between the current
estimation pipe and faster losses.

Honest summary at the 100-forecast milestone: this system currently
demonstrates *well-calibrated abstention* — it knows when it doesn't know,
increasingly precisely — and has not yet demonstrated *edge*. The two
settled wins that look like edge (official-print info-race, cross-market)
remain n=2. That is not nothing: abstention calibration is the
precondition for cheaply hunting edge. But no one should mistake the
clean CPI batch for alpha; it was the filter working.

## (b) Audit of the hourly agent's edits

Window: 6 FULL cycles (06:33, 10:19, 13:36, 17:20, 21:19 on Aug 12;
01:15, 04:14 on Aug 13 — 7 by the awk count across the exact 24h window),
min_full_cycles_per_day=4 met. Settlement-triggered retros written on the
correct ticks every time (1535's 16-row CPI batch, 1817's Iran
settlement, 0211/0311's forecast batches) — the settle-tick-writes-retro
discipline is now clean.

1. **playbook.md — non-party-primary-source rule enacted (c81dcb7,
   18:18Z) — KEEP.** Textbook execution of a pre-registration: both pair
   legs settled lost, the rule moved from caution to general condition of
   the info-race class the same tick, the status paragraph updated to
   2W/4L with the full settlement facts. Nothing to sharpen; this is the
   process working exactly as designed since DEEP-2026-08-02 wrote the
   pre-registration.
2. **tags.py CI fix (ae7b34e, 01:16Z) — KEEP.** First live test of
   CYCLE.md step 0c: red CI (ruff F401, agent's own file), diagnosed and
   fixed in one line within the same cycle, CI green since. The one
   blemish is upstream of the fix: the unused import shipped on
   2026-08-12 and stayed red for ~19h of ticks before step 0c existed to
   surface it — that latency is why the operator built the probe, and
   the fix's speed once the probe existed validates it.
3. **Pacing (schedule.json) — KEEP.** Deferrals event-anchored (PPI
   watch on light ticks, ~3h caps respected), reasons carry the v4
   count verbatim, LIGHT ticks in the gaps were genuinely empty. No
   deferral-to-avoid-research pattern: the window's hard research (CPI
   siblings, Japan GDP contradiction, Kazakh/Zambia exploration) all
   happened on FULL ticks.
4. **Sensing (discovery.py unedited; pool 849-984/cycle, econ-tag query
   steady at 15-18) — healthy, one attention-inefficiency found.** The
   AMAT cooldown recheck ran 4 times in 11h with an unchanged estimate
   and identical conclusion — individually defensible, collectively
   churn; fixed with a recheck stop rule, see (f). Exploration budget
   was genuinely exploratory this window: SF weather station-pinning
   test (negative result, recorded), Kazakhstan Kurultai (unreachable,
   recorded), Zambia general election (first out-of-sample test of the
   election bar, forecast recorded). Each tested a named hypothesis and
   left a graded record — this is what the 2026-08-05 mandate asked for.
5. **Zambia decline (fa185b55a5c3, 04:14Z) — right call, wrong label.**
   Declining to bet against a 0.93 favorite on qualitative-only evidence
   is exactly the primaries lesson applied. But it was logged
   `outside-view-veto` at claimed edge ~0.05 — the veto is defined as
   the >0.10 boundary, and its 6-for-6 record is only meaningful if the
   label stays coextensive with the definition. Not reverted (the
   forecast row is core-owned history and the decision was correct);
   fixed forward with a `category-bar` taxonomy label and an explicit
   general-election extension of the bar, see (f).
6. **Funnel/forecast coverage — CLEAN.** Reconciled all 21 researched
   entries across the window's 6 funnel rows against forecasts.jsonl:
   every estimate-bearing skip either has a forecast_id or is a
   documented duplicate correctly rejected by record()'s one-live-row
   rule (CSCO x2, AMAT x3). No invented estimates on
   benchmark-unreachable skips.

## (c) Estimation errors of the day

The window's one settled bet error is the already-graded Iran ceasefire
(reasoning wrong: state-media corroboration treated as fact-grade; the
mechanism rule is enacted). Beyond that, the window produced almost no
new estimation errors because it produced almost no disagreements — the
CPI batch, the MLB/WNBA sweeps, and the earnings rechecks were all
agreement rows that settled at market. The genuinely instructive
non-error: the UK GDP cluster stayed untouched through its ambiguity
window rather than being forced into an estimate, and the pre-registered
zero-cost bracket-gap test is still pending the actual ONS release
(watch item). The systematic error, restated with new n: **when this
system disagrees with a liquid market by more than the floors, it has
been wrong every settled time** (sweep: all buckets negative; veto rows:
market 3-for-3). The response is not a bigger model of the same inputs —
it is the standing redirection toward mechanical/official-number
categories where the estimate isn't an interpretation.

## (d) Discipline violations

None. Zero bets placed, so the bet-floor rules had nothing to bind on.
Odds budget inside the guard (quota file consistent with funnel-passed
spending only). Confirmation-sweep cap respected (one sweep, 03:14Z,
composition-changed slate; the 04:14Z cycle correctly declined a second).
Cycle-count claims all v4-computed. The Zambia mislabel in (b).5 is a
taxonomy fray, not a violation — the taxonomy rule as written
(DEEP-2026-08-11) only mandates forecast-row linkage, which was
satisfied; the label boundary is legislated as of today.

## (e) Proposals review

- **2026-08-11 unshallow-before-divergence — stays endorsed**, third
  dated instance appended (today's session: shallow clone, spurious
  "forced update", benign only by luck of HEAD position). This is now
  3-for-3 deep-retro sessions; the guard belongs in the trigger prompt.
- **2026-08-10 forecast.py revision support — stays endorsed**, second
  consecutive window with zero cost from the workaround; low urgency
  re-confirmed.
- **No new operator proposals.** Everything this window needed was
  inside strategy/ and is applied below. Status pass appended to
  proposals.md.

## (f) Actions applied today

All in strategy/, each citing settled evidence:

1. **playbook.md §Odds-API rule 5 — confirmation sweeps demoted from
   daily to weekly per feed.** Evidence: mlb-spreads settled forecast
   n=21, brier_delta -0.0003, stable across three consecutive slates;
   additional at-market rows carry ~zero information while spending
   credits and minutes. Revert condition stated (any clean-feed edge ≥
   min_edge on a spot-check).
2. **playbook.md §Skip-reason taxonomy — new `category-bar` label** for
   declines under a category bar, reserving `outside-view-veto` for the
   >0.10 numeric boundary. Evidence: fa185b55a5c3's label would pollute
   the veto slice at settlement.
3. **playbook.md §Durable rule — election bar extended to general
   elections** without credible numeric polling or a mechanical anchor;
   Zambia marked as a test in progress, not evidence. Formalizes what
   the 04:14Z cycle derived ad hoc.
4. **playbook.md §earnings — recheck stop rule**: two consecutive
   no-new-info rechecks close a market-confirms candidate until
   resolution. Evidence: AMAT researched 4x/11h, unchanged estimate.
5. **playbook.md §Primary batch — official-settlement note**: 3 rows
   settled on-chain, all matching provisional grades.
6. **risk.json — dated evidence note, no numeric changes** (n=20; every
   sweep bucket negative; floors non-binding again; z -3.21).
7. **proposals.md — status pass appended** per (e).
8. **No reverts.** Both substantive hourly-agent edits (rule enactment,
   CI fix) keep. A disciplined window on their side.

## Watch items for tomorrow

- **PPI settles today** (print flows into resolution; position
  b35963f465b4 + 9 sibling forecast rows): grade the bet and the
  base-effect method separately, including what the method would have
  done on the three wide-book-vetoed legs.
- **UK GDP release (ONS, ~06:00Z today per the date-mismatch finding)**:
  run the pre-registered zero-cost test — did an off-grid print sweep
  all listed brackets No? Grade the bracket-gap reading.
- **Zambia election resolves ~Aug 14** (fa185b55a5c3): first
  out-of-sample test of the election bar's logic on a general election.
- **Primary batch remaining UMA settlements** (WI winner legs,
  Hong-<5%): grade only at resolution; provisional grades holding so
  far (3/3).
- **First `category-bar` usage**: next election-family decline should
  use the new label; check the funnel row.
- **tags.py weekly pass** due ~Aug 19 (weather 1474 re-check included).
- **Sweep demotion compliance**: next mlb-spreads sweep should be ≥Aug
  20 unless slate composition plus a named reason justifies a
  spot-check.
