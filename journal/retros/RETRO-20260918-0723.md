# RETRO — 2026-09-18 07:23Z

TRIGGERED cycle (newmarket:4674703,4674702,4674701). 1 forecast settled
since the last retro (0537Z): 0 ledger bets settled (8 open positions, 1
past end date). Graded per the settle-anything-on-a-light/triggered-tick
practice.

- **e3b016d8a48f** (Will Solana reach $105 on September 18?, market
  4666433, `crypto-threshold`, recorded 2026-09-18 04:24Z): est 0.998 vs
  mkt 0.999 → **WON**. Brier: agent (0.998-1)²=0.000004 vs market
  (0.999-1)²=0.000001, delta +0.000003 — both essentially perfect, no
  signal either way. The market-agrees call was correct: spot was already
  past the touch-anytime threshold at record time, so this was a
  near-certainty read, not a forecasting win. No lesson, no playbook edit.

## This cycle's research (TRIGGERED, no bet)

Three new sibling markets on the same event (RCD Espanyol vs. Elche CF,
La Liga, kicks off 19:00Z today): O/U 6.5/7.5/8.5 combined goals — a
line-constructed ladder (`sportsMarketType: totals`, opticOdds-fed) that
`screener-filters.json`'s `line_constructed` regex would normally drop
before it ever reaches a screening pass; these reached the watcher only
because they're brand-new markets, not screener output.

Devigged `core/odds.py odds soccer_spain_la_liga --markets totals` (5
books quoting the 2.5/2.75 lines, power devig): P(total>=3) ≈ 0.502
across books → fit a single-parameter Poisson(λ=2.68) to combined goals
and extrapolated to the three PM lines:

| line | model P(Over) | PM ask/bid (Over) |
|------|---------------|--------------------|
| 6.5  | 0.0199        | 0.024 / 0.02       |
| 7.5  | 0.0064        | 0.012 / 0.01       |
| 8.5  | 0.0018        | 0.005 / 0.001      |

Model tracks the market within ~0.001-0.005 on both sides at every line —
no edge anywhere near `min_edge` 0.04. Recorded all three as
`market-agrees` forecasts (b7a906306935, 3d222342cabd, 3d61d950565c) for
calibration; no bet. Confirms the screener filter's premise from a
different angle: on this specific ladder shape the "market already prices
it right" finding held not because the line sits near 0.50 (these don't —
they're deep in the tail) but because a five-book Poisson tail estimate
lands right on top of the quoted price, i.e. the opticOdds feed pricing
these markets is itself already doing a reasonable job of long-tail
extrapolation. Does not feed the existing Poisson-derivative-pricing
hypothesis tally (that experiment records divergence >= 0.04 instances
only; this was ~0 divergence on every rung) — no playbook or risk.json
edit.

## Open-position monitor (quote.py on token_id, 1 of 8 past end date)

- **e746d7e1ba99** (Andersson-PM No, entry 0.21): live bid 0.06 / ask
  0.07, mid 0.065, move (0.065 − 0.21) = **-0.145 ADVERSE**, unchanged
  from the 02:34Z/04:26Z/04:36Z/06:44Z marks — already called out
  repeatedly, held to resolution past its Sep 13 end date (UMA lag).

Other 7 positions not past end date, out of scope for this tick's
monitor.
