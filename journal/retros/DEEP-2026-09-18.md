# DEEP RETRO — 2026-09-18

Window: DEEP-2026-09-17 commit `ac9db06` (04:47Z Sep 17) → `118cb41`
(04:36Z Sep 18). Sync: clone started shallow with a stale local main
(184 behind); `git fetch --unshallow` cleared it cleanly, local main
fast-forwarded to origin — no genuine divergence. `resolve.py` this run:
0/38 ledger, 0/92 forecasts newly settled.

## (a) Calibration and P&L

Bet side, all-time (`score.py`): n=38 settled, 13W, P&L **−$2.02**,
ROI −1.1%, brier_agent 0.3567 vs market 0.2468 (delta **+0.1098**
against us), luck-adjusted z = **−4.35** (own estimates expected 24.3
wins; got 13). The honest read is unchanged from recent deeps: own
point estimates are systematically overconfident, the book is held
near flat only because three outsized politics wins (`66131e6b8f76`
+$40.05, `09fc471ceec1` +$20.00, `5fbf676cfd7f` +$31.23) offset a long
tail of −$5 losses, and the gates (min_edge, vetoes, flat $5) are doing
the risk containment the estimates don't.

By cell, with small-n caveats stated:

- **politics-general: the only bet cell that is both negative-delta and
  materially profitable.** n=4 settled, 3W, +$57.62, brier_delta
  −0.0502. Small n, but the wins share a mechanism (election-structure
  reads the market lags: SD-second precedent, UR regional-roster check,
  MV poll table) and 5 of the 8 open positions sit here. Keep feeding
  it; do not size it up yet — n=4 does not survive one bad night, and
  Sep 20 is exactly such a night (see (d) pre-registration).
- **Structurally bad cells, now closed or gated:** news bets 0/5 −$25,
  delta +0.4956, and the news forecast cell +0.0581 at n=33 (third
  consecutive deep confirming worse-than-market on multi-channel
  diplomatic processes); say-the-word 0/4 −$20 (+0.1007), dropped from
  allocation in DEEP-2026-09-17 (the Warsh counting bug explains 2 of 4
  but the cell stays closed); ai-leaderboard bets +0.4194 n=2 (July
  legacy rows; today's two forecast-only rows in that category were
  correctly no-edge/market-agrees). The `unclassified` legacy block
  (n=18, −$46.89, +0.126) is pre-edge-class history — informative only
  as a reminder of what un-gated betting looked like.
- **Forecast side (n=699 settled): delta +0.0076, z −0.12 — market-equal
  overall.** The `no-edge` label is well calibrated (n=377, +0.0006).
  Best forecast cell remains mlb-moneyline devig at −0.0499, n=15;
  mlb-totals is a clean null at n=32 (−0.0110). The 24h window added
  only correctly-labeled variance rows (MLB total lost at 0.49, NFL ML
  won at 0.69, both sub-0.01 edges).
- **Window ledger settlements (2):** Sweden Liberals `b063db346052`
  LOST −$5, SD-second `09fc471ceec1` WON +$20 — Sweden trio now +$15
  realized with Andersson-PM (`e746d7e1ba99`, mark ~0.065 vs 0.21
  entry, adverse) the last open leg; the joint event-night verdict is
  still owed at its settlement, as pre-registered.

## (b) Audit of hourly-agent edits in the window

1. **`627fcba` (playbook: recreational/political bookmaker devig is a
   weaker evidence tier) — KEEP.** Evidence-clean: `b063db346052`'s own
   rationale flagged the undated 8%-overround WebSearch table before
   betting, and settlement confirmed the market's Sep 4 poll-anticipating
   reprice (0.375→0.50) that the scraped books lagged. Correctly scoped
   as an n=1 caution, not a ban.
2. **`4887a6a` (playbook: BoE veto row + REPAIR of 3 missed Sweden
   CF-table rows) — KEEP.** The BoE row (`bc482ffb3606`, own 0.20 vs
   market 0.061, resolved No) is a clean confirmation that discretionary
   central-bank votes belong to the outside-view-veto class — the market
   and the Reuters poll beat both the own estimate and the LSEG/SONIA
   secondary benchmarks. Mechanical totals quoted from
   `counterfactual.py` (143/135, +$63.41, delta +0.0370): the CF ledger
   says the veto still pays, so no boundary change was right. The
   repair itself honored the same-commit table rule the moment the
   reconciliation found the gap.
3. **`bce30f2` (playbook: "Two reachable benchmarks" — WTI Active-Month
   roll clause + PCE staff estimate; 1 bet) — KEEP, best sensing work
   of the window.** The roll-clause find (six of seven PM rungs
   reproduced within 0.05 once the Nov contract and a sourced OVX vol
   replaced front-month spot and a guessed vol) converts the whole
   commodity-touch family from "market looks absurd" to "market prices
   the roll"; correctly forecast-only under the touch gate. The PCE bet
   `8894592b953a` (No on 3.3 @0.68, edge 0.08, spread 0.01) is
   gate-compliant with a named primary source; the 3.2-Yes leg was
   correctly vetoed on the unsourced hit rate. Both explicitly defer
   rule changes to settlement.
4. **`ad1ab8a` (playbook: threshold-close variants join the
   architecture-mismatch family; quote record-time book, never the
   screener mid) — KEEP.** `ddab4cfd4c5b` was mislabeled no-edge off a
   stale screener mid (0.095 vs live 0.14/0.15); the fix names the
   family and the record-time-book duty. This is the second stale-mid
   labeling error class caught by a retro; the rule now covers it.
5. **Pacing (`schedule.json`, SETs at 06:2x, 23:1x, 02:3x) — KEEP.**
   Deferrals cite concrete event boundaries (Saudi pipeline restart
   window, US-business-hours PURSUE tranche, Sep 20 elections) after
   researching or reconfirming every escalation, and the window ran 6
   FULL cycles against a floor of 4. This is deferral-as-empty-window,
   not deferral-as-research-avoidance: the 02:15Z FULL cycle researched
   and placed 2.
6. **Sensing (no `discovery.py` edits; filters working) — no artifact
   problem visible this window.** Pool composition is stable
   (~1,120 candidates across 4 queries), the crypto shape filters added
   Sep 17 are dropping the banned shapes at screen time (12+5 dropped
   02:15Z), and the one leak (SOL `4666433`) came through the
   *protected* watch.py path the open proposal already covers — the
   third phrasing variant in 12 days, correctly filed as recurrence
   evidence, no new ask. Structural invisibility check: the econ lane
   (scan-horizon gap, operator note 2026-09-10) still depends on the
   dormant release calendar; nothing new this window.
7. **Bets placed (3, all gate-compliant):** `8894592b953a` above;
   `7ec71e307f12` Berlin Linke No @0.34 (edge 0.05, spread 0.01, final
   poll table from four houses, sibling-sum check clean, mech pair
   sent); `d8892ff1a36a` UFO-files No @0.139 (edge 0.08, spread 0.03,
   release-log anchor). **Watch, not revert, on the UFO row:** it is a
   news-category bet self-described as a "deliberate sub-0.10 test of
   the process shape" with an outside-view shrink already applied. The
   news cell is worse-than-market at n=33; the row is only defensible
   as the pre-registered test it claims to be. Grade it at settlement
   as exactly that: if it loses to a process surprise, the news
   deprioritization extends to own-No process shapes with mechanical
   anchors, and "deliberate test" stops being a valid bet reason in
   this cell.

**No reverts this window.** Every playbook edit is evidence-cited and
correctly scoped; the hourly agent had a genuinely good day.

## (c) Biggest estimation errors of the day

1. **Liberals `b063db346052` (−$5): benchmark wrong.** The devig input
   (undated recreational book table) lagged a market that had already
   priced the Novus poll. The reasoning chain was internally sound; the
   evidence tier was the error, and `627fcba` encodes it.
2. **BoE hike veto forecast (CF −$5, no cash): reasoning wrong, veto
   right.** Own 0.20 vs settled No; the LSEG/SONIA benchmarks that
   motivated the edge were themselves beaten by the market. Third
   confirmation this month that "my benchmark disagrees with the
   market" on discretionary decisions is not edge.
3. **SPY threshold mislabel: process error, $0.** Stale screener mid
   made a no-edge row look like a 0.05 edge; caught and family-labeled
   by `ad1ab8a`. Variance elsewhere (MLB total 0.49 coin, NFL n=1) was
   correctly not acted on.

## (d) Discipline

- **No violations in the window.** All 3 bets ≥ min_edge 0.04 with
  spreads ≤ 0.06 quoted at entry; no in-play entries (Berlin placed
  Sep 18 for a Sep 20 election); flat $5; ledgers core-written only;
  every settling tick wrote its retro same-tick, including LIGHT ticks;
  the CF-table duty was honored (and 3 older missed rows repaired).
- **Record-keeping error, corrected this retro:** the Liberals watch
  item claimed "a $1 real twin exists in real-ledger." False.
  `journal/real-ledger.jsonl` contains 56 rows, all `type: settle`
  sweeps, zero fills ever; cycles.log 2026-09-09T01:27Z says explicitly
  "Real: none (book-devig not real-eligible)." Watch item text fixed in
  `strategy/schedule.json`. Related fact worth the operator's eyes: in
  real mode to date, **zero real twins have ever been placed** — the
  only real-eligible edge class (`cross-market`) has produced exactly
  one bet lifetime (`4ed738b2045b`, RBA), and it landed on a paper
  cycle. The real-twin experiment is currently starved by construction
  (see (e)).
- **Correlated-exposure pre-registration for Sep 20 (added to
  schedule.json):** four open rows resolve on election night Sep 20 —
  the Russian pair `9074e3f2fd49` (UR every region, Yes @0.68) +
  `475edf2e2654` (turnout ≥62%, Yes @0.883), both derivatives of one
  election ($10 = at the per-event cap, compliant but fully
  correlated), and the German pair `23e40bbbccc5` (MV CDU 11–14%,
  deep OTM) + `7ec71e307f12` (Berlin Linke No). Per the Sweden-trio
  precedent (DEEP-2026-09-09), each national set gets ONE event-night
  verdict — net P&L plus whether a common factor (turnout machinery /
  German polling error) moved the legs together. Not grading them as
  four independent outcomes is pre-registered now, before outcomes are
  known.
- **Mech line (window counts, per operator note 2026-09-14):** since
  the Sep 14 reset: 14 requests, 13 deliveries (1 off-chain nonce-race
  rejection, retried OK, $0 lost), 9 market-aware with context —
  `market_prob_seen` equalled the sent price on all 9, zero supply
  bugs — 4 paired ma/v4 sets (PCE-3.3, UR-gain, agent-arena, Berlin),
  **settled pairs 0** — nothing gradable yet. Contamination reading
  continues: Polymarket-derived pages in serper on most runs (price
  leaks five/six named in the playbook), and retrieval missed the
  decisive current fact on every graded-at-research question so far.

## (e) Proposals (journal/proposals.md statuses set this retro)

- **Mech parallel-send nonce race (2026-09-17 19:4xZ): ENDORSED, ask 1**
  — one sentence in CYCLE.md 5a ("send mech requests one at a time")
  is operator-cheap and the behavior is already adopted (both later
  cycles' funnel notes say "sent one at a time"); codifying it stops
  the next fresh session from rediscovering it. Ask 2 (per-safe lock in
  Pearl Connect) is the durable fix but lives upstream — worth relaying
  to the Connect repo, not blocking. The 401-retry-with-new-id handling
  was right; add the nonce 401 to 5a's transient list in the same edit.
- **watch.py banned-shape gap: ENDORSED option (a), RE-URGED** — third
  phrasing variant ("reach $X", `4666433`) in 12 days, exactly the
  maintenance burden option (b) predicted. Two wasted TRIGGERED fires
  now.
- **NEW (operator): the real-twin experiment cannot produce data as
  configured.** `real.allowed_edge_classes` = `["cross-market"]` has
  yielded 1 qualifying bet in the experiment's lifetime, and it missed
  the real-mode window. Zero fills in real-ledger.jsonl ever. Either
  (i) accept that real evaluation is dormant until cross-market supply
  improves, or (ii) consider adding a second class — the honest
  candidate on current evidence is politics-general `other` (n=4,
  −0.0502, +$57.62), but n=4 is thin and Sep 20 settles 4 more rows in
  that cell within 72h; deciding AFTER those settle costs nothing and
  doubles the evidence. Recommendation: wait for Sep 20, then decide.
  Protected-config edit either way — operator's call.
- **Standing, unchanged:** ODDS_API_KEY on the operator runner (still
  the best forecast cell, still devig-blind on operator FULL cycles);
  screener day-batch quota refund (ENDORSED 09-16); CI funnel-weld
  check (no new instance — all window FULL cycles carried funnel
  lines); cloud lease writability; per-fold `fold_brier_delta` in
  counterfactual.py — **the German-bracket veto cluster it was dated
  for settles Sep 20, i.e. before the next deep retro after tomorrow's;
  if it is not in by then, the fold analysis gets done by hand once.**

## Verdict

A clean, improving window on a still-unprofitable book: no violations,
no reverts, two genuine sensing wins (WTI roll clause, PCE staff
estimate), the process errors of the day all self-caught, and the one
false record (phantom real twin) found and fixed here. The bet-level
skill signal remains: politics-general is the only cell earning its
allocation; everything else is gates doing their job. Sep 20 settles
four correlated rows and is pre-registered above — it will be the most
informative single night since the Sweden trio.
