# DEEP RETRO — 2026-09-10

Audit window: 2026-09-09 ~04:30Z → 2026-09-10 ~05:00Z. Sync: clone was
shallow at container start, `git fetch --unshallow` succeeded; after the
unshallow local main == origin/main at 27d1069, no divergence (the
pre-unshallow "forced update" fetch line was the usual shallow-boundary
artifact, per the 2026-08-11 instances). resolve.py: 0 settled this run
(8 ledger positions and 72 forecasts still open); score.py run fresh.

## (a) Calibration and P&L

**One bet settled this window:** `2659709d25f9` Ternus "Hardware"
(say-the-word, Yes @0.52, est 0.60) LOST −$5.00. Overall book: n=31,
+$0.84, brier_delta +0.1086, luck-adjusted z −3.79. The standing honest
read is unchanged: cumulative P&L is barely positive only because of two
longshot wins (5fbf676cfd7f +$31.23, 66131e6b8f76 +$40.05) while the
calibration deficit is broad and real. One −$5 settlement moves nothing.

By edge class (unchanged in shape): book-devig n=1 −0.0732
(insufficient), other n=8 +0.0287 (flat), info-race n=4 +0.2354 (bad,
walled off), unclassified n=18 +0.126 (legacy closed population).

**Forecast side: 17 graded non-superseded Yes/No settlements this
window, window brier_delta −0.0444 (agent ahead).** Honest decomposition
before anyone celebrates: the beat is two rows. Chewy "Consumable"
(ffc3fcdcbaa6, est 0.93 vs mid 0.34, Yes; row delta −0.431 — but the
0.34 mid was an empty-book artifact, ask was 0.58, so the realizable
beat is roughly half the scored one) and the final Ternus supersede
(ce1a01a0049e, est 0.12 vs mid 0.49, No; row delta −0.226 — earned by
TRIGGERED re-checks during the keynote, i.e. the info-timing loop
working, not a calibration miracle). The residual 15 rows are
approximately flat vs the market. Say-the-word is developing a clean
split (see (c)); n is still far below the ~15-row category threshold.

**Tracked conditions (recomputed this pass):**

- **gnhf policy v3 forward test — JUDGEABLE for the first time, and it
  FAILS.** `replay.py --after 2026-09-02T00:14:36Z`: 135 forward rows,
  **16 bets** (≥15 bar crossed), pnl +$28.40, roi +0.355, win rate
  0.688, **cw_return −0.1557 < 0 → fail** on the pre-registered
  criterion. The concentration criterion fails independently: the
  largest single bet (5ee44ae81c9f, politics-general, +$39.64) exceeds
  the entire net pnl, let alone half of it. Per the 2026-09-02
  pre-registration: nothing in strategy/ changes, policy.py stays
  untouched, the dead zone is NOT adopted. Tracking entry closed
  resolved-negative in proposals.md. (The forward-test.yml issue is the
  operator's channel; verdict recorded here so no one re-litigates it.)
- **Blend[disagreement]: n=169, w_opt 0.760, delta −0.0027 — the bar
  (w_opt ≤0.80 at n≥150, delta ≥0.002) is MET on this pass, the FIRST
  of the two consecutive deep-retro passes "sustained" requires.**
  Caveat recorded in advance: the move from yesterday (0.843/−0.0011 →
  0.760/−0.0027) came from 4 new disagreement rows, dominated by the
  Chewy row whose scored market mid (0.34) is an empty-book artifact —
  exactly the kind of single-row leverage that produced the 2026-08-25
  false fire. If tomorrow's pass still clears at n≥150, the operator
  ask gets filed per the 2026-09-03 note; nothing ships before that.
- **Outside-view-veto relaxation fork: NOT MET** (playbook Status
  2026-09-10 updated): 124 rows / 116 CF trades / 82 events / +$109.91
  / dBrier +0.0264; per-fold dBrier by the read-only recipe f3 +0.0981,
  f4 +0.0434 — both most-recent folds positive while fold pnl is
  positive (f4 +$90.56). Money without calibration; fork stays shut.
  The per-fold `fold_brier_delta` operator ask stands (this pass again
  had to hand-compute it).
- **Mechanical-econ carve-out:** no carve-out settlements this window;
  bar and kill switch unchanged (rows 5/5, independent events 1/3).
  The Aug 2026 US PPI print (Sep 10 12:30Z, watch item) is the next
  live test of the sourced-dispersion gate.
- **Screener-value switch bar (first tracking entry per the 2026-09-09
  operator note):** fit 564 settled rows / 414 events (bar: 1,000 /
  700); `rank` on the live pool decided **0 of 15 slots** (bar: 8/15) —
  the whole top-15 sits in one 51-way tie group ordered by divergence,
  families {sports moneyline: 15}. Far from the bar; dormant, as
  designed.
- **Screener replay re-baseline (rev 6796568):** attempted this pass;
  the `screen_replay.py outcomes` cache refresh was still running
  (network-bound, >20 min) at commit time. s_exc/s_ez remain unquoted
  — the freeze and the stale-baseline flag from DEEP-2026-09-09 both
  stand until a pass completes the re-baseline.
- **Mech:** no new deliveries this window (last 2026-09-09 01:2xZ, all
  blind). The market-aware-with-context paired count (2026-09-09
  ~21:45Z operator note) has not started — cloud cycles have no signer;
  it begins with the first operator-machine cycle whose build exposes
  `request_context`.
- **Real ledger:** 56 rows, all settle sweeps, zero real fills — dry by
  design, unchanged.

## (b) Audit of hourly-agent strategy edits in the window

Window: 7724d66..27d1069, 28 commits, 2 of them operator (gnhf run 5
landing, mech request_context). Agent strategy-file edits, all audited:

1. **d110243 — playbook: China CPI veto-slice grading (3 econ-cpi
   settlements).** **KEEP.** Both fillable counterfactual trades lost —
   both declines correct; the unexecutable third row correctly excluded
   from the trade count. Arithmetic re-summed and checks out (side
   splits cross-foot to totals). Correctly carries the base-effect
   method confirmation and the TE-vs-Lundgreen consensus-conflict data
   point as n=1 hypotheses rather than rules. This is the veto-ledger
   discipline working exactly as specified.
2. **c364c6a — playbook: Chewy "Consumable" veto CF win (+$3.62) +
   three pre-registered gradings.** **KEEP — best edit of the window.**
   It grades a row where the veto COST money and still rules "no
   boundary change at n=1", explicitly refusing the anecdote. The (a)
   mechanical-subclass observation (the tool's own tagger filed the row
   fact-finality) is the right kind of evidence to accumulate toward a
   future carve-out shape; the (c) finding — the ask was takeable at $5
   size, so the wide-spread veto alone would have been overcautious —
   is honest instrument-checking. Table re-summed, cross-foots.
3. **Watchlist edits (4a31ff5 Russia-UR add, 1dbf85c Apple-keynote add,
   b6d2ab6 conversion→removal after the keynote fired, b0ff525 RNC
   add).** **KEEP.** Each was created with expiry and grading hook, and
   the Apple one actually fired its TRIGGERED conclusion-check, which
   produced the 0.60→0.48→0.12 supersede chain — the watch mechanism
   earning its keep.
4. **schedule.json pacing.** Honest. 5 FULL / 5 LIGHT / 3 TRIGGERED
   ticks in the window against a floor of 4 FULL; every LIGHT/TRIGGERED
   tick that settled anything wrote its retro on the same tick
   (RETRO-1615, -1723, -2014, -0013, -0412 — no deferred grading this
   window, the 8c604893bdea failure mode did not recur).
   next_full_cycle_after 08:00Z with the PPI print at 12:30Z is
   compatible (the 08:00+ FULL tick precedes the print; the print-day
   grading lands on the following ticks).

**Sensing (discovery.py, unchanged this window):** pool 938–1076
candidates/cycle, 300 screened, 15 escalated; the escalations this
window included politics (Russia UR), say-the-word (RNC), econ (PPI
watch) alongside the sports bulk — no category went structurally
invisible relative to yesterday's audit. No change.

**Pacing verdict:** deferral used on genuinely empty windows only; both
window bets came from FULL cycles with real benchmark work. No change.

**No reverts. Sixth consecutive clean window.**

## (c) Biggest estimation errors of the day

1. **Ternus "Hardware" 0.60 (bet, −$5).** I endorse the hourly verdict
   (RETRO-20260909-2214): estimate wrong, process right — the entry was
   an explicitly-flagged generic-priors behavioural guess at edge 0.08,
   below the veto boundary by design, and the in-event re-check chain
   moved on evidence, not price. What I add: the say-the-word split now
   reads **fact-finality 3/3 direction-right** (Chewy pair, Trump
   insult a6a8177980af, Tunnel-to-Towers ed15c319b4bf) vs
   **generic-priors 0/1** (Ternus). At these n's no rule changes; the
   pre-registered check is: if generic-priors say-the-word rows reach 5
   settled with negative CF pnl, the category bar proposal gets written.
   Until then the open Trump-"Afford" bet (e77eef5d06ad, researched
   stump-theme evidence, so NOT pure generic-priors) is the next data
   point — note at grading time it sits between the subclasses.
2. **China CPI 0.7-0.8 bracket (19cf14c87979, est 0.33, settled Yes).**
   The mixture model was on the wrong side because its two consensus
   inputs disagreed by a bracket; the veto caught the resulting fake
   edge. Reasoning wrong (input conflict unresolved), benchmark
   ambiguous, veto correct. Already graded in d110243; nothing to add
   beyond: this is the property-2 lesson again wearing a new series.
3. Nothing else material — the remaining window rows were market-agrees
   skips that settled as priced.

## (d) Discipline

- All three window bets compliant: 9074e3f2fd49 (edge 0.08, evidence-
  cited roster check), 2659709d25f9 (edge 0.08, spread 0.01), and
  e77eef5d06ad (edge 0.06, researched theme evidence). No under-floor
  bets, no in-play entries (the RNC bet was placed ~14h pre-event), no
  spread breaches found in the window retros or ledger rows.
- Correlated exposure: RNC Sep 10 carries one bet + one correctly
  vetoed forecast (87736f3e8ab9, MAGA at claimed edge 0.18-0.20 —
  veto applied where it binds, bet placed where it doesn't; the
  boundary working as written). Swedish trio unchanged, pre-registered
  as one event-night grading for Sep 13 (DEEP-2026-09-09).
- Settlement-grading discipline: clean — every settling tick wrote its
  retro and extended the CF tables in the same commit where required.
- Boundary check: no agent commit in the window touched operator paths.

## (e) Proposals

- **No new proposals from the hourly agent this window.** Nothing to
  endorse or reject.
- **Closed this pass: gnhf v3 forward test → resolved-negative** (see
  (a); mechanical criteria, judged at 16 bets).
- **Open operator asks after this pass: 2, both standing** — (1) lease
  writability / simultaneous-start collision fix (no new divergence
  this window, but every cloud cycle still logs written=false); (2)
  per-fold `fold_brier_delta` in core/counterfactual.py (cost again
  today: hand-computed folds for the fork check).

Verdict: a disciplined window with one ordinary loss on the money side
and the forecast machinery visibly earning: the veto graded both its
window outcomes correctly (one cost, one save) and refused the anecdote
both times; the first pre-registered test to reach its bar (gnhf v3)
was judged and failed exactly as mechanically as it was registered —
which is the system working, not a disappointment. Two things are now
one pass from mattering: the blend bar (first of two consecutive passes
met, Chewy-leveraged, treat with suspicion) and the Swedish election
night (three legs, Sep 13). The screener replay re-baseline remains the
one item this pass could not finish.
