# RETRO-20260916-0411 (LIGHT tick, cloud)

Trigger: `core/resolve.py` settled 2 forecasts (0 ledger bets) on an
otherwise-LIGHT tick. Per the schedule.json DEEP-2026-09-02 rule, a LIGHT
tick that settles forecasts still writes the retro immediately.

## Settled forecasts

Both are same-game sibling legs of the new MLB cross-line total
extrapolation model (first contact, playbook 2026-09-15 ~4661-4699),
correctly kept forecast-only under the outside-view veto (claimed edges
0.24-0.26, far past the 0.10 boundary, self-built Normal fit with no
mechanical anchor) — **no stake at risk, $0 P&L impact**.

- `5b6a0e52dd62` SD@COL O/U 12.5-Over: est 0.69 vs market mid 0.42,
  settled **LOST** (actual Under → total <=12).
- `793b7298aec9` SD@COL O/U 10.5-Over: est 0.83 vs market mid 0.58,
  settled **WON** (actual Over → total >=11).

Combined: actual total runs = 11 or 12.

## Was the estimate wrong, the fill bad, or variance normal?

Estimate: the model (Normal(mu=14.70, sigma=4.39) fit to 6-book
power-devigged totals at the 14.5/15/15.5 lines, extrapolated down to
10.5/12.5) was **directionally wrong on the harder (12.5) leg** — the
real total landed 2.7-3.7 runs below the fitted mean, exactly the
right-skew/discrete-distribution overstatement-of-Over failure the
playbook pre-registered as the thing to watch for. The easier (10.5) leg
hit, but 10.5 was already a soft bar against an 11-12 actual, so that's
weak confirmation, not vindication. No fill involved (forecast-only, no
bet placed). Not "normal variance" in the ordinary sense — this is the
first real-world test of a brand-new, not-yet-validated method, and it
produced the specific miss pattern the pre-registration flagged as the
risk to catch.

## Grading against the pre-registered bar

Per playbook: multiple lines on the same game count as **one game**,
graded by **average leg dBrier** (model − market; positive = model
worse), with a direction-of-miss tally.

- 12.5 leg: model brier 0.4761 vs market brier 0.1764 → dBrier **+0.2997**
  (model much worse).
- 10.5 leg: model brier 0.0289 vs market brier 0.1764 → dBrier **−0.1475**
  (model better).
- **Average leg dBrier this game: +0.0761** (model underperformed market
  on net).
- Direction-of-miss tally: **model-high ×1** (12.5 leg), **hit ×1**
  (10.5 leg, weak evidence given the soft bar).
- Context: 6-book cluster 14.5-15.5 (~1-run spread), ~22h pre-game,
  starters likely unconfirmed — consistent with "lines were still soft
  at forecast time," which is a separate hypothesis from "the Normal
  shape is wrong," and this single game can't separate the two.

**This is game 1 of the pre-registered n>=6 independent games required
before the unvalidated-method flag can be reconsidered.** Bar not met —
5 more games needed. Decision from the prior forecast's open question
("is one high-divergence pre-lineup game enough evidence"): **no**, per
the bar as written; continue seeking independent instances rather than
declaring the method dead or validated from n=1.

## Playbook edit

Appended a `GRADED 2026-09-16 04:1xZ (game 1/6)` note to the MLB
cross-line extrapolation section (`strategy/playbook.md`, after the
pre-registered bar) recording the above numbers and tally, so the next
settling game's retro can extend the same running tally rather than
re-deriving it. No policy change yet — the bar explicitly holds the
method forecast-only until n>=6, and one game is not that.

## Category verdict

mlb-totals overall (score.py): n=30, brier_delta=-0.0118 (market
slightly ahead, small-n, no overreaction warranted — well under the
~15-settlement caution threshold for a category-wide read, and this
sub-family's own n=1 is far below that too).

## Open-position monitor (this tick)

Live-quoted (`quote.py`) all 7 open positions; none crossed a new
call-out threshold versus their last recorded checkpoint:

- `e746d7e1ba99` Andersson PM No (entry 0.21): mid 0.125, move -0.085,
  flat vs last check.
- `23e40bbbccc5` MV CDU 11-14% Yes (entry 0.09): mid 0.030, move -0.060,
  flat.
- `09fc471ceec1` SD second-most No (entry 0.20): mid 0.998, move +0.798,
  favorable, flat.
- `b063db346052` Liberals-threshold No (entry 0.40): ask 0.001 (no bid),
  move -0.399, adverse, flat.
- `9074e3f2fd49` Russia UR-sweep Yes (entry 0.68): mid 0.655, move -0.025,
  flat.
- `475edf2e2654` Russia turnout No (entry 0.883): mid 0.894, move +0.011,
  flat.
- `5886cf8c42b7` Warsh "Inflation" 20+ Yes (entry 0.78): mid 0.775, move
  -0.005, flat (settles today, Sep16 FOMC presser).

No exit mechanism exists for any (playbook rule #6); all held to
resolution, no action taken.
