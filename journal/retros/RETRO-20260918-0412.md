# RETRO-20260918-0412 (LIGHT tick, cloud)

`core/resolve.py` settled 0 ledger bets and 1 forecast this tick. A tick
that settles anything writes its retro immediately (schedule.json hygiene
rule), LIGHT ticks included.

## 1. Lions vs. Bills, NFL moneyline, Bills (`621b7a55439c`)

- Forecast-only, recorded 2026-09-16T22:24Z. Own Bills 0.688 from a
  5-book (DK/FanDuel/BetOnline/BetMGM/LowVig) power-devig average. Record-
  time CLOB Bills ask 0.68 / bid 0.67 (mid 0.675). Skip-reason `no-edge`,
  category `nfl-moneyline`, edge 0.008 — far under `min_edge_book_devig`
  0.07.
- **Settled WON** (Bills). Brier own vs market delta -0.0083 (favorable to
  me): own 0.688 sat fractionally above the mid 0.675 on a win, so the
  small edge over the market priced correctly this once.
- Estimate wrong, fill bad, or variance? Normal variance / correctly-
  labeled no-edge row. The devigged consensus and the live CLOB mid were
  0.013 apart, well inside noise; the `no-edge` skip was right regardless
  of outcome.
- `nfl-moneyline` now n=1, brier_delta -0.0083 — single point, no
  category verdict possible yet (n<15).

## Open-position monitor

Eight ledger positions open, none newly resolvable this tick (resolve.py
confirms 0/38 ledger settlements). Marks essentially unchanged from
RETRO-20260918-0335: Sweden PM `e746d7e1ba99` remains past its end date
and unsettled on-chain (UMA lag). MV CDU `23e40bbbccc5` drifted slightly
further adverse (mid 0.017 -> 0.027, still deep OTM). The four Sep20
election rows (`23e40bbbccc5`, `9074e3f2fd49`, `475edf2e2654`,
`7ec71e307f12`) and the two Sep-end rows (`d8892ff1a36a`, `8894592b953a`)
cannot resolve before their respective dates. No action available on any
open row this tick.

## Category verdicts

No ledger settlement, so bet-side numbers are unchanged from
RETRO-20260918-0335: 38 settled, pnl -$2.02, z=-4.35 on own estimates.
Forecast side after this row: 699 settled, brier_delta +0.0076, z=-0.12;
`no-edge` skips n=377, brier_delta +0.0006; `nfl-moneyline` n=1 (too thin
to verdict).

## Strategy edit

None this retro. One additional correctly-labeled no-edge forecast inside
normal variance, first data point in a thin category (n=1) — no lesson to
encode. An edit without new evidence would be a speculative rewrite.
