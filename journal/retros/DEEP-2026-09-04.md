# DEEP-2026-09-04

Window: 2026-09-03 ~04:30Z → 2026-09-04 ~04:30Z (since DEEP-2026-09-03).
Sync note: session started on a SHALLOW clone with a stale main (the
familiar fake "forced update" pattern); after `git fetch --unshallow`,
local main was strictly behind origin/main by 136 commits with a clean
merge-base — fast-forwarded, no real divergence. resolve.py: 0 ledger
settlements (2 open), 0 forecasts newly settled at run time.

## (a) Calibration and P&L

**No ledger bets settled this window** (last settlement 2026-08-31), so
the bet-side table is unchanged: n=26, 8W, net −$34.24, brier_agent
0.3396 vs market 0.2089, delta +0.1307, luck-adjusted z=−4.25. That z is
the headline number of the whole experiment so far: by the agent's own
stated probabilities it expected ~17 wins and got 8 — systematic
overconfidence, not variance.

Where delta is genuinely negative: nowhere with usable n. mlb-moneyline
−0.7421 is one Cubs longshot win (5fbf676cfd7f, n=1); mlb-spreads −0.057
(n=2), econ-ppi −0.023 (n=1), book-devig class −0.073 (n=1). All
insufficient data. Where it is genuinely positive (bad) with pattern
support: news +0.476 (n=4, 0W — including two sequential "Lake America"
losses on the same joke-news shape), product-release +0.680 (n=1),
ai-leaderboard +0.419 (n=2). The narrative/info-race side keeps paying
for the whole book; info-race class is +$16.23 P&L only because one
longshot hit, its brier delta is +0.235.

Forecast side (where the volume now is): outside-view/wide-spread veto
counterfactual ledger reached **85 realizable trades, 39W/46L, net
−18.07u** — the veto remains clearly net-saving even after the GTA VI
batch's +5.64u. Tennis-moneyline forecasts n=19, delta −0.0001: the
9-book devig exactly matches Polymarket mids; clean-feed-null
re-confirmed for the third straight day. Weather-Gaussian CF class
3W/4L, −2.18u; the market's implied sd (~0.55C on Ankara) beat the
model's fixed 1.2C.

Real ledger: 22 rows, all `settle` sweeps, all empty positions — **no
real fills have occurred yet**, so no paper-twin comparison exists. The
first live real-twin candidate is the NFP bracket settling today 12:30Z.

Tracked condition lines (as promised to the operator):
- **Blend:** blend[disagreement] n=122, w_opt=0.837, delta −0.0012.
  Bar is w_opt ≤ 0.80 at n ≥ 150 sustained with delta ≥ 0.002 — not
  met, not close, trajectory still drifting toward the market.
- **Screener replay:** 18,047 rows scored; live prompt_rev f7ddad12
  excess +0.0033, exc_z +2.1 — unchanged from the operator's gnhf run 2
  reading. screener-prompt.md untouched, per the freeze.
- **gnhf policy.py v3 forward test:** 34 rows settled forward, policy
  bet 3 of them, pnl +2.71, brier_delta −0.0114. The pre-registered bar
  is ≥15 forward BETS; at 3 bets this is insufficient data, hands
  stayed off.

## (b) Audit of the hourly agent's strategy edits

1. **Same-day weather anchor rule** (7f64784, playbook): **KEEP.**
   Grounded in three settled rows (e48e803b2103, 3cf21078d1a0,
   32d046d6981c): sd=1.2C applied to a half-observed day at 10:22/12:18
   local is a category error the rule now prevents; "no fetch → no
   forecast" is the right fail-closed default. Zero capital exposure
   (category is no-bet), so this is free calibration hygiene.
2. **Ankara grading + ensemble-spread test at ~6 next-day rows**
   (d1c8764, playbook): **KEEP.** Correctly resisted editing the sd on
   n=2 next-day rows; pre-registered a test instead.
3. **GTA VI CF batch + narrative** (8e98208, playbook): **KEEP, and
   SHARPENED this retro.** The arithmetic re-sums check out (−15.75 +
   −2.32 = −18.07 ✓). The narrative correctly identified the
   anchored/unanchored split but left no forward rule; this retro
   distilled it into the **cumulative-count anchor rule** (playbook,
   after the batch table): dated count + observed pace or no forecast at
   all, skip reason `no-anchor`; market re-pricing is not an anchor.
   Evidence: brier 0.7225/0.3025 unanchored vs 0.09 anchored on the
   same market.
4. **schedule.json pacing** (multiple ticks): **KEEP.** Pacing audit —
   deferral is being used honestly. The 04:29Z FULL cycle ran the full
   scan/screen (300/300, 15 batches) and researched before deferring
   3h; 8 FULL cycles ran in one 24h stretch against a min of 4;
   deferral reasons cite per-candidate no-edge conclusions, not effort
   avoidance. The settlement-carrier rule was honored (both LIGHT-tick
   settlements this window wrote same-commit retros with CF-table
   extensions).
5. **strategy/screener-filters.json** (landed in d9158ee): **NOT an
   agent edit — excluded from this audit.** Operator gnhf-run-3
   artifact (its own comment says dormant, only protected
   core/screen_rank.py reads it, nothing in the live cycle calls it),
   swept into a `cycle:` commit by `git add -A`. Left in place;
   boundary consequences below.
6. **discovery.py (sensing):** no edits in the window; last touched
   Aug 5. Structural check: the four queries give volume-first breadth
   plus exactly one category-targeted probe (econ tag 100328). The pool
   (~1050-1080 candidates, 247 sibling groups) is not obviously an
   artifact, and the one asymmetric-floor probe is precisely the query
   that produced the only mechanically-resolving positives (PPI win,
   the NFP carve-out bet). The month-old promise "climate-weather tag
   1474: 0 live markets, revisit if that changes" has never been
   revisited — but weather is no-bet and the market beats our sd model
   there, so surfacing more weather is worth nothing; the promise is
   discharged as moot rather than executed. What IS structurally
   invisible: thin official-print markets outside the econ tag
   (election-certification, court-ruling, agency-decision shapes with
   free official benchmarks). No edit today — adding tags untested
   against live gamma from a retro session would be a blind edit — but
   the next FULL cycle that finds itself deferring for emptiness should
   spend the idle window verifying one candidate tag (elections/legal)
   the way tag 100328 was verified on Aug 5, and add it to
   discovery.py only with a live-verified footprint.

Nothing to revert this window — first window in several with zero
questionable strategy edits.

## (c) Biggest estimation errors of the day

- **b3fbd3c3eef7 (GTA VI <20M, est 0.85, brier 0.7225): reasoning
  wrong.** Followed the market's re-pricing to 0.875 and recorded
  agreement as confirmation while the underlying count climbed through
  ~17M against the position. Now covered by the anchor rule.
- **Ankara 30C (aac6d9fc2b4a, est 0.287 No at 0.565, CF −1.00):
  benchmark wrong**, not reasoning — fixed sd 1.2C vs market-implied
  ~0.55C; the pre-registered ensemble-spread test is the right
  response.
- **Sakkari (9dc91a996257, 0.630 vs 0.625): variance.** A 63/37 line
  loses 1-in-3; devig and market agreed; correctly no-edge.

## (d) Discipline

**Agent: clean.** One bet placed (84167af841f7, NFP 0-50k No @0.67):
edge 0.06 ≥ min_edge 0.04, 1c spread, mechanical-econ carve-out, $5
flat, no category or event-cap breach, no in-play. The duplicate NFP bet
from the concurrent cloud cycle was correctly DECLINED (funnel
2026-09-04T00:16:34Z) after the push rejection revealed the collision —
the discipline machinery worked under a race it wasn't designed for.

**Boundary event (not agent misconduct): d9158ee** carries
core/screen_rank.py + operator journal files under a `cycle:` prefix and
fails CI's boundary guard. The hourly agent's diagnosis (operator files
staged in the working tree, swept by the cycle commit) is convincing —
the file's docstring self-identifies as protected core referencing
operator-notes. Curing it requires touching core/ history/attribution,
which is operator-only; proposal filed below. The related
screener-quota.json lost-update race (both cycles read 0, wrote 15;
hand-corrected to 30) is the same read-modify-write shape as the ledger
collision and is folded into the same proposal.

## (e) Proposals pass

See journal/proposals.md 2026-09-04 deep-retro section: the
collision-guard structural finding is ENDORSED with a concrete remedy
sketch (both halves are operator-owned mechanisms); the d9158ee
boundary cure is filed as a new operator ask. No other proposals were
open.

## Housekeeping

- **Playbook: 3,573 lines** after this retro's rule (trigger: restructure
  at >3,600 at a deep retro). Not tripped today; at the current ~60
  lines/day CF pace, **DEEP-2026-09-05 will almost certainly do the
  consolidated-table + archive-pointer restructure** — it should budget
  for that first, before the day's grading.
- Calendar: NFP print today 12:30Z settles 84167af841f7 (and would be
  the first real-twin comparison if a real fill occurred on the
  operator machine); AfD Sachsen-Anhalt Sep 6 (position +$1.17
  unrealized at last monitor); Core CPI Sep 11.

## Verdict

A disciplined near-zero-bet window: every strategy edit the hourly agent
made survives audit unmodified (first clean sweep in a while), the one
capital decision was rule-compliant, and the day's real defects — the
cycle-collision race and the boundary-crossing commit — are
infrastructure races only the operator can close, now filed as such. The
book's problem remains unchanged and unhedged: z=−4.25 overconfidence
concentrated in narrative categories, while every mechanically-resolving
probe (PPI, devig confirmations, the NFP carve-out) is the only place
the process shows signs of being at or above par. The experiment's
next informative datapoints arrive by calendar, not by more betting:
NFP at 12:30Z today and AfD on Sep 6.
