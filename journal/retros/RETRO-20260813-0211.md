# RETRO-20260813-0211

LIGHT tick settlement grading (schedule.json: "if resolve.py settles ANYTHING on
a light tick, that same tick writes the retro"). Zero ledger positions settled
(1 still open); two forecasts settled.

## Settled forecasts

- `4f951b2398e4` — earnings — "Will Virgin Galactic Hldgs (SPCE) beat quarterly
  earnings?" (Yes), est_prob 0.87, market_prob_at_record 0.925, skip_reason
  no-edge. Rationale at record: SPCE 4/4 recent-quarter beat streak is public
  data the market already prices; nominal No-side edge (0.04) sat exactly at
  min_edge floor, declined per the 2026-08-08 BLS-ladder at-floor-is-noise
  finding. Settled **WON** (Yes) — market's own lean was correct, my No-side
  lean (implicit in a below-market est_prob) would have lost had it been
  betted. Correctly skipped.
- `96776cb354e8` — mlb-spreads — "Spread: Detroit Tigers (-1.5)" (Detroit),
  est_prob 0.3548 vs market_prob_at_record 0.36, skip_reason no-edge.
  Cheap-confirmation sweep via core/odds.py devig, edge ~0.01, under floor.
  Settled **LOST** (Cleveland Guardians won) — market's implied lean (favoring
  Cleveland/against Detroit -1.5) was correct; my estimate was near the
  market's and also on the right side of a coin-flip-adjacent line. Correctly
  skipped (no stake at risk either way).

Was the estimate wrong, the fill bad, or normal variance? Both are the normal-
variance case for correctly-skipped no-edge forecasts: no bet was placed, no
pnl impact, and both estimates sat close to the market's own price. Neither
surfaces a new mechanism — SPCE reconfirms the earnings-category
public-beat-streak-is-priced-in finding (already established across 6 prior
earnings forecasts, e.g. CSCO 8865ab581184 same cycle window, same reasoning,
same WON outcome); Detroit Tigers reconfirms the mlb-spreads
confirmation-sweep clean-feed finding (repeated across many prior sweeps,
edges consistently ~0.00-0.02, at-floor).

## Category verdict

- earnings: n=6 (score.py), brier_delta +0.0096 — small, near zero, consistent
  with correct no-edge skipping. Below the ~15-settlement threshold; no
  category-wide verdict warranted.
- mlb-spreads: n=18 (score.py), brier_delta -0.0004 — near zero, continues to
  confirm the clean-feed finding (PM's MLB spread book already reflects the
  same devig'd odds this strategy computes, so cheap-confirmation edges stay
  under min_edge). n=18 now clears the ~15 threshold with a flat/negative
  brier_delta — no evidence of exploitable edge in this feed; the existing
  playbook treatment (log to funnel, sweep for confirmation, don't force a
  bet at floor) remains correct.

## Action

No strategy edit. Both settlements confirm existing, already-encoded findings
(earnings public-beat-streak no-edge; mlb-spreads clean-feed no-edge) rather
than surfacing anything new. mlb-spreads now has enough settled n (18) to
treat the clean-feed finding as reasonably solid rather than provisional —
noted here for the next deep retro, no playbook change needed since the
playbook already treats this feed as low-edge.
