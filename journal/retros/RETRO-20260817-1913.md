# RETRO-20260817-1913

LIGHT tick same-tick retro per schedule.json carrier rule (a settlement on a
light tick is graded immediately, never deferred).

## Settled: the remaining leg of the Musk Aug15-17 2-day set (watch item)

`8f663df4a762` (<40 tweets, `outside-view-veto` skip): est_prob(Yes)=0.0146
(14-day empirical bootstrap) vs market ask 0.20 (mkt_prob 0.195), No-side
edge 0.175 — vetoed, no bet. **Outcome: Yes** (actual tweet count landed
below 40).

This is the leg RETRO-20260817-1813 flagged as the one that actually
matters: with the two mid brackets (40-64, 65-89) both settling No, the
true count had to be <40 or ≥90, and it landed <40. The outside-view-veto
model was **very wrong** on this specific row — 1.46% on the outcome that
happened — while the market's 20% ask, though also low, was 13x closer.
score.py's `[outside-view-veto]` bucket moves to n=15 brier_delta=+0.0531
(positive = market beat model on average across the bucket) — this
settlement pulls the bucket further from the model, not closer.

**Counterfactual ledger update** (playbook §"Outside-view veto: settled
counterfactual ledger") — this row IS in scope (skip_reason is
`outside-view-veto`, not `category-bar` like its two settled siblings), and
edge (0.175) clears the >0.10 boundary. Added as a 13th row: Side=No,
Result=Yes, CF P&L=**−1.00u** (flat-1u convention). New totals: **13
realizable disagreement trades, 5W/8L, net −3.33u (−$16.65 at $5 flat)**.
Model-generation split: bootstrap sub-class (the only prior generation with
a winning record) moves from 2W/0L (+3.06u) to **2W/1L (+2.06u)** — still
net positive, but the perfect record is gone. Edited
`strategy/playbook.md` directly (table + totals + bootstrap sub-class
narrative); see that file's diff for the exact figures.

## Per DEEP-2026-08-17's own fork discipline: this does NOT decide anything

The playbook is explicit that off-fork rows (this whole 2-day set is
off-fork; the fork is the Aug 18 weekly legs) do not settle the "does
bootstrap deserve different treatment" question — only the pre-registered
Aug 18 weekly set does. So this loss is logged, folded into the ledger
arithmetic, and otherwise left alone: no veto-boundary change, no
bootstrap-generation carve-out. The pattern worth watching for real: if the
Aug 18 weekly legs (settling 2026-08-18T16:00Z, already a watch item) show
the same "confident middle, wrong tails" shape as this off-fork set (mid
brackets overconfident per RETRO-20260817-1813, tail bracket badly wrong
per this retro), that is the point to actually revisit the bootstrap
model's variance — not before.

**Verdict**: no risk.json or discovery.py edit. Playbook edit only, and
it's a bookkeeping update (ledger arithmetic), not a policy change — the
0.10 veto boundary and the fork-decides-later rule both stay as they are.
Updated `schedule.json`'s watch item to drop the now-fully-settled Musk 2d
set and note the Aug 18 weekly fork's decision test remains the live item.

No open ledger positions this tick (0/21 open) — nothing to monitor.
