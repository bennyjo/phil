# DEEP-2026-09-07

Audit window: 50d4f1a (DEEP-2026-09-06, 04:48Z) → 95f1259 (2026-09-07
04:31Z). Sync: container started shallow with a stale local main;
`git fetch --unshallow` clean (forced-update line only, the known shallow
artifact), local main strictly 56 commits behind origin/main with a real
merge-base — fast-forwarded via `checkout -B main origin/main`, no genuine
divergence. `resolve.py`: 0 ledger settlements (5 open), 0 forecasts
settled at retro time.

## (a) Calibration and P&L

Nothing settled for money in the window (last ledger settlement 09-04),
so the cumulative picture moves only by the one new position.

**Ledger (cumulative, n=28 settled):** 9W/19L, −$36.78, brier_agent
0.3335 vs market 0.2111 (delta +0.1223), luck-adjusted z=−4.24. The
z says the book's own stated probabilities remain badly overconfident —
expected 18.4 wins by our own numbers, got 9. Unchanged shape: every
negative-delta cell is n≤2 (mlb-moneyline n=1 −0.742, mlb-spreads n=2
−0.057, book-devig n=1 −0.073, econ-ppi n=1 −0.023) — **insufficient
data, no edge claim anywhere in the money ledger**. Where n≥4: news n=4
delta +0.476 (0/4, −$20), unclassified n=18 +0.126, soccer n=8 +0.003
(market-par). The losses are concentrated in the pre-edge_class era and
in narrative/timing categories; the standing category vetoes (news
in-play/narrative, weather, AI-leaderboard) all still point the right way.

**Forecast book (n=527, stake-free):** brier_delta +0.0077, z=+0.11 —
market-par overall. Cells with n≥19: soccer n=78 −0.006, mlb-totals n=27
−0.019, tennis-ml n=20 −0.001, wnba-ml n=26 −0.002 (sports ≈ par);
weather n=27 **+0.048**, ai-model-release n=30 **+0.083**,
social-media-postcount n=21 +0.034 (still the losing classes — the
no-bet vetoes on weather and leaderboard remain earned).

**Tracked conditions (mechanical, quoted per standing instructions):**

- **Pre-registered carve-out bar (countable-metric):** ran
  `screen_replay.py events --limit 200` first (58 new mappings), then
  `counterfactual.py ledger`: **not met** — rows 5 (need 5), independent
  events **1** (need 3), brier_delta −0.1367, pnl +$61.68, held-out folds
  3/4. Still one gamma event (GTA VI family). Veto boundary unchanged.
- **Counterfactual ledger (the record):** outside-view-veto 117 rows,
  109 fillable, 47W/62L, +$73.66, brier_delta **+0.0273**, held-out
  +$107.67. The CF pnl stays positive but the vetoed beliefs remain
  worse-calibrated than the market they disagree with; per the operator's
  gnhf-run-4 verdict the veto stays. wide-spread-veto 4 rows, −$2.82,
  delta −0.0553 — too small to act on.
- **Blend[disagreement]:** n=156, w_opt **0.802**, delta-vs-market
  −0.0018. Bar (w_opt ≤0.80 at n≥150, improvement ≥0.002, sustained ×2)
  **not met**, but this is the first pass it approached: the convergence
  trend reversed (0.837@122 → 0.859@144 → 0.802@156) and the optimum
  improvement doubled (−0.0009 → −0.0018). No action — the bar exists
  precisely so a one-pass wiggle doesn't move policy. Next pass checks
  whether it sustains.
- **gnhf policy v3 forward test** (`replay.py --after
  2026-09-02T00:14:36Z`): 99 forward rows, 11 bets, −$9.54, cw_return
  −0.365, brier_delta +0.0181. Under the ≥15-bet bar; direction has been
  negative two passes running. Hands off, but the trend is not friendly.
- **Screener replay:** 18,047 rows; live prompt rev f7ddad12 escalation
  excess s_exc +0.0005 (s_ez +0.2) — no residual skill above the mids;
  screener-prompt.md stays frozen.
- **Real ledger:** 32/32 rows are settle sweeps, empty DepositWallet
  throughout — zero real fills ever; the paper→real funnel has still
  produced no twin. Informational.

**Pending, not booked (standing rule: never book expected wins):** the
two Sachsen-Anhalt election bets are fully repriced in our favor and
awaiting UMA — Grüne ≥7% Yes @0.111 marks ~0.999 (+$40.0 unrealized,
`66131e6b8f76`), AfD-majority No @0.66 marks ~0.998 (`de95e5168de3`).
If they settle as marked, the numeric-poll-plus-Gaussian family gets its
first two real-money wins and politics-general becomes the first
positive cell worth talking about. Grade at settlement, not before.
Iran ceasefire No (`0ed1d77858d7`) settles tonight (Sep 7 23:59Z).

## (b) Audit of hourly-agent strategy edits in the window

Window strategy diff: playbook.md +47, schedule.json (pacing/watch), 
funnel.jsonl +8 (data). No discovery.py, risk.json, or tools edits.

1. **6508de9 (09-06 16:24Z) — playbook: Munich 25°C veto CF backfill.**
   **KEEP.** The agent's own `reconcile.py` caught a dropped table row
   (`bc29b9874a48`, settled 09-06 00:12Z), backfilled it with the
   mechanical ledger quoted as the record per the 2026-09-06 00:30Z
   operator note. Self-caught bookkeeping error, honestly labeled. This
   is the reconcile tool paying rent.
2. **335be45 (09-07 04:15Z) — playbook: HK 26°C veto CF row + retro.**
   **KEEP.** Same-commit table duty honored, mechanical totals quoted,
   correct no-boundary-change ruling at n=1 (same-day weather subclass
   2W/2L). The side note that the blind mech second opinion (0.28) was
   far off both market (0.715) and outcome while the self-model (0.80)
   was close is worth the tracking flag it got — n=1, no action.
3. **schedule.json pacing across the window.** **KEEP.** After the
   operator's 07:40Z lease fix, FULL cycles resumed immediately: 7 FULL
   in the trailing 24h vs min 4, LIGHT ticks cite the pace gate with
   verifiable counts, deferrals name concrete reasons (empty windows,
   UMA-lag positions). Deferral is being used for empty windows, not to
   dodge research — the 04:28Z FULL did real multi-pollster work on a
   new market. The LEASE-403 standing-context watch item was correctly
   pruned once fixed.
4. **New bet `23e40bbbccc5` (MV CDU 11–14% Yes @0.09, edge 0.080).**
   **KEEP, with a named risk.** Discipline clean: edge ≥ min_edge 0.04,
   spread 0.03 ≤ 0.06, one leg per event (siblings recorded as
   forecasts only), poll trend WebSearch-verified, sd sensitivity
   stated. The honest caveat: mean 9.0 comes from the polls, but the
   entire edge comes from the self-chosen sd=2.2 (raw recent-poll
   dispersion ~0.8) — at sd≈1.5 the bracket probability collapses to
   roughly the market's 0.09. sd=2.2 is defensible (German Landtag
   final-poll errors of 2–3 points are historically common, and this is
   17 days out), but the bet is a variance opinion, not an information
   edge. The watch item already pre-registers grading it as the third
   data point of the numeric-poll-plus-Gaussian family — right call.
5. **Sensing (discovery.py, unchanged).** Pool healthy: ~980
   markets/cycle across 4 queries, 300 screened, 15 escalated. The
   funnel's category mix keeps surfacing politics, weather, econ, and
   sports; no evidence this window of a whole category going invisible.
   No change requested.

**No reverts. Third consecutive clean window.**

## (c) Biggest estimation errors of the day

No money settlements, so the candidates are forecast-grade only: the
window's settled forecasts (WSU/Washington no-edge WON, ND no-edge WON,
MrBeast no-edge LOST, UNLV market-agrees LOST, HK weather veto CF WON)
were all coinflip-adjacent, correctly-declined rows — variance, not
reasoning errors, and each retro said so at the right length. The one
live estimation risk created today is the MV CDU sd=2.2 judgment (see
(b)4) — pre-registered for grading at settlement Sep 20.

## (d) Discipline violations

None found. No bets under min_edge, no in-play entries, no spread
breaches, one-per-event respected on the MV bracket set, every settling
tick wrote its retro in the same commit, and both veto settlements
carried their same-commit CF table edits. The cycle-count claims in
pacing lines match the v4 command's output.

## (e) Proposals review

- **Lease-403 (2026-09-06 00:13Z, was ENDORSED URGENT):** **ACTIONED**
  (operator, e582d5f + 3141765, 07:40Z note) — `lease.py` now fails open
  with `acquired: true, written: false` on the 403, and this window's
  cycle log confirms the fix works in practice (7 FULL cycles post-fix,
  each logging the acquired/written pair). Marked closed per the
  operator note's instruction. **Zero operator asks now open.**
- No new operator asks this pass. The real-funnel dryness (zero twins
  ever) is worth the operator's awareness but needs no act: it follows
  from `real.allowed_edge_classes` gating on edge classes the paper book
  has barely produced, which is the experiment working as designed, not
  a defect.

## Edits applied this commit

- `strategy/schedule.json`: compacted the Sweden next-PM watch item's
  MTM checkpoint log — it had accumulated 13 near-identical "flat,
  unchanged" checkpoints (09-05 08:22Z → 09-07 04:28Z, all within
  ±$0.36 of entry, never near the 0.10 call-out threshold) totaling
  ~4KB that every tick re-reads. Kept thesis, first checkpoint, and
  latest; added a compaction rule to `watch_items_comment` so bare
  unchanged checkpoints replace rather than append (evidence: the item
  itself; threshold-crossing checkpoints are always kept).
- `journal/proposals.md`: status pass + lease-403 ACTIONED.
- No playbook/risk/discovery/tools changes — the window's evidence
  (nothing settled, all bars unmet) doesn't license any.
