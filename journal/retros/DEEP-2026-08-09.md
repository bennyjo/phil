# DEEP RETRO — 2026-08-09

Window since DEEP-2026-08-08 (~04:40Z): **0 placements, 0 settlements**
across ~24 ticks (7 FULL: 05:33, 08:15, 11:15, 14:16, 18:11, 22:13, 02:12;
rest LIGHT, incl. one LIGHT correctly force-stopped on a transient
Polymarket egress block at 23:13Z). Cash $953.11, n stays 19 settled,
z -2.73, overall brier_delta +0.1237 — unchanged because nothing settled.
Sync this session: clean fast-forward 6bb2664 → 3c12a2c, no divergence.

The window's substance is (1) the odds API went live and immediately
converted the drought's dominant skip reason from "benchmark-unreachable"
into measured "no-edge", and (2) an audit finding **about the deep-retro
process itself**: yesterday's retro claimed an instrument fix it never
applied.

## (a) Calibration and P&L

Nothing settled, so every category/edge-class line is unchanged (n=19;
book-devig -0.0732 at stamped n=1, other +0.2703 at n=1, unclassified
+0.1267 at n=17; only mlb-spreads (-0.0568, n=2) and book-devig are
negative, both deep in small-n territory). No new evidence for or against
any threshold. The one moving number:

- **`d2dd24206542` (US x Iran ceasefire, No @ 0.92)**: oscillated mid
  0.105 → 0.165 → 0.155 across the window, still `closed: false`, still
  zero UMA proposals, 9 days past end date. Open MTM at the last mark
  (mid 0.155, 5.435 shares) ≈ **-$4.16**. The pre-registration stands:
  settles-as-marked ⇒ info-race 2W/4L by decision, state-media rule
  enacts. Nothing actionable; noted so the mark's drift stays visible.

**The window's real calibration event involves no settlement:** the first
clean multi-book feed (odds.py, 2 credits) devigged 7 favorite-framed MLB
-1.5 spreads and 2 WNBA markets in one cycle. Max nominal edge found:
**0.018** (MLB, at the mid — worse at the ask), 0.025-0.035 (WNBA). For
context, the entire settled book-devig graveyard (0/7 pre-power-devig-fix)
came from claimed edges harvested off scraped/stale lines. When the line
is clean and current, the measured edge against tight PM books is ~0.02,
i.e. **below even base min_edge**. This is the strongest evidence yet that
the sports-devig drought is genuine market efficiency, not blocked
sensing — and it arrived on the feed's very first day, before any
settlement. Small-n caveat: one slate, one day, ~9 devigged markets.

## (b) Audit of the hourly agent's edits — all KEEP; one deep-retro claim found unapplied

1. **Odds-API integration section (7fed551, 22:13Z) — KEEP.** Faithful
   transcription of the operator's ground rules (benchmark of record,
   budget on funnel-passed candidates only, channel cited in rationale,
   tennis status back in scope), written before first use. Clean.
2. **EGRESS_BLOCKED vs key-missing distinction (32af7cf, 22:16Z) —
   KEEP.** The agent hit the proxy CONNECT-403 on first cloud use,
   verified it at the proxy layer (`$HTTPS_PROXY/__agentproxy/status`),
   distinguished it from the anticipated "key not provisioned" exit, and
   patched its own just-written playbook section so future cycles log the
   right skip reason. Filed the proposal. This is exactly the "escalate
   suspicious absences" standing rule working.
3. **xtracker post-count exploration (84e218c, 02:12Z) — KEEP; best edit
   of the window.** New category tested under the exploration budget with
   a named hypothesis; found the resolver's own live API
   (xtracker.polymarket.com), caught its `pace` field undercounting
   partial days, extrapolated correctly (~200 vs the field's 176), and
   honestly concluded no-edge because PM's mass already sat on the answer.
   Category ruled IN with a specific reuse condition (late-period, low
   extrapolation variance). The caveat about not trusting the API's own
   pace field is a durable, checkable finding.
4. **Transient-egress durable note (84e218c) — KEEP.** All five
   previously-blocked hosts reachable at 02:12Z; the note ("re-verify
   each cycle, never assume yesterday's block holds") is the right
   general rule and closes the loop on the 2026-08-08 proposals.
5. **Pacing (schedule.json) — KEEP the decisions, with one carried
   defect.** 7 full cycles (min 4), every deferral ≤3h with
   candidate-specific reasons anchored to real boundaries (first pitches,
   CPI dates, slate-in-progress). The 23:13Z forced stop on the gamma
   block followed CYCLE.md's operational note to the letter. But the
   cycle-count instrument is still the buggy one — see (d).
6. **Sensing (discovery.py untouched) — pool healthy; the odds feed
   resolves the standing selection question rather than the queries.**
   Window funnel: 31 researched, 0 bets — benchmark-unreachable 21,
   no-edge 8, market-agrees 1, budget-exhausted 1. The unreachables again
   concentrate in mlb-spreads researched via WebSearch (wrong-day,
   cross-source-contradiction, single-sided traps — all pre-odds.py
   cycles). The last cycle, with odds.py working, had **0 unreachable in
   4 groups**. The sensing layer was never the problem this week; the
   benchmark channel was, and it's now fixed-when-not-flapping. No query
   edits needed today. tags.py taxonomy pass due ~2026-08-12, carried.

Compliance: no agent commit touched protected paths; ledger untouched.
One cosmetic lapse: the 00:15Z commit message dropped its "(LIGHT tick)"
suffix while the log line has it — log is authoritative, no action at n=1.

**Deep-retro process finding (yesterday's session, my predecessor):**
DEEP-2026-08-08 §(f).5 claims "schedule.json — count instrument fixed
(timestamp-anchored count, `(LIGHT tick` exclusion, worked example)".
The commit (6f3e61b) shows schedule.json got only an encoding rewrite —
**the claimed fix was never applied**, and every cycle this window kept
running the buggy `grep -v "LIGHT tick"` command the retro had diagnosed
(counts drifted 7-9 vs hand-counts of 6-8; floor never actually at risk).
A retro that writes down an action it didn't take is a worse failure
class than the bug it described, because retros are the evidence layer.
Fixed for real today (see (f).1) with the concrete command in the file.

## (c) Estimation errors of the day

No settlements, no realized errors, and — for the first time in days — no
near-miss estimation either: every researched candidate died at the
benchmark or the edge stage before an estimate was even at risk. The one
graded judgment call: the 11:15Z cycle discarded a WebSearch China-CPI
"forecast" that claimed a July print released before July ended
(internally impossible — correctly flagged unreliable rather than used).
Benchmark hygiene, not estimation, did all the work this window.

## (d) Discipline violations

**None.** No bets, so no sizing/edge/spread/in-play exposure possible.
Settle+monitor ran on every tick including the egress-blocked one (which
stopped exactly where CYCLE.md says to stop). Odds budget discipline:
6 credits used of the ~10-12/day guidance, all on funnel-passed
candidates, quota file updated. The cycle-count instrument defect carried
from (b).5 is again an instrument bug, not a violation: claimed counts
were copied from the (still-buggy) mandated command, and the true count
(7 full cycles, hand-verified from timestamps) exceeded the floor of 4
all window.

## (e) Proposals review

Three entries carried open; all three are aspects of one story (egress
flapping), resolved by the 02:12Z reachability re-check:

- **2026-08-08 odds.py EGRESS_BLOCKED** — Status set: **rejected (moot —
  transient)**. api.the-odds-api.com was reachable at 02:12Z and served
  the 6-credit sweep; no operator action visibly occurred, so the block
  was flapping, not a standing allowlist gap. Re-file citing flapping if
  the CONNECT-403 pattern recurs.
- **2026-08-08 gamma/clob EGRESS_BLOCKED** — Status set: **rejected
  (moot — transient)**, same evidence (both hosts served every cycle
  after 23:13Z).
- **2026-08-09 informational status update** — Status set: **endorsed by
  deep-retro** as the closing observation; the "re-verify each cycle"
  playbook note is the durable residue. If the pattern recurs, the new
  proposal should cite at least two dated instances so the operator can
  distinguish flapping infra from a stale allowlist.

**No new operator asks today.** The odds-API loop is closed (provisioned,
integrated, first sweep done, budget honored). Everything else actionable
is inside strategy/ and done below.

## (f) Actions applied today

1. **schedule.json notes — count instrument ACTUALLY fixed** (the
   DEEP-2026-08-08 claimed fix, now really in the file): count full
   cycles with a timestamp-anchored awk over lines NOT containing
   "(LIGHT tick" (the parenthesized form only real light lines use), with
   the literal command and a worked example (2026-08-09 window = 7).
   Also records the claimed-vs-applied gap so the failure class is
   visible to future sessions.
2. **playbook.md, Odds-API section — first-sweep evidence appended**:
   clean-feed devig found max edge 0.018 (MLB) / 0.035 (WNBA) across 9
   markets in one slate — allocation guidance: sports devig sweeps are
   now cheap confirmation (2 credits) but a low-yield place to spend
   research minutes; the marginal research hour goes to
   mechanical-resolution non-sports (CPI cluster Aug 10-12, post-count
   brackets late-period, econ-tag markets) where the settled evidence
   (2W-0L mechanical) and the architecture argument already pointed.
3. **risk.json — dated evidence note appended** (no numeric changes;
   n=19 justifies none; first clean-feed test of the min_edge_book_devig
   0.07 floor recorded: non-binding at 0.018 max, the floor has still
   never blocked a clean-feed bet).
4. **proposals.md — three Status lines set** per (e).
5. No reverts: every hourly edit this window was evidence-cited and
   survived audit.

## Watch items for tomorrow

- **China CPI ~Aug 10, US CPI Aug 12 (08:30 ET)**: the econ pivot's
  first big test, now with FRED/BLS direct pulls AND the sibling
  sum-check precedent. The YoY-vs-MoM base-effect arithmetic
  (operator-notes 2026-08-05) is the standing cross-market candidate —
  this is the week the cross-market class either produces its first
  stamped rows or the pivot needs rethinking.
- **Verify the count fix sticks**: next full cycle's line should quote
  the new timestamp-anchored command and its number should match a hand
  recount. (Also: tomorrow's deep retro should verify THIS retro's
  claimed actions actually landed — diff the commit, don't trust §(f).)
- **`d2dd24206542`**: no change; pre-registered grading ready.
- **tags.py taxonomy first pass due ~2026-08-12.**
- **Odds budget pace**: 6 credits on day 1 vs ~15/day linear budget —
  fine, but watch that multi-sport days don't double-spend on slates
  the funnel then kills; the 10-min cache only helps within a cycle.
- **Post-count brackets**: revisit the Aug 4-11 set near period end
  (~Aug 10-11) when extrapolation variance collapses — the stated reuse
  condition from (b).3.
