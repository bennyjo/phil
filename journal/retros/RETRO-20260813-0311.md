# RETRO-20260813-0311

LIGHT tick settlement grading (schedule.json: "if resolve.py settles ANYTHING on
a light tick, that same tick writes the retro"). Zero ledger positions settled
(1 still open, PPI PPI-YoY market, ends 03:59Z, not yet reached); three
forecasts settled, all mlb-spreads cheap-confirmation-sweep no-edge skips from
the 2026-08-12 slate.

## Settled forecasts

- `640ad5053d79` — mlb-spreads — "Spread: New York Yankees (-1.5)" (2026-08-12
  SEA@NYY), est_prob 0.3573 vs market_prob_at_record 0.355, skip_reason
  no-edge (edge ~0.002, core/odds.py 9-book median devig). Settled **WON**
  (Yankees covered). Estimate and market agreed closely; correct no-bet call
  either way.
- `019a7d95be5b` — mlb-spreads — "Spread: Boston Red Sox (-1.5)" (2026-08-12
  BOS@TOR), est_prob 0.3967 vs market_prob_at_record 0.395, skip_reason
  no-edge (edge ~0.002). Settled **LOST** (Toronto won, Red Sox did not
  cover). Estimate and market again agreed closely; a near-coinflip-adjacent
  line landing this way is normal variance, not a miscalibration signal.
- `9431aa902d71` — mlb-spreads — "Spread: Chicago White Sox (-1.5)"
  (2026-08-12 CIN@CWS), est_prob 0.3981 vs market_prob_at_record 0.40,
  skip_reason no-edge (edge ~0.002). Settled **WON** (White Sox covered).
  Same pattern: estimate essentially at market, correctly skipped.

Was the estimate wrong, the fill bad, or normal variance? All three are the
normal-variance case for correctly-skipped no-edge forecasts: no bet was
placed on any of them, no pnl impact, and all three estimates sat within
~0.005 of the market's own recorded price — this is the cheap-confirmation
sweep working as designed (flag when the devig'd fair price and PM's price
already agree, don't force a bet at the floor). 2-for-3 on direction here is
incidental; the sweep isn't claiming directional skill, only price agreement.

## Category verdict

- mlb-spreads: n=21 (score.py), brier_delta=-0.0003 — flat, at/below zero,
  consistent with every prior sweep on this feed. Now well past the ~15
  threshold with a stable near-zero brier_delta across three consecutive
  slates (n=18 → n=21, delta barely moved: -0.0004 → -0.0003). The clean-feed
  finding (PM's MLB spread book already reflects the same devig'd consensus
  this strategy computes, so cheap-confirmation edges stay under min_edge) is
  now solid, not provisional — third independent confirmation.

## Action

No strategy edit. This is a third consecutive confirmation of an
already-encoded finding (mlb-spreads clean-feed, no exploitable edge in the
confirmation-sweep) with a stable brier_delta — nothing new to encode. If a
future deep retro wants to reduce sweep frequency on this specific feed to
save research budget (three straight slates confirming the same null result),
that's worth considering then; not acting on it unilaterally here since it
touches sensing cadence rather than a graded lesson.

## Open-position monitor

`b35963f465b4` (PPI YoY≤5.1% Yes) — entry 0.78, est 0.84. Live quote
(quote.py, full token_id): bid 0.78 / ask 0.82, mid 0.80, spread 0.04.
Not past end date (2026-08-13T03:59:00Z, ~47 min out from this tick — BLS PPI
print not yet released). No action.
