# DEEP RETRO — 2026-09-20

Window: DEEP-2026-09-19 (2aa1eeb, 2026-09-19 04:48Z) → 9bde0ed
(2026-09-20 04:22Z). Sync clean: clone was shallow, `git fetch
--unshallow` completed, local main was strictly 28 behind origin and was
fast-forwarded; no manufactured divergence. `core/resolve.py` this run:
settled 0 of 39 ledger bets (8 open), 0 of 106 open forecasts. The
window itself settled 9 forecasts (all skips, graded same-tick by the
hourly agent) and placed 0 bets. This is the quiet day before the event
that matters: Berlin + Mecklenburg-Vorpommern + Russia settle tonight.

## (a) Calibration and P&L

**Settled-bet book: unchanged.** No ledger settlements this window, so
lifetime numbers stand exactly where DEEP-2026-09-19 left them: n=39,
13W/26L, -$7.02, brier_delta +0.1078, luck-adjusted z -4.38 (the
documented early-book long-shot bias; static). Every cell-level verdict
from yesterday carries forward untouched. The politics-general test
(n=4, +$57.62, delta -0.0502 — thin) settles its deciding rows tonight.

**Forecast stream: 9 settled, all skips, collectively well-calibrated.**
Per-row own-vs-market Brier (negative = beat the mid):

| settled | row | est / mkt | result | dB |
|---|---|---|---|---|
| 09-19 10:11 | WTI touch LOW95 (`18672a24a234`) | 0.89 / 0.815 | hit | -0.0221 |
| 09-19 15:38 | Osasuna ML (market-agrees) | 0.380 / 0.375 | miss | +0.0034 |
| 09-19 20:11 | Moderate Party ≥20% (`no-edge`) | 0.02 / 0.05 | miss (No) | -0.0021 |
| 09-19 20:11 | SD 18–20% bracket (`b019781165c6`) | 0.46 / 0.485 | miss (17.72%) | -0.0236 |
| 09-19 20:11 | Inter ML (market-agrees) | 0.366 / 0.365 | miss | +0.0004 |
| 09-19 22:11 | CS MIBR/FURIA (no-edge, from trigger) | 0.72 / 0.69 | hit | -0.0177 |
| 09-20 04:11 | Colorado/Northwestern (no-edge) | 0.38 / 0.375 | miss | +0.0038 |
| 09-20 04:11 | BYU/Colorado St (no-edge) | 0.903 / 0.885 | hit | -0.0038 |
| 09-20 04:11 | New Mexico/Oklahoma (no-edge) | 0.9502 / 0.935 | hit | -0.0017 |

Sum dB -0.063, mean -0.007/row: the skip machinery is doing what a
calibrated skip machinery should — tracking the market inside noise and
not bleeding. Two rows matter beyond hygiene:

- **SD 18–20% bracket:** certified SD share 17.72%, outside the bracket.
  This is the fifth settled vote-share Gaussian row and it lands the
  same way as the prior four — the realized outcome outside the modeled
  bracket. It feeds the pre-registered empirical-sd fit (5fefc45),
  which runs once tonight's German rows settle. Not re-judged here;
  that is exactly what the pre-registration is for. Note the row itself
  still beat the mid (its sd=1.5 was already wider than raw poll
  dispersion).
- **WTI LOW95:** won, -0.0221 vs mid, but correctly held for the joint
  five-rung ladder grading (~Oct 1) per RETRO-20260919-1011 — one rung
  of a correlated ladder is not a verdict.

Stream-level (score.py, n=718 within its filter): delta +0.0073.
Negative cells at n≥8: mlb-moneyline -0.0499 (n=15), commodities-touch
-0.0498 (n=10), crypto-touch -0.0250 (n=9), commodities -0.0175 (n=9),
box-office -0.0151 (n=12), mlb-totals -0.0110 (n=32), soccer -0.0053
(n=95). Worst: ai-model-release +0.0806 (n=31), market-microstructure
+0.0750 (n=9), weather +0.0529 (n=33), news (+0.0364 at score.py's n=20;
canonical full-stream figure remains ~+0.056 at n=35 per the
DEEP-2026-09-19 reconciliation). All small-n caveats stand; none of
these cells moved enough this window to change any allocation.

**Cell-hygiene finding:** forecast `32f25ca85db8` (BYU vs. Colorado
State — an NCAAF game) was recorded under `mlb-spreads` on the 14:19Z
FULL cycle, flagged as mis-recorded in the same cycle's own summary
line, and settled today. Rows are core-written and immutable, so the
mlb-spreads forecast cell now permanently carries one alien row.
Playbook edit made (see (b)/step 4): verify the category tag before
recording; quote the mlb-spreads cell with the caveat.

## (b) Audit of hourly-agent strategy edits since 2aa1eeb

The hourly agent touched exactly two strategy files all window:
`schedule.json` (pacing + watch items) and `funnel.jsonl` (append-only
telemetry). Zero edits to playbook.md, risk.json, discovery.py, or
tools/ — and that is the correct restraint: nothing settled this window
that licensed a rule change, and the agent didn't invent one. Zero
reverts needed.

1. **schedule.json maintenance (all cycle commits) — KEEP.** Watch-item
   hygiene held: settled forecasts cleared, the Sep-20 joint-grading
   pre-registration and empirical-sd fit items carried forward intact,
   Canada GDP and RBA items carry sourced benchmarks. Deferral reasons
   are dated, quote resolve.py output, and cite the empty pre-election
   calendar honestly.
2. **Pacing — KEEP, with the min exactly met.** Trailing 24h: 4 FULL
   (10:20, 14:19, 00:21, 04:12), 2 TRIGGERED (15:43 newmarket, 20:40
   pricemove), 7 LIGHT. min_full_cycles_per_day=4 met exactly, not
   exceeded — acceptable this window because the calendar genuinely is
   empty until tonight (next econ prints Sep 29-30) and the FULL cycles
   did real work: the 04:12Z cycle re-checked screener divergences
   before deferring, and the 14:19Z cycle researched and correctly
   declined crypto-touch candidates for lack of a named dated vol
   source (touch.py's rule enforced live, second consecutive pass).
   This is effort-saving on empty windows, not research avoidance.
3. **Both TRIGGERED fires legitimate.** 15:43 newmarket (CS
   MIBR/FURIA) produced a properly recorded no-edge forecast that
   settled and beat the mid; 20:40 pricemove fired on the *held*
   Russia UR-sweep token — monitoring open exposure is precisely the
   watch tool's job. Zero banned-shape fires; the watch.py regex ask
   neither gains nor loses pressure this window.

**Sensing audit:** discovery.py unchanged; pool_by_query stable across
the window (liquid-multiday 120–133, by-liquidity 460–582, active-today
343–419); screener collects clean at 300/batch with quota respected.
No new evidence of query-artifact pool shaping; the >336h econ horizon
blind spot remains the known operator-owned gap (lane-coverage memo;
calendar-tier bets since the memo: still 0). One instrumentation
failure, though — see the funnel-weld item below.

**Funnel-weld miss (new, concrete):** the 14:19Z FULL cycle ran the
full funnel (screened=300, escalated=15, 3 forecasts recorded) and
wrote its funnel summary into cycles.log, but wrote **no
`strategy/funnel.jsonl` row** (`grep -c 2026-09-19T14 funnel.jsonl` =
0; commit 690f339 touches no funnel file). Every other FULL cycle in
the window wrote one. This is the first concrete instance since the
2026-09-14 CI funnel-weld proposal was filed — that ask has been
"open, no new instance" for six days and now has its instance. Status
upgraded in (e). Not revertable (the row is simply missing); recorded
here so the funnel-completeness series knows this window is 4-of-5.

## (c) Biggest estimation errors of the window

Nothing bet-sized settled, so the candidates are thin:

1. **SD 18–20% bracket (miss): verdict deferred, correctly.** Whether
   sd≈1.5 is still too tight is exactly the question the
   pre-registered post-Sep-20 empirical-sd fit answers on all settled
   vote-share rows at once; grabbing the answer early off one more row
   would repeat the overreaction the 5fefc45 edit was designed to
   prevent. For the record the row beat the mid.
2. **Colorado/Northwestern (miss at 0.38): variance.** Estimate and
   market within 0.002 of each other; a ~38% underdog winning is what
   calibration looks like, and no edge was ever claimed.
3. **No benchmark errors found.** Across six sports rows, multi-book
   power-devig fair landed within 0.002–0.013 of the live ask — the
   devig pipeline and the market are reading the same books, which is
   the null it is supposed to produce on clean feeds.

## (d) Discipline

Zero bets placed → no min_edge, spread, in-play, or sizing rules were
exercisable; none were violated. Cash $952.98 stable across all 13
window cycles. Open exposure unchanged: 8 positions, the Russia pair
at exactly the $10 event cap and pre-registered for joint grading.
Ledgers written only by core. One standing-infrastructure note: the
14:19Z cycle again ran with the lease acquired but unwritable ("push
refused, proceeding unprotected") — no collision occurred, but that is
luck, not protection; the lease-writability item stays open with one
more dated instance.

Real book: `journal/real-ledger.jsonl` unchanged at 56 rows, all settle
sweeps, zero fills ever. Starvation finding stands; decision dated
post-Sep-20 (i.e., tomorrow, with tonight's settlements in hand).

Mech: zero new requests this window (all cycles cloud, no signer).
Window counters (reset 2026-09-14) unchanged: requests 14, deliveries
13, market-aware with context 9 (seen == sent on all 9), paired ma/v4
sets 4, settled pairs 0.

## (e) Proposals — status pass

No new proposals from the hourly agent this window. My one new item is
an evidence upgrade, not a new ask:

- **CI funnel-weld check (2026-09-14): ENDORSED, RE-URGED — first
  concrete miss.** The 14:19Z 2026-09-19 FULL cycle wrote its funnel
  line to cycles.log but no funnel.jsonl row (commit 690f339). The
  proposal predicted exactly this failure shape (a FULL cycle whose
  funnel telemetry silently drops); it now has a dated instance.
  Operator: the check belongs in core/ci.py or validate.py — a FULL
  cycle commit whose cycles.log line contains "FULL cycle" must touch
  funnel.jsonl or fail.
- **Mech parallel-send nonce race (2026-09-17): ENDORSED, unchanged,
  with operator.** No mech traffic this window (cloud, no signer).
- **watch.py banned-shape regexes: ENDORSED option (a), open.** Zero
  banned-shape fires this window (both triggers legitimate); pressure
  unchanged from the three-variants-in-12-days record.
- **Real-twin starvation: open, decision lands tomorrow.** Dated
  post-Sep-20; tonight's politics-general settlements are the input.
  Real ledger still 56 sweeps / 0 fills.
- **Screener day-batch quota refund (2026-09-15): ENDORSED, unchanged,
  with operator.** No failed-tier instance this window.
- **ODDS_API_KEY on operator runner: open, unchanged** (mlb-moneyline
  still the best negative forecast cell, -0.0499 n=15).
- **Lease writability: open, one new dated instance** (14:19Z
  unprotected FULL cycle, above).
- **fold_brier_delta in counterfactual.py: final notice CARRIES to
  DEEP-2026-09-21.** Verified today: the flag is still not in
  core/counterfactual.py. But the trigger condition — the
  German-bracket veto cluster settling — has not occurred yet (those
  markets resolve tonight). Per the dated commitment, the first deep
  retro after settlement does the fold analysis by hand and converts
  the ask to informational; that is tomorrow, not today. No early
  conversion.
- **Mech window line (reset 2026-09-14):** requests 14, deliveries 13,
  market-aware with context 9 (seen == sent on all 9), paired ma/v4
  sets 4, settled pairs 0. Unchanged — no signer this window.
- **Sep 20 pre-registration (standing, re-confirmed for tonight):**
  the four open election rows are TWO event-night decisions (Russia
  set `9074e3f2fd49` + `475edf2e2654`; German set `23e40bbbccc5` +
  `7ec71e307f12`), graded jointly; the empirical-sd fit across all
  settled vote-share rows (now including SD 18–20 at 17.72%) runs once
  the German rows settle.

## Strategy edits this retro (step 4)

One edit, no reverts:

- **playbook.md — record-time category immutability rule** (cited to
  `32f25ca85db8`): verify the category tag before recording, since
  journals are core-written and a wrong tag is permanent; reading rule
  that the mlb-spreads forecast cell carries one NCAAF row. This is a
  hygiene rule protecting the per-cell brier_delta instrument the
  allocation rule steers by — cheap, evidenced, and narrow.

No risk.json change (nothing settled), no discovery.py change (pool
healthy and stable), no schedule.json override (the hourly agent's
pacing was correct and tonight's obligations are already carried by
watch items).

## Verdict

Thin, clean window held to the standard: nine skip-settlements tracked
the market inside noise, the hourly agent edited nothing it couldn't
justify and maintained pacing at exactly the floor with honest
deferrals, and the one real defect found is instrumentation (the
funnel-weld miss), now escalated with its first dated instance.
Everything that will actually move the book — the German/Russia joint
gradings, the empirical-sd fit, the real-twin allowed-classes decision,
and the by-hand fold analysis if the operator hasn't shipped the flag —
is pre-registered and lands tomorrow.
