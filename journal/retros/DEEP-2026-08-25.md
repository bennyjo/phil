# DEEP RETRO — 2026-08-25 (run ~05:2xZ)

Window since DEEP-2026-08-24 (~05:0xZ): **0 bets placed, 0 ledger
settlements, 7 forecasts settled** (2 of them by this session's own
resolve run). The window's real story is infrastructure: the operator
shipped two new instruments (the screener tier and the event-trigger
watcher) plus all four outstanding endorsed proposals in one evening —
the proposals file is now, for the first time since Aug 10, down to a
single open ask. The hourly agent integrated everything promptly and
correctly, handled the first three TRIGGERED cycles with the right
declines, and surfaced a genuine screener defect with evidence. Two
process slips found and repaired here: a funnel taxonomy error that
tripped reconcile (census rows labeled `no-edge`), and a FULL cycle that
forgot to advance its own pacing pointer.

Sync note: session started shallow; unshallowed per the trigger guard,
local main strictly behind origin (0/71 with clean merge-base), plain
fast-forward — no genuine divergence. resolve.py this session: 0/21
ledger settled (AfD open), 2/88 forecasts settled (Kazakh Respublica,
Mariners −5.5).

## (a) Calibration and P&L

**Bets: n=21 settled, unchanged — 7W, −$45.48, ROI −0.433, z −3.08.**
Nothing settled; nothing new to claim. Threshold sweeps unchanged from
yesterday's read: Yes-side negative in every bucket (edge≥0.10 n=7,
0 wins, brier_delta +0.1176), No-side materially better-behaved but
still non-positive (edge≥0.10 n=11, ROI −0.005, brier_delta +0.0190).
By edge class: book-devig still the only settled class with negative
(good) brier_delta, still n=1 — insufficient data, no floor changes.

**Open position MTM checkpoint (pre-registered duty):** AfD
Sachsen-Anhalt No (de95e5168de3, entry 0.66): mid 0.62, unrealized
−$0.30. Trend across checkpoints: −0.04 → −0.23 → −0.30. Yes has
drifted 0.34 → 0.38 against the politpro-derived 0.29 estimate. This is
inside noise for a 13-day-horizon election market, and the watchlist
price-move trigger (±0.05 from entry) plus `--supersede` now cover the
"react if it becomes real" path mechanically. No action.

**Forecasts: n=250 settled (+7 this window... 248 at score time +2 this
session), delta +0.0046, z +0.12 — flat, as it has been all month.**
The 7 settlements:

- **EPL clean-feed batch COMPLETE at 31/31** (Fulham/Chelsea ML + draw,
  both within 0.01 of mid). The opening-weekend characterization —
  soccer 1X2 feeds at this liquidity are efficient, no-edge is the
  correct standing call — is now confirmed at full batch scale with
  zero exceptions. Watch item closed.
- **BTC touch-$80k (fc4a9d5caace, outside-view-veto): Yes won, veto
  RIGHT again.** est 0.5034 vs No ask 0.386 — the first settled
  touch-anytime self-model row, and a real DVOL input did not rescue
  the architecture. Counterfactual ledger correctly extended same-commit
  by the 16:22Z retro (24 trades 8W/16L −9.42u; sums verified). The
  self-model-veto pattern is now confirmed across every family it has
  been tested on.
- **Kazakh Respublica leg (2c297b5fec4a, market-agrees): lost as both
  agent and market expected** (est 0.06 vs 0.09) — clean market-agrees
  row; the foreign-election-reachability exploration hypothesis waits
  on the still-open Auyl leg.
- **Three in-play/architecture-mismatch rows** (WNBA GSV/MIN no-edge;
  MLB Rangers/White Sox O/U est 0.55 vs mkt 0.47 WON; Mariners −5.5 est
  0.335 vs mkt 0.44 WON). The architecture-mismatch skip class is now
  4/4 wins with brier_delta +0.0659 — n=4 and these are calibration-only
  rows recorded mid-game against a moving book; no conclusion beyond
  "the decline rule did its job" (the 02:56Z cycle measured the book
  moving 0.10–0.24 past the research snapshot before the forecast could
  even be recorded — direct evidence for the mismatch rule).

By category nothing crossed a decision threshold. mlb-spreads forecasts
remain the worst sizeable slice (n=26, delta +0.0049, win rate 0.269 on
mostly longshot sides) but it is within noise; social-media-postcount
(n=18, +0.0407) stays under its category bar.

## (b) Audit of the hourly agent's edits

Six substantive acts this window; verdicts:

1. **playbook.md integration of the four operator tool changes (c3492ce)
   — KEEP.** Accurate on all three fronts I could verify: the No-side
   sweep numbers quoted match score.py's actual output; `--supersede`
   documentation matches the shipped semantics including the
   revised_away caveat; the `--confirm-extreme` note correctly warns the
   next genuine wide veto will hit the guard. Integration-before-scan on
   the 20:17Z cycle was the right priority call.
2. **watchlist.json seed (dc9d5ef) — KEEP.** Exactly what the operator
   asked: AfD open position with a sane ±0.05/60-min trigger expiring at
   election day, and four calendar entries whose release times were
   independently verified (PCE Aug 26 12:30Z, BoK Aug 27 01:00Z, Canada
   GDP Aug 28 12:30Z, UMich Aug 28 14:00Z — all correct). This converts
   next week's highest-value windows from "hope an hourly tick lands
   nearby" to "woken within 15 minutes."
3. **reconcile.py TRIGGERED exemption (54bb621) — KEEP.** Correct scope
   fix, not a loosening: TRIGGERED ticks skip the broad scan by design,
   so pool fields are structurally absent; three consecutive
   false-positives trained nothing. The check still binds every FULL
   line. Self-amending one's own audit tool warrants scrutiny — I
   re-read the diff; the FULL-line branch is untouched.
4. **TRIGGERED-cycle judgment (00:41Z, 02:24Z, 02:56Z) — KEEP,
   commendation.** All six new-market fires were live in-play MLB
   markets; all six correctly declined as architecture-mismatch under
   the standing reaction-speed rule, with calibration-only forecasts
   recorded and the dedup/open-position checks run first. The first
   TRIGGERED cycles could easily have become "the trigger fired so I
   should bet"; they didn't.
5. **Screener defect escalation (proposals.md 04:16Z) — KEEP, with one
   correction applied.** The 140-market loss is real and correctly
   attributed, but "first full-scale run, 47%" is wrong: the 00:19Z run
   collected 300/300. Day-1 truth is 7/30 batches (23%) with 0%→47%
   per-run variance. Status set to endorsed with that correction and a
   concrete cheapest-fix recommendation (tolerant unwrap in collect).
6. **Funnel skip_reason on census rows — REVERTED/RELABELED (the
   window's one real error).** The 04:13Z FULL line logged the
   Valencia/Betis sibling-census check as `no-edge` with "no independent
   estimate formed" in the same note — an estimate-bearing label on an
   estimate-free row, which tripped reconcile check 2 at this retro. Two
   identical mislabels on 2026-08-22T20:11Z had already aged out of the
   24h check window unflagged. All three relabeled `census-consistent`;
   taxonomy addition written into the playbook next to the other label
   splits. reconcile now passes clean.

**Sensing (discovery.py unedited; 4/4 queries, pools 908–989).** The
candidate pool is no longer bounded by the agent's own four queries: the
operator's stratified screener now sweeps 300 markets/run including a
code-enforced random tail, which is precisely the audit lane that
answers "is the pool an artifact of the queries?" — day-1 escalations
were mostly stale/exotic (correctly declined), and one screener
escalation (Stevenage/Reading halftime-draw) got a real research pass
that failed on benchmark coverage, honestly. Structural invisibility:
LoL/esports remains benchmark-unreachable (two named failures), not
invisible. No category went dark.

**Pacing (schedule.json).** 8 FULLs + 3 TRIGGERED in trailing 24h vs
min 4 — no deferral abuse; every LIGHT tick had a machine-counted
justification; the 03:12Z collision-guard demotion was correct. One
slip: the 04:13Z FULL never advanced `next_full_cycle_after` (left
stale-past at 03:15Z), which would have made every subsequent tick FULL
— the fail-safe direction, but a contract violation. Repaired here
(→07:15Z, the 3h cap from 04:13Z); prose flag only, first instance;
weld into reconcile if it repeats.

## (c) Biggest estimation errors of the day

**BTC touch-$80k, est 0.5034 vs market ~0.61 Yes — reasoning wrong, not
variance, and the veto already priced that in.** A driftless GBM barrier
model with measured DVOL still called a coin flip on something the
market had at 0.61, and Yes won. This was the named property test for
"does a sourced vol input fix the touch-anytime family?" — answer, on
n=1: no. The class stays vetoed; the counterfactual ledger banked the
avoided loss. Nothing else gradeable: the other six settlements were
at-market rows or in-play calibration rows whose gaps are book-movement
artifacts, not estimation errors.

## (d) Discipline violations

Betting: **none.** No bets placed; no correlated exposure (single open
position); no in-play bets (six in-play candidates all declined); no
spread breaches; odds-quota respected.

Process: **two, both found and repaired here** (neither is a betting
rule): the census `no-edge` mislabel (reconcile FAIL — relabeled, welded
into the playbook taxonomy) and the stale pacing pointer (repaired,
prose flag). Also noted: the 04:13Z funnel line DID correctly carry the
new screener fields (screened/escalated/screener_batches) including the
degraded screened=160 — honest instrumentation under partial failure,
which is the behavior the funnel weld doctrine exists to produce.

## (e) Proposals (journal/proposals.md updated in this commit)

The operator actioned all four endorsed asks on 2026-08-24 (detached-HEAD
guard bf0e8be; revision support 6b29ccd; inverted-outcome guard 0980816;
No-side sweep 877a133) — statuses set to ACTIONED with commit refs. The
new screener output-format proposal is endorsed with the n=2-runs
correction (23% day-1, high variance) and a recommended cheapest fix
(tolerant unwrap of the known wrapper shape in `screen.py collect`).
That is now the only open operator ask. No new asks from this retro.

## (f) Actions applied today

1. **strategy/funnel.jsonl** — three census rows relabeled
   `no-edge` → `census-consistent` (reconcile FAIL on the 04:13Z line;
   2026-08-22 pair same defect). reconcile verified OK post-edit.
2. **strategy/playbook.md** — taxonomy addition: `census-consistent`
   (estimate-free) for consistency checks that form no probability;
   evidence and relabel record inline.
3. **strategy/schedule.json** — pacing repaired (next_full_cycle_after
   03:15Z stale-past → 07:15Z with reason note flagging the 04:13Z
   omission); EPL watch item closed at 31/31; Kazakh item updated
   (Respublica leg settled, Auyl pending); AfD item extended with
   today's MTM checkpoint (−$0.30, trend noted, no action).
4. **journal/proposals.md** — status pass per (e).
5. **strategy/risk.json — deliberately untouched:** zero settled bets
   this window; no floor has new evidence; the sweeps still say the
   floors stay.
6. **No reverts of substance.** Commendations: the TRIGGERED-cycle
   discipline (6/6 correct in-play declines on day one of the watcher),
   the screener escalation with evidence and an honest "not mine to
   fix", and same-commit counterfactual-table maintenance on the BTC
   veto settlement.

## Watch items (carried in schedule.json)

**PCE tomorrow Aug 26 12:30Z — first calendar-triggered cycle; 9 open
rows incl. 3 veto legs.** Then BoK Aug 27 01:00Z (largest open
disagreement, est 0.38 vs mkt 0.69 — the single most informative
settlement pending anywhere in the book); Canada GDP + UMich Aug 28
(**fe954ed9f325 exclusion mandatory** in that grading); MLB weekly sweep
≥Aug 27; screener failure-rate re-check each FULL cycle (n=2 runs);
commodity touch brackets + gas-price set + market-cap ranks ~Aug 31;
JOLTS/ISM Sep 1–3; NFP Sep 5; AfD MTM each deep retro through Sep 6.
