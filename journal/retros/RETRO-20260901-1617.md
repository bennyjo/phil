# RETRO-20260901-1617

Settled this cycle: 0/26 ledger (1 still open, not past end date), 6/47
forecasts (`core/resolve.py`). No bets settled — grading forecasts only,
against `core/score.py`'s stake-free mid baseline.

## Bank of Israel decision pair — BOTH labels settle as one event (watch items closed)

Four forecasts settled, all `market-agrees`, all under `min_edge`, no bets
placed:

| id | question | est | mkt ask | outcome | result |
|---|---|---|---|---|---|
| `631d616bf794` | cut 25bps (Aug label) | 0.20 | 0.24 | Yes | WON |
| `64c89e162ea1` | hold (Aug label) | 0.80 | 0.78 | No | LOST |
| `4a5f7889b9de` | cut (Sept label) | 0.15 | 0.12 | Yes | WON |
| `a989d14ba8ff` | hold (Sept label) | 0.85 | 0.90 | No | LOST |

Both pairs resolved identically (cut / not-hold). This **confirms** the
structural note both were recorded under: the Aug-dated pair (end_date
Aug31, market_ids 2363939/2356727) and the Sept-dated pair (end_date Sep1,
market_ids 2870692/2870693) were two Polymarket labels for the *same*
underlying decision, rescheduled from Aug31 to Sep1. Per the watch item's
own pre-registered rule, agreement (not divergence) closes the item as a
non-event — no filter proposal needed, just count the four-market cluster
as ONE independent outcome, which score.py's per-market brier_delta
accounting already does correctly (it doesn't double- or quadruple-weight
a single real-world fact into the category stats beyond what four
separately-priced order books actually paid/lost).

**Substance of the miss:** both my qualitative read and the market's price
assigned low probability (0.20/0.24 and 0.15/0.12) to what actually
happened (a cut). Neither side had a quantified survey — the Aug17
research note says explicitly "no numeric survey found (qualitative 'most
forecasters do not expect another cut' only)". This is exactly the
category the mechanical-econ carve-out's gate 2 already excludes
("Named, reachable external benchmark, quoted at research time" — a
qualitative "consensus expects X" quote does not qualify). Both forecasts
stayed in the ordinary no-edge/market-agrees path and risked nothing.
First concrete instance of a qualitative-only central-bank read missing
the actual decision: validates gate 2 rather than contradicting it (a
numeric-survey requirement would have flagged this exact case as
unbenchmarked, correctly). n=1 real event — no rule change, logged as
evidence below in the playbook's carve-out section per the existing
BoK-notes pattern. Both BoI watch items (schedule.json) closed.

## ISM Manufacturing PMI (Aug) bracket — clean pre/post-release pair

- `700dfea982ec` (pre-release, 2026-08-26): est 0.28 vs ask 0.26, no-edge
  (edge 0.02 < min_edge 0.04). Modeled N(55.0, sd~1.3) off July's 55.6
  actual and a ~55 TE consensus. LOST — actual printed 54.6, outside the
  55.0-55.9 bracket. A defensible model-implied estimate that missed by
  less than half a bracket width; the no-edge skip correctly avoided
  risking capital on a genuinely close call (edge would have needed the
  ask to be materially cheaper, not the estimate to be more confident).
- `97ed1f58a737` (post-release, supersedes the above, same cycle
  2026-09-01T14:26Z): est 0.001 vs ask 0.001, market-agrees. LOST as
  expected — this forecast was recorded *after* the official print was
  known and correctly assigned near-zero probability; it exists purely
  for calibration bookkeeping (correctly-called near-certainties), not a
  forecasting test. No lesson.

The JOLTS Jul / ISM Manufacturing Aug watch item (schedule.json) updated:
ISM Manufacturing leg is now fully settled and graded above; JOLTS Jul
consensus was never reachable this cycle window and no forecast was ever
recorded for it, so that half of the item stays open only as a dead
watch-item remnant — nothing left to check (JOLTS Jul has already
released; a stale unreachable-consensus watch serves no purpose past its
own release date). Pruning it from schedule.json.

## Category check (mind small n)

`econ-rates` n=6 settled forecasts, brier_delta -0.0009 (flat vs market,
essentially at parity) — no category-level signal, consistent with small
n and this cycle's finding that both sides were guessing on an
unbenchmarked question. `econ-pmi` n=1, too small to read anything.

## Strategy edit

Playbook: added a short evidence note to the Mechanical-econ carve-out
section (§BoK watch-item grading notes) recording this BoI instance as
supporting evidence for gate 2's numeric-survey requirement. No parameter
or rule change — the existing gate already handled this case correctly.

Schedule.json: closed and removed both BoI watch items (Bank of Israel
decision ~08-24 pair; BoI September-decision pair) and the ISM
Manufacturing PMI half of the JOLTS/ISM watch item (JOLTS Jul half
pruned as stale/unreachable-past-release with nothing further to check).
