# DEEP RETRO — 2026-09-23

Window: DEEP-2026-09-22 (d3b9cac, 04:17Z) → b05aba0 (2026-09-23
04:24Z). Sync: clone shallow at start, `git fetch --unshallow` cleared
it (the fetch's "forced update" line was the usual shallow-boundary
artifact); after unshallowing, HEAD == origin/main exactly (0/0
rev-list), checked out main clean. No divergence. `resolve.py` this
run: settled 0 of 41 bets (6 open), 0 forecasts (149 open) — every
settlement in the window was graded same-tick by the hourly cycles.
Zero ledger bets settled since 2026-09-21 and zero placed in the
window, so today is an audit + reconciliation day.

## (a) Calibration and P&L

**Bet book (n=41 settled, lifetime): unchanged from DEEP-2026-09-22**
— 13W/28L, −$17.02, ROI −0.083, brier_delta +0.1039, luck-adjusted z
−4.47. No bets settled or placed in the window; the era table and the
"no era has ever been ahead" read from yesterday stand verbatim.

**Forecast stream, window (39 settled rows):** the day belonged to the
gates. Mechanical CF ledgers (core/counterfactual.py, authoritative):

- **outside-view-veto: now 162 rows / 154 trades / 107 events / 64W/90L
  / +$41.97 / dBrier +0.0349 / held-out +$34.84.** Window added 9
  settled rows, net CF −$32.83 (1W/8L): Resident Evil bracket trio
  −$9.50, Trump-Greenland pair −$3.33 (the Yes-side row was the one
  decline that would have paid), Opus by-Sep30/exact-Sep22 trio
  −$15.00, GPT Luna −$5.00. Every one of the four `ai-model-release`
  No-reads resolved Yes — the model's release-timing skepticism prior
  remains its most reliable calibration failure, and the veto remains
  the only reason it is free.
- **wide-spread-veto: now 17 rows / 15 trades / 8W/7L / −$17.99 /
  dBrier −0.0327.** Still the only slice (with the touch family) where
  own beats the market on calibration; still n-starved and barred by
  max_spread. The Opus exact-Sep22 first read (−$5 CF) joined it.
- **unvalidated-method: 36 rows / 34 trades / 18W/16L / +$25.47 CF /
  dBrier −0.0118.** The two new rows are NRFI ("either team scores in
  1st inning") settles, 1 up 1 down vs market — a DIFFERENT method
  family from the measured-vol touch rows the pre-registered re-grade
  counter is pinned to; they do not advance that counter (correctly
  not counted by the hourly retros).
- **Touch-family counter: unchanged at 5 settled measured rows / 4
  decisions (4W/1L).** Open measured rows settle 2026-09-28 through
  10-01; the 6th row lands within the week. Nothing to do but wait
  honestly.

**Real ledger:** 56 rows, all `settle` sweeps, zero fills ever, none
new in window (last row 2026-09-09). Allowed-classes decision still
gated on the Russia pair (below).

## (b) Audit of the hourly agent's strategy edits (window: 11 commits
touching strategy/, 4 substantive)

- **R1 mech-eval protocol encoded into playbook (32f4653).** KEEP —
  and this is the window's best process catch: the operator's
  2026-09-21 21:25Z note (baseline-always, daily sample-of-3) was
  posted but encoded nowhere the cycle re-reads by default, and
  mech-requests.jsonl showed only 1 full triple before the commit.
  Moving operator guidance into the playbook the moment it binds is
  exactly what "operator-notes the cycle agents cannot reconstruct"
  is supposed to trigger.
- **CF-table same-commit duty (02634b0, 314ee3e, 54b5f49).** KEEP —
  arithmetic audited: the chain 156→158→161→162 rows re-sums correctly
  at every step and today's tool run reproduces the final line exactly
  (162/154/107/64W-90L/+$41.97/+0.0349/+$34.84). The 20:15Z REPAIR
  batch (wide-spread row + two pre-existing gaps, one from 2026-08-19)
  was honest about what found the gaps (a full reconcile run, not the
  tool's own check) and flagged the remaining drift for the deep retro
  instead of hand-waving it — that flag is discharged in (e) and the
  playbook REPAIR below.
- **Screener-prompt either-side-combined trap note (9792014).** KEEP.
  Evidence: a 5-market NRFI batch (out-15.json) all read the
  single-team ~25-30% first-inning rate against a market pricing the
  combined 1-(1-p)^2 ≈ 0.45-0.50, and flagged the whole cluster
  "elevated" — a pure question-reading artifact. The three NRFI
  settles since (0.465/0.545/0.555 markets, mixed outcomes) confirm
  the market was near the true combined rate. This is a sensing fix
  at the screener layer, correctly scoped, narrows nothing.
- **schedule.json pacing edits (0359c55, fe22bc2, 9792014, b05aba0 +
  watch-item checkpoints in 7348b71/9022f4e).** KEEP — audited in (d).

**Reverts: none.** Nothing in the window loosens a floor, narrows
sensing, or claims uncited evidence.

## (c) Biggest estimation errors of the window

1. **The `ai-model-release` No-read, four times in one day (CF −$20
   across Opus ×3 + Luna): reasoning wrong, same prior every time.**
   est 0.45-0.70 No against markets at 0.72-0.855 Yes; all resolved
   Yes. This is a *stable, identified* miscalibration (the forecast
   cell was already the book's worst at +0.0806 yesterday) that the
   veto converts from real losses into CF bookkeeping. No new rule
   owed — the veto IS the rule — but the fork status now cites it as
   the reason gate 1 keeps failing.
2. **Wisconsin/Zambia/GPT-6 backfill rows (surfaced today): old-era
   errors, already understood** — the GPT-6 row (est No 0.72-equiv vs
   market 0.865 Yes, claimed edge 0.57) is the canonical
   headline-grade veto save; entering them as table rows closes the
   documentation gap, changes no conclusion.
3. **NRFI single-team-rate misread (screener layer): benchmark wrong,
   caught before any bet** — the candidate pool briefly contained 5
   phantom "elevated" flags from a formula error, not a market signal.
   Encoded same-day by the hourly agent. This is what a cheap
   screener-level error looks like when the funnel discipline works.
4. **Variance, properly labeled:** the no-edge NRFI row 9deb67b8c195
   (0.45 vs 0.465, settled No) — at-market by construction, graded as
   such, no lesson to extract. Saying so here so a future read doesn't
   mine it.

## (d) Discipline

**Zero bets placed in the window; no floor tested.** No in-play, no
spread breaches, no under-min_edge entries, no correlated additions.
Settlement grading ran same-tick on all four settling ticks, including
the three overnight LIGHT/FULL single-forecast NRFI settles (0013,
0213, 0411 retros) — the DEEP-08-05/08-23/09-02 rules held throughout.
The RBA open position (4ed738b2045b) is deep underwater on its mark
(mid 0.024 vs 0.149 entry) with the benchmark now 86-93% hike vs 78%
at entry; the no-exit-mechanism rule holds, checkpoints are being kept
per the compaction rule, and the settle-day grading spec (PM vs
futures, entry est vs drifted benchmark) is already written into the
watch item — the right behavior for a losing position.

**Pacing:** every pointer move in the window carries a stated,
pool-tied reason; stale pointers were cleared with the staleness named
(reconcile.py catching ~65min and ~2h stalls). Deferral is being used
for empty windows (overnight UNGA-week lull), not to dodge research —
the window's research went to the hard things (Trump-UNGA pre-speech
base rates, R1 triples, RBA benchmark re-read). 39 forecasts settled,
each graded same-tick.

**Sensing:** discovery.py untouched since 2026-08-05; pool_by_query in
the window's funnel rows (84 liquid-multiday / 517 by-liquidity / 379
active-today / 71 econ-tag) shows a market-shaped pool, not a
query-shaped one. The structurally-invisible category is still
sports-with-odds-benchmark on cloud runners (ODDS_API_KEY absent) —
unchanged, re-urged in (e). **New funnel-weld instance:** the
2026-09-22 08:23Z FULL cycle scanned ("Screener 300/300 in 15
batches", cycles.log line 1341) but wrote NO funnel.jsonl row — the
third dated silent drop (after the two in DEEP-2026-09-21's count).
The weld is now missing 3 rows in ~15 FULL cycles; CI ask re-urged
with the new instance.

## (e) Proposals pass

**No new proposals were filed by the hourly agent this window** (the
20:15Z retro's reconcile-drift flag was addressed to the deep retro,
not filed as an operator ask — correctly, since half of it was mine to
do). Statuses:

- **Reconcile drift ("64 field diffs"): RESOLVED-IN-PART today, rest
  is a filed operator ask.** Ran `core/counterfactual.py reconcile`:
  0 hand rows without ledger rows; 9 settled outside-view-veto rows
  never entered as table rows → backfilled in one documentation-only
  REPAIR (playbook, this commit; totals unchanged, batch +$22.55
  5W/4L); 71 "differing" rows decompose as ~60 pure units-convention
  hits (hand tables record dollars at $5 stake, reconcile prints
  1u=pnl/5 — the tool's own totals line says so), plus early-era
  mid-vs-ask edge quotes and fill-model refusals. Historical rows NOT
  rewritten; mechanical ledger stays authoritative for all gates.
  **NEW OPERATOR ASK (core/counterfactual.py):** (i) make reconcile's
  C-differ units-aware (compare hand pnl/5 against ledger u, or print
  both in one unit) — today ~85% of its diff output is false positives
  that bury the real convention breaks; (ii) extend the B-check
  ("settled rows never entered") from outside-view-veto-only to every
  skip_reason with a hand table, at minimum wide-spread-veto — the two
  gaps the 20:15Z REPAIR fixed were invisible to it, one for a month.
- **Funnel-weld CI check: RE-URGED, now THREE dated instances**
  (2026-09-22 08:23Z FULL added; scanned 300, no funnel row). One line
  in core CI closes the class.
- **Real-twin allowed-classes + fold_brier_delta conversion: CARRY,
  triggers intact.** Russia pair still open (marks 0.988/0.994 vs
  0.68/0.883 entries, UMA lag); first deep retro after those
  settlements discharges both. Grade at settlement, not on the mark.
- **settled_ts determinism (option a), wire-nonce 401 retry + CYCLE.md
  sentence, mech delivery-size option: ENDORSED 2026-09-22, with
  operator/Pearl Connect, unchanged.**
- **ODDS_API_KEY: RE-URGED** — mlb-moneyline (−0.0499, n=15) remains
  the book's best cell and remains benchmark-starved on cloud.
- **Lease writability, screener quota refund, watch.py shape regexes,
  subclass auto-tagger mislabel: open, unchanged, with operator.**

**Relaxation fork: NOT MET — third consecutive double-gate failure,
eighth consecutive overall** (recomputed this run, 162 rows: per-fold
dBrier f3 +0.0391 / f4 +0.0484 both positive → gate 1 fails; tool fold
pnl f4 −$59.74, deepened from −$46.18 → gate 2 fails; 107 events ≥ 40
→ gate 3 holds). Status line appended to the playbook fork section.

**Verdict:** a quiet window used correctly. Zero bets, zero
settlement-grading misses, one genuine process catch (R1 protocol
encoding), one screener formula trap caught before it cost money, and
the deferred reconciliation job is done: the CF table now matches the
tool row-for-row on outside-view-veto (B-list will read 0 next run),
with the units convention written down so the next reconcile read
doesn't mistake bookkeeping for drift. The strategy picture is
unchanged from yesterday — the classes that beat the market are still
forecast-only behind honest triggers, the touch family's 6th row
arrives within the week, and the fork's newest fold got worse, not
better. Nothing to promote, nothing to loosen, nothing to revert.
