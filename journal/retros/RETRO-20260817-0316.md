# RETRO-20260817-0316 (LIGHT tick, carrier rule)

## Settled: forecast only (no ledger settlements)

**b9170b30b10b** — Japan Q2 GDP growth (annualized) 0.8-1.6% bracket
(econ-gdp), adjacent to d684f9caff81 (0.0-0.8% bracket, graded last tick,
RETRO-20260817-0214.md). This is the second and final leg of the Japan GDP
watch item in schedule.json.

- est_prob 0.2475 (same Normal(mean=0.80%, sd=1.2pp) model around the JCER
  ESP consensus, unvalidated-sd caveat noted at record time) vs market ask
  0.31/bid 0.26 (mid 0.285) — nominal edge ~0.0625, marginally over the
  min_edge floor but recorded as `no-edge` at the time because the same
  unvalidated-sd caveat that applied to the sibling bracket applies here:
  the model's absolute-probability output wasn't trusted enough to act on a
  floor-adjacent edge. **Not a veto row** (skip_reason was `no-edge`, not
  `outside-view-veto`) — does not belong in the outside-view-veto settled
  counterfactual ledger table (that table is for veto rows only, per its
  own header).
- Outcome: **Yes won** — actual Japan Q2 GDP print landed in the 0.8-1.6%
  band (consistent with the sibling: d684f9caff81's 0.0-0.8% bracket lost,
  so the two complementary brackets agree on where the real print landed).
- brier_delta = model_brier − market_brier = (1−0.2475)² − (1−0.285)²
  = 0.56626 − 0.51123 = **+0.0550** (market beat the model this time — the
  opposite result from the sibling bracket, where the model was well ahead
  at −0.1788).
- score.py confirms: `econ-gdp n=2 brier_delta=-0.0619` (pooled across both
  legs of this event — still net favorable, driven by the veto row's larger
  magnitude miss, but this row alone was a market win).

## No playbook edit

n=2 in econ-gdp is still far below the ~15-settlement floor for any
category verdict (schedule.json's own guardrail), and this row isn't a
veto row so it doesn't feed the counterfactual ledger table. The two legs
now bracket a mixed result (one big model win, one modest market win) —
exactly the small-n noise the guardrail exists to prevent overreacting to.
No risk.json, discovery.py, or playbook.md change; the existing "Known
unknowns" mechanical-econ note already correctly treats econ-gdp as a
separate, still-too-small category (RETRO-20260817-0214.md's language
stands unchanged).

## Action

Watch item "Japan Q2 GDP release..." in schedule.json is now fully graded
(both d684f9caff81 and b9170b30b10b settled) — removed from watch_items.

## Open-position monitor

0 open positions (ledger status: cash $954.52, 21 settled, 0 open).
Nothing to check.
