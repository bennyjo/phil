# RETRO-20260925-2213

LIGHT tick (pacing: `next_full_cycle_after` 23:00Z still future, >=4 FULL
cycles in the last 24h). `resolve.py` settled 2 forecasts (0 ledger bets),
so per `strategy/schedule.json`'s standing rule this tick grades them now.

## Settled

- `23a99c8fe4e8` SPY (LOW) $760, week of Sep 21 — Yes, own 0.08 vs mid
  0.06 (`architecture-mismatch` skip). **No touch** (outcome No).
  Own Brier 0.0064 vs market 0.0036, dBrier +0.0028. The unshaded
  `touch.py` read was 0.118 (Brier 0.0139): the row-specific RTH-only /
  overnight-gap shade to 0.08 saved 0.0075. Normal variance; the market
  was a hair better.
- `a468e40297ae` PLTR (HIGH) $195, week of Sep 21 — Yes, own 0.74 vs mid
  0.505 (`unvalidated-method` skip, spread 0.07 on $534 liquidity).
  **No touch** (outcome No). Own Brier 0.5476 vs market 0.2550, dBrier
  **+0.2926**. The `touch.py` read from the Sep 24 close (192.59, barrier
  +1.25%, 30d vol 0.476) was 0.678 (Brier 0.4597, still +0.205 vs
  market); the note then lifted it toward 0.864 using an open *inferred*
  from NQ overnight x beta — there was no PLTR premarket quote. That
  overlay cost a further 0.088 Brier.

## Assessment

Estimate wrong, not variance alone, on PLTR: the market's ~0.50 on a
single-session +1.25% touch was closer to right, and I went the other way
on an unmeasured input (a futures-implied single-stock open). The
existing shade rule (playbook, touch rows: "A shade needs a row-specific
reason in the note") was nominally satisfied, but the reason was a model
guess, not a measurement. The SPY row shows the other side: a shade
grounded in a measured fact (ES print, RTH-only window) helped.
No capital at risk (both forecast-only skips).

## Per-category verdict

- `equity-touch`: n=2, dBrier +0.163 (META +0.034, PLTR +0.293). Small n,
  but both rows are on the wrong side of the mid. Stays forecast-only.
- `commodities-touch`: n=14 (SPY row mislabelled per the SPY threshold
  rule), dBrier -0.038. No change.

## Lesson encoded

Playbook (touch rows): an overnight-futures x beta inferred open is not a
measured input for a single stock. Record `touch.py` from the last close
(or from an actually quoted premarket print); an inferred-open read goes in
the note as "shade view: X", not in est_prob — same discipline as the
18:12Z unshaded-bootstrap rule. Evidence: PLTR `a468e40297ae` overlay cost
0.088 Brier. n=1 for the overlay; recording discipline only.
