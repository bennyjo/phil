# RETRO-20260919-1811 (FULL cycle, operator machine)

`core/resolve.py` settled 0 ledger bets and 1 forecast this tick. The row is
`unvalidated-method`, not an `outside-view-veto` or `wide-spread-veto`, so the
playbook counterfactual table is untouched. The cloud runner settled and
graded the same row at 10:11Z (origin `ddfe73f`, RETRO-20260919-1011); local
main is still diverged from origin, so the local copy of
`journal/forecasts.jsonl` settled it again here and this retro covers the
local history. The two retros agree.

## 1. WTI LOW $95 touch in September (market 4413392)

- Forecast `18672a24a234`, category commodities-touch, skip reason
  unvalidated-method, recorded 2026-09-17T15:57Z. Own 0.89 (touch.py GBM
  reflection 0.909 on the November contract at 96.27 with OVX 57.49, shaded
  for a stale quote) versus mid 0.815 (bid 0.80, ask 0.83). **Settled WON.**
- Brier own 0.0121, market 0.0342, delta -0.0221. The rung went the model's
  way.
- Estimate wrong, fill bad, or variance? No fill, because the touch-family
  gate is forecast-only. The estimate rested on one reading of the rules that
  the market had underweighted: the Active Month rolled from October (100.78)
  to November (96.27) at the start of the Sep 18 session, which put the barrier
  1.3% away instead of 5.5%. The November contract traded 94.83 on its first
  active session. The edge was a rules read plus a sourced vol input, and it
  resolved inside one session. A counterfactual $5 at the 0.83 ask returns
  +$1.02.
- This is one rung of a seven-rung correlated ladder (LOW95, LOW90, LOW85,
  LOW80, HIGH105, HIGH110, HIGH115). The schedule.json watch item
  pre-registers grading the ladder as ONE event at full settlement (about
  Oct 1). One rung is not a verdict on touch.py with measured vol, so the
  joint grade stays pending.

## Mech row (evaluation window from 2026-09-14, so this row counts)

- Request `phil-20260917-1558-wti95-ma-25`, service 25,
  `superforcaster-market-aware`, context sent with market_prob 0.805. Delivery:
  p_yes 0.79, p_independent 0.74, market_prob_seen 0.805, cls R, r 0.95,
  parse_tier clause.
- Brier at settlement: own 0.0121, market 0.0380 (at 0.805), mech p_yes
  0.0441, mech p_independent 0.0676. Order: own, market, market-aware,
  market-aware blind. **Market-aware did not beat the price, and it did not
  beat me.** Seeing the price helped it (0.74 to 0.79) but it still ended
  below the market on a Yes outcome.
- Why it missed: none of its sources carried the November contract price,
  which the roll clause in my prompt made decisive. Three of its top five
  sources were Polymarket-derived pages. The tool rated the question R 0.95
  and named the right source class (live candles for the active month), then
  did not fetch one. High researchability with the decisive number absent is
  the pattern to watch in the 09-14 window.
- No paired v4 request exists for this market, so there is no with-price
  versus blind pair to grade here.

## Per-category verdict

commodities-touch forecasts with a SOURCED vol input: n=1 settled of the WTI
ladder, delta -0.0221. Too small for any verdict. The touch-family gate stays
CLOSED (forecast-only) until the n>=6 measured-vol re-grade the playbook
pre-registers.

## Lessons and edits

None. A single won rung of an open correlated ladder is not evidence for an
edit, and the ladder grading is pre-registered for full settlement.
`strategy/` is unchanged apart from a settled-rung note on the WTI watch item
in `strategy/schedule.json` at the end of this cycle.
