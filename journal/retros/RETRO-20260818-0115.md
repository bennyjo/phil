# RETRO-20260818-0115

LIGHT tick, carrier rule triggered: `resolve.py` settled 4 forecasts (no
ledger bets). All four are `outside-view-veto` box-office rows from the
six-row watch item in `schedule.json` (5/6 now settled; only the
20-23m Oak Street leg, 561a07b38a98, no-edge, remains open — resolution
lag).

## Settled this tick

| id | question | est vs mkt | skip | outcome_won |
|---|---|---|---|---|
| f0672a1a69e7 | Oak Street 17-20m | 0.29 / 0.455 | outside-view-veto | No |
| 9ad9a1e605a5 | Spider-Man BND 3rd wknd <66m | 0.42 / 0.31 | outside-view-veto | No |
| db6bd4ff0cb7 | Spider-Man BND 3rd wknd 66-68m | 0.31 / 0.54 | outside-view-veto | No |
| 64695326b358 | Spider-Man BND 3rd wknd 68-70m | 0.19 / 0.0695 | outside-view-veto | No |

`score.py` box-office bucket: n=5, brier_delta=-0.0424 (agent beats
market mid on this settled set so far, all self-modeled forecasts, no
bets placed — correctly declined per veto).

## Per-row: wrong estimate, bad fill, or normal variance?

- **Oak Street 17-20m**: model favored No (0.71 vs No-ask 0.57,
  realizable edge +0.14), outcome No — **correct call, would have won**
  counterfactually (+0.75u). Not a wrong estimate.
- **Spider <66m**: model favored Yes (0.42 vs Yes-ask 0.40, thin
  realizable edge +0.02 despite an 0.11 nominal mid-edge), outcome No —
  **wrong estimate**: actual 3rd-weekend total landed ≥66m, above the
  model's N(66.5, 2.5) median-adjacent bracket; the model underweighted
  how much same-day tracking data the market had that it didn't.
- **Spider 66-68m**: model favored No (0.69 vs No-ask 0.50, realizable
  edge +0.19, and the note flagged this as "textbook veto" given a 0.23
  mid-disagreement), outcome No — **correct call, would have won**
  counterfactually (+1.00u), and the largest single divergence in the set
  resolved in the model's favor.
- **Spider 68-70m**: model favored Yes (0.19 vs Yes-ask 0.129, realizable
  edge +0.061), outcome No — **wrong estimate**, same underlying miss as
  the <66m leg (both were Yes-side bets against the market's better
  same-day info; both lost). Given <66m and 66-68m and 68-70m all
  resolved No, the actual 3rd weekend total was ≥70m — the market's
  tighter, higher-centered distribution was closer to reality than the
  model's N(66.5, 2.5).

**Pattern**: the two losses are both Yes-side (model over-estimating a
bracket), the two wins are both No-side (model correctly rejecting a
bracket the market also rejected, just more confidently). This is the
same asymmetry noted in the general veto ledger (Yes-side chronically
worse than No-side) — see the ledger correction below. Read across the
whole Spider-Man 3rd-weekend triple: the market's implied distribution
was shifted right relative to the model's, consistent with the market
having live same-day numbers the agent's tracker-quote research didn't
capture at record time (2026-08-15, one day before opening).

## Ledger extended + a correction found

Extended `strategy/playbook.md`'s outside-view-veto settled counterfactual
ledger with these 4 rows (fill arithmetic: flat 1u at the recorded
ask on the model's side). New totals: **17 realizable trades, 7W/10L,
net −3.58u** (was 13 trades, 5W/8L, −3.33u). Added a new sub-class
paragraph for box-office (2W/2L, −0.25u; same model generation as the
pre-bootstrap behavioral rows but a third distinct domain — kept OUT of
the Aug 18 bootstrap-fork claims per the watch item, and not folded into
the pre-bootstrap self-model 1W/7L line).

While recomputing the running total from all 17 rows individually (not
trusting the prior subtotal), the side-split figures didn't reconcile:
the doc's existing "No-side 4W/4L (+0.17u)" was stale — that P&L value is
the pre-Musk-2d-\<40-settlement subtotal (7 rows), never updated after
that row's loss was added to the 4W/4L count, and got carried forward
mislabeled as the post-settlement total. Recomputing from the table
directly: Yes-side 1W/6L (−4.50u); No-side 6W/4L (**+0.92u**), i.e. the
No-side stream is solidly positive, not "roughly flat" as previously
written. Corrected in the playbook with the recomputation shown, per the
same discipline as the 2026-08-14 PPI fill-arithmetic correction.

## No playbook rule change

n=2 independent box-office events (Oak Street, Spider-Man 3rd weekend) is
far below any category-verdict threshold. The veto boundary and
edge-class taxonomy are unchanged. This tick's only actions: extend the
ledger with evidence, fix the arithmetic error found while doing so, and
update the watch item to reflect 5/6 settled.
