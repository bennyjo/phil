# DEEP RETRO — 2026-08-04

Window since DEEP-2026-08-03: **1 settlement** (`84ec821167d5` box-office,
lost -$5.00), **0 placements** across ~23 cycles, 2 playbook edits by the
hourly agent, and the operator's harness surgery landing mid-window
(core/scan.py `--min-total-volume`, agent-owned `strategy/discovery.py` +
`strategy/schedule.json`, CYCLE.md pacing step, journal/proposals.md).
Cash $953.11, $10 committed across 2 open positions (Iran pair), realized
P&L **-$36.89** on 16 settled.

## (a) Calibration and P&L

### By category (cumulative, n=16 settled)

| category | n | pnl | brier_delta | read |
|---|---|---|---|---|
| soccer | 8 | -$10.92 | +0.0027 | at market; no verdict until ~15 |
| ai-leaderboard | 2 | -$10.00 | +0.4194 | graded, closed (resolver read) |
| box-office | 1 | -$5.00 | +0.3969 | method failure (fact-finality), not a category verdict |
| wnba-* (3 cats) | 3 | -$15.00 | +0.037..+0.06 | n=1 each, nothing to say |
| commodities | 1 | +$0.26 | -0.0016 | n=1 |
| mlb-spreads | 1 | +$3.77 | -0.0405 | n=1 |

Overall brier_delta **+0.0854** — worse than the market, and the calibration
curve is uniformly overconfident: every estimate bucket below 0.6 realized
0-40% against estimates of 25-57% (0.2-0.5 buckets: n=5, 0 wins). The only
negative (good) deltas sit at n=1. Honest summary unchanged: **no category
has demonstrated an edge; the demonstrated results are two specific failure
modes** (proportional devig, now fixed; provisional/interpretable resolution
facts, now fenced by rules).

### By edge class (the more informative cut)

- **Structural, settled, graded by decision: 2W / 2L.** Wins were both
  *mechanical final facts* (`2dc417ed68f6` NG official print +$0.26;
  `1e8dec1078ba` cross-market sibling +$1.76). Losses were both
  *provisional or interpretation-dependent facts* (ai-leaderboard pair
  `7e753de88823`+`0bf9fe3785c6` one decision -$10; `84ec821167d5`
  Sunday-estimate box office -$5). That is now a clean 4-decision pattern
  in one direction, and the fact-finality rule (enacted 2026-08-03 on
  method grounds, confirmed by settlement 2026-08-04) encodes exactly it.
- **Iran pair still open**, 4+ days past end date: `b21e42c123a1` Yes mid
  ~0.022 vs 0.15 entry, `d2dd24206542` No mid ~0.475 vs 0.92, the latter
  oscillating 0.355-0.545 across the window — the market is treating a
  thesis we logged as "structurally impossible" as a coin flip. MTM of the
  $10 committed ≈ $2.9; if both settle as marked, realized goes to ≈ -$47
  and the structural class to 2W/4L. Pre-registered grading
  (DEEP-2026-08-02) stays armed, untriggered.
- **Book-devig**: no new settlements (none placed). Post-power-devig-fix
  settled aggregate is now 6W/4L, **-$1.89** (DEEP-2026-08-03's +$3.11
  minus the box-office loss, which was structural-class but post-fix by
  revision). The 0.07 floor blocked zero bets again — the max clean
  same-line devig edge seen all window was ~0.028 (Tempo/Valkyries) — so
  the floor remains untested rather than restrictive.

### Settlement grading applied

`84ec821167d5` settled lost; the hourly agent applied DEEP-2026-08-03's
pre-registered grading verbatim (method error, not variance; rule already
enacted; no new edit). That is precisely how pre-registration is supposed
to work — no hindsight relitigating, no double-counting the lesson.

## (b) Audit of the hourly agent's window (~23 cycles)

1. **Playbook: tennis `end_date` is a fallback deadline (26cd399) — KEEP.**
   Evidence was concrete (Berrettini/Navone end_date 08-10 vs actual 08-03
   16:35Z) and the finding re-confirmed independently on UCL qualifiers
   twice (21:15Z, 01:15Z cycles: end_date 08-03 18:00Z, real kickoff 08-04).
   A genuine instrument-limitation discovery.
2. **Playbook: scan doesn't flag in-play for single-game moneylines
   (c209378) — KEEP.** WSH/PHI and STL/NYY looked exactly like book-devig
   edges on $170k-295k books and were 13-38 min in-play. This catch
   prevented what would very likely have been two losing "edges". Both
   lessons are sharpened today into one consolidated pre-research rule
   (§f.1) — they were correct but scattered across 40 lines.
3. **RETRO-20260804-0313 — sound.** Applied the pre-registered grading
   exactly, resisted writing a new rule for an already-encoded lesson.
4. **Open-position monitoring: 23/23 cycles, up from ~50%.** The
   DEEP-2026-08-03 §f.2 sharpening (explicit required log line) worked
   immediately and completely. Target met; drop from watch list.
5. **Skip discipline: the best it has been.** ~50 candidates researched
   with named, checkable skip reasons, including several catches that
   would have been losses or garbage estimates: the AOL "Draper stuns
   Tsitsipas" article correctly discarded as a 2022 story; Columbus vs
   Atlas (not Atlanta) same-city mixup caught; a stale Valkyries -310 line
   rejected against a spread-consistent -1087; Gen.G odds date-labeled for
   a different fixture discarded; two in-play MLB games and one in-play
   Dota series detected and skipped. Zero forced bets in a 23-cycle
   drought is discipline, not timidity — every no-bet had a stated
   benchmark failure.
6. **Sensing (strategy/discovery.py) — operator-seeded, agent untouched;
   KEEP as-is.** The three queries produced 424-489 candidates per cycle
   spanning 0-6 days, no fallback triggered. Artifact check: the pool is
   no longer the bottleneck — it now surfaces tennis ($200k+ books),
   UCL qualifiers, MLB/WNBA, earnings, primaries, Musk-count markets. What
   IS structural: the pool is dominated by categories whose *benchmarks*
   are unreachable from the datacenter IP (odds portals, live-score sites,
   xtracker all 403), so visibility moved without researchability moving.
   That is not a query problem; changing discovery.py cannot fix it.
   Watch item: no macro/econ-release candidate (CPI, central banks) has
   appeared in any cycle log despite the operator's test scan finding a
   Bank of Mexico decision — if none appears by DEEP-2026-08-06, add a
   targeted tag query rather than concluding the category doesn't exist.
7. **Pacing (strategy/schedule.json) — never used, and it should have
   been.** 23/23 ticks ran full cycles. The 01:15Z and 02:15Z cycles
   re-derived identical no-edge conclusions on the same UCL ties 55
   minutes apart and said so ("no new information"); 03:20Z partially
   re-checked them again. Deferral exists exactly for this state (every
   candidate recently researched or benchmark-gated). This is the
   opposite failure from the laziness the guardrail fears — it's
   diligence with zero marginal information value. Concrete criteria
   added to schedule.json notes and the playbook (§f.2/f.3). To be
   explicit on the audit question: deferral is NOT being used to dodge
   hard research — it isn't being used at all.
8. **Compliance: clean.** No agent commits touched core/,
   config/protected.json, CYCLE.md, loop.sh, or CLAUDE.md (the CYCLE.md /
   core/scan.py changes in-window are operator commits). Ledger diffs came
   only from core/resolve.py settlements. Nothing to revert this window —
   first window since 07-31 with zero corrective action needed on the
   hourly agent's edits.

## (c) Biggest estimation error of the day

The only settlement (`84ec821167d5`) was graded before it settled; nothing
new to extract from it. The day's real error is **process-level and now
precisely diagnosable: the reachability problem has widened from odds to
event status.** At 04:16Z the agent found real bookmaker lines on two
tennis matches (Draper ~-160/-250, Tsitsipas -175) with plausible paper
edges vs PM's mid-range books — and correctly refused to bet because it
could not establish whether the matches had already been played: Sofascore,
Flashscore, ESPN, TennisExplorer and Olympics.com all 403'd. The
highest-liquidity category the new scan window surfaced (ATP/WTA, $200k+
books) is thus structurally untradeable: not invisible (sensing fixed
that), not unpriceable (lines exist), but *unverifiable*. Escalated to the
operator as a proposal (§e.1) — no strategy edit can fix a blocked
instrument, per the 2026-08-03 lesson about the limits of self-improvement
inside a fixed harness.

Benchmark-wrong vs reasoning-wrong vs variance for the window: no bets, so
no new estimation errors — the drought itself is the datapoint, and it is
benchmark-availability, not thresholds (max clean edge seen 0.028 vs floor
0.07).

## (d) Discipline violations

- **Bets: none possible, none forced.** No under-min_edge fills, no
  correlated additions, no in-play entries (three actively dodged), no
  spread-rule breaches.
- **Process: none.** Monitoring 23/23 (§b.4). The prior window's silent
  rule-skipping pattern did not recur.

## (e) Proposals

`journal/proposals.md` reviewed — one proposal on file (2026-08-03
escalate-suspicious-absences), already actioned by the operator; its
standing rule for the hourly agent is endorsed and now has its first test
case pending (§b.6 econ-candidate absence). Four proposals added/carried
today, all operator-only:

1. **Benchmark + event-status reachability (4th consecutive day, scope
   widened).** Now blocking both estimation (odds 403s) and eligibility
   (live-score 403s → tennis untradeable, §c). An odds API key
   (the-odds-api free tier) plus any one live-score/schedule source would
   unblock the two largest liquid categories in the window.
2. **Iran pair manual UMA look — overdue** (DEEP-2026-08-02 e.4 set
   2026-08-04). 4+ days stuck, ceasefire No marked ~0.475 and oscillating
   ±0.10/day. Only a human can see the UMA proposal/dispute history; the
   grading of $10 of exposure turns on it.
3. **loop.sh git hardening — carried** (checkout -B main origin/main at
   cycle start). Cheap insurance; the 46h silent fork shape remains
   possible.
4. **core/score.py: `edge_class` per ledger row + mark-to-market of open
   positions in score output — carried,** now also matching the operator's
   own 2026-08-03 note ("mark-to-market losses on stuck positions should
   be reported in the score view"). Class-level brier_delta is currently
   hand-assembled in every deep retro; it is the experiment's primary
   question and should be mechanical.

## (f) Actions applied today

1. `strategy/playbook.md` — the two 2026-08-03 timing findings consolidated
   into one **pre-research event-time check** rule (pin actual start
   time/status before research; started or unverifiable → skip), with the
   original evidence blocks kept beneath it. Evidence: both findings plus
   the 04:16Z tennis status-unverifiable skips.
2. `strategy/playbook.md` — new **re-research cooldown + pacing** rule: a
   no-edge/no-benchmark conclusion stands ~2h absent new information;
   when the whole window is concluded-or-blocked, set
   `schedule.json next_full_cycle_after` to the next event boundary
   instead of running another full cycle. Evidence: 01:15Z/02:15Z/03:20Z
   redundant re-research (§b.7).
3. `strategy/schedule.json` — notes sharpened with the concrete deferral
   criteria (all-candidates-concluded test, ≤3h, never past a known event
   start, min_full_cycles_per_day guardrail unchanged).
4. `strategy/risk.json` — **no numeric changes** (n=16 justifies nothing).
   Dated sentence appended to `edge_class_notes`: structural class 2W/2L
   by decision with the win/loss split exactly on fact-finality; 0.07
   book-devig floor blocked zero bets again (max clean edge 0.028).
5. `journal/proposals.md` — §e entries appended.

## Watch items for tomorrow

- Iran pair settlement or UMA movement → apply DEEP-2026-08-02 grading.
- Pacing: is `next_full_cycle_after` actually being set when the window is
  dead? (First real test of §f.2/f.3.)
- Econ/macro candidate absence (§b.6) — query fix due DEEP-2026-08-06 if
  still empty.
- Reachability proposal (§e.1) — if actioned, tennis + major-league
  book-devig finally give the 0.07 floor real tests.
- soccer verdict still pending at n=8.
