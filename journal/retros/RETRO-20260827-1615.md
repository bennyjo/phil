# RETRO-2026-08-27 16:15Z (FULL cycle, forecast settlement)

`core/resolve.py`: 0/22 ledger positions settled (2 still open, neither past
end date — see monitor below). 2/79 forecasts settled since the last retro
(RETRO-20260826-2319):

- `1ad92f91bf9e` Tokyo lowest-temp 26°C on Aug 27 (market 3866784) — est 0.60
  (real-forecast-informed from AccuWeather/world-weather.info, explicitly
  *not* the self-modeled Gaussian architecture) vs mkt mid 0.765, skip
  `category-bar` (weather-Gaussian family is playbook-closed to no-bet
  regardless of edge size, per the KL/Amsterdam inconclusive fork). **LOST**
  (No won — the 26°C bucket did not hit). Recorded for calibration only, no
  unique claim made, no fork this row belongs to. Unremarkable.
- `1fc754021357` BTC >$80k at Aug 27 noon-ET Binance close (market 3746437)
  — est 0.14 vs mkt mid 0.1095, skip `no-edge` (edge 0.03 < min_edge 0.04,
  same self-modeled-diffusion architecture as the touch-anytime family the
  counterfactual ledger already tracks net -9.42u). **WON** (Yes hit — BTC
  closed above $80k despite the model's 14% read). Brier: agent
  (1-0.14)²=0.7396 vs market (1-0.1095)²... using best_ask 0.132 at record,
  (1-0.132)²=0.7534 — agent slightly *beat* the market here despite the low
  probability, but this was a no-edge skip below the betting threshold, not
  a bet claim, and n=1 on a single diffusion draw is not evidence the vol
  assumption is good. No new lesson; the touch-anytime/diffusion critique
  already in the counterfactual ledger stands as-is (this row isn't in that
  ledger — it's a `no-edge` skip, not an `outside-view-veto`, so it doesn't
  belong in that table per the ledger's own scope).

## Grading

Both rows are calibration-only data points that confirm existing, already-
graded architectural conclusions (weather-Gaussian family closed to no-bet;
self-modeled-diffusion no-edge skips are noisy at n=1). Neither introduces a
new pattern, contradicts a standing verdict, or is a pre-registered
watch-item settlement. No playbook edit, no counterfactual-ledger edit.

## schedule.json

No watch-item touches this cycle's settlements — neither market_id/forecast
id appears in `watch_items`. Left untouched.

## Open-position monitor

2 open (AfD Sachsen-Anhalt No @0.66 `de95e5168de3`, ends 2026-09-06; GTA VI
trailer Yes @0.36 `c6f16acc55d9`, ends 2026-09-01) — neither past end date,
no action this tick. Both continue to be covered by the 0.10/60min
watchlist price trigger independent of this pacing clock.
