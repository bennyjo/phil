# RETRO-20260907-1614 (LIGHT tick, cloud)

resolve.py settled 1 ledger bet, 3 forecasts this tick. Per
`strategy/schedule.json`'s standing rule ("settles ANYTHING" covers
forecasts as well as bets, ledger and forecast alike), grading is not
deferred to a later cycle.

## 1. Grüne ≥7% second votes, Sachsen-Anhalt (`66131e6b8f76`, bet)

Entry est 0.18 vs market 0.111 (edge 0.069), $5 Yes stake, placed
2026-09-04 08:28Z. Official result: Grüne 8.9% — comfortably clears the
7% threshold, settled **Yes, WON +$40.05**, brier_delta −0.1179 (agent
beat market: (1−0.18)²=0.6724 vs (1−0.111)²=0.7905).

The estimate wasn't well calibrated in absolute terms — the underlying
4-poll set (INSA 5 / FGW 6 / Infratest 6 / FGW-newest 6, mean ~5.9%) fed
into a sd=1.2 Gaussian that gave only 0.18, while the actual result
(8.9%) was a +3.0pt miss on the poll mean, ~2.5 standard deviations out
under that same-sd model. This is the same poll-miss already flagged in
RETRO-20260907-1006 grading the sibling AfD bet ("the Grüne poll miss
(+3.0 pts) shows the model's inputs can be badly wrong even when its
structure is right"). The bet still won because direction (Yes) was
correct and the market was pricing it even lower (0.111) than the
already-too-low estimate (0.18) — right for a plausibly wrong-strength
reason, not a calibration validation. Fill was clean (entry matched
market_prob_at_entry, no adverse-fill gap). Variance conclusion: this is
now a **second data point** (after the AfD seat-model's own note) that
the sd=1.2 small-party-near-threshold dispersion was too tight for this
specific election's Grüne turnout — n=1 on the bet itself, n=2 counting
the AfD retro's observation of the same poll-mean miss, still below the
~15-settlement bar for a category-level policy change. No risk.json edit
yet; flagging for the next deep retro to watch for recurrence on other
small-party-near-threshold brackets before generalizing.

Paired forecast `5ee44ae81c9f` (same market, `skip_reason: bet`) settled
WON on the same est/outcome — no separate grading needed, it's the
forecast-side record of this exact bet.

## 2. SPD ≥9% second votes, Sachsen-Anhalt (`3df069438d36`, outside-view-veto)

Pre-registered in RETRO-20260907-1006 as a sibling forecast resolving off
the same official Sachsen-Anhalt count. Est 0.22 vs market 0.083 at
record (ask 0.088), Yes-side edge ~0.13 — over the 0.10 outside-view
boundary, declined per standing policy (numeric-polling election, not an
econ print, so the mechanical-econ carve-out doesn't apply). Official
result: SPD 9.3% → **Yes**. The declined Yes-side counterfactual trade
**WINS**: bought at ask 0.088, $5 stake → 56.82 shares → payout $56.82,
CF pnl +$51.82 (+10.36u) — a large win because the ask was far below the
eventual 1.0 payout. This is a veto **miss** (a large one), same failure
shape as the AfD-seat-model precedent flags for behavioral point
estimates that turn out directionally right but the market was priced
even more wrong than the model. Per DEEP-2026-08-23, extending the
playbook's counterfactual ledger table is part of this retro's commit
(below).

## 3. Tokyo lowest temperature 22°C, Sep 7 (`ff7e0fda3407`, outside-view-veto)

Same-day weather self-model, RJTT METAR observation-based, est P(No, i.e.
NOT exactly 22°C)=0.17 vs ask 0.10 (No-side edge 0.07, over floor but
under veto boundary given wide-spread + self-model-class double trigger).
Official result: the JST-day low **was** 22°C → outcome **Yes**, so the
declined No-side counterfactual **LOSES**: bought at ask 0.10, $5 stake
→ 50 shares → resolves worthless, CF pnl −$5.00 (−1.00u). This is a
same-day-weather veto **correct decline** (avoided a loss), consistent
with the standing subclass read (Shanghai/Munich/Miami/Wellington all
correct declines against Guangzhou as the one miss) — reinforces keeping
the veto rather than loosening it on this bracket shape.

### Playbook table extension (see `strategy/playbook.md`, Outside-view veto section)

Ran `python3 core/counterfactual.py ledger --skip-reason
outside-view-veto` after this settlement: 120 settled declined
forecasts, 112 fillable CF trades, 48W/64L, pnl +$115.48 (+23.10u),
brier_delta +0.0271, held-out +$149.49 (was 118 rows, 110 trades, 47W/63L,
+$68.66/+13.73u, brier_delta +0.0293, held-out +$102.67 before these two
rows) — net this batch **+46.82 dollars (+9.36u), 1W/1L**: the SPD miss
(+10.36u) outweighs the Tokyo correct-decline (−1.00u) by a wide margin,
because the SPD ask (0.088) was far cheaper than the Tokyo ask (0.10) for
a similarly-sized edge. By side: no 86 rows/78 trd/39W-39L/+$94.66; yes
34 rows/34 trd/9W-25L/+$20.82 (SPD's win nudges yes-side pnl up but the
yes-side win rate, 9/34 ≈ 26%, stays far below no-side's 50% — yes-side
declines remain the worse class in aggregate even after this win).

Ruling: no boundary change at n=1 on either row. The SPD miss is a real
cost but consistent with the standing read that Yes-side vetoes are the
worse-performing class (9W/25L vs No-side 39W/39L) — this row is a
reminder that occasional large misses are the expected cost of holding
that boundary, not new evidence to loosen it (loosening would have also
let through the Yes-side losses already on this table, e.g. Musk
140-159, TI VISION/Yandex, UMich <49.0). Watch for recurrence on other
numeric-polling near-threshold brackets before proposing a
category-specific carve-out.

## Per-category verdict (ledger, mind small n)

- **politics-general**: n=2 (AfD majority + Grüne ≥7%, both this
  election), win=1.00, pnl=+$42.62, brier_delta=−0.0747 (agent ahead of
  market on both). Both wins on the same election, correlated evidence
  not independent — do not read this as 2 independent confirmations of
  the category.
- **weather**: n=1 forecast (Tokyo 22°C, no ledger bet — vetoed), the
  veto's decline was correct. No ledger-side weather n to grade this
  tick.

Both n's are well under the ~15-settlement bar; no category-limit changes
in `risk.json`.

## Open-position monitor

`python3 core/ledger.py status` shows 4 open positions, none past their
market end date as of 2026-09-07T16:14Z: Sweden next-PM No
(`e746d7e1ba99`, ends 2026-09-13), Iran ceasefire No (`0ed1d77858d7`,
ends tonight 2026-09-07T23:59Z, not yet past), MV CDU 11-14% Yes
(`23e40bbbccc5`, ends 2026-09-20), Bank of Russia hold Yes
(`cd8b25268fe1`, ends 2026-09-11). open-position monitor: none past end
date — no live-book fetch owed this tick.

## Runner lease

`core/lease.py acquire` returned `acquired: false` (held fresh by
`operator`, age 104s of 3000s TTL) — another runner is mid-cycle, so this
invocation ran as a LIGHT tick per step 0, regardless of pacing state. No
lease was acquired here, so none is released in step 9.
