# RETRO-20260909-1425

Settled this cycle (`core/resolve.py`): 0 ledger bets (7 still open), 2
forecasts (70 still open) — both records of the same market/question,
one superseded by the other minutes apart on 2026-09-08.

## Chewy "Consumable" say-the-word forecast — WON

- `e7a452fef1be` (2026-09-08 20:19:57Z, skip_reason `wide-spread-veto`,
  own est 0.93) → superseded 2 min later by
- `ffc3fcdcbaa6` (2026-09-08 20:21:38Z, skip_reason `outside-view-veto`,
  same est 0.93, "LABEL ERRATUM, same estimate" — the DEEP-2026-08-14
  tiebreak correctly reclassifies a >0.10-claimed-edge row to
  outside-view-veto even when the spread gate also fires).

Market: "Will Chewy say 'Consumable' during earnings call?" (4200041),
Q2 FY2026 call 2026-09-09 12:00Z. Settled **Yes** — the term was said.
`ffc3fcdcbaa6` is the graded row (`e7a452fef1be` counts only in
score.py's revised-away bucket, same estimate, no double-count).

**Was the estimate right, the fill bad, or normal variance?** The
estimate was right, decisively: 0.93 vs a settled Yes, on evidence that
"Consumables" is Chewy's own reported revenue segment name, used 5-6
times in the prior-year Q2 transcript. This was never a coin-flip
call — the outside-view veto blocked a real edge (est 0.93 vs ask
0.58-0.59, realizable edge +0.35), and `core/counterfactual.py` grades
the declined trade a win: pnl **+3.62** (yes side, fact-finality
subclass — the tool's own subclass tagger, not a manual label, filed
this next to the fact-finality rows rather than as unlabelled judgment,
which is itself informative, see (a) below).

Per the schedule.json watch item, three specific questions were flagged
for this settlement:

**(a) Should the >0.10 boundary distinguish mechanical-utterance base
rates from behavioural self-models?** This row is evidence for yes, but
weak (n=1). The claim wasn't "I predict Chewy's management will choose
to say a word" (a behavioural guess) — it was "a public company will
use its own official, recurring reported-segment name on an earnings
call," which is close to a structural certainty, closer in kind to the
fact-finality subclass than to a narrative/self-model guess. The
counterfactual tool's automatic subclass tagger independently filed it
under `fact-finality`, not `unlabelled` — that's a second, mechanical
signal pointing the same way. Not enough to propose a carve-out (n=1,
one category, no walk-forward folds of its own), but worth flagging as
a candidate shape if more say-the-word/reported-terminology rows settle:
distinguish "company will repeat its own standard reporting vocabulary"
(mechanical) from "person will improvise particular phrasing in a live
event" (behavioural, e.g. the still-open Vance utterance rows, which
have no comparable "this is their own official vocabulary" anchor).

**(b) Grade the mech's read against the outcome.** `superforcaster-market-aware`
(service 44, blind) returned 0.92, within 0.01 of the own pre-mech
0.93, and correctly resolved Yes. It classed the question
`research_class=NR-utterance`, `researchability=0.15` — i.e. it flagged
the question as *not researchable* while still committing to a
high-confidence number that turned out right. That's the same pattern
flagged for the still-open Vance rows (service 25, also NR-utterance).
One data point, but it now has a confirmed outcome: for this
say-the-word/reported-terminology shape, the mech's confident number was
trustworthy despite its own low-researchability self-report. Retros on
the Vance settlements should compare against this baseline once they
resolve.

**(c) Was the 0.58-0.59 ask ever really takeable at size?** Yes, at the
strategy's $5 stake size. The book was bid 0.10 / ask 0.58 with only 40
shares offered — nominally thin (spread 0.49 vs max_spread 0.06), but a
$5 stake against a 0.58 ask is ~8.6 shares, well inside the 40 available.
So the wide-spread veto, on its own, would have been an overcautious
reason to skip this trade at this stake size; the real, correct blocker
was the outside-view veto (claimed edge 0.35 > the 0.10 boundary), which
DEEP-2026-08-14's tiebreak correctly routes this row through. No rule
change indicated — the spread gate protects against a different failure
mode (execution risk at larger size / on genuinely illiquid books), and
this row doesn't test that mode.

## Counterfactual ledger table extension (DEEP-2026-08-23 rule)

Per DEEP-2026-08-23, a settled `outside-view-veto` (or `wide-spread-veto`)
row's retro commit must extend `strategy/playbook.md`'s hand-kept
counterfactual ledger table in the same commit. Done below (new row,
re-summed skip-reason and side-split totals, event-mapping refresh via
`screen_replay.py events --limit 200` run first).

## Category verdict

`say-the-word` now has n=1 forecast row settled (n=1 ledger-adjacent via
the counterfactual ledger) — far below the ~15-settlement threshold for
a category-level verdict. No playbook boundary change from this alone
(consistent with every prior single-row settlement in this ledger:
Jeddah, Toronto, China CPI all ruled "no boundary change at n=1/n=2").
The lesson this cycle encodes is the (a)/(b)/(c) grading above, written
into the ledger table's ruling text so future say-the-word settlements
have a baseline to compare against, not a numeric threshold change.

No `strategy/risk.json` or `strategy/tools/` edit warranted — no
category limit, edge floor, or spread rule is implicated by a single
correct veto-side settlement.
