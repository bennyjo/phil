# RETRO-20260922-2015

FULL cycle, cloud. Sync: repo was shallow at container start (local
`main` stale at `f772af1`, `origin/main` already at `59501cd` — a
detached-HEAD-from-stale-shallow-cache artifact, not real divergence);
`git fetch --unshallow` cleared it, `git fetch origin main` then showed
zero divergence, `git checkout -B main origin/main` reattached cleanly.
Collision guard: tip (`59501cd`, the 18:15Z cycle) ~1h46m old, well past
the 20min window. Lease: `PHIL_LEASE` unset, `core/lease.py acquire` ->
`acquired=true written=false` (remote refused the custom-ref write, same
cloud-credential limitation as every prior cloud tick) — proceeded
unprotected per the `written:false` rule, never a demotion trigger.
Pacing: `next_full_cycle_after` (16:15Z) had passed, so FULL regardless
of the trailing-24h full-cycle count. CI (`core/ci.py`): `"status":
"success"`. Operator-notes priority check: most recent dated section
(2026-09-21 ~21:25Z, R1 baseline-always) was already integrated into
`playbook.md` in an earlier cycle (`32f4653`) — nothing new to fold in
before scanning.

`resolve.py` settled 0 ledger bets, 6 forecasts (0/41 ledger open
unchanged at 6; forecasts 6/785 settled this pass, 108 still open).

## Settled forecasts, by family

**Claude Opus release markets, two markets / four rows, both resolved
Yes:**

- `3669895` ("...released by September 30, 2026"): `b52006c6d15c`
  (first read, 2026-09-17, est 0.70 vs mid 0.805, `outside-view-veto`)
  superseded by `7f1358aa709a` (re-check, 2026-09-20, est **0.60** vs
  mid 0.855, `outside-view-veto`) — both WON. The re-check moved the
  estimate *down* (0.70 -> 0.60) on a "the leak clock has run out
  without a launch" read, while the market kept climbing (0.805 ->
  0.855) toward the correct answer. Both stayed correctly Yes-leaning
  and both were vetoed (no bet either time), so no P&L follows, but the
  second read moving away from the eventual outcome while the market
  moved toward it is the same shape the category's standing finding
  already names.
- `4627752` ("...released on September 22, 2026", the exact-day leg):
  `3de604a7cf7a` (first read, 2026-09-21, est 0.48 vs mid 0.6855,
  `wide-spread-veto`, spread 0.136 over the 0.06 max) superseded by
  `9ca6adf96fc8` (re-check, 2026-09-21, est 0.62 vs mid 0.732,
  `outside-view-veto`) — both WON. Both mech pairs on this leg (R1
  market-aware + R1 blind + GPT-4.1 baseline, logged in the forecast
  notes) landed well below both own and market (0.10-0.20 range) and
  none of the three retrievals reached the leak that was in fact real —
  a clean case for the R1 record: on this row the mechs were the least
  informative of the four readings, own beat mechs, market beat own.

Read across all four rows: the direction was right every time (Yes),
the market was closer to the truth than the model on every row
(0.805/0.855/0.6855/0.732 vs 0.70/0.60/0.48/0.62), and the vetoes
correctly kept all four forecast-only — this is `ai-model-release`
doing exactly what its standing finding says it does. Category
`ai-model-release` forecast cell after this batch: n=34, brier_delta
+0.0797 (score.py, unchanged bucket-flat since these are the same
outside-view-veto/wide-spread-veto shape already priced into that
number) — no new lesson, no playbook edit owed by this batch alone.

**Say-the-word, Trump UNGA remarks (2 rows, both correctly no-bet, both
LOST but well-calibrated relative to the market):**

- `bc19a0b7b907` ("Million"/"Billion"/"Trillion" 20+): est 0.47 vs mid
  0.51, `no-edge` (No-side edge 0.03, under the 0.04 floor). Outcome No.
  Own brier (0.47-0)^2=0.221 vs market brier (0.51-0)^2=0.260 — own
  narrowly beat the market here, transcript-based reasoning (density
  1.7-2.8/1000 words across three past UNGA speeches) did its job even
  though the floor correctly blocked a bet on a sub-floor edge.
- `01738d0db33f` ("Teleprompter"): est 0.68 vs mid 0.71, `market-agrees`
  (essentially at-market, no realizable edge). Outcome No. Own brier
  0.462 vs market 0.504 — again own slightly ahead, again correctly no
  bet (edge was real but too small to act, not a floor case, just an
  at-market read).

`say-the-word` forecast cell: n=44, brier_delta -0.0019 (flat-to-ahead
of market) — this pair sits right in that band, no lesson, no edit.

## Category verdicts

Neither `ai-model-release` nor `say-the-word` crosses out of its
existing brier_delta band on this batch. No category rule change.

## Mandatory ledger-table duty (schedule.json DEEP-2026-08-23 rule)

Three of this batch's four `ai-model-release` rows are veto rows
(`b52006c6d15c` and `9ca6adf96fc8` outside-view-veto, `3de604a7cf7a`
wide-spread-veto — `7f1358aa709a` is also outside-view-veto but its
sibling-row duty is covered by the same table entry) and go into
`strategy/playbook.md`'s counterfactual ledger table in this same
commit.

While preparing that entry, `core/counterfactual.py reconcile` (run
this cycle to find every settled veto row absent from the table, not
just this batch's) turned up two pre-existing gaps neither caught by a
prior retro nor by the reconcile tool's own outside-view-veto-only
check (it doesn't scan `wide-spread-veto`, so these were invisible to
every automated check that's run against this table before):

- `3723b83673c1` (Berlin Linke 5-10% margin, `wide-spread-veto`, settled
  2026-09-22T02:03:42Z) — graded narratively in RETRO-20260922-0211, and
  independently flagged as a missing table entry by the 10:25Z TRIGGERED
  cycle today ("flagging for the next FULL/retro to backfill"). This is
  that backfill.
- `df7062f3e89d` (Lowe's GAAP EPS beat, `wide-spread-veto`, settled
  2026-08-19T15:21:17Z) — graded narratively in RETRO-20260819-1522,
  never caught since. Its pnl was always inside the mechanical ledger's
  running totals (the gap was purely a missing table row, not a missing
  trade), so no total needed correcting for it, only the row itself.

Both are added below as a REPAIR alongside this cycle's three rows,
same commit, so the reconcile-surfaced gap doesn't sit open a second
time after being found.

See `strategy/playbook.md`'s "Outside-view veto: settled counterfactual
ledger" section for the table rows and re-summed totals.

## Playbook edit

No rule change beyond the mandatory ledger-table extension and REPAIR
above — this batch is four more confirmations of the existing
`ai-model-release` finding (market beats model, veto correctly withheld
the bet every time) and two well-calibrated `say-the-word` no-edge/
market-agrees skips. The `counterfactual.py reconcile` full run (not
just this batch) still shows a much larger pre-existing drift between
the hand table and the mechanical ledger (64 field diffs, hand's own
stated running total does not re-sum against its own rows) that this
cycle did not attempt to fix — that is a deep-retro-scale reconciliation
job, not an hourly one; flagging it here so it isn't lost.
