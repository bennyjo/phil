# DEEP RETRO — 2026-09-12

Window: 1ec9233 (DEEP-2026-09-11, 05:01Z) → 1e2db19 (cycle 20260912-0424).
Sync: repo shallow at start, `git fetch --unshallow` cleared it; local main
strictly behind origin/main by 6 (no divergence after unshallowing),
fast-forwarded via `checkout -B main origin/main`. resolve.py this pass:
0/33 ledger settled (6 open), 0/66 forecasts. Zero bets placed in the
window; two ledger settlements and ~23 forecast gradings landed inside it
(all graded by the hourly ticks on time except as noted in (d)).

## (a) Calibration and P&L

**Bets (33 settled, all-time):** 12W/21L, pnl −$2.02, ROI −1.2%.
brier_agent 0.324 vs brier_market 0.2214 → **delta +0.1026, still well
behind the market**, and luck-adjusted z = −3.72 (own estimates predicted
21.1 wins; got 12). The near-flat pnl is carried by two outliers
(mlb-moneyline 5fbf676cfd7f +$31.23, politics-general 66131e6b8f76
+$40.05); the book's *stated beliefs* remain overconfident. Cells where
brier_delta is negative (ahead of market): mlb-moneyline (n=1, −0.742),
politics-general (n=2, −0.075), mlb-spreads (n=2, −0.057), econ-rates
(n=1), econ-ppi (n=1), and edge-class book-devig (n=1, −0.073). Every one
is n≤2 — **insufficient data; no category has a proven edge**. The
persistent negative masses are ai-leaderboard (n=2, +0.419), news (n=4,
+0.476), product-release (n=1, +0.680) and the unclassified legacy block
(n=18, −$46.89) — all pre-taxonomy shapes the current gates already
exclude.

**Forecast stream (n=591 settled):** brier_delta +0.0069, luck z +0.31 —
market-level overall, no longer worse-than-market. By skip_reason the
model is AHEAD on the rows it either bets or declines for method reasons
(bet −0.0273 n=10; wide-spread-veto −0.0719 n=7; unvalidated-method
−0.0222 n=18; architecture-mismatch −0.0106 n=20) and WORST exactly where
the outside-view veto fires (+0.0392, n=111). The veto keeps standing on
its own evidence.

**Window specifics:** Bank of Russia hold won (cd8b25268fe1, +$2.14; the
consensus-poll benchmark is now 2/2 on decision events per
RETRO-20260911-1244, with the cut sibling d8d34757ae8e consistent —
correctly held low).
Aug CPI cluster: 10 forecast-only rows, 3W/7L; the market's tighter
implied sd beat the unsourced Knotek-Zaman sd on 3 of 4 bracket families
(gate-2 evidence #5, RETRO-20260911-1615) — the one leg the model won
(core MoM 0.2% No, CF +$5.87) got its edge from a *sourced mean shift*,
not the sd. RNC utterance family completed 2W/4L: broad/evergreen phrases
won, narrow thematic guesses lost (see (c) and the new gate).

## (b) Audit of hourly-agent strategy edits in the window

Window diff touches playbook.md, schedule.json, watchlist.json,
funnel.jsonl (data) only — no discovery.py, risk.json, tools, or
protected paths (verified `git log --name-only 1ec9233..HEAD`).

1. **5cad347 — CF table extension, RNC Endorse/America First (−$5.00
   each).** **KEEP.** Totals re-summed correctly (126→128), same-commit
   rule followed, ruling appropriately minimal.
2. **894fa85 — wide-spread-veto catch-up: 3 rows incl. 2 backlog rows
   found by reconciling the tool total against the table.** **KEEP —
   best edit of the window.** It caught its own pipeline's miss (the
   07:28Z triggered tick had skipped a settlement duty, see (d)), added
   the superseded-row subtlety honestly, and kept the n=9 ruling at "no
   boundary change".
3. **0a9105e — playbook: bracket-sibling verification / immediate-post-
   release-book trap.** **KEEP.** The near-miss was real (a 0.2% core
   CPI read off a still-moving book vs the actual 0.3% print; 11 open
   forecasts would have been graded against the wrong number) and the
   rule extracted is precise: an early CLOB read is not settlement
   corroboration; wait for proposed-resolution convergence or a primary
   source.
4. **e081582 — gate-2 CPI evidence note.** **KEEP the note** (arithmetic
   spot-checked against the forecast rows; the 3-of-4-families claim
   holds), **but the commit violated the 2026-08-23 same-commit rule**:
   two outside-view-veto rows settled with that cluster and the CF table
   was not extended. Repaired this pass (playbook DEEP-2026-09-12
   catch-up batch: 5a6d5321c25a −$5.00, 79e72c3002c3 +$5.87, totals now
   130/122/88, 53W/69L, +$105.30, dBrier +0.0276). Logged in (d).
5. **schedule.json pacing (7 edits).** **KEEP.** min_full_cycles_per_day
   = 4 held; every deferral names its evidence, and the 00:14Z FULL
   cycle is the pattern we want: researched the top screener divergence
   (Sept FOMC 25bps leg), found real hawkish catalysts by search, and
   skipped at `market-agrees` instead of forcing a bet. Deferral is
   being spent on empty windows, not on avoiding research.
6. **1e2db19 — watchlist prune (4 expired calendar entries, 3 expired
   price-moves) + SNB Sep24 calendar entry + 2 re-open watches on
   no-edge skips (ManU/ManCity draw devig, SNB no-change).** **KEEP.**
   Each removal names its settled/expired reason; the re-open watches
   are cheap optionality on fresh devig checks.

**Sensing (discovery.py):** unchanged in the window; pool healthy
(1,034–1,073 candidates, all four queries yielding; econ-tag 11). The one
structural blindness — econ prints sitting past the 336h scan horizon —
is core-owned and already documented in `journal/lane-coverage-decision.md`
with a dormant emitter and an operator bar; nothing for the agent to edit.
**Pacing:** no evidence of research-avoidance (see 5).

## (c) Biggest estimation errors of the window

1. **RNC "America First" (11ea1286d8c7, est 0.62 vs mkt 0.26, dB +0.317)
   and "Endorse" (16e13abfec2f, 0.58/0.43, dB +0.164): reasoning wrong.**
   Thematic guessing ("the speech will be econ-themed") with no
   transcript base rate, at claimed edges of 0.15–0.34. Same shape as
   the two only real-money utterance losses ever (Afford e77eef5d06ad,
   Ternus "Hardware" 2659709d25f9, −$5.00 each). RETRO-20260911-0624's
   own caveat is honored: the RNC misses are probably ONE correlated
   theme-miss, so the response is a sourcing gate, not a category ban —
   **enacted: utterance-market base-rate gate** (playbook, DEEP-2026-09-12):
   Yes-side utterance bets require a quoted frequency base rate from ≥2
   comparable prior transcripts; otherwise forecast-only. Pre-registered
   review after 5 settled rows under the gate.
2. **Aug CPI headline family (5a6d5321c25a dB +0.170, ddaab9499bcd
   +0.121): benchmark wrong, not reasoning.** The unsourced sd is the
   failing input — fifth line of evidence; gate 2 already excludes it
   from betting and was vindicated at settlement. No new rule needed;
   the sourced-mean-shift half of the same model is the part that won.
3. **Near-miss process error (not a settled loss): the 0.2% core-CPI
   wrong read** off an immediate post-release book — caught by the
   provisional hedge before it mis-graded 11 forecasts. The hourly agent
   already wrote the rule (0a9105e); nothing further.
4. **Variance, not error:** Tigers ML forecast aa073f2818ef (est 0.92,
   mkt 0.91, lost) — the market agreed; a 9% event happened.

## (d) Discipline

- **Violation 1: e081582 settled two outside-view-veto rows without the
  same-commit CF-table extension** (2026-08-23 rule: "a veto retro
  without a table edit is a violation on its face"). Repaired this pass;
  the narrative grading WAS done on time, so impact is table-lag only.
- **Violation 2: the 2026-09-11 07:28Z TRIGGERED cycle skipped its
  settlement duty** on 906a1a65abc8 (logged "settled 0" while the row
  had settled at 07:23Z). Self-caught and graded one tick later by
  894fa85. Second instance of the class RETRO-20260908-2250 already
  legislated; no new rule — but if a third instance appears, propose a
  mechanical check (resolve.py count vs funnel line) instead of prose.
- No bets under min_edge, no in-play bets, no correlated exposure, no
  spread-rule breaches (zero bets placed). No protected-path edits by
  agent commits.

## (e) Proposals pass

No new proposals from the hourly agent this window. Open operator asks
after this pass: **3** (unchanged) —
1. **Lease writability / simultaneous-start collision** — every cloud
   cycle still logs `acquired=true written=false`. Stands.
2. **Per-fold `fold_brier_delta` in core/counterfactual.py** — hand-
   computed a fourth time today; note for the implementation: the
   all-rows frame (superseded included) is the one that reproduces the
   tool's printed fold pnl exactly, so that is the frame the flag should
   use.
3. **Blend-bar decision (2026-09-11 ask)** — today would be **pass 3**:
   blend_sweep[disagreement] n=184, w_opt 0.768, improvement 0.0024 vs
   market — letter of the bar met again, but improvement weakened
   (0.0030 → 0.0024) exactly as the single-row-fragility analysis
   predicted. The ask stands as filed; recommendation unchanged (require
   robustness, don't ship on the letter).

**Tracked conditions (one-liners):**
- **Veto relaxation fork: NOT MET, fifth consecutive reading** — 130
  rows / 122 CF trades / 88 events / +$105.30 / dBrier +0.0276; per-fold
  dBrier f3 +0.0754, f4 −0.0092. Honest flag: **f4 negative for the
  first time**; if the next batch of declines is also better-calibrated
  than the market, condition (1) comes into genuine contention —
  recompute before assuming stasis.
- **Blend: pass 3 with weakening improvement** (see ask 3).
- **Mechanical-econ carve-out:** no carve-out-band settlements this
  window (the CPI cluster stayed forecast-only under gate 2 — correctly,
  per (a)); tallies unchanged from DEEP-2026-09-11 (rows 6/5 ✓, events
  2/3 ✗, held-out folds 1/3 ✗) — **bar not met, kill switch unarmed**.
- **Screener-value switch:** dormant (fit 573/422 vs bar 1,000/700).
- **Release-calendar memo bar:** emitter dormant, 0 of first 20
  emit-sourced calendar fires accrued.
- **Screener replay re-baseline (rev 6796568): COMPLETED this pass**
  (fourth attempt; the warm cache from yesterday's checkpoints plus a
  resumed refresh sufficed). `screen_replay.py score --since-rev
  6796568`: 3,316 rows / 1,065 markets / 1,047 events / 27 batches.
  Escalation skill: prec 0.906 vs random 0.897 (lift +0.009, z +1.2),
  s_lift +0.0017 of which mids explain +0.0029 → **s_exc −0.0011,
  s_ez −0.3 — no excess screening skill beyond the price under the new
  prompt rev**, same verdict as the old baseline. Blend: w_opt 1.212
  overall (screen adds nothing over mids); divergence≥0.05 slice n=62,
  oos +0.0166 (worse held-out). The stale-baseline flag CLEARS; the
  DEEP-2026-09-01 freeze (stop chasing replay skill; screener is a
  cost-control rank, not an edge source) is re-confirmed on current
  numbers and stays.
- **Mech:** Sweden Moderates-2nd delivery (mech 0.62 conf 0.6 vs own
  0.14 / market 0.18) grades tomorrow with the election. v4-only regime
  per operator note 2026-09-10; market-aware paired count still 0.
- **Real ledger:** 56 rows, zero real fills ever — dry by design.
- **Sep 13 pre-registrations coming due tomorrow:** Swedish election
  night (three open No-side legs e746d7e1ba99, 09fc471ceec1,
  b063db346052 + the DEEP-2026-09-09 watch item) and the Russia UR
  regions bet 9074e3f2fd49. Tomorrow's deep retro must grade all of
  them against the pre-registered benchmarks.

## Verdict

Another zero-bet, zero-new-risk window in which the machinery mostly
graded itself on time; the two lapses (a skipped settlement duty on a
triggered tick, a veto settlement without its table row) were both
caught — one by the hourly agent, one here — and repaired. The real
content of the day: gate 2 collected its fifth and cleanest line of
evidence (the CPI cluster), the utterance family's losses finally had
enough contrast with its winners to support a cheap sourcing gate
(enacted, with review pre-registered), and the veto fork produced its
first honestly interesting fold reading in five passes. The bet book's
all-time z of −3.72 remains the number to stay humble about: the paper
edge, where it exists, is in the declines, the devigs, and the
mechanical prints — not yet in the judgment bets.
