# RETRO-20260926-0015

FULL cycle. `resolve.py` settled 3 forecasts (0 ledger bets), all on the
Trump Truth Social Sep 18-25 weekly (resolved **100-119**).

## Settled

- `f79de91b8c75` 100-119 Yes, own 0.52 vs mid 0.745 (03:22Z, superseded).
  WON. Own Brier 0.230 vs market 0.065, dBrier +0.165.
- `512b4f987fa9` 100-119 Yes, own 0.15 vs mid 0.856 (12:14Z). WON.
  Own 0.7225 vs market 0.021, dBrier **+0.70**.
- `c5737eab7350` 80-99 Yes, own 0.85 vs mid 0.006 (12:14Z). LOST.
  Own 0.7225 vs market 0.00004, dBrier **+0.72**.

## Assessment

Estimate wrong, not variance. At 12:13Z the xtracker API cumulative was
93 and needed +7 in 3h46m; the live Sep 22-29 tracking confirms only ~1
Trump post landed 12-16Z Sep 25 (hourly bins: 12Z 1, 13-15Z 0). So a
burst did not happen — the resolving "Post Counter" figure for the Sep
18-25 period was >= 100 while the API cumulative I read said 93. The
book (80-99 at 0.003/0.009, $10.5k liquidity) knew; both of my notes said
"reason not visible to me" and recorded the API model anyway. The Sep
18-25 tracking has since dropped out of the user's tracking list, so the
exact final count cannot be re-read from the API.

Category-bar skips — no capital at risk.

## Per-category verdict

- `social-media-postcount` forecasts: two rows alone add ~+1.4 Brier
  against the market; the bar on betting this category stays, and this is
  the first case where the resolver-authoritative API itself was not the
  resolving figure.

## Lesson encoded

Playbook (after the 20:15Z resolver-series rule): when a liquid (>= $1k)
sibling prices a bracket < 0.02 / > 0.98 against my API/bootstrap read and
I cannot name why, record within 0.10 of that mid; the model read goes in
the note as "api view: X". n=1 event, recording discipline only.
