# DEEP RETRO — 2026-08-11

Window since DEEP-2026-08-10 (~05:30Z): **0 placements, 0 ledger
settlements** — bet n stays 19, z -2.73, bet brier_delta +0.1237, cash
$953.11, all unchanged. The forecast stream did the window's work: 46
settled (was 18), 93 recorded, and the **first true disagreement row
settled — PLBY, and the outside-view veto passed its first out-of-sample
test** (declined side lost). Everything else the experiment is waiting on
settles in the next ~30h: WI/SC/MN primaries tonight, Elon bracket 15:59Z
today, CPI cluster tomorrow 12:30Z.

Session note (sync): this clone was SHALLOW (2-commit boundary), and
`git fetch` reported a spurious "forced update" — local vs origin looked
like two unrelated 50-commit lineages with no merge-base. `git fetch
--unshallow` revealed plain fast-forward, no divergence, no force-push.
Recovered cleanly, but the trigger prompt's "warn and continue on local
state" rule would have had a less careful session doing surgery on a
3-day-stale tree. Proposal filed (operator owns the trigger prompt).

## (a) Calibration and P&L

**Bets: nothing settled; every slice unchanged from DEEP-2026-08-10.**
n=19, book-devig -0.0732 (stamped n=1), mlb-spreads -0.0568 (n=2) — the
only negative slices, both far under any honest n floor. The one open
position `d2dd24206542` (Iran ceasefire No @ 0.92) is 11 days past end
date, mid 0.065, MTM ≈ -$4.65, still zero UMA proposals ever.
Pre-registered grading unchanged: settles-as-marked ⇒ info-race 2W/4L
and the state-media rule enacts.

**Forecasts: n=46 settled, aggregate brier_delta +0.0041 — still
at-market, still mostly by construction.** By skip reason (hand-computed;
score.py doesn't slice this yet — proposal filed): no-edge n=41 at
-0.0004 (dead on market); market-agrees n=2 at -0.0089;
architecture-mismatch n=2 at **+0.1093** (the market was much
better-calibrated on both SPX/SPY same-day rows — exactly what the skip
reason predicts, and why it exists); smoke-test n=1. The est-bucket
calibration curve shows 0.2-0.4 buckets realizing 0/16, but est ≈ mid on
every one of those rows (correlated same-slate sports skips), so it is a
shared miss with the market, not a differential signal — insufficient
data, and structurally the wrong stream to read calibration-vs-market
from.

**The disagreement gap narrowed by one settlement, and the result was a
loss.** 10 of 93 rows now carry ask-edge ≥ 0.02 (was 6/55). The first to
settle: PLBY beat-earnings, est 0.28 vs ask 0.25 — resolved No. On the
only settled row where we disagreed with the market, the market was
right (threshold sweep: edge_min 0.02 and 0.03 buckets, n=1, ROI -1.0).
n=1 is n=1, but the direction matches everything else on record: all
five >0.10-claimed-edge bets lost, and now the first 0.02-0.03 forecast
disagreement lost too. **We still have zero settled instances of being
right when we disagree with the market.** That is the honest state of
the experiment after 12 days.

**Veto pre-registration, graded (partial):** the PLBY leg is decisive
within its n. The 04:16Z re-verification produced est 0.33 vs ask 0.19 —
claimed edge 0.14, declined per the >0.10 veto — and Yes resolved No. A
$5 bet at 0.19 would have lost. **Veto validated at zero cost, instance
one.** Sharp nuance for the revision proposal below: the "materially
sharper" 04:16Z revision (0.33) was WORSE than the stale 00:23Z row
(0.28) against the realized outcome. Revisions are not automatically
improvements; the proposal's suggestion to score revised-away rows as
their own slice is the right design, not a courtesy. Remaining
pre-registrations still open, settling tonight/tomorrow: WI Hong margin
brackets (claimed edges up to 0.117), SC Nordone/Fry round-1 rows
(claimed 0.27 and 0.196 — the two largest disagreements ever recorded),
MN Craig/Flanagan, CPI cluster (15 rows, Aug 12 12:30Z). Tonight is the
single most informative settlement batch this experiment has had; the
next deep retro must grade every one explicitly, veto-validated or
veto-over-fires, per row.

## (b) Audit of the hourly agent's edits

Three playbook additions, zero risk/discovery/tools edits, routine
schedule.json pacing updates. All three playbook edits SURVIVE audit:

1. **Same-day index/ETF direction ⇒ architecture-mismatch (18:26Z
   Aug 10) — KEEP.** Now has settled evidence behind it: both rows
   settled, skip-reason slice brier_delta +0.1093 — the market was far
   better calibrated than the Brownian-bridge model on its own claimed
   edge, on the world's most liquid underlying. The rule correctly
   predicts its own evidence. (The SPY row "won" directionally; the
   23:19Z retro correctly refused to count that as validation of the
   model — right reasoning discipline.)
2. **Low-coverage international elections exploration note (14:39Z
   Aug 10, Zambia) — KEEP.** Model exploration conduct: named
   hypothesis, honest "directional confirmation only, no credible point
   estimate", no invented forecast (honest-estimate rule held), bounded
   revisit condition (an Afrobarometer-covered race). This is what the
   2026-08-05 mandate §3 asked for.
3. **AI-model-release-date exploration note (02:21Z Aug 11, Gemini) —
   KEEP.** Correctly distinguished market-agrees (direction confirmable
   via Bloomberg/Google statements) from benchmark-unreachable (date
   precision not confirmable), and ruled the category out for
   date-precision bets pending a primary source. Fine exploration; note
   it tested reachability of a new resolution-fact TYPE, so it clears
   the DEEP-2026-08-10 anti-loophole rule.

**Pacing (schedule.json) — KEEP, and the count instrument finally
verifies.** v4 first-marker awk prints 6 for the last 24h; hand count of
the window's FULL cycles is 6 (07:47, 11:42, 14:39, 18:26, 22:18,
02:21) — first window since the instrument was built where the published
number, the command output, and a hand recount all agree.
min_full_cycles_per_day=4 genuinely met. Deferral reasons are
event-anchored and honest (researched-recently + next boundary named);
LIGHT ticks in the gaps were genuinely empty windows. No
deferral-to-avoid-hard-research pattern: the FULL cycles in this window
took on exactly the hard stuff (CPI sibling-completeness diff, SC/WI/MN
primary modeling, two category explorations).

**Sensing (discovery.py, unedited this window):** pool 780-1016 per full
cycle across the 4 queries, no starvation. One drift worth watching, not
acting on: the econ-tag query returned 25 → 12 candidates over the
window (CPI markets aging toward resolution); post-CPI it may thin
further. The real sensing debt is the **tags.py taxonomy pass, mandated
2026-08-05, due ~Aug 12 — i.e. now.** No cycle has run it. Tomorrow's
deep retro should treat an unstarted taxonomy pass as an audit finding,
not a watch item. Structural-invisibility risk it would address: the
current 4 queries are volume/tag-anchored, so whole mid-liquidity
categories (weather re-check is the named example) stay invisible.

**Funnel/forecast coverage — CLEAN, with two bookkeeping frays fixed by
rule today (see (f)):** every estimate-bearing skip in the window has a
forecast row (verified by market_id reconciliation, including the 02:21Z
cycle); benchmark-unreachable skips correctly recorded no estimate
(ETOR, GETY, Japan GDP, Zambia). The frays: (i) the 02:21Z funnel entry
omitted forecast_id fields, which made today's audit needlessly
indirect; (ii) `outside-view-veto` is being used for two different
things — "estimate formed, bet declined, forecast recorded" (WI, SC
14:39Z — correct) and "declined to form an estimate at all" (MN Gov
04:16Z, SC 07:33Z — really a no-independent-estimate case). The audit
rule keys on estimate-bearing rows, so the blur weakens exactly the
reconciliation that makes the veto falsifiable.

## (c) Estimation errors of the day

One settled true disagreement, and we were wrong: PLBY (est 0.28 stale /
0.33 revised, market 0.24 → 0.19, resolved No). Classification: the
REASONING was wrong, not the benchmark — the consensus-EPS read was
correct raw material ("~breakeven consensus, bad Q1 miss"), but both
estimates weighted the beat scenario more than the market did, and the
market's extra pessimism (ask drifting 0.25 → 0.19 into the print) was
information, not noise. Same failure family as the standing z=-2.73
overconfidence: when our number disagrees with a liquid price, the price
keeps winning. SPX/SPY: not estimation errors in a fixable sense — the
model was known-naive, the rows existed to price the architecture-
mismatch veto, and they did (+0.1093 against us). The 0.2-0.4
realized-0/16 calibration bucket: shared with the market, correlated
slates — variance, insufficient data.

## (d) Discipline violations

No bets, so no edge/sizing/spread/in-play exposure to violate. Odds
budget: ~45/450 for the month, within guidance, spend on funnel-passed
candidates. **One process breach, self-flagged by the hourly agent:**
the 06:41Z Aug 10 LIGHT tick settled 5 forecasts and wrote no retro
("no retro required"), violating schedule.json's settling-tick-writes-
the-retro rule; the 07:33Z cycle caught it, back-filled the table, and
correctly noted no data was lost (score.py recomputes from
forecasts.jsonl). Fail-soft, honestly disclosed, and the rule already
exists — no new rule, but it goes on the record as this window's one
violation, and a repeat should escalate to proposals.md per the 07:33Z
retro's own framing.

## (e) Proposals review

- **2026-08-10 forecast.py revision support — ENDORSED.** The mechanism
  gap is real and now has a settled worked example: PLBY's graded row
  was the stale 00:23Z estimate while the materially different 04:16Z
  read went unscored. The settlement adds a design lesson the proposal
  already anticipates: the revision was *worse* than the original
  (0.33 vs 0.28 against a No outcome), so score revised-away rows as a
  separate slice — "do my revisions help?" is itself an open empirical
  question, and folding revisions silently into the main stream would
  hide it. Status set below. Low urgency stands; the funnel-note
  workaround held up this window.
- **New proposal (filed): deep-retro trigger prompt — unshallow before
  divergence judgment.** Evidence: today's session. Shallow clone +
  stale refs produced a textbook false divergence (spurious "forced
  update", no merge-base, 50 "local-only" commits). The prompt's
  warn-and-continue-on-local-state rule is correct for real divergence
  but destructive on a shallow artifact: a session that trusted it
  would have audited and edited a 3-day-stale strategy tree. Ask: step
  1 gains `git rev-parse --is-shallow-repository` → `git fetch
  --unshallow origin` before any behind/diverged determination (CYCLE.md
  step 0 arguably wants the same guard; operator's call).
- **New proposal (filed): score.py forecasts section — by_skip_reason
  slice.** Every deep retro now hand-computes brier_delta by skip
  reason, and it is the single most informative forecast slice (the
  architecture-mismatch +0.1093 vs no-edge -0.0004 split IS the
  selection-grading the 2026-08-05 mandate asked for). Machine-computed
  beats hand-computed for the same reason the count command exists.

## (f) Actions applied today

1. **playbook.md, Forecast-ledger section:** PLBY pre-registration
   graded in place (veto validated, instance one; revision-was-worse
   nuance recorded); tonight's WI/SC/MN batch re-pre-registered with
   the explicit grade-every-row instruction.
2. **playbook.md, Funnel instrumentation:** taxonomy rule added — a
   funnel row may use `outside-view-veto` ONLY when a forecast row
   exists (the veto blocks bets, never estimates); "no honest
   independent estimate formable" is `benchmark-unreachable`, whatever
   the reason research stopped. Funnel rows must carry forecast_id.
   Cites: MN-Gov 04:16Z and SC 07:33Z vs SC 14:39Z inconsistency,
   02:21Z missing-ids entry.
3. **risk.json:** dated evidence note appended (no numeric changes at
   bet n=19; floors non-binding again — max clean-feed edge 0.035;
   first out-of-sample veto instance recorded; threshold sweep's first
   settled bucket noted).
4. **proposals.md:** revision proposal endorsed + status set; two new
   proposals filed per (e).
5. **No reverts:** all three hourly-agent playbook edits keep, pacing
   edits keep.

## Watch items for tomorrow

- **Grade tonight's primary batch row by row** (WI Hong 8 brackets, SC
  Nordone 0.27-claimed / Fry 0.196-claimed / Norman, MN Craig
  0.15-claimed / Flanagan): the two largest recorded disagreements
  settle at once; each row is veto-validated or veto-over-fires,
  explicitly. If the vetoed sides sweep losses, the veto has real
  multi-instance support; if Fry or Craig hits, that is the first
  over-fire evidence and the "well-evidenced polling exception"
  question (playbook line 511) reopens.
- **CPI cluster settles Aug 12 12:30Z** — 15 rows, econ-pivot grading,
  plus the standing YoY-vs-MoM base-effect cross-market check.
- **tags.py taxonomy pass is due** — unstarted as of this audit;
  tomorrow it's a finding, not a watch item.
- **Elon Aug 4-11 brackets** settle 15:59Z today (edges 0.03-0.04 both
  directions at record).
- **d2dd24206542**: pre-registered grading unchanged; day 12 past end.
- **Forecast n=46 settled**: crosses 50 within a day; with the primary
  and CPI batches it may reach ~75 by the next deep retro — the first
  n where est-bucket calibration on DISAGREEMENT rows starts meaning
  something.
