# RETRO-20260904-1813

(LIGHT tick — 0 ledger bets settled, 1 forecast settled. Per
`schedule.json`'s DEEP-2026-08-05/09-02 rule, settlement grading on
forecasts is never deferred to a later tick, so this LIGHT tick writes
the retro in full despite skipping scan/research.)

## Settlement this tick

`6aff2db6ddbe` — Shanghai highest-temperature-31°C-on-Sep4 bracket
(same-day row, ZSPD partial-day-max method). est Yes=0.58 vs market
Yes=0.885 (bid 0.86/ask 0.91) at record time. Outcome: **Yes** (31°C
was the actual high) — **WON** as a forecast (est 0.58 was on the
correct side of 0.5), but the *disagreement* with the market ran the
other way: the model was less confident on Yes than the market, so the
relative-value side was No (No ask 0.14, i.e. `1 - best_bid`).

## Was the estimate wrong, the fill bad, or variance normal?

Not a bad model — directionally correct (0.58 > 0.5, right side) and
using the playbook's mandatory same-day partial-day-max conditioning
(ZSPD METAR showed 31°C already on the record by 03:30Z with a further
+0.5–0.9°C of runway modeled at sd=0.6). The market was simply more
confident than the model given the same partial-day data: it priced
Yes at 0.885 against a live top-of-book that had already touched 31,
while the model's Gaussian runway calc left real (if minority) mass on
32+. Brier-wise this is a case of correct direction, insufficient
confidence — worth noting for the weather-Gaussian sd calibration but
not, at n=1, a reason to touch sd=1.2/0.6 (unvalidated, standing no-bet
discipline, see below).

## Counterfactual ledger duty (DEEP-2026-08-23 rule: any settled
outside-view-veto/wide-spread-veto row extends the table same-commit)

This row's `skip_reason` is `outside-view-veto` (weather category is
playbook-closed to real capital regardless of edge size). Added to
`strategy/playbook.md` §Outside-view veto: settled counterfactual
ledger — one new row (No side, realizable edge +0.280, entering No at
the 0.14 ask; actual outcome Yes → **loss, −1.00u**). Resummed totals:
**94 realizable trades, 46W/48L, net +20.29u**; Yes-side unchanged
4W/21L −10.61u; No-side 42W/27L +30.90u (adds this loss). Check:
−10.61 + 30.90 = 20.29 ✓.

## Ruling

No category-verdict change: this is a single same-day weather row, the
category stays no-bet per the standing weather-Gaussian discipline
(unvalidated sd, n too small, prior contradictory KL/Amsterdam pair).
The veto correctly avoided a real loss here (the relative-value side
lost), which is the same shape as the Astra/NFP batches graded
recently — declining a large nominal disagreement is not free money
when the market's confidence turns out to be the better-calibrated
read. No playbook edit beyond the ledger table; nothing else settled
this tick to grade.
