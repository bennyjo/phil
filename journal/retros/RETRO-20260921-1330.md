# RETRO-2026-09-21 13:30Z

LIGHT tick (pacing), operator machine, paper mode. `resolve.py` settled no
ledger bets (still 40 settled / 7 open) and two forecasts since
RETRO-20260921-1224. Neither row was ever bet.

- **MV Landtag, Linke third-most seats** (`4c12ab5dc28e`, market 3393069,
  own Yes 0.90 vs mid 0.91, `market-agrees`): **WON**. Own Brier 0.0100 vs
  market 0.0081, market ahead by 0.0019. Three polling houses agreed on a
  2-3 pt Linke lead over CDU and the result held. Estimate fine, skip
  correct, variance normal. Nothing to learn beyond one more clean
  `market-agrees` row in the numeric-polling general-election family.
- **Russia, UR gains most seats** (`5ece5e87793d`, market 1130012, own No
  0.35 vs mid 0.245, `ambiguous-resolution`): **LOST**. Own Brier 0.1225 vs
  market 0.0600, market ahead by 0.0625. This is the third and last leg of
  the seat-GAIN event; RETRO-20260921-1120 already graded the cause on the
  preliminary count (UR 355 seats, +31 vs NL +7 on the 2021 baseline, wider
  on the current-factions baseline). The provisional numbers in that retro
  are now the booked numbers. The clause ambiguity was moot; the estimate
  was wrong because the UR seat centre (331, built from the VCIOM forecast)
  undershot by 24 seats. The playbook managed-election centring note from
  that retro covers it; its `pending` marker on this row is replaced with
  the settled numbers in this commit.

## Seat-GAIN event, closed

Three forecast-only legs, 0 for 3 against the book: NL +0.0211, LDPR
+0.0019, UR +0.0625 (sum +0.0855, mean +0.0285 per leg). All three errors
share one cause, so this is one draw, not three. The decline itself was
right: the stated edge on this leg was 0.10 on the No side, and a bet
would have lost $5. The reason on the label (`ambiguous-resolution`) was
not the reason the number was wrong, which is the more useful finding and
is already encoded.

## Mech pair on 1130012: first settled pair in the post-2026-09-14 window

Service 21, same prompt, same context, requests dated 2026-09-17 (rows 66
and 68 of `journal/mech-requests.jsonl`; row 67 is the nonce-race
rejection, unpaid). Question frame, P(UR gains most), outcome Yes:

| source | p | Brier |
| --- | --- | --- |
| market-aware `p_independent` | 0.88 | 0.0144 |
| market-aware `p_yes` (price shown, `market_prob_seen` 0.755) | 0.86 | 0.0196 |
| book mid | 0.755 | 0.0600 |
| own, post-mech (recorded) | 0.65 | 0.1225 |
| v4, blind | 0.62 | 0.1444 |
| own, pre-mech | 0.59 | 0.1681 |

- Market-aware beat the price, me, and the paired v4 request. That is the
  operator's question answered Yes on this pair.
- The price did not help it: `p_independent` 0.88 scored better than the
  price-informed 0.86. The advantage over v4 came from retrieval (page
  bodies against snippets only, noted at request time), so this pair is
  weak evidence about "shown the price" and better evidence about the
  tool's research path.
- The mech moved my recorded number from 0.59 to 0.65, the right
  direction, worth 0.0456 of Brier on this row. I under-weighted it: the
  delivery had researchability 0.95, class R, `parse_tier` clause, no
  truncation, and I took about a quarter of the gap.
- Caveat from the request note stands: the serper hits included third-party
  odds (cryptorank Yes 0.74, the Polymarket embed at 63c), so
  `p_independent` was not fully price-blind.

Window tally after this settlement: market-aware settled rows 2 (UFO:
beat own, lost to mid; UR-gain: beat own, mid and v4), settled pairs 1
(market-aware ahead of v4). n is far too small for a weighting rule; no
playbook change to how I weight mech deliveries yet. Re-read at 5 settled
pairs.

## Category read

`politics-general` forecasts take one win and one loss. Bets in the cell
are unchanged at n=5, brier_delta -0.0328, pnl +$52.62. No cell crosses
any threshold on these two rows.

## Strategy edits

- `strategy/playbook.md`: the managed-election centring note now carries
  the settled UR row instead of `pending`, and a dated mech-pair note
  records the table above with the 5-pair re-read trigger.
- `strategy/schedule.json`: the seat-GAIN watch item is discharged and
  removed; the Sep 20 joint-grading item keeps the two open Russian bet
  legs.

## Operator warning: local main and origin/main diverged again

At sync (13:26Z) local was ahead 2 / behind 1, merge-base `4e25d5a`. The
cloud runner and this machine both ran a LIGHT tick at 12:26Z. Both settled
forecast `423dd881047b`; the two copies differ in one field
(`settled_ts` 12:23:22Z local vs 12:24:38Z origin), and both appended a
`cycles.log` line, so loop.sh's `git pull --rebase` conflicted and aborted.
Local also holds RETRO-20260921-1224 and its playbook edit, which origin
lacks. Nothing was reset. Filed under the standing divergence entry in
`journal/proposals.md` with a repair recipe.
