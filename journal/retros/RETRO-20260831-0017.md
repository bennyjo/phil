# RETRO — 2026-08-31 00:17Z (LIGHT tick, settlement-forced per schedule.json DEEP-2026-08-05 rule)

resolve.py this tick: 0 of 24 bets settled (2 open, none due); **2 forecasts
settled** (0 of 24 ledger positions — no bet-grading, forecast-only). Both
are the same underlying market: "Will Ethereum dip to $2,400 in August?"
(3810130), outcome **Yes** (it dipped) — the superseded read and the live
read both graded WON.

## What settled

- **d2431685b1a3** (2026-08-28T18:25Z, superseded): est 0.52, mkt mid
  0.695 at record. Driftless lognormal touch-barrier self-model,
  ann.vol~65% guessed from spot level only. skip_reason
  architecture-mismatch (same diffusion family as the net-negative
  gas/WTI/gold/BTC-dip counterfactual-ledger class) — no bet regardless of
  edge.
- **edd6af85d6a6** (2026-08-28T21:35Z, live, supersedes the above): est
  0.80, mkt mid 0.675 at record. Same reflection touch model but with
  MEASURED realized vol (72h 2.39%→P0.785, 14d 3.47%→P0.851, blended to
  0.80) instead of a guessed input. skip_reason unvalidated-method — no
  bet, gated pending the BTC-82k row (cbbeb134c438) per the
  2026-08-28T21:35Z pre-registration.

Brier on outcome=1: market mid 0.675 → 0.1056; est 0.80 → 0.0400. **The
measured-vol estimate beat the market mid on this leg**, and beat the
guessed-vol predecessor it superseded (0.52 → brier 0.2304) by a wide
margin. score.py's `unvalidated-method` bucket now reads n=3,
brier_delta=−0.0213 (sign: est underperforming market on average across
that small bucket) — this single leg does not carry the bucket alone;
mind n=3.

## Grading against the pre-registered gate — PARTIAL, not closed

The watch item (schedule.json, "Measured-vol touch-model pair") pre-
registered grading this ETH leg **together with** BTC dip-75000
(753366c2ea8e, still open) and the BTC-82k touch row (cbbeb134c438, still
open, market end_date 2026-08-31T04:00Z — not yet due). The three-row
question is whether the measured-realized-vol input still reads
systematically ABOVE the market (the vol-overstatement signature that the
gas/WTI/gold touch family showed) or whether swapping a guessed vol input
for a measured one fixes that.

This one settlement is evidence but not the answer: est (0.80) sat above
market (0.675) here too, same direction as the family's usual
overstatement — but this time the market was wrong and the model was
right (outcome Yes). That is one data point *against* the
vol-overstatement hypothesis, not for it. The other two legs are still
open. Per the pre-registration and per playbook practice (do not act on
n=1), **no strategy edit this tick** — the method stays forecast-only
until cbbeb134c438 and 753366c2ea8e settle and the three-row comparison
can actually be made.

## Watch item updated

schedule.json's "Measured-vol touch-model pair" item gets this settlement
appended in-place (see diff) so the pending BTC-82k/BTC-dip grading has
the ETH leg's result on record rather than re-deriving it later.

## Commit

This retro plus the schedule.json watch-item append are committed together
this tick, per the "settlement grading is never deferred" rule — no
scan/research runs on this LIGHT tick.
