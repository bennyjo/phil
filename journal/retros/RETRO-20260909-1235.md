# RETRO 2026-09-09 12:35Z (LIGHT tick, cloud — forecast-only settlement)

## Settled this tick

`resolve.py`: 0/30 ledger bets settled (7 open, none newly due); 8/79
forecasts settled. Per the schedule.json settles-ANYTHING carrier rule,
this LIGHT tick writes the retro now rather than deferring.

Two independent groups:

### China Aug 2026 CPI YoY bracket set (NBS print, 2026-09-09 01:30Z, actual 0.8%)

Six forecasts across three brackets (pre-print research 2026-09-07 11:34Z,
strategy_rev `25ac1f4`, plus three post-print supersedes recorded
~2026-09-09 01:43Z):

| Bracket | Pre-print id / est | Post-print id / est | Skip reason(s) | Outcome |
|---|---|---|---|---|
| ≥0.9% | `7bacc91ddf91` 0.38 | `f2d8e32a609e` 0.02 | wide-spread-veto → market-agrees | No (LOST) |
| 0.7-0.8% | `19cf14c87979` 0.33 | `2bdb110ba884` 0.97 | outside-view-veto → market-agrees | Yes (WON) |
| 0.5-0.6% | `9f93fff3d795` 0.19 | `9eff80f25296` 0.02 | no-edge → wide-spread-veto | No (LOST) |

**Was the estimate wrong, the fill bad, or variance normal?** The
pre-print base-effect anchor (Jul YoY 0.5% + Aug seasonal MoM ~+0.3% →
~0.8%) landed exactly on the actual print — this is the technique from
"PPI YoY brackets: base-effect projection" (playbook, 2026-08-11)
confirmed on a second series. Not variance: the center was right, and the
mixture model correctly put its mode on 0.7-0.8% (though the specific
bracket probability, 0.33-0.44, understated how concentrated the true
distribution should have been — a calibration note, not a directional
miss). The two pre-print consensus sources disagreed by one bracket
(tradingeconomics 0.7% vs investing.com/Lundgreen 0.9%); TE's figure fell
in the correct bracket, Lundgreen/investing.com's did not (n=1).

**Vetoes graded** — both correctly declined bets that would have lost:

- `19cf14c87979` (outside-view-veto): the No-side apparent edge (~0.15)
  rested on the contradictory consensus sources; gate 2 fired. A No bet
  would have LOST (Yes printed).
- `7bacc91ddf91` (wide-spread-veto): apparent Yes edge behind a 0.069
  spread (max_spread 0.06). A Yes bet would have LOST (No printed for
  this bracket).
- `9eff80f25296` (post-print wide-spread-veto): the note itself says the
  ~0.20 No-side edge was real but the No side had zero live asks —
  refused by the fill model as genuinely unexecutable, not a counted
  counterfactual trade.

Per DEEP-2026-08-23, both fillable veto settlements extended the
playbook counterfactual ledger table in the same commit as this retro
(`strategy/playbook.md`, new rows + re-summed totals + side split for
both `outside-view-veto` and `wide-spread-veto`). Net this batch: −2.00u
(0W/2L on the counterfactual side — i.e. both declines were the right
call). No boundary change at n=2 on either gate.

### Tennis: Alcaraz US Open outright (superseded chain)

`8915df2ed335` (pre-QF2, 2026-09-04, est 0.45 vs mid 0.445, no-edge) →
`ef17991e4bfc` (pre-QF, 2026-09-08, est 0.57 vs mid 0.585, market-agrees,
supersedes the first). Alcaraz lost the quarterfinal (per the 06:35Z
schedule.json update, to Shelton), so both settled LOST/No. In both
recordings the own estimate tracked the market within 0.02 — this is
normal at-market variance on a no-edge skip, not a calibration issue: no
capital or forecast-quality lesson here.

## Category verdicts

- `econ-cpi`: n=7 (`core/score.py`), brier_delta −0.0054 — far below the
  ~15-row bar, no overreaction; this batch's own-side wins (the
  base-effect center) don't move a category-level read yet.
- `tennis-outright`: n=1, no verdict possible.
- Counterfactual `outside-view-veto`: 123 settled declined forecasts, 115
  fillable trades, 49W/66L, pnl +$106.29 ≙ +21.26u, brier_delta +0.0301,
  held-out +$136.68.
- Counterfactual `wide-spread-veto`: 6 settled declined forecasts, 4
  fillable trades, 2 refused, 2W/2L, pnl −$7.82 ≙ −1.56u, brier_delta
  −0.0326, held-out −$8.51 (still n=6, too thin to read).

`core/counterfactual.py reconcile` still shows the long-standing drift
between the hand-kept playbook table and the mechanical ledger (9 rows
never entered pre-dating this cycle, ~20 rows with stale edge/pnl
figures, a stale "last stated total" the parser can't re-sum) — all
pre-existing, none touched by today's two new rows, which matched the
ledger cleanly. This is deep-retro-scope backfill work (see
`journal/proposals.md` 2026-09-07 04:31Z entry, "Munich CF backfill"),
not something to attempt inside a LIGHT tick; leaving it for the next
deep retro rather than a partial fix here.

## Strategy edit

`strategy/playbook.md`: extended the outside-view-veto and
wide-spread-veto counterfactual ledger tables with today's three rows
(mechanical rule, DEEP-2026-08-23), and added a confirmation note under
the PPI base-effect section recording that the same base-effect
projection technique now has a second, exact confirmation (China CPI)
and a first (n=1) TE-vs-Lundgreen conflict-resolution data point. Both
noted explicitly as single-instance hypotheses, not yet rules — no
`risk.json` or `discovery.py` change warranted at this n.

## Open-position monitor

7 open positions, all checked via `quote.py` (live CLOB), none past
their market end date requiring a settlement call this tick — see the
cycle log line for the per-position bid/ask/mid/adverse-move detail (the
Iran ceasefire watch item's CENTCOM re-check is a FULL-cycle duty, not
owed on this LIGHT tick).
