# DEEP RETRO — 2026-08-06

Window since DEEP-2026-08-05 (~04:40Z): **1 settlement** (`2363018c118b`,
WON +$5.00, graded same-tick), **1 placement** (the same bet, 17:19Z),
across ~17 ticks (7 FULL, ~10 LIGHT). Cash $958.11, $5 committed in 1 open
position (`d2dd24206542`, unchanged zombie). Realized P&L **-$36.89** on 18
settled. This deep-retro session synced cleanly via the new trigger-prompt
step-1 line (fetch + `checkout -B main origin/main`) — first run since the
2026-08-05 proposal was actioned, and it worked; no stale-clone recovery
needed.

## (a) Calibration and P&L

### The one settlement

`2363018c118b` (Astros/Blue Jays, Blue Jays +1.5 @ 0.50, est 0.5795,
book-devig) WON +$5.00. RETRO-20260805-2216 graded it in the same LIGHT
tick that settled it — **first test of the DEEP-2026-08-05 settlement-
grading rule, passed** (the b21 failure mode did not recur). The grading
holds up on review: single-book DK line, power devig, ML cross-check
consistent, estimate formed before the PM quote.

### Cumulative (score.py, n=18)

- Overall brier_delta **+0.1156** (still worse than market), win rate 6/18,
  **z = -2.31** (was -2.61) — one win nudges it, systematic overconfidence
  remains the read on the legacy rows.
- **Book-devig post-power-devig-fix: 2W/0L** (`509650e5ec31` edge 0.10,
  `2363018c118b` edge 0.0795). The 0/7 record was the proportional-devig
  era. score.py's `by_edge_class` shows book-devig delta -0.0732, but that
  is n=1 (only stamped rows count; stamping began 2026-08-04). n=2 by
  hand-count — say it plainly: insufficient for any verdict, but the fix's
  record is no longer purely theoretical.
- Categories: mlb-spreads is now the only multi-bet category with negative
  delta (n=2, 2W, +$8.77, delta -0.0568) — n=2 proves nothing. Soccer, the
  only n≥5 category (n=8), sits exactly at market (+0.0027). Nothing has
  the n≈15 a verdict needs.
- **New this window: `min_edge_book_devig` 0.07 became BINDING for the
  first time.** funnel.jsonl logged ~10 clean power-devig computations
  skipped at edges 0.003–0.050 (largest: A's/Reds total 0.050). Every
  prior window reported "0.07 blocked zero bets"; its cost is now measured
  in real skipped candidates. At 2 settled post-fix wins, no change is
  justified — and the blocked 0.02–0.05 band on tight MLB books is exactly
  the sharp-counterparty zone the 0/7 record came from. Keep, keep
  measuring.

## (b) Audit of the hourly agent's window — all KEEP, nothing reverted

1. **`strategy/tools/siblings.py` + book-depth caveat (212943d) — KEEP.**
   Cross-market sensing with an honest first-live-test failure note (17
   placeholder-priced siblings summing to 4.24 on ~$100 books correctly
   read as data-quality, not arb).
2. **`strategy/tools/kalshi.py` + reachability correction (93963e4) and
   `quote.py` curl fallback (401a2be) — KEEP.** The 13:45Z cycle re-tested
   from the runner as the operator asked: Kalshi and Manifold now 200,
   Metaculus still 403 site-side. First Kalshi use reconciled CPI/payroll
   ladders against PM (validation, no edge) — the cross-venue channel is
   open and behaving as a benchmark, not a signal, so far.
3. **`strategy/discovery.py` econ-tag query (dd19883) — KEEP.** Feeds a
   steady 39-candidate econ pool each scan; it is what put the
   unemployment brackets and CPI cluster into triage. Sensing-artifact
   check: post-336h-widening the pool runs 900–970 across 4 queries, and
   the window's research touched earnings, econ, weather, soccer, MLB,
   WNBA, esports — no category the thesis cares about is structurally
   invisible on current evidence. The agent also corrected its own
   tag-1474 assumption (weather markets surface via general queries; ~79
   in pool). Remaining blind spot is the unexplored tag taxonomy — the
   operator's 2026-08-05 ~19:55Z note moved that inside the agent's
   mandate (weekly-ish); no tags.py yet. Due this week, not overdue.
4. **Fit rubric + funnel.jsonl + exploration budget (4007e32) — KEEP.**
   Working as mandated: 3/3 full cycles since creation wrote funnel
   entries — 34 researched candidates, every one with fit_score and
   skip_reason (tally: 15 benchmark-unreachable, 11 no-edge, 8
   market-agrees). Three exploration tests ran with named hypotheses and
   recorded negative findings (HK weather: forecast divergence 3°C >
   bracket width 1°C; PAOK: UEL-qualifier odds attribution unverifiable;
   LCK: coverage real but not fixture-specific). Ruled-out categories
   with evidence, exactly what the mandate asked.
5. **Playbook trap additions (eee67c8, 378ebb3) — KEEP.** Weather
   forecast-divergence, stale-year actuals (fxstreet 2025 catch),
   best-odds cross-book composite, ABNB GAAP-unfindable fails-closed.
   Each cites the concrete instance.
6. **Pacing (schedule.json) — KEEP, one sharpening.** 7 full cycles in
   ~17 ticks, every deferral ≤3h with candidate-specific reasons, min 4
   full/day exceeded throughout, deferral never used to dodge research
   (the full cycles did the hard passes). **But: the 01:13Z LIGHT tick
   asserted "144 full cycles already ran in last 24h" — arithmetically
   impossible (true count: 7), and a direct breach of the computed-counts
   rule added <24h earlier after the identical "147"/"21" fabrications.**
   The rule as written asked for computation; it got another asserted
   number. Sharpened harder (§f.4): copy the literal grep output, and any
   count >24 is a self-check failure that must not be published.

Compliance: no agent commit touched protected paths; the ledger changed
only via core (`resolve.py` settlement, one `place` row). Clean.

## (c) Estimation errors and selection grading

No estimation error to grade — the only bet won on a sound estimate. So
this section grades **selection**, per the operator mandate:

- **CRCL and OXY skips: graded CORRECT.** Both resolved YES (gamma:
  `3074337`, `3074403`, outcome 1/0). The repeated "consensus matches
  PM's ≥0.80 price, no quantified edge" skips held up — the market was
  right, no edge was foregone, and declining to buy an unmodeled favorite
  is not a miss even though Yes won. First outcome-grounded evidence for
  the "market-confirms, no edge" earnings rule (n=2).
- **Process miss found while grading it:** DEEP-2026-08-05's watch item
  ("grade the CRCL/OXY skips when they resolve, 13:00Z/21:00Z today") was
  never executed by any hourly cycle — deep-retro watch items have no
  carrier in the hourly loop. Not re-fixing the hourly loop for this:
  funnel.jsonl is the durable record of skip calls, and grading them
  against outcomes is a deep-retro job. Made explicit in the playbook
  funnel section (§f.3).
- **Gradeable tomorrow:** the four market-agrees calls on the July
  unemployment brackets (print 2026-08-07 08:30Z — PM's 4.2/4.3 split vs
  the real forecaster split), and today's seven earnings dispositions
  (FOXA/ED/AKAM/ABNB/USFD/DBX report 13:00–21:00Z, UAA Aug 7). The ABNB
  fails-closed call is the interesting one: if ABNB's GAAP number lands
  near consensus, the skip was cheap insurance; if it diverges, the rule
  saved a coin-flip on the wrong basis.
- Yesterday's allocative critique (research burned on known-blocked
  sports while mechanical-benchmark clusters sat unresearched) **did not
  recur**: 03:13Z ran priority-ordered research — earnings first (7
  candidates dispositioned in one pass), then soccer with clean
  single-book lines, then scheduled-release triage outside cooldown, then
  a budgeted exploration probe. That is the shape the mandate asked for.

## (d) Discipline violations

None on the betting side: the one bet cleared its class threshold
(0.0795 ≥ 0.07) on a 0.01-spread book, pre-pitch confirmed via
description time, edge_class stamped, no correlated exposure, category
cap untouched. Monitor line present on every tick. The window's only
process breach is the 01:13Z fabricated pacing count (§b.6) — a log-
integrity failure, not a trading one, but the same class of failure
(asserted numbers nobody computed) that the funnel exists to prevent;
treated accordingly in §f.4.

## (e) Proposals

`journal/proposals.md` has **no open items** — every proposal is
actioned or rejected. Reviewed anyway:

- **Deep-retro trigger sync (2026-08-05, actioned):** confirmed working
  by this very session — clean fetch + fast-forward at start, no stale
  refs. Confirmation appended to its status line.
- **Odds-API rejection (re-open condition):** tally day ~3 of the ~week
  the condition requires. This window's funnel: 15 of 34 researched
  candidates died benchmark-unreachable, but the mix has changed
  post-allowlist — line mismatches (PM 8.5 vs book-quoted 8/9) and
  search-summarizer artifacts, not 403s. An odds API would have
  unblocked roughly 8–10 of the 15 (the MLB totals/one-sided-line
  cases). Not re-raising: the operator's terms require the econ/earnings
  pivot to fail too, and its two real tests (unemployment print
  tomorrow, CPI Aug 12) haven't happened yet. If those pass with
  placements still ~0, the quantified case re-opens on the operator's
  own terms with the funnel as evidence.
- **No new proposals.** Nothing in the window needs operator action:
  sensing channels are open, the zombie position is a market-side wait
  (no UMA proposal exists to escalate), and everything else is inside
  agent-owned files.

## (f) Actions applied today

1. `strategy/playbook.md` — Edge class 3: post-power-devig record 2W/0L
   recorded with ledger ids; 0.07 floor noted as binding for the first
   time (funnel evidence), explicitly kept unchanged at n=2.
2. `strategy/playbook.md` — earnings section: CRCL/OXY skip grading
   (both resolved Yes as priced; market-confirms rule gets its first
   outcome evidence).
3. `strategy/playbook.md` — funnel section: deep retros own grading skip
   calls against outcomes (first pass done today); watch items alone are
   not a carrier.
4. `strategy/schedule.json` — notes: pacing-count rule sharpened after
   the 01:13Z "144" breach (copy the literal grep output; any count >24
   is impossible hourly and is a self-check failure, do not publish it).
5. `strategy/risk.json` — no numeric changes (n=18 justifies none);
   dated evidence note appended (book-devig 2W/0L post-fix, 0.07 now
   binding, z -2.31, d2dd pre-registration unchanged).
6. `journal/proposals.md` — sync-proposal status confirmed working.

## Watch items for tomorrow

- **Unemployment print 08:30Z:** grade the four market-agrees skips
  against the actual; if PM's bracket peak was wrong and the forecaster
  split said so, that's a selection error to name.
- **Today's earnings settle:** grade all seven dispositions, especially
  ABNB fails-closed.
- `d2dd24206542`: pre-registration unchanged — if it settles as marked,
  info-race goes 2W/4L and the state-media rule enacts.
- **tags.py / taxonomy first pass** due within the week (operator
  mandate 2026-08-05 ~19:55Z).
- **CPI cluster (Aug 12):** as the release nears, the first direct
  FRED/BLS series pull (the operator pinned the three series) should
  replace WebSearch headline-parsing; the YoY-vs-MoM base-effect
  arithmetic is a genuine cross-market candidate — the class real mode
  now exclusively mirrors.
- Soccer verdict still pending at n=8.
