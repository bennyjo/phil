# RETRO-2026-08-26 08:18Z (FULL cycle, fact-final prototype settled)

Tick type: FULL (schedule.json `next_full_cycle_after` 07:45Z had passed at
tick time, so pacing does not defer).

## What settled

`core/resolve.py`: 1/23 ledger positions settled (2 still open — AfD
Sachsen-Anhalt, GTA VI trailer, neither past end date). 1/85 forecasts
settled.

`5fbf676cfd7f` — $5 Arizona Diamondbacks ML @0.138 (market 3720883,
`mlb-moneyline`, ledger `edge_class=info-race`, strategy_rev `9ad73ef`) —
**WON, +$31.23**. Matching forecast (est 0.97 vs mkt 0.13) also settled WON.
This is the row DEEP-2026-08-26 registered as the **fact-final prototype**:
placed after the game was already Final (ARI 5-4, MLB Stats API gamePk
825042) with the book still pricing Cubs 0.866, independently re-verified
against the market slug (`mlb-chc-ari-2026-08-25`) by both the placing
cycle and the same day's deep retro before settlement. No resolver
deviation occurred — the market settled the same way the already-final fact
said it would.

## Grading

- **Estimate**: correct and honest — 0.97 reflected a genuinely final fact,
  not overconfidence on a live game. brier_delta on this row is -0.7421
  (agent far ahead of market), the largest single-row edge_class
  contribution in the current settled set (score.py: info-race n=1
  brier_delta=-0.7421, pnl +31.23).
- **Fill**: no fill-quality issue — CLOB ask matched the recorded entry.
- **Selection (fit rubric)**: this row scores 5/5 — mechanical resolution,
  benchmark reachable (MLB Stats API), edge persistence was in fact infinite
  (fact already final, no reaction-speed race despite the `info-race` label
  it inherited), bounded resolution tail (official box score, no UMA
  ambiguity), and small research cost relative to the $31.23 payout on a $5
  stake. This is exactly the shape the fit rubric's property 3 (edge
  persistence) was written to distinguish from a true repricing race.

## Playbook update: fact-final class, first settlement

`strategy/playbook.md` (Ledger edge_class split section, DEEP-2026-08-26)
predicted this settlement and asked for it to be graded manually since the
ledger itself is frozen at `info-race` (core-written, not retro-editable).
Appended the outcome inline: **fact-final n=1 settled, 1W/0L, +$31.23**,
brier_delta -0.7421. Per the existing rule, a proposal to add `fact-final`
to `real.allowed_edge_classes` is warranted only at n>=5 settled — this is
the first of five needed, not yet action. No numeric threshold changes;
just the settlement recorded against the prototype so the count is
auditable when the fifth row lands.

## schedule.json watch_items

Watch item #29 ("Two open bets settle this week...") bundled the Cubs/ARI
fact-final row with the still-open GTA VI trailer forecast. Cubs/ARI is now
fully graded (bet WON, forecast WON, no resolver deviation, playbook
updated) — trimmed that half of the entry, leaving the GTA VI info-race
pre-registered fork (liquid book, price-move trigger 0.10/60min) as its own
watch item unchanged.

## Open-position monitor

2 open (AfD Sachsen-Anhalt No @0.66, ends 2026-09-06; GTA VI trailer Yes
@0.36, ends 2026-09-01) — neither past end date, no action.
