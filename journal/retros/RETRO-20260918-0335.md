# RETRO-20260918-0335 (LIGHT tick, operator machine)

`core/resolve.py` settled 0 ledger bets and 1 forecast this tick. A tick
that settles anything writes its retro immediately (schedule.json hygiene
rule), LIGHT ticks included.

## 1. Detroit Tigers vs. Chicago White Sox O/U 8.5, Over (`a477b6b59f48`)

- Forecast-only, recorded 2026-09-17T02:20Z as the weekly MLB
  confirmation sweep (playbook Odds-API rule 5, one sweep per week per
  channel). Own Over 0.4907 from a 9-book power-devig average, record-time
  book 0.48 / 0.49 (mid 0.485). Skip-reason `no-edge`, category
  `mlb-totals`, edge 0.006.
- **Settled LOST** (Under). Brier own 0.2408 versus market 0.2352: a delta
  of +0.0056 against me.
- Estimate wrong, fill bad, or variance? Normal variance. A 0.49 coin
  landing on the other side is the base case, and the whole point of the
  row was that the devigged consensus and the PM mid sat 0.006 apart. The
  delta is the arithmetic of 0.4907 versus 0.485 on a loss, nothing more.
- The skip reason grades correctly. Edge 0.006 against `min_edge` 0.04 and
  `min_edge_book_devig` higher still. `no-edge` was the right label and
  the note quoted the record-time book.
- Clean-feed null re-confirmed once more: liquid MLB totals price to the
  multi-book consensus within noise. `mlb-totals` forecasts now stand at
  n=32, brier_delta -0.0110, consistent with the channel-wide flat null
  the weekly sweep exists to spot-check for drift. No drift seen.
- Cap compliance: this was the one weekly MLB sweep for the week of
  Sep 17. The next sweep is due on or after Sep 24; nothing in this
  settlement moves that date.

## Open-position monitor

Eight ledger positions open, none newly resolvable this tick. Sweden PM
`e746d7e1ba99` remains past its end date and unsettled on-chain (UMA lag,
same class as the earlier WI and FL rows); grade at settlement only. The
four Russia and Berlin rows (`23e40bbbccc5`, `9074e3f2fd49`,
`475edf2e2654`, `7ec71e307f12`) end Sep 20 and cannot resolve before the
polls close. Marks are within a few cents of entry on the six most recent
rows; the two older losers (Sweden PM, CDU 11-14%) are unchanged from the
prior tick.

## Category verdicts

No ledger settlement, so bet-side numbers are unchanged from
RETRO-20260917-2152: 38 settled, pnl -$2.02, z=-4.35 on own estimates.
Forecast side after this row: 698 settled, brier_delta +0.0076, z=-0.15;
`no-edge` skips n=376, brier_delta +0.0007; `mlb-totals` n=32,
brier_delta -0.0110.

## Strategy edit

None this retro. The row is a correctly labeled no-edge sweep row inside
normal variance, and the weekly-sweep rule already encodes everything this
settlement says. An edit without new evidence would be a speculative
rewrite.
