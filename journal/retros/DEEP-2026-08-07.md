# DEEP RETRO — 2026-08-07

Window since DEEP-2026-08-06 (~04:40Z): **1 placement** (`d6d71ab454dc`,
WTI closes above $77 — No @ 0.71, 12:40Z), **1 settlement** (the same bet,
LOST -$5.00, 23:19Z, retro written same-tick), across ~17 ticks (8 FULL,
~9 LIGHT). Cash $953.11, $5 committed in the unchanged zombie
(`d2dd24206542`). Realized P&L **-$41.89** on 19 settled, z **-2.73**
(was -2.31 — the WTI loss on a confident estimate is exactly what moves
that line). This session synced cleanly (fetch + `checkout -B main
origin/main`; origin had been force-updated, local detached HEAD matched
the new tip exactly, no local commits at risk).

## (a) Calibration and P&L — the day's central finding

Slicing all 19 settled bets by **claimed edge at entry** (computed from
the ledger, not eyeballed):

| cohort | n | W-L | P&L | brier_agent | brier_market | delta |
|---|---|---|---|---|---|---|
| claimed edge > 0.10 | 5 | 0-5 | -$25.00 | 0.5714 | 0.1127 | **+0.4587** |
| claimed edge ≤ 0.10 | 14 | 6-8 | -$16.89 | 0.1828 | 0.1787 | **+0.0041** |

The five large-edge bets: `7e753de88823` (0.535), `0bf9fe3785c6` (0.617),
`b21e42c123a1` (0.75), `84ec821167d5` (0.49), `d6d71ab454dc` (0.17).
Every one was an interpretive forecast where the agent held no information
the market lacked — a UI-toggle reading, two war-news readings, a
box-office press reading, and yesterday's intraday-vol extrapolation from
public spot data. The ≤0.10 cohort is *market-level* (delta +0.004): the
whole measured overconfidence (z -2.73) is concentrated in five bets that
claimed to know 15-75 points better than a liquid book.

Honest caveats: the cohort was chosen after seeing the data, the 0.10
boundary is fitted to it, and n=5. But the mechanism is principled (on a
liquid book, a huge disagreement is more likely my model error than the
market's), it agrees with the operator's 2026-08-05 architectural note,
and the cost is concrete. Enacted as a **test, not a threshold**: playbook
Estimation §4 now requires any claimed edge > 0.10 to name the specific
fact or arithmetic the market structurally cannot have priced, else shrink
toward the market or skip.

Other slices: `other` class first stamped settlement LOST (delta +0.2703,
n=1). Book-devig post-power-fix stays 2W/0L (nothing new tested — this
window's clean MLB devig edges were 0.00-0.022, so the 0.07 floor wasn't
even the binding constraint; plain min_edge was). mlb-spreads remains the
only negative-delta multi-bet category (n=2 — insufficient). Soccer n=8,
delta +0.0027: dead-even with the market, verdict still pending.

## (b) Audit of the hourly agent's window — all KEEP, one sharpened

1. **Weather round-2 note (475f74e) — KEEP.** Atlanta/KATL test found
   NWS's own product carries a 4°F intraday spread on convective days —
   physical uncertainty, not sourcing. Correctly scopes the category to
   stable-weather days rather than ruling it out. Named-hypothesis
   exploration, negative result recorded. Exactly what the mandate asks.
2. **MLB verify-true-favorite rule (333c321) — KEEP.** Caught three
   backward/contradictory favorite attributions in WebSearch run-line
   results (Twins/Royals, Diamondbacks/Padres, funnel-logged 15:41Z and
   21:15Z) before any devig was trusted. Evidence-cited, prevented bad
   benchmarks from becoming bets.
3. **Kalshi ladder findings + kalshi.py call-site note (b36793a) — KEEP.**
   The agent caught its own mis-blame of the tool (event-ticker vs
   series-ticker), corrected the playbook before commit, and corrected its
   earlier "spread-veto" framing to the accurate "edge never cleared
   min_edge". Good epistemics. Politics exploration (Clacton brackets vs
   Survation) graded the category reachable-but-efficient — recorded.
4. **WTI retro + conflict-commodity caution (33aebac) — KEEP, SHARPENED.**
   The caution is correct but scoped narrowly ("don't extrapolate realized
   range on conflict-linked commodities"). The bet is the fifth instance
   of a broader class — large claimed edge with no information advantage —
   and it passed every *written* rule (edge 0.17 ≥ 0.04, spread 0.06 ≤ max
   0.06). The rulebook had the gap, not compliance. Generalized into the
   outside-view veto (§a); the narrow caution stays as the
   commodity-specific instance of it.
5. **Pacing (schedule.json) — KEEP, clean window.** 8 full cycles (min 4),
   every deferral ≤3h with candidate-specific reasons, anchored to real
   event boundaries (WTI settlement, MLB first pitches, and 07:15Z set
   deliberately *before* the 08:30Z BLS print rather than across it). The
   23:19Z LIGHT tick settled the WTI bet and wrote its retro same-tick —
   second consecutive pass of the settlement-grading rule. **The
   fabricated-count failure did not recur**: every cycle line this window
   shows the literal awk/grep command and its output (6-9). The
   DEEP-2026-08-06 sharpening held on its first full day.
6. **Sensing (discovery.py untouched this window) — no artifact found.**
   Pool steady at ~950-1000 across 4 queries, econ-tag stable (32-41).
   Window research spanned econ, earnings, MLB, WNBA, soccer, weather,
   politics, commodities — no category the thesis cares about is
   invisible. Cheap noise in the soccer stream (preseason friendlies with
   no odds coverage) was identified and skipped for pennies. The
   **tags.py taxonomy first pass remains undone** — mandated 2026-08-05
   as weekly-ish, so due by ~2026-08-12. Not overdue yet; flagged so it
   doesn't quietly die.

Compliance: no agent commit touched protected paths; ledger written only
by core (one place, one settle). Clean.

## (c) Estimation errors and selection grading

**The WTI estimate (P(Yes)=0.12 vs market ~0.29, actual: Yes).** The
reasoning was wrong, not the benchmark and not variance — and wrong twice
over: (i) as the hourly retro said, same-day realized range is not a vol
bound on a headline-driven underlying (WTI closed +3.37% on the day);
(ii) more fundamentally, the estimate was built entirely from public spot
prints the market also had, plus a catalyst read that pointed the wrong
way. There was no answer to "what do I know that the market doesn't?" —
which is now the written test for any large claimed edge.

**Selection grading (Aug-6 earnings slate, all six resolved YES):**

- **ED skip — CORRECT, and a measured save.** The tentative ~0.045 edge
  was on *No*, built on self-inconsistent beat-rate data; skipping on
  data quality avoided a -$5 loss.
- **AKAM/Yelp/DBX/NET market-agrees skips — CORRECT.** All resolved as
  priced; the market-confirms rule is now outcome-graded at n≈6 with zero
  foregone edge.
- **MNST — foregone win, by rule.** The GAAP-basis trap plus a 0.10
  spread vetoed a bet whose bullish signal (4/4 beats) proved right. The
  fails-closed rules now have one entry in the cost column against
  several in the savings column; at n=1 keep the rules, keep both
  columns.
- **ABNB fails-closed — neutral.** Beat, consistent with its high price;
  cheap insurance.
- **Unemployment ladder (5 rechecks, "noise, not edge")** — ungradeble
  until the 08:30Z print, ~4h after this retro. Carried as the top watch
  item; the recheck discipline itself (spread narrowing tracked
  0.07-0.10 → 0.01, edge never stable above min_edge, flipping legs read
  as snapshot noise) looks sound regardless of outcome.

## (d) Discipline violations

None. The window's one bet cleared every written threshold, was
edge-class-stamped, pre-close, uncorrelated, and monitored every tick;
the count-fabrication class did not recur. The honest observation is the
inverse: full compliance still produced the worst-calibrated bet class in
the book — the fix belongs in the rules (§a), not in enforcement.

## (e) Proposals

`journal/proposals.md` has **no open items**; nothing new needs operator
action this window (sensing channels open, zombie is a market-side wait
with no UMA proposal to escalate, everything else agent-owned). Reviewed
the standing re-open condition on the odds-API rejection: day ~4 of ~7.
This window again logged benchmark-unreachable skips an odds API would
have unblocked (Twins/Royals, Diamondbacks/Padres, Marlins alt-line, J1
mixed-book), but the operator's terms require the econ/earnings pivot to
fail too — its first real test is the BLS print hours from now, then CPI
Aug 12. Not re-raising yet; the funnel keeps the tally.

## (f) Actions applied today

1. `strategy/playbook.md` Estimation §4 — **outside-view veto**: claimed
   edge > 0.10 requires naming what the market structurally cannot have
   priced; full cohort evidence and ledger ids cited; boundary marked
   post-hoc.
2. `strategy/playbook.md` earnings section — Aug-6 slate graded (ED save,
   MNST cost, market-confirms n≈6, ABNB neutral).
3. `strategy/risk.json` — dated evidence note (n=19, z -2.73, other-class
   first loss, cohort split, 0.07 floor non-binding this window). No
   numeric changes; n justifies none.
4. No reverts: every hourly edit this window was evidence-cited and
   survived audit.

## Watch items for tomorrow

- **BLS print 08:30Z (today):** grade the five "noise" reads on the
  Kalshi ladder and PM's 4.2/4.3 split against the actual; a stable
  post-print mispricing found early would also be the pivot's first
  positive test.
- **UAA settles today** (still open, priced ~0.52 from 0.62 at last
  check): grade the market-agrees skip when it resolves.
- `d2dd24206542`: pre-registration unchanged — if it settles as marked,
  info-race goes 2W/4L and the state-media rule enacts.
- **tags.py taxonomy pass due by ~2026-08-12.**
- **CPI cluster (Aug 12):** first direct FRED/BLS series pull should
  replace WebSearch headline-parsing; YoY-vs-MoM base-effect arithmetic
  is a genuine cross-market candidate.
- **First outside-view-veto invocation:** the next candidate with claimed
  edge > 0.10 must show the named-fact test in its funnel entry / bet
  rationale — audit that it actually ran.
- Soccer verdict still pending at n=8 (delta +0.0027, dead even).
