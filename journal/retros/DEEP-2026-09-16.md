# DEEP RETRO — 2026-09-16

Sync: clone started shallow with a stale main; `git fetch --unshallow` then
`checkout -B main origin/main` (local strictly behind by 111, no real
divergence — the "forced update" fetch line was the known shallow artifact).
resolve.py: settled 0 of 34 (7 open), 0 forecasts (90 open). Window audited:
since DEEP-2026-09-15 (5f8e647, 09-15 04:49Z).

## (a) Calibration and P&L

**Bets (n=34 settled, unchanged +1 settlement this window):** 12W/22L,
P&L −$7.02, ROI −4.1%, brier_delta +0.1164, luck-adjusted z = −4.08. The
overconfidence remains concentrated where it always was: `unclassified`
legacy rows (n=18, delta +0.126, −$46.89) and `info-race` (n=5, delta
+0.303, now 1W/4L settled by decision after the Iran loss). Every
negative-delta bet cell is n≤2 (book-devig n=1 −0.0732, mlb-moneyline n=1
−0.7421, mlb-spreads n=2 −0.0568, politics-general n=2 −0.0747) —
insufficient data to claim any bettable class is proven; what IS proven at
n=9 settled is that >0.10-claimed-edge interpretive bets lose (0W/9L, −$45,
tally extended this window by the Iran row).

**Forecast stream (n=637 settled):** delta +0.0071, z −0.03 — market-flat
overall, i.e. the filters mostly forecast at-market and the stream is
honest. Per-category cells worth naming (delta = brier_agent −
brier_market; negative = beating the market):

- **Persistently worse than market, n≥9:** ai-model-release +0.0806
  (n=31), weather +0.0529 (n=33), politics-primary +0.0427 (n=14, betting
  already banned), news +0.0362 (n=15), social-media-postcount +0.0304
  (n=23), econ +0.0292 (n=19), market-microstructure +0.0750 (n=9). These
  are the categories where fast research reads worse than the price at
  meaningful sample sizes. None is currently a betting category except via
  gates, and the numbers say keep it that way.
- **Better than market, small n (candidate hunting grounds, NOT proof):**
  mlb-moneyline −0.0499 (n=15), commodities-touch −0.0529 (n=9),
  politics-general −0.0490 (n=9), say-the-word −0.0243 (n=8),
  product-release −0.0921 (n=4). At these n every one of these deltas is
  within noise; the honest use is research-allocation priority, not edge
  claims. Large-n cells are all flat (soccer n=91 −0.0055, soccer-moneyline
  n=33 −0.0010, mlb-totals n=30 −0.0118, wnba-moneyline n=26 −0.0015,
  tennis-moneyline n=25 −0.0006): the clean-feed-null read holds — liquid
  sports price to within noise of fast research everywhere we've looked.

Playbook edit applied (see step 4) recording this table with the
allocation rule.

## (b) Audit of hourly-agent edits since DEEP-2026-09-15

1. **6276e19 (playbook: wide-spread-veto table, F1 + Emmys rows) — KEEP.**
   Repaired RETRO-20260915-0414's same-commit-rule violation (Emmys row
   missing), arithmetic re-checked (−0.25−5.00=−5.25 ✓). The fact-finality
   subclass observation (veto forgoes only small gains on ~0.90-ask
   fact-final rows) is correctly left as observation, not rule, at n=4.
2. **0391055 (playbook: Iran 0ed1d77858d7 LOST grading, tally 0W/9L) —
   KEEP, and the "position-holding bias" flag is the best observation of
   the window.** The grading honors the 2026-09-07 pre-registration
   (clause-mapping error, not fact-quality). Sharpened by me in step 4:
   the bias flag is now a pre-registered graded pattern rather than a
   loose note, so the next held-to-resolution settlement must grade it.
3. **c798a8d (playbook: disfluent-transcript method note; bet
   5886cf8c42b7 Warsh "Inflation" ≥20) — KEEP.** The method note is a
   real, reusable trap (exact-phrase search returned 0/2 where the truth
   was 2/2); cheap and well-scoped. The bet is discipline-clean (see (d)).
4. **f172787 (playbook: MLB cross-line game 1/6 graded) — KEEP.** Grades
   against the pre-registered n≥6 bar, resists the temptation to legislate
   off one game, and honestly notes the model-high miss landed exactly on
   the pre-registered right-skew failure mode. Correct restraint.
5. **schedule.json (two SET blocks, next_full_cycle_after →
   2026-09-16T15:00Z) — KEEP.** Pacing audit: 09-15 ran ≥5 FULL cycles
   (02:27, ~04:2x op, 15:30, 18:20, 20:12, 22:11) against the min of 4;
   LIGHT ticks covered the genuinely empty overnight window; the current
   deferral is calendar-anchored to the Sep 16 FOMC decision/presser
   (~18:00Z), which is exactly what deferral is for. The 20:12Z and 22:11Z
   FULL cycles did the hard research (Iran settlement grading, 6 word-count
   candidates to concrete estimates) rather than avoiding it. The
   DEEP-2026-09-15 reason-field hygiene rule was followed (PRIOR blocks
   trimmed). No effort-avoidance shape anywhere in the window.
6. **watchlist.json (dc89f6b: Iran price-move entry pruned) — KEEP.**
   Correct prune of a settled position's trigger. Cosmetic: the edit
   dropped the file's trailing newline; restored in step 4.
7. **Sensing (discovery.py) — no edits this window (unchanged since
   Aug 5); audited anyway.** Funnel shows pool 930–995/tick across the 4
   queries (liquid-multiday 43–58, by-liquidity 514–580, active-today
   336–362, econ-tag 10–22), screener escalating 15/15 after the 09-15
   00:37Z outage. Artifact check: the forecast stream spans 60+ categories
   including thin econ brackets the volume queries would bury, so the
   econ-tag asymmetric-floor design is doing its job; weather surfaces via
   the general queries (n=33 forecasts) and reads +0.0529 AGAINST us, so
   its structural under-weighting costs nothing. No query change warranted
   on this evidence. One standing invisibility remains operator-side, not
   query-side: sports-devig on operator ticks (no ODDS_API_KEY — see (e)).

No reverts this window. All four playbook edits cite settled rows and none
moved a boundary — this is what the hourly loop is supposed to look like.

## (c) Biggest estimation errors of the day

1. **Iran Sep7 ceasefire (0ed1d77858d7, −$5): reasoning wrong.** Entry
   read the clause backwards (window "arithmetically impossible" when the
   clause explicitly lets it run past the end date). The 09-07 correction
   caught it in 2 days; no exit existed. Secondary shape worth tracking:
   six post-correction re-checks each found a reason to stay above the
   market's No, and the market was closer at every checkpoint from Sep 9
   on — now pre-registered as a graded pattern (step 4).
2. **MLB cross-line 12.5-Over leg (forecast-only): model wrong, benchmark
   right.** est 0.69 vs market 0.42, actual Under; dBrier +0.2997 against
   the model on that leg. The symmetric-Normal right-skew overstatement
   the bar was written to catch. $0 cost — the unvalidated-method gate did
   its job.
3. **EFL Cup draw forecast + three soccer settlements: variance.** All
   at-market skips settling within noise; correctly not acted on.

## (d) Discipline

No violations found this window. The one bet placed (5886cf8c42b7, Warsh
"Inflation" ≥20x, $5 Yes @0.78): edge 0.09 ≥ min_edge 0.04 and under the
0.10 veto boundary; spread 0.02 ≤ max_spread 0.06; utterance base-rate
gate satisfied (2/2 from official Jun17/Jul29 Fed transcripts, word-boundary
count, both ≥32 occurrences vs threshold 20). The two sibling vetoes
(Anchor at claimed 0.12, MAHA at 0.13, both over the 0.10 boundary) fired
correctly. No in-play bets, no correlated adds (max_stake_per_event
untouched), ledgers written only by core. Settlement-grading duties: the
04:14Z LIGHT tick that settled 2 forecasts wrote its retro same-tick per
the schedule rule — compliant.

## (e) Proposals adjudicated

- **2026-09-15 04:1xZ screener day-batch quota refund (core/screen.py):
  ENDORSED → operator.** Evidence is solid (15 batches charged, 1 usable,
  280 null placeholder rows in screener.jsonl); asks 1–2 are correct and
  small; ask 3 optional. Status line set in proposals.md.
- **CI funnel-weld check (2026-09-14, PROPOSED): unchanged, no new
  instance this window** (the 09-15 02:43Z TRIGGERED tick carried its
  funnel line). Still 10 instances, still operator's call.
- **ODDS_API_KEY on operator runner (2026-09-15, PROPOSED): RE-URGED.**
  cycles.log still logs "odds key not provisioned" on operator FULL
  cycles, and the weekly MLB devig spot-check comes due ~Sep 17 — if the
  key isn't there it lands on a cloud tick or slips. The only
  negative-delta forecast cells with n≥9 include mlb-moneyline and
  commodities-touch; sports-devig blindness on half the runners is the
  most concrete sensing gap we have.
- **Lease writability / simultaneous-start: unchanged** (no new divergence;
  written=false lines keep accumulating).
- **Per-fold fold_brier_delta in counterfactual.py: unchanged** — Sep 20
  lands three correlated German-bracket veto rows at once; still worth
  having before then, operator's call.
- **Blend bar: no recompute** (no relevant settlements this window).
- **Mech window line (counter reset 2026-09-14): requests 0, deliveries 0,
  market-aware/v4 pairs 0, settled pairs 0.** Blocker unchanged:
  PEARL_CONNECT_STORE unset on operator-machine cycles (operator setup
  gap; Pearl 1.9.8 + Connect v0.1.4 verified ready per operator-notes).
- **NEW (deep-retro internal, no operator action): playbook re-read cost.**
  strategy/playbook.md is 320KB/4,941 lines and grew ~113 lines this
  window; every FULL cycle re-reads it. Next deep retro that lands on a
  zero-settlement window should do a structured compaction: move fully
  settled narrative sections (PPI first-instance, primary batch, the
  per-window veto-ledger prose already superseded by the summary table)
  to a strategy/playbook-archive.md, keeping rules, gates, open
  pre-registrations and the counterfactual tables in the live file.
  Not done today: settlement gradings took priority and a rushed 320KB
  edit risks the pre-registered text the Sep 20 gradings depend on.

## Verdict

The loop is healthy: zero reverts needed, every hourly edit
evidence-cited, pacing calendar-anchored, the one bet gate-compliant, and
the window's two real errors (Iran clause-mapping, MLB right-skew) were
both caught by mechanisms pre-registered before settlement. The honest
big picture is unchanged: no proven positive edge anywhere yet (bets
z=−4.08 on legacy overconfidence, forecasts flat at +0.0071 on n=637);
what has accumulated is negative knowledge (which categories and edge
shapes lose) and intact discipline. Sep 20 (MV/German brackets) is the
next real evidence cluster.
