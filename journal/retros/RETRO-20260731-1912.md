# Retro — 2026-07-31 19:12Z

## Settled since last retro (n=1, but see operator-notes for supporting evidence)

**`7e753de88823` Moonshot Yes — LOST (-5.00)**, est 0.60, market 0.065 at
entry, resolved No. `market_id 2431243`, rev `44b0923`.

Rationale at entry: live check of the named resolution source
(arena.ai/leaderboard/text, style control off) showed kimi-k3-max rank 11 >
best Qwen rank 21 → read as "Moonshot has the best Chinese model." Market
priced this at 6.5c (implying ~93% No), a huge disagreement between my
literal source read and the crowd.

This would normally be dismissed as ordinary noise on a single settlement.
It isn't, because of `journal/operator-notes.md` (2026-07-31 18:35Z): the
operator checked the exact named source **three times** spanning the entry
(20:15Z), 4h before the resolution check-time (11:50Z), and 2.5h *after*
Moonshot had already resolved No (18:35Z) — kimi stayed rank 11, best Qwen
stayed rank 21, unchanged all three checks, no new Qwen model appeared. The
underlying fact I bet on did not move. The resolution went against it
anyway. That converts this from "my estimate was wrong" (normal variance)
to "my estimate of the fact may have been right but the RESOLUTION PROCESS
reads the fact differently than I do" (a process/methodology error, which
one clean data point is enough to act on — it's not a calibration-noise
question, it's "did I bet on the right question").

Candidate mechanisms (operator-notes, not fully disambiguated at n=1):
style-control toggle default may be inverted from my assumption (press
coverage had Qwen 3.7 Max at #5 under style-control-ON, which the 92c
Alibaba price is consistent with); or the resolver reads a different
table/mirror than arena.ai; or UMA-style resolution defers to market
consensus when undisputed. Whichever it is, the market's 93%c price on
Moonshot-No was a better predictor of the actual resolution than my direct
source read was — that is the actionable fact regardless of which
mechanism caused it.

## Category verdict

`ai-leaderboard`: n=1 settled (Alibaba leg of the same pair still open,
must be graded as ONE event decision per the existing sibling-pair rule,
not counted twice). brier_delta +0.3558 — badly behind market, consistent
with "the market knew something about the resolution process that the
literal-reading thesis didn't." Far too small to set a category-wide
verdict, but per playbook precedent (DEEP-2026-07-31 accepted a 0/7
qualitative pattern as actionable without waiting for n=15 when the
mechanism was legible) — the mechanism here is unusually legible (operator
verified the source didn't move across 22h spanning the resolution) and
warrants an edit now rather than waiting for more resolution-thesis bets to
accumulate losses.

## Playbook edit

Added a new rule to `strategy/playbook.md` §Estimation: when a literal
resolution-source reading contradicts a >0.90 market consensus, treat the
consensus as evidence about the RESOLVER's behavior (which view/table they
use, dispute economics, precedent), not just noise to be outweighed by a
confident source read. Require naming what the crowd might know about the
resolution process specifically before betting against that kind of
consensus — a confident fact-read alone is not sufficient for this bet
type. This demotes "resolution-source interpretation requiring a
judgment-call reading (UI toggles, table selection)" below the existing
structural classes, and keeps it distinct from mechanical resolution-source
plays (a number printed in a filing/API) which aren't implicated by this
evidence.

No risk.json change: `max_stake_per_event_usd` (10.0, added DEEP-2026-07-31)
already capped this pair's total exposure at $10, which it did — the
existing correlated-exposure control worked as intended. The gap here is
methodological (which bets to take), not sizing, so the fix belongs in the
playbook, not risk.json.

## Open item

`0bf9fe3785c6` (Alibaba No) still open per resolve.py — same thesis, same
event. Grade as one decision with the Moonshot leg once it settles; do not
retro it again as an independent data point.
