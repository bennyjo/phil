# RETRO 2026-09-22 04:15Z (LIGHT tick, cloud, paper)

Pacing: `next_full_cycle_after` (06:30Z) still in the future and 9 FULL
cycles already ran in the last 24h (min 4), so this tick runs LIGHT per
schedule.json's rule — settle + open-position monitor only. resolve.py
settled 0 bets and 4 forecasts. Per the "settles ANYTHING" rule (forecasts
count), this retro grades the 4 forecast settlements; one is
`outside-view-veto` and gets its mandatory same-commit counterfactual
ledger table extension (DEEP-2026-08-23 rule) in `strategy/playbook.md`.

## Rows

| id | market | own / mid | skip reason | result | dBrier |
|---|---|---|---|---|---|
| 8601f47e8b85 | Berlin Grüne 14-17% second votes | 0.50 / 0.625 | outside-view-veto | Yes | unfavorable |
| e7d88c06753b | Musk 180-199 tweets Sep15-22 (superseded) | 0.36 / 0.455 | category-bar | No | favorable |
| badc245f27c9 | Musk 180-199 tweets Sep15-22 (superseded) | 0.16 / 0.12 | category-bar | No | small unfavorable |
| bedf2ed1d9ce | Musk 180-199 tweets Sep15-22 (final) | 0.01 / 0.0265 | category-bar | No | favorable |

## Was the estimate wrong, or was this variance?

**Berlin Grüne 14-17%:** the veto's job is declining a bet whose sign or
size depends on an unvalidated sd choice. At sd=2.2 the No-side edge was
0.116 (above the 0.10 boundary); the note's own sensitivity sweep showed
sd=2.0 giving 0.074 (under floor) and sd=2.5 giving 0.169 (deeper veto
territory) — not robust either way, so the decline was correct process
even though the market (Yes at 0.625) beat the point estimate (0.50) at
settlement. This is the same behavioral-point-estimate-Gaussian shape the
counterfactual ledger already grades as the weakest class (now 1W/9L+1
after this row) — one more confirming instance, not a new failure mode.
No playbook rule change; the veto did its job (avoiding a real loss),
even though the row itself reads as "wrong" on its own estimate.

**Musk 180-199 (three-snapshot supersede chain, same market
`4489698`):** all three graded `lost` (actual outcome No — the live count
climbed past the bracket ahead of the Sep22 16:00Z deadline: 166 tweets
at first record, 183 at the second, 194 with ~19h left at the third,
consistent with a final count landing at 200+ rather than settling back
into 180-199). Own estimate tracked the accelerating count correctly at
each snapshot (0.36 -> 0.16 -> 0.01) and, checked against the market
mid at each snapshot, beat it twice (own Brier below market Brier at the
first and third snapshots) and lagged it marginally once (second
snapshot, 0.16 vs mkt 0.12, both already low). This is ordinary
supersede-chain behavior — the model updates on fresh count data faster
than a "reconsider the model" event, not a wrong estimate — and the
category-bar skip (this category's blanket forecast-only rule) correctly
kept it out of the ledger regardless of the directional read. No playbook
edit: consistent with the existing bar's rationale, not new evidence
against it.

## Category verdict

- politics-general: n=47 (score.py), brier_delta +0.0174 — small-n
  caution; consistent with the outside-view-veto's standing weak read for
  self-modeled Gaussian buckets in this category, no shift owed.
- social-media-postcount: n=24 (score.py), brier_delta +0.0291 — the
  Musk chain's per-snapshot performance was mixed-to-good, but this stays
  a forecast-only category by standing rule (category-bar); no change.

## Open-position monitor

score.py live marks (`core/score.py --json` open_mtm), 6 open positions,
checkpoints vs the RETRO-20260921-2215 read:

- `e746d7e1ba99` Andersson-PM No: entry 0.21, mid 0.125, move -0.085
  ADVERSE, unchanged vs the last checkpoint. Still under the 0.10
  call-out threshold. PAST END DATE (Sep 13), unresolved on-chain (normal
  UMA lag for this class). No re-forecast, held to resolution.
- `9074e3f2fd49` Russia UR sweep Yes: entry 0.68, mid 0.992, move +0.312
  favorable (further favorable vs the prior 0.987 mid / +0.307). PAST END
  DATE (Sep 20), unresolved on-chain. No re-forecast, held to resolution.
- `475edf2e2654` Russia turnout No: entry 0.883, mid 0.99, move +0.107
  favorable, essentially flat vs the prior +0.111. PAST END DATE (Sep
  20), unresolved on-chain, normal lag.
- `4ed738b2045b` RBA No: entry 0.149, mid 0.042, move -0.107 ADVERSE,
  marginally improved vs the prior -0.112. Not past end date (Sep 29).
  No action, LIGHT scope.
- `8894592b953a` Core PCE No: entry 0.68, mid 0.81, move +0.13 favorable,
  improved vs the prior +0.105. Not past end date (Sep 30).
- `05333272be9d` Canada GDP Yes: entry 0.57, mid 0.565, move -0.005,
  flat/immaterial, unchanged. Not past end date (Sep 29).

No position crossed a fresh call-out threshold; no watch-item checkpoint
text needed updating beyond what is already current in
`strategy/schedule.json`. No trigger conditions available to act on
within LIGHT scope; no new research performed, per tick type.

## Playbook edit (this commit)

- `strategy/playbook.md`: outside-view-veto counterfactual ledger table
  extended with the Berlin Grüne 14-17% row, re-summed totals (151 rows,
  143 fillable trades, 60W/83L, pnl +$55.11, brier_delta +0.0342,
  held-out +$63.30) and the side split (no 106/98/46W-52L/+$53.49; yes
  45/45/14W-31L/+$1.62), per the DEEP-2026-08-23 same-commit rule. Also
  noted a minor labeling mismatch (tool auto-tags the row `fact-finality`
  subclass; the row's own note explicitly argues it is a self-modeled
  Gaussian bucket, not fact-final) for a future reconcile pass — not
  acted on here, LIGHT scope.
