# DEEP RETRO — 2026-09-09

Audit window: 2026-09-08 ~05:00Z → 2026-09-09 ~04:30Z. Sync: clone was
shallow, unshallowed cleanly; local main was strictly 128 behind origin,
fast-forwarded to 156278d. No divergence at audit time. resolve.py: 0
settled (6 ledger positions and 81 forecasts still open); score.py run
fresh.

## (a) Calibration and P&L

**Nothing settled in the bet ledger this window.** Bet-side numbers are
unchanged from DEEP-2026-09-08: n=30, +$5.84, brier_delta +0.1092,
luck-adjusted z −3.61. The standing honest read holds: the positive P&L
rests on two longshot wins (5fbf676cfd7f +$31.23, 66131e6b8f76 +$40.05)
while the calibration deficit is broad; nothing new to update it with.

By edge class (all unchanged, restated for the record): book-devig n=1
dBrier −0.0732 (insufficient), other n=7 +0.02 (flat), info-race n=4
+0.2354 (bad, walled off by the fact-finality gate), unclassified n=18
+0.126 (legacy rows, closed population).

**Seven forecast-only settlements this window**, all graded by hourly
retros on the correct tick except one graded 4h late (8c604893bdea — see
(d)):

- 6abf6afb7504 earthquakes WON — clean-feed-null confirmation, correct
  skip at ask 0.997 vs est 0.97.
- 96065826ed50 Go Ahead Eagles LOST, dBrier +0.2573 — the window's big
  row, see (c) and the edit audit.
- 571bae2cdaba Musk 220-239 LOST — directionally right (91% No), market
  tighter; the expected tweet-bracket family shape.
- ed15c319b4bf Trump "Tunnel to Towers" WON (market-agrees, +0.0099);
  8c604893bdea "American Dream" LOST with the agent tighter (−0.0117).
- 833243734e3d / 5aae0f8bc8e3 NH-01 pair — category-bar cost nothing
  (DiLorenzo est 0.68 was already below the 0.87 ask); Noveletsky was a
  pre-flagged moot sibling.

**Tracked conditions (recomputed where the slice moved):**

- **Blend[disagreement]:** n=165, w_opt 0.843, delta −0.0011. The
  DEEP-2026-09-06 reversal did NOT sustain — w_opt moved away from the
  bar (0.802 → 0.843) and the improvement shrank (−0.0017 → −0.0011).
  Bar (w_opt ≤0.80 at n≥150, delta ≥0.002, sustained ×2) not met; this
  is exactly the one-pass wiggle the bar exists to ignore. Keep
  watching, no action.
- **gnhf policy v3 forward test** (`replay.py --after
  2026-09-02T00:14:36Z`): 117 rows, 14 bets, cw_return −0.2081, pnl
  +$26.27, brier_delta +0.0034. Still under the ≥15-bet bar (14), and
  cw_return worsened vs yesterday (−0.1375 → −0.2081). Insufficient
  data, hands off.
- **Outside-view-veto relaxation fork:** slice byte-identical to
  yesterday (122 rows / 114 CF trades / 49W/65L / +$111.29 / dBrier
  +0.0289; fold pnl f3 +1.56, f4 +91.94) — no veto-class settlements
  this window, so the approximate per-fold dBrier quoted yesterday
  (f3 +0.049, f4 +0.020, both positive) is unchanged. **Bar NOT MET,
  veto untouched.** The per-fold-dBrier operator ask stands.
- **Mechanical-econ carve-out kill switch:** no carve-out settlements
  this window; unchanged (rows 5/5, independent events 1/3 — bar and
  switch both un-triggered).
- **Screener replay:** the frozen baseline rev f7ddad12 is now stale —
  f872b51 edited screener-prompt.md (see (b)). Next replay pass must
  re-baseline against rev 6796568 before quoting s_exc/s_ez.
- **Real ledger:** 56 rows, all settle sweeps, zero real fills ever —
  dry by design (allowed_edge_classes=["cross-market"], and the paper
  book has still never produced a cross-market bet).

## (b) Audit of hourly-agent strategy edits in the window

Window diff (bfe8336..156278d): playbook.md +60, schedule.json,
screener-prompt.md +7, watchlist.json, funnel.jsonl (data). No
discovery.py, risk.json, or tools edits. Five substantive edits:

1. **fa14b26 — playbook taxonomy: `no-edge` vs `wide-spread-veto` +
   erratum on 96065826ed50.** **KEEP — best edit of the window.** The
   arithmetic checks out on its own basis (forecast-stream no-edge
   n=307 × +0.0009 ≈ +0.276 aggregate, of which +0.2573 is this one
   row; residual mean ~+0.0001, flat). It closes the third leg of the
   DEEP-2026-08-14 label ruling with a bright-line test (did the EDGE
   fail, or did the SPREAD block it?) and honestly flips the
   wide-spread-veto slice's story from "agent better on every row" to
   "neutral, n=5" — an edit that makes the agent's own record look
   worse is the kind to trust.
2. **f872b51 — screener-prompt: host-name mention markets are not
   near-random.** **KEEP, with a freeze note.** Evidence is real
   (ed15c319b4bf screened 0.62 "low/near-random", researched to 0.82,
   settled at a 0.85 market — a wasted research slot). This is a
   deny-list/cost-side addition, not skill tuning, so the purpose of
   the DEEP-2026-09-01 freeze (stop chasing replay skill) is intact —
   but the replay baseline is stale now; re-baseline before quoting it.
3. **c4ebddf — playbook mech section: supplied facts steer both tools
   to near-certainty.** **KEEP.** Cites both request ids, extracts a
   testable protocol (clause-only prompt vs fact-added prompt as a
   paired comparison), and logs the fourth price-leak instance with a
   new twist (the leaked number was stale AND on the wrong side).
   Grading is correctly deferred to the Sep 7 leg's settlement.
4. **f872b51 — schedule.json `_comment`: TRIGGERED ticks grade
   settlements too.** **KEEP.** Correct generalization of the
   DEEP-2026-08-05/09-02 rule, enacted the same day the gap bit
   (8c604893bdea graded 4h late by the next FULL cycle).
5. **686ac2d — watchlist: Liberals entry converted to open-position
   watch, expiry past Sep 13.** **KEEP.** Correct bookkeeping for
   b063db346052.

**Sensing (discovery.py, unchanged):** pool 991–1076 candidates/cycle
across all four queries, 300 screened, 15 escalated; politics, econ
prints, sports ladders, news and mention markets all surfaced this
window. No category is structurally invisible; the categories that lose
money are being surfaced and correctly declined, which is the veto's
job, not the queries'. No change.

**Pacing (schedule.json):** honest. 10 FULL cycles in the trailing 24h
against a floor of 4; LIGHT ticks cite the pace gate with verifiable
counts; triggered cycles fired on real events (price moves on the open
Iran position, new NFL/CFB markets, China CPI calendar). Deferral is
being used to skip empty windows, not to dodge research — the window's
two bets both came from FULL cycles that did real benchmark work.

**No reverts. Fifth consecutive clean window.**

## (c) Biggest estimation errors of the day

1. **Go Ahead Eagles (96065826ed50, est 0.91, settled No, dBrier
   +0.2573).** The collision produced twin retros (1612 operator, 1617
   cloud) grading the same row; both land on variance, and 1612 is the
   keeper — its 0.9–1.0 bucket check (n=36, realized 0.89 → ~4 losses
   expected) is the right instrument, and its pre-registered claim
   ("when I name an unmodelled structural factor and the market sits
   materially below my base rate, the market's discount is evidence
   about that factor's size") is properly pre-registered rather than
   legislated. I endorse: **variance on the estimate, process error on
   the label** — the label fix (fa14b26) is where the actual lesson
   lives. The benchmark (Sky Sports 2-goal-lead base rate) was real and
   correctly sourced; what was wrong was neither reasoning nor
   benchmark but a one-point token adjustment for a factor the market
   was pricing at fifteen points. That claim now has a grading hook
   (next three novel-structure departures from base rate).
2. **Trump "Tunnel to Towers" screener miss.** Reasoning wrong at the
   SCREENER stage, not research: "near-random" on a phrase that is the
   event host's own name. Cost one research slot, fixed same-window
   (f872b51). The research-stage estimate (0.82 vs market 0.85) was
   fine.
3. **Musk 220-239.** Neither reasoning nor benchmark wrong — the
   playbook's tail-bracket tempering was applied and the 91%-No read
   was right; the market was tighter still. Known family shape,
   no action.

## (d) Discipline

- Both window bets fully compliant: 09fc471ceec1 (edge 0.08 ≥ 0.04,
  spread 0.01) under the numeric-polling election carve-out;
  b063db346052 (edge 0.08 ≥ book-devig floor 0.07, spread 0.01, ten-
  bookmaker benchmark quoted). No in-play bets, no spread breaches, no
  under-floor bets.
- The 8c604893bdea grading was 4h late on a wrong triggered-cycle scope
  reading — self-caught, codified same window (edit 4). Violation
  noted, remedy already in place.
- **Correlated exposure — the one real flag.** Three open No-side legs
  now ride on the 2026-09-13 Swedish election night: e746d7e1ba99
  (Andersson PM No @0.21), 09fc471ceec1 (SD second-most No @0.20),
  b063db346052 (Liberals seats No @0.40) — $15 total against
  `max_stake_per_event_usd` $10. This is not a clean violation: the cap
  as written binds "correlated sibling/derivative legs on one
  underlying event" (evidence base: same-game spread+ML pairs), and
  these three legs are on largely distinct axes — L-No is a partial
  HEDGE of Andersson-No (L missing the threshold shifts seats away
  from the right bloc), SD-vs-M is an inner-bloc question roughly
  orthogonal to both. To the hourly agent's credit, both pairwise
  correlation calls WERE argued at entry in the schedule.json watch
  items — the gap is in the rulebook, not the diligence: risk.json has
  no definition of "event" for an election night, so whether $15 on
  one night was compliant was undecidable from the written cap.
  Enacted (playbook, this pass): an explicit event definition — legs
  count toward the cap only if pairwise positively correlated, hedge
  claims must be argued at entry — under which the trio is compliant;
  and the trio is pre-registered for grading as ONE event-night
  decision at settlement (schedule.json watch item), including whether
  a common polling-error factor moved all three the same way.

## (e) Proposals

- **2026-09-08 ~17:52Z lease/divergence proposal: ENDORSED.** Evidence
  verified end-to-end: RETRO-20260908-1612 and -1617 are two runners
  grading the same forecast three minutes apart, and the cycle log
  confirms both burned a research cycle. The diagnosis (cloud cannot
  write `refs/phil/lease`, so the lease is one-directional) matches
  the 2026-09-06 operator fix note, and the agent was right to refuse
  workarounds in its own paths. This folds into the standing lease
  ask: either grant the cloud credential `refs/phil/*` write access or
  move the lease into a file on `main`. Status set.
- **Open operator asks after this pass: 2** — (1) the merged
  lease-writability/simultaneous-start ask (now carrying its first
  realized divergence), (2) per-fold `fold_brier_delta` in
  `core/counterfactual.py` (unchanged; cost today was zero only
  because the veto slice didn't move).

Verdict: a quiet, honest window. Zero settlements on the money side,
seven on the forecast side, all graded; the hourly agent's edits were
uniformly evidence-cited and one of them (the label taxonomy) repaired
its own instrument at the cost of a better-looking record. The open
book is now concentrated on two event nights (Swedish election Sep 13:
three legs; Iran window Sep 15: one leg at est 0.40 post-supersede) —
the next two deep retros will have real settlement evidence to work
with.
