# RETRO-20260925-1105

LIGHT tick (operator machine, paper). No ledger settlements (0 of 44).
`resolve.py` settled 3 forecasts, all from the Russian State Duma election
(Sep 20 2026 vote, CEC results now final). All three were skip-only rows
(no stake), so no counterfactual veto table entry is owed.

## Settled forecasts

- **`9deb18ac05d1`** "Will LDPR win the third-most seats?" resolved **Yes**.
  Own 0.55 vs mid 0.52, `market-agrees` (edge 0.02, below the per-event cap
  headroom anyway). Own Brier 0.2025 vs market 0.2304 (delta -0.0279, own
  better).
- **`5bf830412682`** "Will New People win the third-most seats?" (sibling,
  same note/sources) resolved **No**. Own 0.40 vs mid 0.39, `market-agrees`
  (edge 0.00). Own Brier 0.16 vs market 0.1521 (delta +0.0079, own
  marginally worse).
- **`56ed434261a5`** "Will Yabloko win at least one seat?" resolved **No**.
  Own 0.10 vs mid 0.235, `outside-view-veto` (in-progress-count,
  no-official-tally shape). Own Brier 0.01 vs market 0.0552 (delta -0.0452,
  own clearly better).

Verdict: **estimates right, skips right.** The LDPR/NL pair correctly
ordered LDPR ahead of NL for third place from the VCIOM/FOM/ExtremeScan
poll synthesis — the sibling-market method separated a near-tie correctly,
even though neither leg had bettable edge. Yabloko is the sharper result:
the market held Yes-side risk at 0.235 (pricing genuine uncertainty from
the missing official SMD breakdown and the pre-election disqualification
noise), while the outside-view-veto skip's own 0.10 landed far closer to
the true 0 outcome. This is a second data point (after RETRO-20260921-1625
and prior election-night items) that the veto correctly avoids betting
into incomplete-count markets even when the resulting estimate would have
looked like edge.

## Per-category verdict

`politics-general` forecasts: n=54 overall, brier_delta +0.0108 (score.py).
These 3 rows are too few to move that number meaningfully; consistent
with, not counter to, the existing category read.

## Lesson

None encoded. All three rows confirm existing playbook policy
(`market-agrees` sibling-ranking method, `outside-view-veto` for
in-progress official counts) rather than contradicting it — no edit
warranted from n=3.
