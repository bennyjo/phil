# DEEP-2026-09-05

Window: 2026-09-04 ~04:30Z → 2026-09-05 ~04:30Z (since DEEP-2026-09-04).
Sync note: session started on a SHALLOW clone again; `git fetch
--unshallow` completed cleanly, local main was strictly behind
origin/main (0 ahead / 14 behind, clean merge-base), fast-forwarded via
`checkout -B` — no real divergence, no repeat of the fake-divergence
pattern. resolve.py at run time: 0 new ledger settlements (3 open), 0
forecasts newly settled (26 open).

## (a) Calibration and P&L

**Bet side (n=28 settled, +2 in window):** 9W/19L, net −$36.78, ROI
−26.3%, brier_agent 0.3335 vs market 0.2111, delta +0.1223,
luck-adjusted z = −4.24. The two window settlements were the NFP
bracket pair: `84167af841f7` (NFP 0–50k No) WON +$2.46, `609c98073a77`
(UR 4.1% No) LOST −$5.00; net −$2.54. The headline z is unchanged and
remains the experiment's core fact: by its own stated probabilities the
book expected ~18.4 wins and got 9 — systematic overconfidence, not
variance.

Where bet-side delta is genuinely negative: still nowhere with usable n.
mlb-moneyline −0.742 (n=1, one Cubs longshot), mlb-spreads −0.057
(n=2), econ-ppi −0.023 (n=1), book-devig −0.073 (n=1). Insufficient
data, all of them. Genuinely positive (bad) with pattern support: news
+0.476 (n=4, 0W), ai-leaderboard +0.419 (n=2), product-release +0.680
(n=1) — the narrative/timing cluster keeps paying for the book.

**Forecast side (n=506 settled):** delta +0.0093, luck-adjusted z
+0.29 — the belief engine as a whole runs at market parity; the paper
losses concentrate precisely where beliefs disagree hard with the
market (see the veto numbers below). Worst high-n category:
**ai-model-release n=30, delta +0.0829** — release-timing beliefs
(Astra, GPT-6) are systematically worse than the market and this window
supplied 8 more CF losses from that exact shape. Best high-n:
mlb-moneyline −0.053 (n=14), weather remains bad at +0.059 (n=22).

**Mechanical counterfactual ledger is now the record** (operator note
2026-09-04 ~23:20Z; dollars ÷5 = units): outside-view-veto class = 105
CF trades, 46W/59L, **+$87.17 (+17.4u), brier_delta +0.0282**, held-out
+$116.17. The hand table in playbook.md says +12.29u on 102 trades; the
gap is the 8 rows the protected caps would refuse plus mid-vs-fill
grading, exactly as the operator's reconcile predicted. Both numbers
carry the same verdict the operator already issued: the apparent
positive P&L is one family (Astra snapshots), the vetoed *beliefs* are
worse-calibrated than the market they disagree with (+0.028), so the
veto stays. From this pass on, retros quote the mechanical totals; the
hand table stays as narrative (playbook edit this commit).

**Real ledger:** 32 rows, all `settle` sweeps, all "DepositWallet is
empty" — still zero real fills, so no paper-twin comparison exists yet.

**Tracked condition lines (standing obligations):**
- **Blend:** blend[disagreement] n=144, w_opt=0.859, improvement at
  w_opt −0.0009. Bar (w_opt ≤ 0.80 at n ≥ 150, sustained ×2, ≥0.002)
  not met; trajectory still converging toward the market (0.837 at
  n=122 → 0.859 at n=144).
- **Screener replay:** 18,047 rows scored (25,200 raw); live prompt_rev
  f7ddad12 excess +0.0033, exc_z +2.1 — unchanged; screener-prompt.md
  untouched per freeze.
- **gnhf policy v3 forward test:** 78 forward rows, policy bet 9 —
  still under the ≥15-bet bar, hands off. For the record the direction
  flipped negative: forward cw_return −0.3315, pnl −$5.42, brier_delta
  +0.0219 (was +$2.71 at 3 bets yesterday). No action; the bar is
  mechanical and not met.
- **Countable-metric veto-narrowing trigger (operator, 2026-09-04
  ~23:20Z): FIRED ON THE LETTER — recommendation DO NOT OPEN. Full
  grading in §(e).**

## (b) Audit of the hourly agent's strategy edits (window)

1. **816b0e9 (RETRO-20260904-1613): NFP CF batch + tail-risk flag —
   KEEP.** Four Aug-23-vintage NFP bracket rows graded against their
   record-time book, totals re-summed correctly, and the ruling is
   honest: the batch nets +5.90u only because the ≥150k leg (a ~6–7%
   shot by both the model and the market) happened to hit — outcome
   luck, not method vindication. The paired flag ("NFP win ≠ wide sds
   validated, UR loss ≠ tight sds validated, n=2, change nothing") is
   exactly the right non-action.
2. **3560970 (RETRO-20260904-1813): Shanghai weather CF row — KEEP.**
   Clean single-row extension, totals check, no gate change at n=1.
3. **2a62f3e (RETRO-20260904-2215): Astra batch + "quote the live
   bid/ask inside the rationale" rule — KEEP; best edit of the window.**
   The agent caught its own rationale-integrity failure: three veto rows
   (`99d1545b2ec7`, `7adc67fe86cc`, `b249eb7256a0`) claimed market
   prices of 0.14/0.315/0.36 while the `best_bid/ask_at_record` stamped
   on the same forecast.py calls read 0.86/0.69/0.64 — the book had
   repriced 50+ points same-day and the notes never looked. The new
   estimation-method rule (write the exact bid/ask about to be recorded
   into the note; a sharp same-cycle move is a signal to re-search, not
   noise) is evidence-cited and self-enforcing at record time. KEEP.
4. **9076c10 (RETRO-20260905-0016): GPT-6/Astra/Munich CF batch —
   KEEP.** Correct application of the revised-away convention
   (`44a62d640ae1` supersedes `846f0e23a43a`), totals check. One
   correction applied by this retro, not a revert: its running total
   (+12.29u) is the hand table's number; the mechanical ledger, which
   the operator has designated the record, reads +17.4u on the same
   class. Playbook edit this commit puts the convention next to the
   table so future updates quote both.
5. **schedule.json pacing (multiple cycle commits) — KEEP.** 8 FULL
   cycles in the 24h window against min_full_cycles_per_day=4; every
   LIGHT tick cites the concrete `next_full_cycle_after` it honored;
   settlement-grading duty was honored on LIGHT ticks (2215 and 0016
   retros both written the same tick their rows settled, per the carrier
   rule). This is deferral saving effort on empty windows, not research
   avoidance. The two bets of the window both came from FULL cycles that
   did real numeric work (FGW poll read for Grüne; three-tracker bloc
   model for Andersson).
6. **Sensing (strategy/discovery.py) — no edits in window; no audit
   debt.** Funnel lines show a stable 4-query pool (~1,016–1,056
   candidates: liquid-multiday ~50–74, by-liquidity ~582–608,
   active-today ~331–372, econ-tag ~27–28), 300 screened, 15 escalated
   per FULL cycle. The window's own evidence argues the pool is not
   query-blind where it matters: it surfaced German state-election,
   Swedish-PM, NFP-bracket and Astra families. The known structural
   tilt (liquid sports dominate by-liquidity) is being handled
   downstream by the operator's pre-filter, which is where gnhf run 3
   decided it belongs. No discovery.py change earned.
7. **strategy/screener-filters.json — untouched by the agent in window**
   (last change is operator commit 5987081). Nothing to audit.

**Reverts this pass: none.** Second consecutive window with zero
capital lost to a strategy-edit regression; edits are converging on
accounting hygiene and method rules rather than gate churn, which is
what the sample sizes actually support.

## (c) Biggest estimation errors of the day

1. **Astra release-timing family (8 CF losses across two batches:
   by-Sep4/5/6/7/15, on-Sep4, plus GPT-6-by-Sep15).** Reasoning wrong
   twice over: (i) the rationale-integrity failure above — three rows
   argued against a market that had already moved 50+ points; (ii) the
   underlying belief was wrong anyway — est P(release by Sep 4) 0.08
   against a market at 0.86, and Astra shipped Sep 4. The market
   aggregated the phased-rollout rumor flow better than one research
   pass. The veto absorbed all of it (zero capital), and the
   fact-finality/rumor pattern is now the dominant loss shape in the CF
   table. This is the strongest current evidence that ai-model-release
   belongs exactly where it is: researched, forecast, never bet against
   the market's direction.
2. **UR 4.1% No (`609c98073a77`, −$5).** Model dispersion wrong, not
   the mean: sd=0.12 vs the market's implied ~0.09 on an exact-bracket
   print. Its NFP sibling won on a too-wide sd the same morning.
   Opposite-signed dispersion errors at n=2 — correctly flagged, no sd
   input changed. Variance until more of this shape settles.
3. **NFP ≥150k print itself.** Both the sourced-sd model (~6%) and the
   market (~14.5% on that bracket) were surprised. Genuine tail event;
   the lesson (Gaussian with survey sd underweights headline-NFP
   outliers) is flagged in the playbook, not yet actionable at n=1.

## (d) Discipline

**No violations found in the window.** The two bets placed:
- `66131e6b8f76` Grüne ≥7% Yes @0.111, edge 0.069 ≥ min_edge 0.04,
  spread within rule, price above the 0.10 longshot floor.
- `e746d7e1ba99` Andersson-next-PM No @0.21, edge 0.065, disagreement
  0.065 < 0.10 (no veto trigger), numeric three-tracker poll base,
  resolution-fallback risk explicitly priced in the note.

Correlated-exposure check: `de95e5168de3` (AfD seats No) and
`66131e6b8f76` (Grüne ≥7% Yes) both settle on the Sep 6 Sachsen-Anhalt
election — $10 combined, exactly at the $10 per-event cap if read as
one event, and the two theses fail together only in a scenario (AfD
absolute majority AND Grüne <7%) that both polls make joint-tail. At
cap, not over it; noted so the next Sachsen-Anhalt candidate is an
automatic decline. No in-play bets, no spread breaches, no bets under
min_edge. The 2215 rationale-integrity failure was a forecast-process
violation, self-caught, self-encoded — the system working.

## (e) Proposals pass

Statuses set in journal/proposals.md this commit:

1. **Collision-guard gap (2026-09-04 00:40Z):** ACTIONED IN PART
   (operator, 5987081/06b4349: per-runner quota files kill the silent
   quota race; union merge for append-only journals). The lease that
   stops two concurrent FULL cycles stays OPEN — operator half.
2. **d9158ee boundary cure:** ACTIONED (operator, 2026-09-04 ~06:20Z:
   blessed as-is, root cause was gnhf in the live checkout, future runs
   use a separate worktree). Closed.
3. **screen.py collect tuple crash (2026-09-04 08:xxZ):** ACTIONED
   (operator, e941cd8, confirmed by the 04:20Z cycle's clean collect).
   Closed.
4. **Astra endDate quirk (2026-09-04 16:2xZ):** ENDORSED as
   informational and CLOSED AS MOOT — the family resolved Yes on the
   actual Sep-4 release before the endDate ambiguity could bite.
   Standing lesson kept: for per-day-created release series, `endDate`
   is unreliable; read the description's stated (ET) deadline.
5. **Blend re-open condition:** already closed resolved-negative
   (2026-09-03); tracking line above, new bar not met.

**NEW operator ask — amend the countable-metric trigger to require
independent events.** The pre-registered trigger ("countable-metric
reaches 5 settled rows with negative brier_delta and positive pnl on
three of four held-out folds") is now numerically met:
`counterfactual.py ledger` shows countable-metric n=5, 4W/1L, +$61.68,
brier_delta −0.1367, fold_pnl [+6.90, +33.46, +5.00, +21.32, −5.00] —
3 of 4 held-out folds positive. But all five rows are **snapshots of a
single event**: the GTA VI Extended Look view-count family
(`b5c5c134d7cb`, `b3fbd3c3eef7`, `944d8e5fc4d0`, `e398cebab2e6` on the
<20M market, `e441fa8f0f8a` on the 20–22M sibling). This is the exact
failure mode the operator's own gnhf-run-4 note flagged on the Astra
family — snapshots of one event spread across folds make a held-out
total read as replication when it is one observation. Recommendation:
do NOT narrow the veto on this firing; amend the trigger to "5 settled
countable-metric rows across ≥3 independent events, negative
brier_delta, positive pnl on 3 of 4 held-out folds." The trigger is
operator-registered, so the amendment is an operator act; until then
this retro holds the carve-out shut and keeps quoting the number each
pass. (Deliberately symmetric with the blend-gate history: fired on
the letter at n=111, closed as artifact, re-registered tighter.)

Open operator asks after this pass: **2** (collision-guard lease;
countable-metric trigger amendment).

## Housekeeping

- Real-mode: 32/32 settle sweeps report an empty DepositWallet — no
  real fill has ever occurred. Informational; the paper→real funnel has
  yet to produce a twin.
- The `.claude` tool-output trap was avoided this run (schedule.json
  read via targeted python, no spill file touched).

## Verdict

No reverts; the hourly agent's window was its best-audited yet (honest
luck attribution on NFP, a self-caught rationale-integrity failure
turned into a record-time rule). The book's one real edge claim this
window — countable-metric — fired its pre-registered trigger on five
snapshots of one event and was correctly held at the gate. The
bet-side z=−4.24 stands; the money-losing shape remains "disagree with
the market on narrative/timing," and every layer of evidence added
this window (Astra ×6, GPT-6, mechanical CF calibration +0.028) says
the veto that blocks it is load-bearing.
