# RETRO-20260814-0045

Ledger: 0 settled (0 open). Forecasts: 1 settled.

## Settled forecast: Musk tweets Aug7-14, 140-159 bracket (b7a58fd571c8)

- est_prob=0.64, market_prob_at_record=0.415 (ask 0.42), skip_reason=outside-view-veto
- Same model as the 120-139 bracket graded last cycle (RETRO-20260813-2130):
  xtracker cumulative 105 @ 4.90d elapsed, normal(150, 10.9) for the 7-day
  total. This was the model's *center* bracket — its highest-confidence leg
  (est 0.64, modeled edge +0.22, the largest edge of the five-bracket set) —
  declined per the outside-view veto (self-generated-Gaussian edges score
  0/5 historically, the SPX precedent that created the veto).
- Outcome: **No** won (final count landed outside 140-159 too) — the veto
  correctly avoided a second loss on the same model.

## Verdict

n=2 now on this specific model instance: 120-139 (est 0.165, edge +0.085,
LOST) and 140-159 (est 0.64, edge +0.22, LOST) are the model's two largest
probability mass brackets — together 80% of its distribution — and both
missed. That's not just "the veto held again," it's a real signal about
*this model*: the true count landed above where a normal(150, 10.9) put
essentially all its mass, i.e. the model's mean and/or variance were both
too low for this week's posting cadence. The market's ask prices on the
still-open siblings (160-179 ask 0.39 vs model 0.189; 180-199 ask 0.10 vs
model 0.003) were already leaning that direction relative to the model —
consistent with the market pricing in more upside variance than the
Gaussian did.

Still n=2, still short of any threshold that would justify a playbook edit
on its own, and the standing rule is not to overreact under ~15
settlements. But this is now two consecutive, same-model, large-edge misses
in the same direction (model underestimated the count), not independent
noise — worth flagging explicitly rather than filing as "veto held" again
without comment. If the remaining two siblings (160-179, 180-199) settle
and the pattern holds (market beats a fresh from-scratch Gaussian on
Musk's day-to-day count), that's evidence the per-instance variance
estimate from ~5 days of data is too tight generally, not just this once —
worth a note in the playbook about widening sd or discounting the
mean-reversion assumption for short-window count models, not just vetoing
edge on them outright. No strategy edit yet; noting the pattern so it
isn't lost before the next two settlements arrive.

## Open positions

None (0 ledger positions open).
