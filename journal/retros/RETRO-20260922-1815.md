# RETRO-20260922-1815

FULL cycle, cloud. `resolve.py` settled 0 bets, 14 forecasts. Runner lease:
acquired, write refused (cloud credential can't push the custom ref) —
proceeded unprotected per the `written:false` rule. Sync: repo was shallow
at container start (fake divergence against a stale local `main`); `git
fetch --unshallow` cleared it, local `main` fast-forwarded cleanly to
`origin/main` at `12f2e4b`. Tip was ~2h old, collision guard clear.
`next_full_cycle_after` (16:15Z) had passed, so FULL regardless of the
24h full-cycle count. CI green. Operator-notes priority check: most recent
dated section (2026-09-21 ~21:25Z, R1 baseline-always) was already
integrated into playbook.md in the prior cycle (32f4653) — nothing new to
fold in before scanning.

## Settled forecasts, by family

**Trump x Greenland deal-by-Sep23 (market 4712116) — 3 rows, all `news`
or `politics`/`politics-general`, deal in fact signed before the
deadline (resolved Yes):**

- `935afbfc7d19` (No side, `outside-view-veto`, est No=0.40 vs mid 0.25,
  researched 2026-09-20 00:18Z) — LOST as a counterfactual trade (-$5).
  Correctly gated: the signing was announced-but-not-yet-happened at
  research time, the fact-finality shape this veto exists for, and here
  the fact-finality bet would have been on the wrong side anyway.
- `a893f32972f0` (Yes side, `outside-view-veto`, est Yes=0.90 vs mid
  0.74, researched 2026-09-20 12:18Z, same day, later) — WON as a
  counterfactual trade (+$1.67 CF pnl at the recorded ask). Same
  fact-finality veto, but here it declined a trade that would have paid:
  edge 0.15 on a signing ceremony that did happen inside the window.
  Both rows are on the ledger now (table + re-summed totals below, per
  the mandatory same-commit rule).
- `3626e56eafd3` (Yes side, `no-edge`, est 0.84 vs mid 0.875, `news`) —
  WON. Price sat inside the model's own 0.80-0.88 range; correctly
  no-edge, not a veto question.

**Sibling Sep25-deadline pair (market 4712115), both `no-edge`, both
WON** (`a095e9efc6d8` est 0.94 vs mid 0.905; `af5aa7725843` est 0.90 vs
mid 0.93) — same event, 2 extra days of buffer, price already inside or
ahead of the model's range both times. Nothing to grade: the edge floor
did its job, not the veto.

Read across the whole Greenland family (5 rows, 1 loss): the model read
the *direction* of the signing correctly every time (Yes leaning higher
as the week went on); the only loss was the earliest, most cautious read
(No side, 0.40, two days before the deal in fact closed) getting vetoed
into a small counterfactual loss while everything researched after it
was right. Small n (5, one event) — no category rule follows from this
alone, and `news`/`politics`/`politics-general` forecast cells are
already at n=23/11/51 with brier_delta in the usual +0.01 to +0.03 band;
this batch doesn't move that picture.

**Musk tweet-count ladder, Sep15-22 window (market family 4489700-3),
6 rows, all `category-bar` (forecast only, no bet possible):**

- 200-219: three successive re-forecasts as the count came in
  (`bf1b90f89b27` 0.55, `dca11be6e3ef` 0.67, `d0be7e0e5c2e` 0.60) — all
  LOST (actual landed in 220-239). The estimate moved *up* through the
  day even though the true bracket was already out of reach by the
  second read (183 counted at 17:38Z already implied more upside than
  200-219 could hold) — a real miss, not just variance: the second and
  third re-forecasts should have been sliding the 220-239 estimate up
  faster and the 200-219 estimate down faster than they did (0.67 and
  0.60 P(200-219) with the count already at 183-194 out of a 219 cap was
  too high).
- 220-239: two re-forecasts (`886d35d16183` 0.16 → `6343e9ea87af` 0.35)
  — both WON (correctly rising toward the actual bracket).
- 240-259: `d1e7b5107e37` 0.035 — LOST-as-recorded but correctly low
  (a Yes bet on this bracket would have been right to avoid; the
  low-probability call was well-calibrated, it's just that "Yes"
  wasn't the outcome recorded for grading here — the row's own note
  flagged the September series topping out at 42 adds, well under the
  46+ needed, and that held).

Net picture: the ladder's shape (correctly declining 200-219 and
240-259, correctly firming up 220-239) was directionally right by the
end, but the 200-219 series was slow to update against the same live
count data driving 220-239's correct update. `social-media-postcount`
forecast cell sits at n=28, brier_delta +0.0296 (behind the market) —
consistent with the standing finding that this category's live-count
bootstrap method is a bit slower than the book at pricing in fresh
counts. Below the ~15-per-family guardrail for a fresh rule on the
200-219 vs 220-239 asymmetry specifically (6 rows, 1 event); flagging
for the next deep retro rather than changing anything now.

**Zelenskyy 80-99 posts, same window (`2338386a33a6`, `category-bar`,
est 0.97 vs mid 0.925)** — WON. Live xtracker count at 89/window-cap 99
with 1.6h left, correctly high-confidence. No lesson.

**Say-the-word, Trump UNGA remarks (2 rows, both `market-agrees`, both
WON):** `9de2d553fd9f` ("Epic Fury", est 0.40 vs mid 0.425) and
`8815beda10ea` ("Oil"/"Gas"/"Gasoline" 5+, est 0.88 vs mid 0.865). Both
priced within noise of the market and both resolved as the market (and
the model) expected — no edge existed, none was claimed. `say-the-word`
forecast cell: n=42, brier_delta -0.0000 (flat vs market) — this batch
is right in line with that, no lesson.

## Category verdicts

No category above crosses from its existing brier_delta band on this
batch (news/politics/politics-general/say-the-word all near-flat to
market; social-media-postcount stays in its documented behind-market
range) and none has fewer than ~10 rows feeding the read except the
single-event Greenland veto pair, which is folded into the mandatory
ledger table, not a category rule. No playbook rule change from this
batch's settlements themselves.

## Mandatory ledger-table duty (schedule.json DEEP-2026-08-23 rule)

Both `outside-view-veto` rows above (`935afbfc7d19`, `a893f32972f0`) are
appended to `strategy/playbook.md`'s counterfactual ledger table in this
same commit, with re-summed totals, side split and the mechanical
`core/counterfactual.py ledger` reconciliation — see that file's
"Outside-view veto: settled counterfactual ledger" section.

## Playbook edit

None beyond the mandatory ledger-table extension above. This batch is
the process working as designed: three correctly-timed no-edge/
market-agrees skips that won, one veto that correctly avoided a bad
early read, one veto that cost a small counterfactual sum on a good
late read (the veto's known, already-quantified cost — see the ledger's
fact-finality subclass, net +$95.69 over 37 rows, this pair nets
-$3.33 within that), and a tweet-count ladder whose net miscalibration
(slow to downgrade 200-219 as live counts ruled it out) is real but at
n=6/one event, below the threshold for a rule change.
