# DEEP RETRO — 2026-09-22

Window: DEEP-2026-09-21 → 98f4de2 (2026-09-22 04:17Z). Sync: clone was
shallow; `git fetch --unshallow` completed (the "forced update" line was
the usual shallow-boundary artifact), local main strictly behind
origin/main (0 ahead / 129 behind), fast-forwarded clean. No divergence.
`resolve.py` this run: settled 0 of 41 bets (6 open), 0 forecasts (151
open) — the window's settlements were all graded same-tick by the hourly
cycles, correctly, so today is an audit day, not a grading day.

## (a) Calibration and P&L

**Bet book (n=41 settled, lifetime):** 13W/28L, −$17.02, ROI −0.083,
brier_delta **+0.1039**, luck-adjusted z −4.47. The window added the two
pre-registered German legs, both lost (−$10, see (c)). By placement era:

| era | n | pnl | dBrier |
|---|---|---|---|
| placed < Sep 1 | 27 | −$31.67 | +0.125 |
| placed Sep 1–10 | 10 | +$34.65 | +0.056 |
| placed Sep 11+ | 4 | −$20.00 | +0.083 |

Every era loses to the market on calibration; the Sep 1–10 P&L is one
MLB moneyline outlier (+$31.23). No era yet shows the bet stream earning
its disagreements. Honest read at n=41: the improvement from +0.125 to
~+0.06–0.08 is real progress in avoiding catastrophic rows (the −1.0-ROI
news/ai-leaderboard/product-release cluster is all pre-Sep), but the
stream has still never been ahead of the market in any era.

**Where the skill actually lives — all-time settled forecasts by
skip_reason** (own vs market Brier, own-side convention, n=846 settled
rows; computed this run, cross-checked against counterfactual.py's
+0.0342 for the veto slice):

| class | n | dBrier | read |
|---|---|---|---|
| bet (entry forecasts) | 23 | **+0.080** | worst class in the book |
| outside-view-veto | 151 | +0.034 | veto keeps being right |
| category-bar | 29 | +0.002 | bar working |
| no-edge | 429 | +0.002 | at-market by construction |
| market-agrees | 122 | +0.006 | fine |
| unvalidated-method | 34 | **−0.013** | measured-vol touch family |
| wide-spread-veto | 16 | **−0.045** | mechanical models, illiquid books |

The structural fact of the experiment right now: **the two slices where
the model beats the market are both forecast-only by rule** (touch family
pending its pre-registered 6th-measured-row re-grade; wide-spread rows
barred by max_spread), **and the slice that bets is the worst one**. This
is not a paradox to fix by fiat today — both promotion paths already have
pre-registered triggers, and preempting them is exactly the post-hoc move
the fork discipline exists to prevent. But it is the day's headline, and
it sharpens what the next two weeks are for: get the touch re-grade to
its trigger, keep the mlb-moneyline cell starved only by the missing
ODDS_API_KEY (operator ask, re-urged below), and place no self-model
election bets that the price-inside-range rule would veto.

**Forecast stream:** n=757 in-filter, delta +0.0075, z −0.07 (flat by
design). Best cells (n≥8): mlb-moneyline −0.0499 (15), commodities-touch
−0.0498 (10), crypto-touch −0.0398 (12; improved from −0.0250 with the
window's three touch settlements), commodities −0.0159 (11), box-office
−0.0151 (12). Worst: ai-model-release +0.0806 (31), market-microstructure
+0.0750 (9), weather +0.0529 (33), news +0.0364 (20). Identity of the
negative cells is stable for a third week.

**Edge classes (bets):** unclassified n=18 −$46.89 +0.126 is the frozen
early book; other n=16 +$18.64 +0.030; info-race n=5 +$11.23 with
calibration +0.303 (P&L is one Lake-America-style win, the class remains
2W/4L by decision quality); book-devig n=2 flat. No class earns a floor
change at these n.

**Real ledger:** 56 rows, all `settle` sweeps, **zero fills ever**. The
allowed-classes decision stays gated (see (e)).

## (b) Audit of the hourly agent's strategy edits (window: 15 strategy
commits, all playbook/schedule plus CF-table duty)

- **Price-inside-the-model-range rule (3602e2e).** KEEP — the day's best
  edit. Evidence is clean: `7ec71e307f12` lost −$5 with the price sitting
  inside the model's own input-sensitivity range; all six Berlin
  Linke/CDU reads lost to a 0.01-spread mid. This rule, not a min_edge
  bump, is the correct fix for the 0.04–0.08 self-model band that
  produced both German losses.
- **German-set joint verdict + "no bet against the poll trend"
  (RETRO-20260921-1625, bf89986).** KEEP, endorsed. The momentum reading
  (trend-side party beat its final polls in the trend direction in all
  three German states; both losing bets sat opposite the trend) is the
  right common factor, and it was derived from realized misses, not
  narrative. The sd fit (3.0 state / 2.5 national, points) is honestly
  labeled a floor at n=2-per-family; the flag that a wider sd would have
  *flattered* the losing MV CDU bet (P 0.20 at sd 3.0 vs 0.083 centred
  on the latest poll) shows the fit wasn't reverse-engineered to excuse
  it. Centre-stress-test replacing the sd sweep is the right robustness
  check — the sweep could never reject a mis-centred model.
- **Managed-election centring note (29dad21).** KEEP. n=2 cycles, one
  country, scoped as a centring rule with a market-agrees fallback when
  the prior miss can't be sourced. Correctly not a betting licence.
- **WTI stale-window sd rule (79a843e).** KEEP. Calibration-only lesson
  (both rows won); the trigger is crisp (day's move > 2 window-sds at
  record time) and the fix (scale from the day's own range, supersede
  near the settle) matched the market's number on re-read.
- **Touch-family counter pinned + 0.75x shade dropped (4f36845,
  b83d983).** KEEP, one caveat. Pinning the counter to measured-vol rows
  with named sources (labels don't count rows; sibling rungs = one
  decision) is exactly the pre-registration hygiene the family needed.
  Dropping the shade is justified on the quantified cost (0.070 + 0.015
  Brier on winning rows) and is risk-free while forecast-only — but the
  shade-drop means the 6th-row re-grade tests the *unshaded* method;
  don't let a re-grade pass be attributed to a method that was still
  shaded for 3 of its rows. The far-barrier (≥10% gap) split is a good
  catch: the driftless tool has no jump term and the book prices far
  barriers above it, twice now.
- **Tennis consistency rule (a7bf420).** KEEP. n=1, but it encodes a
  process error (number contradicted every signal in its own note on one
  unverified scrape), and the failure mode defaults to market-agrees —
  conservative.
- **CF-table same-commit duty (437253e, e0b60ad, edcc72b).** KEEP —
  audited the arithmetic: the trail 148→150→151 rows with side splits
  re-sums correctly, and today's tool run reproduces the final line
  exactly (151 rows / 143 trades / 60W/83L / +$55.11 / +0.0342 /
  held-out +$63.30). Zero drift between playbook and tool.
- **Sibling-coverage census per EVENT (playbook) + siblings.py
  `my_open_forecasts` (212943d).** KEEP. Fixed a real double-research
  (BanRep, twice in 8h); sensing tool, no query narrowing.
- **schedule.json pacing edits (91ad38a, edcc72b, cfb717d).** KEEP —
  audited in (d).

**Reverts: none.** Nothing in the window's edits narrows sensing, loosens
a floor, or claims evidence it doesn't cite.

## (c) Biggest estimation errors of the window

1. **The German pair (−$10, the pre-registered event-night decision):
   reasoning wrong, in two distinct ways, both now encoded.** Berlin
   Linke No: the "edge" was an input choice (price inside own model
   range). MV CDU 11-14: the centre was stale against a year-long
   downtrend (official 5.3% vs entry-time centre ~9–11); the sd
   sensitivity check varied only the width and could never catch it.
   Momentum, not a signed national polling bias, was the common factor.
   Not variance — the market was better-calibrated on every one of the
   ~14 settled German rows this cycle.
2. **MV SPD veto pair (CF −$10): benchmark wrong (own Gaussian tails),
   market right.** The veto declined both and the declines LOST as CF
   trades — the veto avoided nothing here, but that is the veto working
   as designed on its weakest class; no rule change owed beyond the sd
   floor already enacted.
3. **WTI 0.92 early read: calibration error, caught and encoded by the
   hourly agent itself** (stale 14-day sd on a 3-sd day; both rows still
   won). The kind of self-catch the experiment is supposed to produce.
4. **Russia UR seat-centre undershoot (open brackets): reasoning wrong
   (mirror-image of a known correction), encoded as the managed-election
   centring note.** The open UR-sweep bet is marked 0.992 vs 0.68 entry;
   grade at settlement, not on the mark.

## (d) Discipline

**Zero bets placed this window** (24 ticks, 8 FULL ≥ the min 4), so no
floor was tested; both settled bets were compliant at entry (edges
0.05/0.08 ≥ 0.04; spreads within max; $5 flat; separate events, so the
event cap was respected — the pre-registration treating them as one
DECISION is a grading construct, not an exposure breach). No in-play, no
spread-rule breaches, no under-floor bets. Settlement grading ran
same-tick on every settling tick, including LIGHT ones (0014, 0211,
0415, 2215) — the DEEP-2026-08-05/08-23 rules held all window. Pacing:
deferrals (e.g. to 06:30Z overnight) each carried a stated reason tied
to the candidate pool, research effort went where the hard work was
(election grading, R1 pairs), and 43 forecasts were recorded — deferral
is being used for empty windows, not to dodge research. The one process
wart: two LIGHT ticks in the same minute (12:26Z cloud + operator)
diverged on a single `settled_ts` wall-clock stamp — infra, proposal
endorsed below, not a discipline violation by either runner.

**Sensing:** discovery.py untouched since 2026-08-05; the window's pool
was election-calendar-dominated, which is the market, not a query
artifact (queries 1–3 are volume/liquidity-first and the econ tag rides
alongside). The structurally-invisible category remains sports with
odds benchmarks on the cloud runner (ODDS_API_KEY absent) — and that is
precisely the best negative cell (mlb-moneyline −0.0499 n=15). The
funnel-weld gap (2 silent funnel.jsonl drops in 13 FULL cycles) is a CI
ask, re-urged.

## (e) Proposals pass and pre-registered triggers

- **Relaxation fork: NOT MET — second consecutive double-gate failure**
  (recomputed this run, 151 rows: per-fold dBrier f3 +0.0386 / f4
  +0.0449 both positive → gate 1 fails; fold pnl f4 −$60.37 → gate 2
  fails; 101 events ≥ 40 → gate 3 holds). Fork stays shut. Status line
  appended to the playbook section.
- **German half of the Sep-20 pre-registration: discharged** (by the
  hourly agent, audited above). **Russia pair still open** (UR-sweep
  marked 0.992, turnout similar; UMA lag) → the **real-twin
  allowed-classes decision and the fold_brier_delta conversion carry,
  triggers intact** — deciding on the German half alone would be
  deciding without the input the pre-registration named.
- **Status lines set today** (proposals.md): settled_ts determinism
  ENDORSED (option (a) preferred); wire-nonce 401 retry + CYCLE.md
  sentence ENDORSED (operator/Pearl Connect); mech delivery-size option
  ENDORSED (Pearl Connect); R1 retrieval notes marked informational.
  New minor proposal: counterfactual.py subclass auto-tagger mislabeled
  a self-modeled Gaussian row as `fact-finality` (8601f47e8b85) —
  operator-side, low priority, labels don't gate anything today.
- **Re-urged operator asks:** funnel-weld CI check (2 dated misses);
  ODDS_API_KEY on runners (best negative cell still starved).

**Verdict:** the machine is disciplined and self-correcting — every one
of the window's losses produced a scoped rule the evidence supports, and
the CF/playbook arithmetic is exactly in sync with the tools. What it
does not yet have is a single betting class with settled evidence of
positive edge. The candidates are queued behind honest triggers (touch
re-grade at row 6; mlb-moneyline behind the odds key; real-twin behind
the Russia settlements). The job is to not flinch: no new bet classes by
fiat, no numeric loosening, land the triggers.
