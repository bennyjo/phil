# DEEP RETRO — 2026-09-15

Window: since DEEP-2026-09-14 (~05:00Z Sep 14) through 04:15Z Sep 15
(HEAD 0b93a41). Sync: clone started shallow; unshallowed per procedure,
the "forced update" fetch line was the usual shallow artifact — after
unshallowing, local main was strictly 80 behind origin with zero local
commits, fast-forwarded clean. resolve.py: 0 of 33 ledger bets settled
(7 open), 0 forecasts settled at retro time.

## (a) Calibration and P&L

**No ledger settlements in the window.** All money numbers are
unchanged from DEEP-2026-09-14: n=33 settled, P&L -$2.02, ROI -0.012,
overall brier_delta +0.1026 (still losing to the market on the whole
book), luck-adjusted z -3.72. By edge class: book-devig is the only
negative delta (-0.0732) at n=1 — insufficient data; info-race
+0.2354 at n=4 (bad, small); unclassified +0.126 at n=18 is the
biggest real drag and remains the pre-taxonomy legacy block. By
category, the negative-delta cells (mlb-moneyline -0.7421 n=1,
mlb-spreads -0.0568 n=2, politics-general -0.0747 n=2, econ-ppi,
econ-rates n=1 each) all sit at n<=2: directionally where the
sports-devig and mechanical-econ theses point, but nothing today
clears a small-n caveat. No new conclusions are licensed by zero new
settlements, and none are drawn.

**Forecast-only settlements: 9 graded in-window**, all promptly, each
with an honest own-vs-market Brier (retros 20260914-1812, -2117,
-2355, 20260915-0414). The skip gates went 9-for-9 on capital
protection where gradable: the declined No side of the AI-rank
persistence read (e31a7529247d) would have LOST money; the Emmys LWT
wide-spread veto (697a7f4f3799) lost as a forecast with $0 at risk;
the two clean-feed soccer no-edge skips settled essentially at market
(dBrier +0.0087/-0.0010); Practical Magic no-edge was a half-cent
agreement with the market. Market beat own estimate on most of these
(AI-rank, Emmys), which is exactly what the gates predict for these
shapes — the gating is doing its job; the estimates inside the gates
are not yet beating anyone.

**Resolution risk is now the whole book**: 4 of 7 open positions are
past their end dates (Sweden trio from Sep 13, Iran from Sep 7 with
the clause window closing 16:00Z TODAY) and price as decided
(SD-second No bid 0.999 favourable; Andersson -0.095, Liberals
-0.395, Iran -0.487 adverse). Election-night reads say roughly
-$10.4 realized on those four when official resolutions land, vs the
provisional +$10 net the trio was graded at on the night — the gap is
the Liberals leg. Nothing to do but let core settle them.

## (b) Audit of the hourly agent's strategy edits (0c5539e..0b93a41)

Four files touched; discovery.py NOT touched.

1. **playbook.md +35 (MLB Normal-fit cross-line extrapolation
   hypothesis, 02:4xZ Sep 15) — KEEP, SHARPENED.** Model discipline
   was right: claimed edges 0.26/0.24 from a self-built Normal fit,
   correctly self-classified as outside-view-veto material, declined,
   forecast-only (5b6a0e52dd62, 793b7298aec9). But it deferred
   pre-registering its instance bar ("set it when/if this method
   recurs") — that is how the soccer method's leg-counting error
   happened, and the correction (2026-09-05: independent MATCHES, not
   legs) was already in the book two pages up. Sharpened today: bar
   pre-registered at n>=6 independent GAMES, same-game lines count
   once by average leg dBrier, direction-of-miss tally (the
   symmetric-Normal-on-right-skewed-counts shape risk predicts
   model-high misses at low lines — testable), plus a book-line
   context column to separate "distribution-fit edge" from "early
   lines are soft" (a timing edge). Both legs settle today; the first
   grading applies this bar.

2. **schedule.json (pacing) — pacing decisions KEEP; bloat TRIMMED.**
   The deferrals were legitimate effort-allocation, not
   research-avoidance: the 02:1xZ Sep 15 FULL cycle did a genuine
   Iran deep-dive (traced the Sep 10 Hormozgan explosions to a
   maritime-origin non-qualifying signature — new information the
   prior 37 checks never surfaced), researched two fresh
   Israel-Lebanon candidates to concrete estimates, and only then
   pushed next_full_cycle_after to 16:15Z — just past the Iran window
   close so the settlement gets graded promptly, with
   min_full_cycles_per_day (4) already exceeded at 5 and watch
   triggers demonstrably live (the 02:43Z TRIGGERED tick fired on a
   new market 16 minutes later). That is the pacing system working as
   designed. The hygiene is not: reason had accumulated 8 "PRIOR
   (CLEARED ...)" blocks (17.4KB), and schedule.json grew from ~20KB
   (yesterday's trim) back to 45KB in ONE day. Trimmed reason to the
   current SET block (4.1KB) and codified in _comment: current block
   only, no PRIOR history — git and cycles.log already keep it.
   watch_items left alone: items are being compacted in place per
   yesterday's rule (checkpoints marked "replaces ..."), and the
   over-cap items (MV CDU 5.9KB, Iran 3.6KB) carry live grading
   pointers for settlements landing Sep 15–20; trimming them hours
   before grading risks more than it saves. Re-measure after Sep 20
   settles and the items get deleted.

3. **watchlist.json (21:3xZ Sep 14 amendment) — KEEP.** Correct
   prunes (election-night windows over for Andersson/Liberals — the
   positions are held-to-resolution with no exit mechanism, so a
   price watch adds nothing; ManU/City match played), and the two
   adds are the right kind of re-open triggers: BoE hike price move
   (own 0.20 vs ask 0.061 — a move toward own re-opens a flagged
   external-benchmark disagreement) and Xi visit (a move toward No
   is a specific, named signal). Labels written as questions per the
   file's own convention.

4. **funnel.jsonl — one weld violation, self-caught.** The 15:26Z
   Sep 14 TRIGGERED tick (pricemove:4168072, commit 62ba798) recorded
   forecast d43dc1a43dac with no funnel line — **instance #10 of the
   forecast-without-funnel class**, again on a triggered tick, the
   day after DEEP-2026-09-14 declared the agent-side ladder exhausted.
   Remediation worked as designed (reconcile.py FAILed at the 18:2xZ
   cycle, backfilled from cycles.log prose in the discovery commit),
   so nothing was lost — but prevention failed again, which is
   exactly the CI-ask's thesis. Evidence row added to the proposal.

**Sensing audit (discovery.py untouched — audited as-is):** all four
queries yielded across the window (pools 822–1087; e.g. 21:16Z:
liquid-multiday 49 / by-liquidity 545 / active-today 298 / econ-tag
8). The visible blind spots this window were NOT query artifacts:
(i) six benchmark-unreachable skips at 21:16Z traced to
**ODDS_API_KEY not provisioned on the operator-machine runner** —
cloud cycles devig fine (3 credits spent at 02:11Z), so sports
candidates are structurally invisible only on operator ticks; that is
an env gap, proposed to the operator below, not a discovery edit;
(ii) the screener's top divergence (Xi visit, 0.82) was a Haiku
knowledge gap, correctly burned one research slot to triage — known
cost of the screener design, tolerable at 1/15. No discovery.py
change warranted on this evidence.

**Pacing audit verdict:** deferral is being used honestly (see #2) —
full cycles land where the information is (Iran window close, poll
re-check windows), light ticks still settle and monitor, and
triggered ticks are catching intra-deferral events.

## (c) Biggest estimation errors of the day

No capital was mis-deployed (zero bets placed, zero settled). Among
forecast-only rows: the **Emmys LWT read (own 0.19 vs mid 0.1015,
lost, dBrier +0.0258 to the market)** is the day's largest graded
gap — the AREA-award decomposition was defensible reasoning but
over-weighted the minority path against a market that had the
favorite right; n=1 in a never-before-settled category, correctly not
acted on. The **AI-rank persistence read (own 0.87 vs market 0.935,
Yes won)** repeated the known unvalidated-method shape: same-benchmark
persistence judgments under-weight snapshot stickiness — the existing
gate already prices this in; no new rule needed. Verdict on both:
reasoning slightly wrong, benchmark right, variance normal — and in
both cases the discipline layer, not the estimate, is what kept P&L
at zero.

## (d) Discipline violations

- **Funnel-weld instance #10** (d43dc1a43dac, 15:26Z Sep 14 TRIGGERED
  tick) — self-caught and backfilled ~3h later; see (b)4.
- Otherwise clean: no bets in the window, so no min_edge / in-play /
  spread / correlation questions arise; every settlement was graded
  on the tick it landed (the 0414 LIGHT tick wrote its retro for a
  forecast-only settlement, per the rule); mech step 5a correctly
  skipped on every tick (cloud: no signer by design; operator 13:24Z
  and 21:16Z: PEARL_CONNECT_STORE unset — an operator setup gap, per
  the operator-notes addendum, and flagged in both cycle summaries as
  required).
- **Mech window line (counter reset 2026-09-14, per operator note):
  requests 0, deliveries 0, paired market-aware/v4 pairs 0, settled
  pairs 0.** The market-aware evaluation cannot start until a cycle
  runs with pearl-connect tools attached; Pearl 1.9.8 + Connect
  v0.1.4 are verified ready on the operator machine, so the blocker
  is only the unset env var.

## (e) Proposals — status pass

Full text in journal/proposals.md (2026-09-15 section):

- **No new proposals from the hourly agent this window.**
- **CI funnel-weld ask: +1 evidence row (instance #10), now 10
  instances.** The ask stands unchanged; the new instance is the
  first to occur AFTER the ladder was declared exhausted, and was
  caught by remediation, not prevention. ENDORSED (my own ask,
  re-affirmed).
- **NEW small operator ask: provision ODDS_API_KEY on the
  operator-machine runner.** Six benchmark-unreachable skips in one
  operator FULL cycle (21:16Z Sep 14); sports-devig is where the
  book's only negative brier_delta classes live, and the weekly MLB
  spot-check comes due ~Sep 17 — on the current setup it will land on
  whichever runner happens to fire, and fail on the operator one.
- **Standing asks 1–3 unchanged** (lease writability: no new
  divergence this window, 80 written=false lines total; per-fold
  fold_brier_delta: no veto settlements; blend bar: no material
  settlements, recommendation stays "require robustness").
- **Release-calendar bar:** no calendar-tier fires this window; bar
  unchanged.
- **Fork status: NOT MET, eighth consecutive reading** — no veto
  settlements again; the f4-negative flag stays armed for the Sep 20
  German-bracket settlements (three correlated veto rows at once —
  remember they are ONE evidence point, not three).

## Verdict

A zero-settlement, zero-bet window in which the process layer did its
job and the hygiene layer slipped: skip gates went 9-for-9 on graded
forecasts, pacing and triggered ticks worked exactly as designed, the
one new hypothesis (MLB cross-line) was correctly declined and is now
properly pre-registered — but the funnel weld broke a tenth time and
schedule.json re-bloated to 45KB within a day of being trimmed, both
of which are now either fixed in place or sitting in the operator's
queue. The book itself is frozen until officials speak: Iran closes
16:00Z today, Sweden certification and the Sep 18–20 elections settle
everything else.
