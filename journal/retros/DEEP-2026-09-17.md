# DEEP RETRO — 2026-09-17

Sync: clone started shallow, detached HEAD; `git fetch --unshallow` then
`checkout -B main origin/main` (HEAD already equal to origin/main after
unshallow; the "forced update" fetch line was the known shallow
artifact). resolve.py: settled 0 of 36 ledger (7 open), 1 forecast (XRP
crypto-touch, market-agrees, won). Window audited: since DEEP-2026-09-16
(c5bec73, 09-16 04:47Z).

## (a) Calibration and P&L

**Bets (n=36 settled, +2 this window):** 12W/24L, P&L −$17.02, ROI
−9.5%, brier_delta +0.1174, luck-adjusted z −4.59. The window's two
settlements were both losses and both the same event: Warsh Sep16
presser, `5886cf8c42b7` "Inflation" 20+ Yes @0.78 (est 0.87) and
`3704ba650a69` "Bet/Betting" Yes @0.61 (est 0.70), −$10 total, event
cap exactly used. say-the-word bets are now 0-for-4 lifetime (−$20).
The Inflation loss is fully root-caused (see (c)); the Bet/Betting loss
is a thin 2-transcript habit base rate going the other way — n=1
outcome, no lesson beyond what the gate checkpoint already recorded.
Class view unchanged in kind: every negative-delta bet cell is still
n≤2; the proven negative knowledge (interpretive >0.10-claimed-edge
bets 0W/9L, legacy unclassified −$46.89) stands.

**Forecast stream (n=750 settled, +58 this window):** overall delta
+0.0086 (recomputed directly from forecasts.jsonl this retro), window
delta +0.0115 — the first clearly negative window in weeks, and it is
concentrated: say-the-word window slice +0.0100 (n=31), and the
mid-priced interpretive sub-slice (market 0.2–0.8) +0.0290 (n=17).
Caveat stated plainly: those 31 rows come from TWO events (Warsh
presser, Trump Gastonia rally), so this is 2 correlated samples.
Category re-rank applied to the playbook allocation table (the
DEEP-2026-09-16 rule's first scheduled re-rank):

- say-the-word −0.0243 (n=8) → **−0.0012 (n=42)** — dropped from the
  promising list; the flat aggregate is carried by ≥0.85-priced habit
  words, while mid-range point estimates lose.
- product-release −0.0921 (n=4) → **+0.0631 (n=5)** — sign flipped on
  one settlement; dropped. Textbook small-n noise.
- news +0.0362 (n=15) → **+0.0581 (n=33)** — hardened against us; the
  Israel×Lebanon by-date family settled Yes against every own-No lean.
- econ-rates **−0.0322 (n=22)** added as a candidate cell with the
  honest deflator that it is mostly market-agrees rows on correlated
  Fed events.
- Holding: mlb-moneyline −0.0499 (n=15), commodities-touch −0.0461
  (n=10), politics-general −0.0443 (n=11).

## (b) Audit of hourly-agent edits since DEEP-2026-09-16

1. **439bc04 (playbook: RI primary category-bar confirmation; Israel×
   Lebanon veto tables) — KEEP.** Both tables carry per-row fill
   arithmetic and re-summed totals; the RI row is a genuine third
   instance of the category-bar shape (market also wrong, large
   undecided share) at zero bankroll cost.
2. **aa2d514 (playbook speaker-only method note; 2 bets; schedule SET
   to post-FOMC) — KEEP, method note is the window's best edit.** It
   caught, pre-settlement, that the Inflation entry's 32/40 base rate
   counted reporter lines (speaker-only: 18/27, threshold met 1-of-2)
   and pre-committed the honest 0.62 re-check that made the loss
   diagnosable. The pacing SET (next FULL at 19:30Z to read the presser
   fresh) is exactly calendar-anchored deferral.
3. **8c44d06 (playbook: Warsh pair settlement grading) — KEEP.**
   Correctly declines to touch the gate at n=2 and correctly identifies
   the counting bug, not gate design, as the loss mechanism.
4. **6d5897f (playbook: 7-row Warsh veto-batch CF table; schedule SET
   to 05:00Z) — KEEP.** Arithmetic spot-checked against
   `core/counterfactual.py` output; the 3W/3L "confident middle, thin
   tails" read is honest.
5. **987696e (playbook: MAHA/Egg CF rows) — KEEP.** Totals match the
   mechanical ledger exactly (139/131/56W-75L/+$83.41 verified this
   retro).
6. **9acd1e8 (playbook: utterance checkpoint correction, n=6 verdict,
   KEEP-the-gate) — KEEP the verdict, with one repair (see (d)).** The
   self-caught undercount (Software settled same-timestamp and was
   missed) and the blocked-YES-edge-only counting correction are both
   right. I formalized its "fresh checkpoint" ask into a counted rule
   (playbook, DEEP-2026-09-17).
7. **88ba849 (screener-filters.json: `crypto_bracket` entry) — KEEP.**
   Sensing edit, audited as such: measured leak (8/1,114 raw rows, 3 of
   15 escalation slots in one cycle), same fix pattern as the two
   existing entries, and its blind spot (watch.py's independent
   new-market path) was found within hours and correctly written as an
   operator proposal rather than a core edit.
8. **Pacing (schedule.json, three SETs across the window) — KEEP.**
   FULL cycles 09-16/17 window: 15:15, 20:23, 22:25, 00:27, 02:26 (5 ≥
   min 4), plus 4 TRIGGERED fires; LIGHT ticks covered the FOMC dead
   zone and overnight. Deferral was calendar-anchored (FOMC decision,
   then post-presser), and the 22:25Z FULL cycle did the heavy lifting
   (29 forecast settlements graded same-tick). No effort-avoidance
   shape. The 04:24Z TRIGGERED fire was wasted, but by a protected-code
   gap (watch.py), not by pacing policy.
9. **Sensing (discovery.py) — unchanged since Aug 5; audited.** Funnel
   lines present on FULL cycles; pool 930–1,114/tick. The one
   structural-invisibility finding this window is the inverse problem:
   a banned shape (sub-daily crypto) is VISIBLE to watch.py because the
   protected new-market path can't see strategy-side shape bans —
   proposal endorsed in (e). Sports-devig on operator ticks remains
   blind (no ODDS_API_KEY), and this window realized the predicted
   cost: the weekly MLB devig spot-check landed on a cloud tick
   (02:26Z) instead.

No reverts. Every edit cites settled rows by id; none moved a floor,
boundary, or gate.

## (c) Biggest estimation errors of the day

1. **Warsh "Inflation" 20+ (bet, −$5): reasoning wrong — a counting
   bug, the most cleanly diagnosed loss in the ledger.** Whole-transcript
   counts (32/40) included reporter lines; speaker-only the base was
   18/27, threshold met 1 of 2, honest est 0.62 < entry ask 0.78. The
   bet would never have cleared min_edge with the right count. The bug
   was found by the agent itself pre-settlement and the method note now
   requires speaker-only per-event counts. Nothing here indicts the
   gate; it indicts the count, which is fixed.
2. **Warsh "Echo" (forecast, vetoed): est 0.75 vs mkt 0.475, said no
   echo — dBrier +0.337, the window's largest single miss.** Same
   family as Anchor (est 0.20 vs 0.325, resolved Yes, +0.184 against).
   The presser interpretive mid-range went 3W/3L on CF fills but the
   Brier damage concentrated in these mid-range point estimates —
   benchmark right, model wrong. $0 cost; the veto held.
3. **Israel×Lebanon by-date family (3 rows, vetoed/skipped): model
   wrong, market right, third consecutive instance of the
   multi-channel-diplomatic-process shape.** Own-No leans (0.55/0.62/
   0.75 vs 0.835/0.935/0.855) all resolved Yes. The news cell is now
   +0.0581 at n=33 — this is a structural weakness, not variance, and
   news remains research-deprioritized without a mechanical anchor.
4. **Variance, correctly not acted on:** Fed no-change ladder rows,
   Leverkusen spread, Palmeiras/LDU — all at-market skips settling
   within noise.

## (d) Discipline

- **One violation, repaired this retro:** the same-commit CF-table rule
  (DEEP-2026-08-23). `3a539d9be02d` (Furniture, wide-spread-veto)
  settled on the 04:1xZ tick; RETRO-20260917-0413 graded it narratively
  but commit 9acd1e8 did not extend the playbook table. Row booked and
  totals re-summed from the mechanical ledger (15/13/8W-5L/−$7.99,
  held-out −$3.67) in the playbook with a REPAIR marker. Second
  lifetime instance (RETRO-20260822-1314 was the first). The rule is
  clear as written; the miss coincided with the checkpoint-correction
  retro absorbing attention. No rule change — but the next instance
  makes it a pattern and should get a mechanical check (see (e),
  reconcile.py note).
- **No other violations.** Both bets ≥ min_edge at entry with quoted
  2-transcript base rates (gate-compliant); spreads ≤0.06; no in-play
  entries (both placed 15:13Z vs 18:30Z presser); the two Warsh legs
  total exactly max_stake_per_event_usd $10 and were pre-registered for
  single-event grading. Ledgers written only by core (verified: no
  non-journal/strategy paths in the window's diffs). Settlement retros
  landed same-tick everywhere, including the 29-row 22:25Z batch.
- **Observation, not a violation (n=1, filed under the holding-bias
  pattern):** the same 15:13Z cycle that learned the Inflation
  position's honest est (0.62) was below market (0.81) — i.e. the
  method behind the open position had just been invalidated — placed a
  second $5 leg on the same event from the same (corrected) method at
  n=2 transcripts. Rules allowed it; the event cap contained it. If a
  second instance of "same-event add-on immediately after an adverse
  method correction" appears, pre-register it as a gate question.

## (e) Proposals

- **NEW from hourly agent, 2026-09-17 04:22Z (watch.py new_market fires
  on banned-shape sub-daily crypto): ENDORSED → operator, option (a)
  preferred.** Evidence verified: one TRIGGERED fire (1 of 6/day
  budget) spent on a market that was both shape-banned and mechanically
  unbettable (ask 0.999 > max_entry_price). Option (a) — teach
  `core/watch.py banned_patterns()` to also load the crypto shape
  regexes from `strategy/screener-filters.json` — keeps the agent-side
  filter list as the single source of truth and needed no protected
  edit the two times this shape mutated TODAY (up/down → bracket →
  dip-to). Status line set in proposals.md.
- **Screener day-batch quota refund (2026-09-15, ENDORSED): unchanged,
  still with operator.**
- **CI funnel-weld check (2026-09-14): unchanged** — no new instance;
  all window FULL cycles carried funnel lines.
- **ODDS_API_KEY on operator runner: RE-URGED, prediction realized.**
  The weekly MLB devig spot-check fell to a cloud tick (02:26Z) exactly
  as the 09-16 re-urge warned. mlb-moneyline is still the best
  negative-delta forecast cell (−0.0499, n=15); the operator machine
  remains devig-blind.
- **Lease writability: unchanged** (written=false lines continue;
  02:26Z logged "write-refused" again).
- **Per-fold fold_brier_delta in counterfactual.py: RE-URGED with a
  date** — Sep 20 lands the German-bracket veto cluster in 3 days.
- **Mech line (window 2026-09-14 → now): requests 0, deliveries 0,
  market-aware/v4 pairs 0, settled pairs 0.** Blocker unchanged:
  PEARL_CONNECT_STORE unset on operator-machine cycles.
- **Deep-retro internal, carried:** playbook compaction (now ~330KB)
  still waits for a zero-settlement window — today settled 60 rows and
  the Sep 20 pre-registrations live in the sections a rushed compaction
  would touch. Also carried: consider adding the same-commit CF-table
  check to reconcile.py (check 5 already reads per-row documentation;
  extending it to fail when the mechanical ledger's row count exceeds
  the playbook table's would have caught (d) automatically) — written
  as a note, not an ask, until a second recurrence.

## Verdict

A losing window with clean books: −$10 on one correlated event whose
loss is fully root-caused to a fixed counting bug, 58 forecast
settlements that flattened the say-the-word hope and hardened the news
weakness, zero reverts among nine audited edits, one same-commit-rule
lapse found and repaired. The allocation table did its first scheduled
re-rank and behaved as designed — two promising cells died on contact
with n. Still no proven positive edge anywhere; the negative-knowledge
ledger and the discipline are what compound. Sep 20 (Russia UR, MV/
German brackets, RBA soon after) is the next real evidence cluster.
