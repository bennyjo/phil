# RETRO 2026-09-08 16:17Z (LIGHT tick, cloud — forecast-only settlement)

## Settled this tick

**Forecast `96065826ed50`** — "Will Go Ahead Eagles win on 2026-09-05?" (FC
Utrecht vs Go Ahead Eagles, Eredivisie match suspended 2026-09-06 at 1-3,
resumed 2026-09-08 12:00Z for the final ~26 min). Recorded `skip-reason
no-edge` (nominal ask-edge 0.041 cleared min_edge but the 0.227 spread failed
the wide-spread info-race exception's 0.30 bar — spread veto correctly
bound, no bet).

- est_prob (Yes = Go Ahead win) 0.91, market ask 0.869 at record.
- Settled: **No** (Go Ahead did not win the resumed match).
- FCAST LOST: own 0.91 vs market 0.76 (score.py's stale market_prob_at_record
  reference).

## Was the estimate wrong, the fill bad, or variance normal?

**Variance, not a bad estimate.** The rationale cited a sourced base rate
(Sky Sports PL-era data: ~89.6-90% win / ~7.4-7.6% draw / ~2.6-2.8% loss for
a team holding a 2-goal lead) and placed the own estimate (0.91) essentially
at that base rate, with only a small downward tempering for Utrecht's shown
comeback capacity and the multi-day-stoppage disruption. A ~10% miss rate is
priced into a well-calibrated 0.90-0.91 estimate — this is one of those
misses, not evidence the base rate or the estimate was wrong. No fill issue:
no bet was placed (spread veto correctly blocked one), so the loss here is
a forecast-calibration data point only, not a P&L event.

Global calibration check (`core/score.py`): the 0.9-1.0 estimate bucket
reads n=36, realized win rate 0.89 — a 0.91 estimate losing is fully
consistent with that bucket's own long-run accuracy, not an outlier.

## Category verdict

`market-microstructure`: n=9 settled, brier_delta +0.0750 (own estimates
still beating the market on average in this category even including this
loss). n=9 is well under the ~15-settlement floor for drawing a category
conclusion — no overreaction. The wide-spread veto that kept this from
being a bet is itself validated separately: `wide-spread-veto` skip-reason
across the book sits at n=4, brier_delta -0.0553 (own estimates in that
bucket have run worse than market historically), which is a mild point in
favor of the veto's caution, not against it.

## Strategy edit

None warranted. This settlement supports the existing playbook rule
(wide-spread info-race exception requiring ≥0.30 ask-edge to bet through a
spread this wide) rather than contradicting it — the market's caution
embedded in the wide spread turned out to be earning its keep here. No
change to `strategy/playbook.md`, `strategy/risk.json`, or
`strategy/tools/`.
