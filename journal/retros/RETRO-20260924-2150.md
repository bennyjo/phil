# RETRO-20260924-2150

LIGHT tick (cloud collision guard clear, ran on operator machine). No new
ledger settlements; 4 forecasts settled since the last retro
(RETRO-20260924-1729). All four were skips (`market-agrees` x1, `no-edge`
x3) — no capital at risk, forecast-calibration signal only.

## Settled forecasts

- **`fd8fb6716609`** Netanyahu says Zohran/Mamdani at UNGA — own 0.93 vs
  market 0.945, `market-agrees`. WON. Own brier 0.0049 vs market 0.0030
  (delta +0.0019, market marginally sharper). Correct call, correct skip
  — no edge to claim against a market already at 0.945.
- **`3e6a57166ff9`** Euroleague Hapoel Tel Aviv vs Bayern Munich — own
  0.74 (8-book power-devig fair 0.7415) vs market mid 0.745, `no-edge`.
  LOST (Bayern won). Own brier 0.5476 vs market 0.5550 (delta -0.0074,
  agent fractionally better). Priced at fair value, lost as ~26% of such
  rows should — normal variance, not a signal.
- **`c70b8b3ff160`** Netanyahu says "Hormuz" at UNGA — own 0.62 vs market
  mid 0.565 (book 0.36/0.78, liq 74), `no-edge` (spread too wide to act).
  LOST. Own brier 0.3844 vs market 0.3192 (delta +0.0652, agent worse).
  The 2026 base rate (repeated public use of "Hormuz") didn't carry into
  this speech; single-row overestimate on a thin book, no bet was ever
  possible at that spread so no P&L cost.
- **`82b8b36696b7`** Netanyahu says "AI"/"Artificial Intelligence" at
  UNGA — own 0.55 (3/3 Laplace-shaded base rate) vs market mid 0.395
  (book 0.21/0.58, liq 41, spread 0.37), `no-edge` (spread over max).
  WON. Own brier 0.2025 vs market 0.3660 (delta -0.1635, agent much
  better). Market underpriced a well-supported base rate; correctly
  identified edge but spread-vetoed out of the bet, so the alpha wasn't
  captured. No taxonomy issue — `wide-spread-veto` is exactly what
  should block a 41-liquidity book regardless of edge size.

## Per-category verdict (mind small n)

- `say-the-word`: n=53 (was already n=53 pre-retro since these forecasts
  count from `ts`, not `settled_ts`), brier_delta -0.0030 — flat to
  market, no directional bias across the category. This tick's three
  say-the-word rows (2W-1L) don't move that.
- `euroleague-moneyline`: n=1, brier_delta -0.0074. Single row, no
  signal.

## Lesson

No concrete strategy change. Three of four rows were correctly-skipped
no-edge/market-agrees calls where the book was too thin or too aligned
to act; the fourth (Hormuz) was a single-row miss on a thin book that
was never bettable anyway. This is calibration noise at n=1 per
question, not a pattern — no playbook or risk.json edit warranted this
cycle.

## Open-position flag (not a settlement, carried to cycle log)

`4ed738b2045b` (RBA +25bps Sep meeting, No @ entry 0.149) has moved
adverse to mid ≈0.029, a -0.12 move past the 0.10 call-out bar. Not past
end date (resolves 2026-09-29) — worth a closer look at a future FULL
cycle rather than acted on here.
