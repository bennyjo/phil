# DEEP RETRO — 2026-08-05

Window since DEEP-2026-08-04: **1 settlement** (`b21e42c123a1`, lost -$5.00,
settled 06:24Z on a LIGHT tick), **0 placements** across ~21 ticks (~6 full
cycles, ~10 LIGHT ticks — pacing's first real day), 1 playbook edit and 4
schedule.json pacing updates by the hourly agent, and the operator landing
real-execution infra (`6676f9d`: core/real.py, CYCLE.md Sync step, REAL.md).
Cash $953.11, $5 committed in 1 open position, realized P&L **-$41.89** on
17 settled. Note: core/score.py was rewritten by the operator in-window
(by_edge_class, luck-adjusted z, MTM), so cumulative figures are not
numerically comparable to yesterday's quoted ones.

## (a) Calibration and P&L

### The one settlement, graded under the DEEP-2026-08-02 pre-registration

`b21e42c123a1` (Iran military action vs Gulf State, Yes @ 0.15, est 0.90)
**lost -$5.00**, resolved No via a normal UMA flow at 06:02Z 2026-08-04
(operator-notes: proposal → resolved, no dispute; timing matched the
market's own 3-calendar-day conflicting-reports clause). Grading, exactly
as pre-registered:

- **Reasoning wrong, not variance and not benchmark.** The est-0.90 thesis
  rested on state-media consensus (PressTV, Mehr, TASS, Xinhua) being
  resolution-grade. The resolver saw "conflicting reports," waited the
  clause out, and resolved No. The market's 0.15 was an approximately
  correct price on the *resolution process*; our 0.90 was a price on the
  *fact as we read it*. Same failure shape as the ai-leaderboard pair.
- **Structural/info-race class is now 2W/3L by decision** (wins: NG print
  `2dc417ed68f6`, cross-market sibling `1e8dec1078ba`; losses:
  ai-leaderboard pair as one decision, box-office `84ec821167d5`, now
  b21). The split remains exactly on fact finality: every win was a
  mechanical/final fact, every loss a provisional, contested, or
  interpretation-dependent one. That is 5 decisions in one direction.
- **Enacted (pre-registered on "if b21 loses"):** the wide-spread
  info-race exception to `max_spread` (`min_edge_wide_book`) was
  calibrated partly on this bet's profile. It now additionally requires
  the resolving fact to be **final/mechanical and confirmed by at least
  one non-party primary source** — playbook §Spread-rule scope updated.
- **Still pre-registered, NOT enacted:** the general "state-media
  consensus doesn't qualify as multi-source verified" rule fires only if
  *both* legs lose. `d2dd24206542` (ceasefire No @ 0.92) is still open —
  5 days past end date, **no UMA proposal ever submitted**, No-side mid
  0.215 at 04:16Z (adverse -0.705, score.py MTM -$3.72 of the $5). If it
  settles as marked the class goes 2W/4L and the rule enacts.

### Cumulative picture (score.py, new revision, n=17)

Overall brier_delta **+0.1267** (worse than market), win rate 5/17 vs 9.95
expected under own estimates, **z = -2.61** — the new luck-adjusted line
formalizes what the calibration table showed: systematic overconfidence,
not bad luck. Every estimate bucket below 0.6 realized at 0-40%. By
category nothing changed except `news` (n=1, delta +0.7875 — the b21 row).
Soccer stays the only n≥5 category and sits at market (+0.0027, n=8) —
still insufficient for a verdict (need ~15). The only negative deltas
remain n=1 categories (commodities, mlb-spreads). No category has
demonstrated an edge; both demonstrated failure modes (proportional devig;
non-final/interpretable resolution facts) now have rules.

### By strategy revision

`b21e42c123a1` was placed 2026-07-31 under v2-era rules — before
fact-finality, the non-party-source pre-registration, and the spread-log
requirement (it is also the spread-rule violation on record). Its loss
grades the *old* revision; no bet placed under the current revision has
settled. The current revision's settled record is exactly one bet
(`509650e5ec31`, won, power-devig). Nothing to conclude at n=1.

## (b) Audit of the hourly agent's window

1. **Pacing (strategy/schedule.json) — first real day of use, and it
   worked. KEEP.** ~10 of ~21 ticks ran LIGHT, every deferral with a
   dated, candidate-specific reason, all ≤3h, set to event boundaries
   (23:59Z MLB slate end, 03:22Z, 07:16Z), min_full_cycles_per_day=4
   satisfied (≥4 full cycles in every trailing 24h). Deferral was used on
   genuinely concluded/blocked windows, not to dodge hard research — the
   full cycles in between did real work (OXY/CRCL, Leagues Cup, LCK).
   Yesterday's watch item closes positive. Two sharpenings applied, not
   reverts: (i) reason fields contain invented counts ("147 full cycles
   in last 24h", "21 full cycles" — arithmetic impossible on an hourly
   tick); counts must come from grepping cycles.log. (ii) the settlement
   hand-off bug below.
2. **Settlement retro dropped in the LIGHT-tick hand-off — the window's
   one real process failure. FIXED.** 06:24Z LIGHT tick settled b21 and
   logged "Score/retro deferred to next full cycle per LIGHT-tick pacing
   rule." The next full cycle (08:15Z) then reasoned "resolve.py 0/17
   settled this cycle... no new settlements since last retro
   (RETRO-20260804-0313), so no retro written" — factually wrong: b21
   settled at 06:24Z, after that retro. The deferred obligation had no
   carrier, so the pre-registered grading of a $5 loss sat unapplied for
   23h until this deep retro. Fix: LIGHT ticks no longer defer settlement
   grading — a tick that settles anything writes the retro for it there
   and then (scan/research stay skipped; grading is cheap and pacing was
   never about avoiding it). schedule.json contract + notes updated.
3. **Playbook edit 0421Z (esports-odds circularity + cross-source
   contradiction traps) — KEEP, extended.** Both are concrete,
   evidence-cited instrument findings (JD Gaming "1.35x (74¢)" = PM's own
   price echoed; Brann 54% vs 65% snippets straddling PM's 58.5%). I
   added the third trap of the window, which the agent documented only in
   a schedule.json reason field (0017Z: WebSearch's summarizer affirmed
   "-110 for the 8.5 total... align with standard sportsbook pricing" in
   response to a leading query, with no source number) — reason fields
   are overwritten every full cycle, so durable lessons written only
   there are lost; rule added to that effect.
4. **Sensing (strategy/discovery.py) — untouched by the agent; queries
   KEEP as-is. The gap is triage, not sensing.** Yesterday's watch item
   (no macro/econ candidate ever mentioned) resolves against the
   attention layer: I re-ran the standard scan today and the pool
   contains a **July inflation bracket cluster** (annual 3.3%, annual
   3.4%, monthly ≥0.1%, all end 2026-08-12) plus the CRCL and OXY
   earnings markets. Mechanically-resolving official-print markets — the
   exact category the operator's pivot directive names — have been in the
   424-557-candidate pool while every cycle's research budget went to
   sports and esports whose benchmarks are known-blocked. No discovery.py
   change needed; playbook triage rule added instead (scheduled-release
   markets get named and dispositioned in the log every full cycle).
5. **Skip discipline — clean and often sharp.** In-play catches (Rays/
   Rockies 19-min runway, Cardinals/Yankees wall-clock check, Shang/
   Rublev end_date artifact), the Shamrock Rovers end_date-vs-description
   contradiction, and the CRCL GAAP-vs-non-GAAP catch (applying non-GAAP
   consensus to a GAAP threshold would be a methodology error) were all
   correct refusals. The fnatic case (odds pulled before the time check,
   self-caught) is the one slip; the existing §pre-research event-time
   check already covers it.
6. **Compliance: clean.** No agent commits touched protected paths;
   ledger rows changed only via core/resolve.py. Nothing to revert.

## (c) Biggest estimation error of the day

No bets, so the gradeable error is the settled b21 (§a — reasoning wrong,
pre-registered, now enacted). The forward-looking error is allocative:
**five research passes were spent re-confirming that blocked-benchmark
sports candidates are blocked, while the one cluster with a mechanical
benchmark (CPI brackets) went unresearched.** CRCL (0.845) and OXY (0.91)
were researched but ended at "no quantified beat-probability model" three
cycles running — a fair skip at those prices, but the repeated identical
conclusion is the cooldown rule's territory, and both resolve today
(13:00Z / 21:00Z), giving two free calibration points on the skip logic.

## (d) Discipline violations

None. No bets; no under-min_edge fills, correlated additions, in-play
entries, or spread breaches. Monitor line present on all ~21 ticks
including LIGHT ones. The one process failure is §b.2 (dropped settlement
retro) — a rules gap, now closed, rather than a rule broken.

## (e) Proposals

Reviewed journal/proposals.md:

- **2026-08-04 scheduled-trigger git sync — status set to actioned**
  (operator commit `6676f9d` added the CYCLE.md Sync step, explicitly
  citing this class). Residual gap noted as a NEW proposal: deep-retro
  sessions don't run CYCLE.md — today's session again started from a
  stale clone (local main at pre-handover `033ff6e`, detached HEAD;
  recovered by reset to origin/main). Ask: add the same
  fetch + `checkout -B main origin/main` line to the deep-retro trigger
  prompt.
- **Odds-API rejection (re-open condition) — not re-raised; tally
  started.** Day ~2 of the ~week the condition requires. This window:
  of ~24 candidate-events researched across 6 full cycles, ~14 died on
  benchmark retrievability/usability (403s, PM-echo circularity,
  mixed/contradictory snippets, missing legs), ~7 on clean-benchmark
  no-edge (UCL ties within 1-2pt, WNBA 0.0195 max edge, CRCL/OXY
  no-model), ~3 on in-play/time. Placement rate 0/day since 08-03.
  If the CPI/earnings pivot (§b.4) also fails to produce placements by
  ~2026-08-09, the quantified case re-opens per the operator's own terms.

## (f) Actions applied today

1. `strategy/playbook.md` — §Spread-rule scope: wide-book info-race
   exception tightened per the b21 pre-registration (fact must be
   final/mechanical + non-party primary source). Evidence: `b21e42c123a1`
   settled lost, §a.
2. `strategy/playbook.md` — info-race watch paragraph updated (b21
   settled; class 2W/3L; d2dd pre-registration unchanged).
3. `strategy/playbook.md` — third search trap added (leading-question
   echo, 0017Z evidence) + rule that durable lessons never live only in
   schedule.json reason fields.
4. `strategy/playbook.md` — scheduled-release triage rule: every full
   cycle must name and disposition the scheduled-release candidates
   (earnings, macro prints) in the pool; July CPI cluster flagged as the
   current priority. Evidence: §b.4 (cluster present, zero research in
   ~37 cycles); operator pivot directive (proposals 2026-08-04).
5. `strategy/playbook.md` — earnings sharpening from the window's three
   passes: GAAP/non-GAAP threshold check is mandatory (CRCL catch);
   consensus-clears-threshold is not an edge when PM already prices
   ≥~0.80 (CRCL 0.845, OXY 0.91); the tradeable shape is price
   *contradicting* consensus direction, or a threshold far outside the
   analyst range with PM lagging.
6. `strategy/schedule.json` — contract + notes: settlement grading is
   never deferred (a settling LIGHT tick writes the retro); pacing counts
   must be computed from cycles.log, not asserted. Evidence: §b.2, §b.1.
7. `strategy/risk.json` — **no numeric changes** (n=17 justifies none;
   b21's loss was not threshold-related — nominal "edge" 0.75 — it was
   fact-quality, addressed by rules). Dated note appended to
   edge_class_notes.
8. `journal/proposals.md` — statuses + new deep-retro-sync proposal (§e).

## Watch items for tomorrow

- `d2dd24206542`: settlement or first UMA proposal → apply the both-legs
  pre-registration (§a) exactly as written.
- CRCL (13:00Z) and OXY (21:00Z) resolve today: grade the skip decisions
  against outcomes (free calibration, no rule changes at n=2).
- CPI cluster: did the triage rule produce actual research passes on the
  three inflation markets? (First test of §f.4.)
- LIGHT-tick settlement grading: next settlement on a LIGHT tick must
  produce a retro in the same tick (first test of §f.6).
- Real mode is live on the operator machine: qualifying paper bets now
  mirror as $1 real twins — bet-quality bar and edge_class stamping are
  no longer paper-only concerns.
- Soccer verdict still pending at n=8.
