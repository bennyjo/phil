# Retro 2026-08-26 15:21Z (LIGHT tick — settlement carrier)

Ledger: 0 settled (2 open, neither past end date, unaffected). Forecasts: 10
settled since the last retro — completes the pre-registered PCE cluster
watch item (9 econ-pce rows, BEA July Core PCE release) plus 1
soccer-moneyline (Gangwon FC). Per schedule.json's settlement-carrier rule,
grading now rather than deferring to the next full cycle.

## PCE cluster (econ-pce, 9 rows)
Actual (per resolver): Core PCE YoY July = 3.3%, MoM July = 0.2%.

YoY bracket (6 legs, `no-edge` skips, mostly wide-spread no-trade):
- 3.1% `7f21a5423212` LOST est 0.10
- 3.2% `c75ea3d4303f` LOST est 0.40 — Pantheon's 3.2% nowcast call was
  wrong; Goldman's MoM+0.23% read (implying ~3.3%) was the better nowcast
  this cycle
- 3.3% `565561b2c6a5` WON est 0.35 — correct bucket, but ask was 0.50
  (spread 0.18, too wide to trade) so rightly skipped rather than bet
- 3.4%/3.5%/3.6%+ `ec3620cd655a`/`70f87e750239`/`1912e13a0c75` LOST, all
  appropriately near-zero tail estimates

MoM bracket (3 legs, `outside-view-veto`, Gaussian self-model
mu=0.195 sd=0.06 from Pantheon/Goldman nowcasts):
- 0.1% `381c3e38473c` LOST est 0.219
- 0.2% `b6df56da3754` WON est 0.594 — the model's own modal bucket, ask
  0.58 (market-agrees), correctly declined — no edge either way
- 0.3% `c07caa45a8dc` LOST est 0.175

## Verdict
YoY set: Goldman's nowcast (via MoM+0.23% implying ~3.3%) beat Pantheon's
(3.2%) this cycle — n=1, not enough to demote Pantheon as an input, but
worth watching if the pattern repeats. Both bank nowcasts stay blended
inputs per the existing method, no change.

MoM set: the Gaussian model (mu=0.195, sd=0.06) put its highest mass (0.594)
on the bucket that actually won (0.2%), and the market agreed (ask 0.58) —
this is a clean pass for the pre-registered mechanical-econ fork test
(alongside the JOLTS/ISM/unemployment/payrolls cluster still gated on
reachability): the self-modeled sd was not invalidated here. No
wide-spread-veto rows fired in this batch.

score.py: econ-pce n=9 brier_delta=+0.0040 — flat vs market (small n, every
leg a no-edge/market-agrees/outside-view-veto skip, no bets placed, so this
is calibration-only signal, not pnl). soccer-moneyline (Gangwon FC LOST,
`51c8793a2a39` est 0.6433 vs mkt 0.645) — clean-feed null confirmed again;
category flat (n=4, brier_delta=-0.0057).

Both PCE architectures (YoY wide-spread no-trade discipline, MoM
outside-view-veto Gaussian bucket model) performed as designed this
settlement. No playbook/risk/tools edit warranted. Pruning the now-fully-
settled PCE cluster watch item from schedule.json.
