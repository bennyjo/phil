# RETRO-20260911-0814

LIGHT tick (`0b` pacing: `next_full_cycle_after` 11:00Z still future, 4
FULL cycles already ran in the trailing 24h, meeting the floor). `core/
resolve.py` settled 0 ledger bets and 1 forecast this tick. A settled
forecast on a LIGHT tick still gets graded same-tick per the standing
rule (DEEP-2026-08-05 / DEEP-2026-09-02) — done below, plus two backlog
catch-ups found while doing it.

## This tick's settlement: Vance "Tax on Tips/Overtime" (forecast `83321f868e0f`)

Recorded 2026-09-08 22:56Z: own est 0.75 (P(exact phrase|speaks)~0.80 x
P(resolver counts the Sep10 session)~0.95) vs mid 0.575 (bid 0.38/ask
0.77). `wide-spread-veto` — spread 0.39 vs max 0.06, only 35 shares at
the ask, no bet. Settled **WON**.

Was the estimate wrong, the fill bad, or the variance normal? Estimate
was directionally right and the veto was correct risk management (the
counterfactual buy at the ask 0.77 still only clears +$1.49 CF pnl on a
0.75-vs-0.77 edge that the note itself flagged as -0.02 against the
fillable price — the real edge claim was against a stale 0.575 mid, not
the book). Nothing to fix in the estimate itself; this is the
wide-spread gate doing its job on a thin book, same as its sibling row
below.

## Backlog catch-up 1: Vance "2028/Campaign" (forecast `906a1a65abc8`) — missed by the prior TRIGGERED cycle

This forecast settled **2026-09-11T07:23:43Z, WON** — before the
07:28:35Z `TRIGGERED cycle: pricemove:3561916` committed, whose own log
line only covers the Sweden Liberals research and reports "settled 0"
with no mention of this forecast. Per the schedule.json clarification
(RETRO-20260908-2250): a TRIGGERED cycle is scoped in its *candidate*
set, never in its *settlement duties* — this was a miss, not a
permitted skip. Grading it now, one LIGHT tick late rather than
deferred indefinitely: own est 0.80 vs ask 0.71 (mid 0.585), edge
+0.09 against a book with bid 0.46/ask 0.71, spread 0.25 vs max 0.06 —
`wide-spread-veto`. Settled **WON**, CF pnl +$2.04.

## Backlog catch-up 2: Chewy "Consumable" wide-spread duplicate (forecast `e7a452fef1be`) — never entered in the table

Found while reconciling `core/counterfactual.py ledger --skip-reason
wide-spread-veto`'s live total (9 rows) against this table's last
manual snapshot (6 rows, 2026-09-09 12:3xZ update) — the 3-row gap was
exactly this forecast plus the two Vance rows above. `e7a452fef1be` was
the original 0.93-est Chewy "Consumable" forecast (2026-09-08 20:19Z),
superseded two minutes later by `ffc3fcdcbaa6` under the same estimate
when the label was corrected from `wide-spread-veto` to
`outside-view-veto` (DEEP-2026-08-14's >0.10-claimed-edge tiebreak).
`ffc3fcdcbaa6` was correctly graded into the *outside-view-veto* table
(RETRO for 2026-09-09 14:2xZ, line ~3444). But `core/counterfactual.py`
deliberately keeps superseded rows (it does not drop them the way
`replay.load_rows()` does — see the tool's own docstring), so the
original `e7a452fef1be` is a second, legitimate settled row under
*wide-spread-veto* that nobody entered into this table. Settled **WON**
(same underlying fact as its successor), CF pnl +$3.47 (ask 0.59 at
record vs successor's ask 0.58 — near-identical, small book move
between the two forecasts two minutes apart).

## Mandatory table extension (`wide-spread-veto`, DEEP-2026-08-23 rule — applies to both outside-view-veto and wide-spread-veto per the rule's own text)

Added to `strategy/playbook.md`'s wide-spread-veto counterfactual table,
row-by-row math shown, re-summed totals, side split — see that file for
the table edit. Summary: 6→9 settled declined forecasts, 4→7 fillable
CF trades (2 refused unchanged), 2W/2L→5W/2L, pnl −$7.82→−$0.81 (all
three new rows won), brier_delta −0.0326→−0.0967 (fresh
`core/counterfactual.py` recompute, agent still behind market on this
slice). Still n=9/7 trades — too thin to move any boundary; the
category read (vance-mention n=2, say-the-word contributes a 4th
already-graded-elsewhere duplicate) doesn't change that.

## Category verdict

`vance-mention` as a category: n=2 forecasts, both wide-spread-veto,
both WON, CF pnl +$3.54. Two wins does not clear the ~15-settlement
floor for any boundary change — same posture as the RETRO-20260911-0624
family-pattern discussion (this convention's utterance markets are
split roughly evenly between broad/evergreen phrases that hit and
narrow/session-specific phrasing that misses; both Vance rows here were
broad phrases an experienced politician says almost by default at a
podium — "campaign" and "tax on tips/overtime" are Vance's actual stock
lines, not narrow one-off content — consistent with the broad-phrase
side of that split, adding weak supporting evidence rather than new
information).

## Strategy edit

None. Both new-evidence categories (vance-mention n=2, overall
wide-spread-veto n=9/7 trades) are far below the ~15-settlement floor
for reacting, and the `wide-spread-veto` gate itself is doing exactly
what it's for here (protecting against unfillable/near-unfillable
books) rather than showing a calibration problem to fix. Noting the
TRIGGERED-cycle settlement-grading miss as a process observation, not a
strategy edit: TRIGGERED cycles already carry the settlement-duty
clarification in `schedule.json`; nothing in `strategy/` caused this
miss (it's cycle-procedure compliance, not a sensing or judgment gap),
so there's nothing under `strategy/` to change. If it recurs, that
would be evidence for a `journal/proposals.md` entry about the
TRIGGERED-cycle procedure itself.

## Open-position monitor

7 open positions, live books fetched via `strategy/tools/quote.py`. Two
cross the >=0.10 adverse-move threshold this tick, both already flagged
in the prior 06:24Z retro and continuing to worsen:

- `b063db346052` (Sweden Liberals win seats, No, entry 0.40, ends
  2026-09-13): live bid 0.056/ask 0.111, mid 0.0835, adverse move
  **-0.3165** vs entry — the steepest checkpoint yet (0.255 at 06:24Z ->
  0.06 per the 07:28:35Z TRIGGERED cycle's book read -> 0.0835 now).
  That TRIGGERED cycle already researched this and found no new poll or
  news (Novus 6-8Sep wave already priced in), reading it as a
  thin-liquidity tail move rather than a fresh information event — no
  new research on this LIGHT tick, holding to the Sep13
  election-night pre-registered grading in `strategy/schedule.json`
  watch_items.
- `0ed1d77858d7` (US-Iran ceasefire, No, entry 0.54, PAST END DATE
  2026-09-07): live bid 0.28/ask 0.29, mid 0.285, adverse move
  **-0.255** vs entry — worse than the 06:24Z checkpoint's -0.195
  (0.345 mid then), reversing that tick's brief recovery. Still awaiting
  the official resolver on the 14-day-window clause (runs to Sep15
  16:00Z). No new research this LIGHT tick.

5 remaining, under threshold: `e746d7e1ba99` (Sweden Andersson, mid
0.175, move -0.035, ends 09-13); `23e40bbbccc5` (MV CDU 11-14%, mid
0.0125, move -0.0775, ends 09-20, continued downward drift from 0.0385
at the last checkpoint but still under the call-out threshold);
`cd8b25268fe1` (Bank of Russia no-change, mid 0.80, move +0.10,
favorable, ends later today 09-11 23:59Z); `09fc471ceec1` (Sweden SD
second-most, mid 0.24, move +0.04, favorable, ends 09-13); `9074e3f2fd49`
(United Russia every region, mid 0.71, move +0.03, favorable, ends
09-20).
