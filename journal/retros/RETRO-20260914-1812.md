# RETRO-20260914-1812

## Settlement 1: claude-fable-5.1-max best AI model on Sep 14 (forecast e31a7529247d)

Forecast-only (skip-reason `unvalidated-method`), recorded 2026-09-13
12:23Z. Own est 0.87 (Yes) vs market mid 0.935 (bid 0.92/ask 0.95).
No-side edge nominally cleared min_edge (0.05 vs floor 0.04) but was
declined: a self-generated persistence judgment on the SAME arena.ai
benchmark the market already prices, no independent source, "the exact
shape the counterfactual ledger shows losing consistently."

Settled: outcome **Yes** (claude-fable-5.1-max held rank 1), 2026-09-14
18:12Z. `status: won`.

### Was the estimate wrong, the fill bad, or the variance normal?

Variance normal, and the skip was the right call for a reason beyond
calibration: had the declined No side been taken (No ask 0.08, edge
0.05), it would have LOST outright — the market's higher confidence
(0.935) was closer to the truth than the own 0.87 read. Own brier =
(0.87-1)^2 = 0.0169, market brier = (0.935-1)^2 = 0.0042 — market beat
own estimate here. This is exactly the unvalidated-method pattern the
playbook already declines on: a same-benchmark persistence judgment
under-weighted how sticky a fresh #1 snapshot actually is versus a
thin CI gap. Confirms rather than changes the existing gate.

## Settlement 2: BTC $76k-78k band, Sep 14 (forecast 46132024ea85)

Forecast-only (skip-reason `no-edge`), recorded 2026-09-14 08:20Z. Own
vol-model est 0.58 (Yes, price stays in band) vs market ask 0.64/mid
0.615, essentially matching the market's own read off the same spot
price — no informational edge on a sub-daily crypto band, consistent
with the standing read that this shape lacks an edge source.

Settled: outcome **No** (price left the band before the 16:00Z close),
2026-09-14 18:12Z. `status: lost` (forecast was for Yes).

### Was the estimate wrong, the fill bad, or the variance normal?

Variance normal. Own brier = (0.58-0)^2 = 0.3364, market brier =
(0.615-0)^2 = 0.3782 — own slightly beats market, consistent with a
correctly-skipped coin-flip-ish short-horizon band where the "edge"
was never real (both readings used the same spot input). No-edge gate
did its job.

## Category verdict

- `ai-model-release` (score.py): n=31, brier_delta=+0.0806 — the
  category's own estimates beat the market on average, but the
  `unvalidated-method` skip-reason class across categories (n=19,
  brier_delta=-0.0203) still underperforms, and this settlement is a
  second confirming data point (after the BLS 4.1% instance cited in
  the forecast note) that self-referential same-benchmark judgments on
  this desk should stay declined even when they clear the edge floor.
  n is still short of the ~15-per-class floor for the skip-reason cut
  specifically; nothing to change yet, just another data point in the
  same direction.
- `weather` (score.py, BTC-band forecasts): n=33, brier_delta=+0.0529 —
  comfortably past the floor, own research at par-to-ahead of market;
  consistent with the standing no-edge read on sub-daily crypto bands.

## Strategy edit

None. Both settlements confirm existing gates (unvalidated-method
decline, no-edge decline on sub-daily crypto bands) rather than
surfacing new evidence that would change a floor, gate, or the
discovery/playbook logic.
