# RETRO-20260910-1813

Two forecasts settled since the last retro (RETRO-20260910-0412): the Aug
2026 PPI YoY bracket set (event markets 3566679-3566688, print Sep 10
12:30Z). No ledger bets settled this cycle (0/31; 8 still open).

## Settled forecasts

- **3e948a4300a4** ("Will PPI YoY be 5.1% or more in August?", market
  3566688): est 0.67 vs market mid 0.705 (bid 0.70/ask 0.71). No-edge skip
  (edge 0.03 < floor). Settled Yes — WON.
- **09d6e627720e** ("Will PPI YoY be 5.0% in August?", market 3566687):
  est 0.067 vs mid 0.0525. Market-agrees skip. Settled No — LOST (correctly
  predicted a loss on the specific-bucket leg).

BLS confirmed (WebSearch, bls.gov/news.release/ppi.htm): **actual Aug 2026
PPI YoY final demand = 5.4%**, driven by a 24.1% diesel spike inside a 4.2%
energy move.

## Grading against the three pre-registered questions (schedule.json watch
item, playbook base-effect chain, 2026-08-11)

1. **Did the chain's central 5.30 hold?** Print 5.4 vs central estimate
   5.30 (TE consensus-implied) — off by 0.10, well inside the model's own
   sd=0.53 band and inside the market's tighter sd=0.45 band too. The
   FRED-level base-effect method (Jul26 NSA/Jul25 NSA = 4.69, Aug25 NSA
   149.466, YoY = 4.88 + 1.05×SA MoM) again lands within noise of the real
   print — second confirmed instance after the July 2026 first application
   (RETRO-20260813-1707, also WON). The method holds; treat it as
   validated for the mean, not just plausible.

2. **Dispersion: does 5.1-5.5 (favors market sd=0.45) or outside it (favors
   own sd=0.53) win?** Print 5.4 is inside 5.1-5.5 — **favors the market's
   tighter sd**. This is the same direction as the UR 4.1% bracket (market
   tighter, own too wide, RETRO-20260904-1613) and the opposite of NFP
   (market too tight, own wide sd right, same retro). Updated tally across
   the sourced-dispersion econ-bracket family: UR tighter-right, NFP
   wider-right, **PPI tighter-right**, CPI still pending (Sep 11). That is
   2-of-3 settled prints now favoring the market's tighter sd over this
   book's more conservative one — still short of the Gate-2 pre-registered
   kill-switch bar (4 events / 6 bets) and still only forecast-only rows
   here (no carve-out bet was placed on either PPI leg, so no P&L is at
   stake from this specific pair), but worth flagging explicitly rather
   than letting a third same-direction data point sit unremarked in a
   watch item nobody re-reads as a trend.

3. **Mech comparison (service 21, market-aware, blind):** delivered p_yes
   0.07 (r=0.95, cls R) against a market at 0.705 and an outcome of 1 — a
   clean miss, and per the note already on file the miss traces to a
   stale-year search trap (the mech's retrieval surfaced a Sep-2025 LinkedIn
   post citing "PPI edged down -0.1%... 2.6% year-over-year" and treated it
   as current). This is the same failure mode already logged for other
   stale-year mech misses, now reproduced a second time inside a
   market-aware delivery specifically. No new action needed beyond what the
   existing v4-only mech policy (operator note 2026-09-10 ~08:10Z) already
   does — that policy was adopted for an unrelated reason (missing
   `request_context`) but incidentally also removes this exact trap's
   current vector, since v4 never had web retrieval as a scan_truncated
   risk in the same way. No playbook change: this is one more instance of
   an already-documented mech weakness, not a new one.

## Verdict

The base-effect mean projection is now 2-for-2 (July confirmed 2026-08-13,
August confirmed here). No change needed there. The dispersion question is
genuinely live and 2-of-3 settled — not yet actionable (small n, explicit
pre-registered kill-switch bar not reached, and the existing playbook
"Gate-2 variance clause" text already requires sourced dispersion rather
than picking sides on directional bias prematurely), but this retro is the
citation trail if the CPI print (Sep 11, next data point) also comes in
tighter than 0.53: three same-direction results would be enough to revisit
whether the book's dispersion inputs for this family should shift toward
the market's typical sd rather than staying conservative-wide by default.
No playbook edit this retro — flagging in place, consistent with the
existing Gate-2 kill-switch discipline, which explicitly says n=2 (now
effectively n=3 across the whole family with PPI joining UR/NFP) is not a
basis for changing an sd input.

Cycle also ran with: repo was shallow at container start, `git fetch
--unshallow` cleared it before any behind/diverged judgment (forced-update
line was the shallow boundary clearing, no genuine divergence); local main
== origin/main exactly at bd7b195, no fast-forward needed, HEAD was
detached and will be reattached before commit. Collision guard: tip
~6h50m old (11:20:02Z), well past the 20-minute window, proceeded as FULL.
Lease: `core/lease.py acquire` returned acquired=true written=false (cloud
credential cannot push `refs/phil/lease`); proceeding as a normal FULL
cycle per the 2026-09-06 fix, no demotion. Pace: `next_full_cycle_after`
(2026-09-10T13:00:00Z, pulled forward from 14:00Z by the prior operator-
machine cycle specifically so this window would run FULL and research the
Bank of Russia leg first) is well past, so a FULL cycle was due regardless
of `min_full_cycles_per_day`. CI green at bd7b195 (`core/ci.py` status
success). Operator-notes.md most recent dated section (2026-09-10 ~08:10Z,
mech v4-only policy) is already reflected in CYCLE.md step 5a (protected,
operator-owned) — nothing for me to integrate into strategy/ this cycle.
