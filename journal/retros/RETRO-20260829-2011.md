# RETRO-20260829-2011

## Settled since last retro

- Forecast `9763244d9e99` (ncaaf-spreads, TCU (-8.5) vs UNC, skip-reason
  no-edge): est 0.50 vs PM ask 0.51 / mid 0.505. Outcome LOST (UNC covered).
  No bet was placed (no-edge, edge ~0.005 under floor).

## Assessment

This was a correctly-identified coin flip, not a miss. The rationale
explicitly checked the sportsbook line (FanDuel/DraftKings both 1.91/1.91
symmetric) and confirmed the market's cover probability was fairly priced
at ~50%, correctly rejecting the screener's spread-vs-moneyline conflation
(already documented in `strategy/screener-prompt.md:31-35`). A true 50/50
call losing is exactly the base rate — one settlement is not evidence of
anything (n=1 in category, category brier_delta +0.0060 not interpretable
at this n; mind the <~15 rule).

## Verdict

No lesson, no strategy edit. The existing screener-prompt.md trap
documentation already covers this failure class and worked correctly here
(the forecast avoided the trap and still called a fair coin toss).

## Category/pnl scan (score.py)

Nothing else settled since the last retro. Sweep tables, blend tables, and
calibration buckets are unchanged in shape from recent cycles (no new
signal at current n); no action.
