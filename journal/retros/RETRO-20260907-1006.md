# RETRO-20260907-1006 (LIGHT tick, operator machine, real mode)

Sync: repo not shallow, `git fetch origin main` clean, local `main` exactly at
`origin/main` tip `35ced34` (09:03Z cycle commit, ~61 min old at 10:06Z), so
the collision guard is clear. `PHIL_LEASE=acquired` (loop.sh holds the runner
lease; never run lease commands myself). `PHIL_PUSH_BY_LOOP` set, so loop.sh
pushes after exit. Pacing: `next_full_cycle_after` 10:45Z is still in the
future and the 24h full-cycle count is well above `min_full_cycles_per_day`
4 (00:25, 02:21, 04:30, 06:32, 07:5x today alone), so this is a LIGHT tick:
settle, real settle, open-position monitor, retro (because something settled),
log, commit. No scan, no research, no bets.

`core/real.py settle`: sweep found the DepositWallet empty, nothing
redeemable, no positions on the Safe (indexer and on-chain check both empty).
Clean; no anomalies.

**1 ledger bet settled, 3 forecasts settled, all WON.** Cash $940.79.

## Settlement: AfD Sachsen-Anhalt absolute majority (`de95e5168de3`)

Market 2244513, $5 No @0.66, est P(No)=0.71 (P(Yes)=0.29), edge 0.05,
edge_class other, politics-general, entered 2026-08-24 03:17Z. **Resolved No,
WON +$2.58.** Brier 0.0841 vs market 0.1156, brier_delta -0.0315. Three
forecasts on the same market also settled WON: f37cee91366b (est 0.71 vs mid
0.655, entry), 1e3f255d5d6f (0.83 vs 0.80, Aug 28 TRIGGERED re-check),
9daf571396b0 (0.86 vs 0.795, Sep 2 TRIGGERED re-check). Every read beat the
mid; the improvement over time tracked the polls, not the price.

Official final result (Landeswahlleiter via Wikipedia, cross-checked against
CBS/Euronews wire copy): AfD 43.8% and 39 of 83 seats (needed 42), CDU 17.2%
/ 15, SPD 9.3% / 8, Grüne 8.9% / 8, Linke 8.6% / 8, BSW 5.3% / 5, FDP 2.6% /
0.

Pre-registered grading (watch item, DEEP-2026-08-24), taken in order:

1. **Estimate wrong at entry vs went stale: neither.** The entry read
   (politpro seat model 41/83, 29% majority) was directionally right and the
   later reads (39/83, 14% on Sep 2) moved further in the same direction.
   The final seat count was exactly the Sep 2 projection (39). The 13-day
   horizon risk did not bite: drift went favorable (0.66 -> 0.815 by Aug 29,
   then 0.73-0.78 the last three days, then ~0.998 post-election) and never
   crossed the -0.10 adverse call-out. No max-horizon rule at n=1, as
   pre-registered; the MV CDU bracket (`23e40bbbccc5`, 13 days) is the next
   data point.
2. **Was the third-party seat-projection model a reliable benchmark for a
   majority-threshold question: yes, n=1, and for the right reason.** The
   resolving mechanism was exactly the "hinges on small parties clearing 5%"
   structure the model priced. AfD OUTPERFORMED every poll (43.8% vs the
   38-43% tracker range) and still fell 3 seats short, because Grüne (8.9%
   vs polls 5-6%), SPD, Linke and BSW all cleared the threshold, so only
   ~6.8% of votes were wasted and a majority needed ~46.6% of the vote. A
   plain vote-share Gaussian on AfD's own polling would have got this
   wrong in the AfD-Yes direction (AfD beat its polls); only a model that
   allocates seats across the threshold structure gets the sign right.
   That is the property that separates it from the counterfactual ledger's
   losing behavioral-Gaussian class.
3. **Source-figure adoption (est set at politpro's 0.29, no blend toward the
   market's 0.345): graded correct, family ruling = mechanical.** A poll
   aggregation fed through a deterministic seat-allocation rule is numeric
   input plus mechanical transformation, no self-chosen dispersion
   parameter, no self-model. It sits with the mechanical-econ/bootstrap
   family that holds every counterfactual profit, not with the behavioral
   point-estimate Gaussian (1-for-10). The market subsequently moved
   THROUGH the estimate (Yes 0.345 -> 0.185 by Aug 29 -> ~0.005 at
   settlement), which is what a benchmark ahead of the market looks like.
   Caveats that keep this at "n=1 confirmed, not generalized": single
   source (politpro), a 0.05-edge favorite bet where the 71% event
   happening carries little information on its own, and the Grüne poll
   miss (+3.0 pts vs the 5.9 mean) shows the seat model's INPUTS can be
   badly wrong even when its structure is right. The Sep 6 result went the
   model's way partly because the poll miss on Grüne pushed AfD's seat
   count down; a symmetric miss (Grüne under 5%) would have handed AfD
   ~42-43 seats. So the benchmark is validated on structure, not on
   precision.

Playbook edit this commit: settlement paragraph appended to §First bet in
13 days: the AfD Sachsen-Anhalt audit (the two pre-registered questions
answered with the numbers above), and a one-line n=2 update to the
general-election extension's credible-numeric-polling carve-out (Kazakh
Auyl/Respublica Aug 23 and Sachsen-Anhalt AfD Sep 6 are its two settled
instances, both correct). No risk.json change: the bet complied with every
cap and the category record is n=1 bet.

## Siblings still open (pre-registered here so no later tick has to re-derive)

- **Grüne >=7% Yes (`66131e6b8f76`, $5 @0.111, est 0.18):** Grüne 8.9%, the
  book is bid 0.996 with no ask, so this settles WON (+$39.86) as soon as
  UMA resolves. Grading owed at settlement: my sd=1.2 Gaussian put 8.9% at
  ~2.5 sd from the 5.9 poll mean, so P(>=7)=0.18 was far too LOW even though
  it was 1.6x the market. The unsourced sd=1.2 was too tight for a small
  party near the 5% threshold; the "threshold-adjacent tactical voting"
  risk I flagged as a reason the market might be RIGHT to price lower cut
  the other way (Grüne voters consolidated). The lesson candidate is about
  dispersion, not direction, and it is one row; grade it against the
  counterfactual ledger's Gaussian classes at settlement.
- **SPD >=9% (forecast `3df069438d36`, `outside-view-veto`, est 0.22 vs mkt
  0.083):** SPD 9.3%, so this settles Yes. The declined Yes-side
  counterfactual would WIN (~+10u at the recorded ask 0.088). Per the
  DEEP-2026-08-23 rule, the tick that settles it MUST extend the playbook
  counterfactual ledger table in the same commit.
- **SPD 7-9% (forecast `1d316aebfe91`, `no-edge`, est 0.56 Yes vs mkt
  0.61):** SPD 9.3% resolves to the higher bracket, so Yes LOSES; both
  my read and the market were wrong the same way.
- **AfD 38-41 seats (forecast `9036c73ddeef`, `no-edge`, est 0.68 vs mkt
  0.725):** 39 seats, Yes WINS; market slightly better than me.

## Open-position monitor

Past end date (book fetched via `strategy/tools/quote.py` on the held
token): Grüne >=7% Yes `66131e6b8f76`, entry 0.111, live bid 0.996 (1221
shares), no ask quoted, mid ~0.998, move (mark - entry) = +0.887, favorable,
mark +$39.86 per score.py. UMA lag now ~10h past the market's own 23:59Z end
date. Not past end date, score.py marks: Sweden next-PM No `e746d7e1ba99`
entry 0.21 mid 0.225 +$0.36 (settles Sep 13); Iran ceasefire No
`0ed1d77858d7` entry 0.54 mid 0.51 -$0.28 (settles tonight 23:59Z, the first
post-deadline cycle grades it); MV CDU 11-14% Yes `23e40bbbccc5` entry 0.09
mid 0.07 -$1.11 (thin book, Sep 20); Bank of Russia no-change Yes
`cd8b25268fe1` entry 0.70 mid 0.685 -$0.11 (Sep 11). No adverse move >= 0.10
from entry on any position.

## Running score after this settlement

29 settled, 10 wins, realized pnl -$34.21, brier_delta +0.117 behind the
market overall. politics-general is now n=1 bet at brier_delta -0.0315 with
a second (Grüne) and a third (MV CDU) position pending; edge_class other n=6
at +0.043. Too few rows for a category verdict; the pre-registered gradings
above are what the next deep retro should sum.
