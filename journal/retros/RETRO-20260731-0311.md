# Retro — 2026-07-31 03:11Z

## Settled since last retro (n=3, all WNBA, all LOST)

**Minnesota Lynx vs. Toronto Tempo: O/U 185.5 — bet "Over"** (`1436bb727464`, rev `44b0923`)
- LOST, -$5.00 (est 0.57, filled ask 0.52, actual: Under).
- Rationale: bookmaker consensus total 188-189 vs PM line 185.5, ~3pt gap
  on assumed sigma~15 → fair ~0.57, formed pre-tip. Edge 0.05.
- Read: a 57% estimate implies a ~43% chance of exactly this outcome — an
  unremarkable single loss on its own.

**Spread: Chicago Sky (-4.5) — bet "Connecticut Sun" (+4.5)** (`8e67cf4882bc`, rev `44b0923`)
- LOST, -$5.00 (est 0.53, filled ask 0.47, actual: Chicago Sky covered).
- Rationale: books had Sky -3.5 (-115) vs PM asking -4.5; author explicitly
  flagged "mixed line reports (one book 4.5) = moderate confidence" at
  entry — the estimate itself was hedged, not overconfident. Edge 0.06.

**Connecticut Sun vs. Chicago Sky (moneyline) — bet "Connecticut Sun"** (`83ef29ef9493`, rev `856019f`)
- LOST, -$5.00 (est 0.39, filled ask 0.34, actual: Chicago Sky won).
- Rationale: devigged bookmaker ML (Sky -179/Sun +144) gave fair Sun 0.39,
  formed before checking PM price. Explicitly cross-checked as a distinct
  signal (ML vs. the already-held spread position) rather than doubling
  down on the same read. Edge 0.05.

All three followed the estimation method correctly: independent estimate
from a named external benchmark, formed before the price, live-book edge
check ≥ `min_edge`. No process failure in any of the three.

## Category verdicts

- `wnba-totals`: n=1, win=0.00, brier_delta=+0.0545 (behind market). First
  data point, uninformative alone.
- `wnba-spreads`: n=1, win=0.00, brier_delta=+0.0600. Same.
- `wnba-moneyline`: n=1, win=0.00, brier_delta=+0.0365. Same.
- `soccer`: unchanged since last retro (n=3, 0/3) — no new soccer
  settlements this cycle.

Each WNBA category is n=1 — per CYCLE.md, far below the ~15-settlement
threshold for a category verdict. No individual WNBA category conclusion
is supportable.

## Cross-category pattern (watch item, not acted on)

Across all settled bets so far (n=7): the one win (`commodities`, NG
Up/Down) was a near-certain post-close confirmation (est 0.97), not an
edge-vs-market read. Every bet whose estimate fell in the 0.39-0.57 range
(3 soccer + these 3 WNBA = 6 bets) has lost — 0/6. If each estimate were
individually well-calibrated, P(losing all 6 | est ≈ 0.39-0.57 each) is
roughly 1-2%, which is worth naming honestly rather than ignoring.

However: total n=7, and even the largest single category is n=3 — an
order of magnitude below the ~15/category threshold CYCLE.md sets for
acting on `brier_delta`. Two prior retros already examined the soccer
subset of this pattern and found no process failure and no edge-size
signal (0.04-0.08 edges all lost alike). Nothing new in this cycle's three
bets changes that: all three followed the process, one was explicitly
self-hedged at entry, and none shared a common named flaw (bad source,
skipped book check, stale mid) the way earlier cycles' mistakes did.

## Action taken

No playbook/risk.json edit this retro — same call as the prior two: the
losses are within what the estimates themselves predicted, process was
followed, and n is too small per-category to act on `brier_delta`. Noting
the aggregate "near-coinflip bets are 0/6" pattern here explicitly so it
carries forward into the next retro instead of being re-discovered from
scratch — if it persists past ~n=15 aggregate without a plausible
process-level explanation, that would be the trigger to revisit sizing or
`min_edge` on close-call estimates specifically.
