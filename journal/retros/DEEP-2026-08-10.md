# DEEP RETRO — 2026-08-10

Window since DEEP-2026-08-09 (04:46Z): **0 placements, 0 ledger
settlements**, cash $953.11, bet n stays 19, z -2.73, overall bet
brier_delta +0.1237 — all unchanged. What DID move: the forecast ledger
had its first real window — 54 forecasts recorded across 4 FULL cycles,
**18 settled at brier_delta +0.001** (z -0.15) — and this session found
the full-cycle count instrument broken a THIRD time, now in the unsafe
direction (overcounting). Sync this session: clean fast-forward
6bb2664 → 81fc3a4, no divergence. resolve.py this session settled 1 more
forecast (Chicago Fire, est 0.63 mkt 0.62, WON — included in the n=18).

## (a) Calibration and P&L

**Bets: nothing settled, every slice unchanged from DEEP-2026-08-09.**
n=19; book-devig -0.0732 at stamped n=1; only mlb-spreads (-0.0568, n=2)
and book-devig are negative, both far under any honest n floor. No new
evidence on any bet threshold.

**Forecasts (first scored window): n=18 settled, brier_delta +0.001,
z=-0.15 — exactly at market.** Soccer, the first slice to cross the ~15
floor (n=15, +0.0009), confirms: when research concludes "no edge, price
is right," the price is in fact right. This is a *calibration
confirmation, not an edge signal* — 17 of 18 settled rows were no-edge
skips whose estimates sat within ~0.015 of the mid by construction.

**The honest headline is the disagreement gap:** of all 55 recorded
forecasts, only **6 have recorded ask-edge ≥ 0.02** (3 politics, 2 econ,
1 earnings — zero sports), and none has settled — the threshold sweep is
n=0 in every bucket. Sports devig cannot produce disagreements: the
estimate (devigged book consensus) and PM's price share the same
information source, so those forecasts test the pipe's arithmetic, not
its judgment. The question that decides this experiment — *are we
calibrated when we disagree?* — is still fed only by independent-source
estimates (polls, official prints, consensus EPS). First tests queued:
WI Hong brackets (edges 0.052/0.084/0.117, settle ~Aug 11), PLBY beat
(0.03, tonight), CPI 3.4% Yes (0.02) and Core YoY 2.5% (0.03, Aug 12).

**Pre-registration:** the WI Hong rows and PLBY are the first
out-of-sample tests of the outside-view veto (>0.10 claimed edge
declined by rule). If the declined sides lose, the veto is validated at
zero cost; if they would have won, that is the first evidence the veto
over-fires — grade either way in the next deep retro, explicitly.

**`d2dd24206542` (Iran ceasefire No @ 0.92):** mid decayed 0.155 → 0.095
over the window, 10 days past end date, still zero UMA proposals ever.
MTM ≈ -$4.4. Pre-registered grading unchanged (settles-as-marked ⇒
info-race 2W/4L, state-media rule enacts). Nothing actionable.

## (b) Audit of the hourly agent's edits

The hourly agent made **no playbook/risk/discovery/tools edits this
window** — only schedule.json pacing updates and funnel/forecast rows.
The audit is therefore of its *decisions* under existing rules.

1. **Pacing decisions — KEEP; count instrument — BROKEN AGAIN, fixed
   today (third iteration, worst direction yet).** The decisions
   themselves were sound: 8 genuine FULL cycles (05:15, 08:15, 11:15,
   14:19, 18:24, 21:15, 00:23, 04:16 — hand-counted from timestamps),
   min 4 comfortably met, every deferral ≤3h with event-anchored
   reasons. But DEEP-2026-08-09's "fixed" COUNT COMMAND (positive-match
   `/\(FULL cycle/`) **overcounts 17 vs true 7-8**, because LIGHT-tick
   log lines quote the count command verbatim and the quoted text
   self-includes them. The lineage: v1 undercounted (continuation
   lines), v2 (exclusion) self-excluded, v3 (positive match)
   self-includes. v1/v2 failed safe (undercount ⇒ extra full cycle);
   v3 fails UNSAFE — an inflated count satisfies the
   min_full_cycles_per_day floor spuriously, so a lazy agent could
   defer indefinitely while the guardrail reads green. No decision was
   actually corrupted this window (true count 8 ≥ 4 throughout), but
   every LIGHT line published a wrong number (10-16). Fixed for real in
   schedule.json today with first-marker classification (classify by
   whichever of "(FULL cycle"/"(LIGHT tick" appears FIRST in the line —
   quoting the command later in the line can no longer flip a
   classification either way); verified against the window: prints 7,
   matching the hand count. Second defect, same file of evidence: the
   00:23Z cycle wrote a **multi-line** log entry whose timestamped
   first line carries no marker — undercounted (fail-safe), but the
   one-line contract is now stated explicitly in schedule.json notes.
2. **Research allocation — MIXED; exploration budget satisfied in
   letter, not spirit.** Good: earnings coverage (QUBT/PLBY/HIMS, GETY
   honestly benchmark-unreachable with no invented estimate), CPI
   cluster via direct FRED pulls, WI margin-of-victory sibling cluster
   (first politics siblings.py test, caught the 0-liquidity placeholder
   leg inflating the sum-check), MN primary watched not acted on, and
   the 08-09 05:15Z weather exploration (named hypothesis, honest
   "not-yet-viable" verdict). Questionable: the 21:15Z cycle's
   "deliberate exploration into a fresh category" was Leagues
   Cup/Brasileirão 3-way devig — a new league on the SAME edge
   machinery (odds.py book-devig) whose ~0.02-edge profile was already
   characterized across 3 days and ~40 devigged markets. It tested no
   new rubric property and predictably found the known result. Also
   14:19Z re-ran the identical MLB slate researched 3h earlier
   ("confirms unchanged"). Cheap in credits (cache), not in research
   minutes. Playbook edits today: exploration must name a property NOT
   yet characterized, and clean-feed sports confirmation sweeps are
   capped at one per day unless slate composition changes.
3. **Forecast coverage (first audited window) — CLEAN.** Reconciled
   funnel.jsonl researched entries against forecasts.jsonl: every
   estimate-bearing skip has a row (26 at 21:15Z, 15 at 00:23Z, 13 at
   04:16Z + the smoke test = 55); benchmark-unreachable skips (GETY)
   correctly recorded NO estimate; re-verifications did not duplicate.
   One mechanism limit surfaced rather than a violation: PLBY's ask
   moved 0.25 → 0.19 between 00:23Z and 04:16Z (claimed edge 0.03 →
   0.14), but forecast.py's one-live-row rule means the ledger still
   holds the stale 00:23Z estimate — revision support is explicitly a
   "v2 question, on evidence" in forecast.py's docstring, and this is
   the evidence. Filed as a proposal (see (e)).
4. **Decline discipline — KEEP, and note the pattern worth keeping:**
   both large claimed edges this window (PLBY 0.14, WI ≥30% bracket
   0.13) were declined per the >0.10 precedent AND still recorded as
   forecasts. Declining the bet while recording the estimate converts
   discipline into out-of-sample evidence at zero bankroll cost. This
   is the forecast ledger doing exactly what it was built for.

Compliance: no agent commit touched protected paths; ledgers written
only by core.

## (c) Estimation errors of the day

No settled disagreements, so no realized estimation errors. The
window's live risk is subtler: **every large claimed edge is now
auto-declined by precedent, which makes the veto unfalsifiable if the
declined estimates are never scored.** That loop is now closed by the
forecast rows (see the pre-registration in (a)). One judgment call worth
recording: the 04:16Z cycle dropped the prior cycle's "HIMS missed Q1 by
$0.44" claim when it couldn't re-verify it — correct handling of an
unverifiable input rather than letting it compound.

## (d) Discipline violations

**None.** No bets, so no sizing/edge/spread/in-play exposure. Pacing
floors genuinely met (8 full cycles, hand-verified). Odds budget: quota
at 33/450 for the month, ~4-9 credits/day, within guidance; spend went
to funnel-passed candidates plus one redundant same-slate re-sweep
(free via cache — flagged in (b).2 as minutes, not credits). The count
misreporting in (b).1 is an instrument defect, not a discipline
violation: the wrong numbers were faithfully copied from the mandated
command, which is precisely why instrument bugs are graded as harshly
as violations here — the log is the evidence layer.

## (e) Proposals review

No open proposals carried in — all prior items are actioned/rejected/
endorsed with statuses set. **One new proposal filed today
(operator-owned file, core/forecast.py):** forecast revision support.
Evidence: PLBY (00:23Z est 0.28 @ ask 0.25; by 04:16Z ask 0.19,
situation materially re-verified, claimed edge 0.14) — the one-live-row
rule leaves the stale estimate as the row that will be graded, and the
sharper 04:16Z read is lost to scoring. Ask: allow a superseding record
(same market+outcome) that links to and closes the prior row, scoring
only the latest at settlement — keeping the anti-flooding intent while
letting revised beliefs be the graded ones. Low urgency; the workaround
(note the revision in funnel.jsonl) is in place.

## (f) Actions applied today

1. **schedule.json — COUNT COMMAND v4** (first-marker classification
   awk), with the v1→v3 failure lineage, the unsafe-direction warning,
   the one-line-per-cycle log contract stated explicitly, and a worked
   example (this window: prints 7, hand count 7-8 with the 00:23Z
   multi-line entry the known fail-safe misser).
2. **playbook.md, Exploration budget — anti-loophole rule:** an
   exploration candidate must test a rubric property not yet
   characterized; a new league/competition on an already-characterized
   edge machinery does not qualify (cite: 21:15Z Leagues Cup batch).
3. **playbook.md, Odds-API section — confirmation-sweep cap:** the
   clean-feed sports-efficiency finding is confirmed (~40 devigged
   markets, 3 days, edges 0.000-0.020); at most one cheap-confirmation
   sweep per day unless slate composition actually changes; the
   marginal research minutes go to independent-benchmark candidates.
4. **playbook.md, new Forecast-ledger subsection under Estimation
   method:** the disagreement gap (6/55 rows with ask-edge ≥0.02, none
   sports, none settled), why devig forecasts structurally cannot test
   disagreement calibration, the record-even-when-declining pattern,
   and the WI/PLBY/CPI pre-registrations.
5. **risk.json — dated evidence note appended** (no numeric changes;
   bet n=19 justifies none; forecast stream summarized; floors
   non-binding again this window — max clean-feed edge seen 0.020).
6. **proposals.md — forecast-revision proposal filed** per (e).
7. No reverts: the hourly agent made no strategy-file edits to revert;
   its pacing decisions survived audit.

## Watch items for tomorrow

- **Grade the pre-registrations as they settle:** WI Hong brackets
  (~Aug 11) and PLBY (tonight) grade the >0.10 veto out-of-sample; CPI
  cluster (Aug 12, 08:30 ET) grades the econ pivot AND is the standing
  cross-market candidate (YoY-vs-MoM base-effect arithmetic). This is
  the week the cross-market class produces stamped rows or the pivot
  needs rethinking — carried from DEEP-2026-08-09, now with dates.
- **Verify the v4 count command sticks:** next LIGHT tick's line should
  quote it and print a number matching a hand recount (and should NOT
  be self-included by quoting it).
- **`d2dd24206542`**: no change; pre-registered grading ready.
- **tags.py taxonomy first pass due ~2026-08-12** (carried).
- **Forecast n will pass 50 settled within ~2 days at current pace** —
  first real calibration curve by est bucket becomes readable; look at
  whether the 0.3-0.5 bucket's bet-record overconfidence (0/4 realized)
  reappears in the stake-free stream.
