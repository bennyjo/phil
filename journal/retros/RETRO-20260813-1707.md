# RETRO-20260813-1707

FULL cycle settlement grading. 1 ledger position settled (0 still open); 10
forecasts settled (the full PPI YoY July bracket sibling set, one bet leg
plus nine pure-forecast legs).

## Settled bet

- `b35963f465b4` — econ-ppi — "Will PPI YoY be 5.1% or less in July?" (Yes).
  entry 0.78, est_prob 0.84, edge 0.06, stake $5. Settled **WON**, pnl
  +1.41. Rationale (2026-08-11): base-effect projection —
  `YoY_next(NSA) ≈ YoY_current(NSA) × (1+MoM_next_consensus) /
  (1+MoM_sameMonthLastYear_actual) − 1`, applied with BLS-confirmed Jun26
  5.5% NSA YoY and Jul26 MoM consensus +0.1% against the unusually hot
  Jul25 MoM actual +0.9%, giving central estimate ~4.7% NSA — comfortably
  inside the ≤5.1% bucket. Playbook (§"PPI YoY brackets: base-effect
  projection, first instance") explicitly pre-registered this as the
  **first out-of-sample test** of the technique and said not to reuse it
  confidently elsewhere until it graded.

Was the estimate wrong, the fill bad, or normal variance? None — this was
a correct, evidence-based call: mechanical resolution (official BLS print),
a reachable benchmark (public consensus + prior-year actual), and the
technique's own stated mechanism (base effects from an atypical prior-year
month) is exactly what materialized. Entry fill (0.78) matched the recorded
ask with a tight 0.02 spread; no fill-quality issue.

## Settled forecasts (same bracket-set siblings, all pure calibration data)

All nine remaining July-PPI brackets (5.2% through ≥6.0%) settled **LOST**
(i.e., did not happen — correct, since only one bracket in a mutually
exclusive set can win), with est_prob ranging 0.05 down to 0.00 — every one
correctly assigned a low probability to a bracket that didn't hit. Two of
the nine (5.3%, ≥6.0%) had been declined under `outside-view-veto` despite
apparent nominal edge, per the existing wide-book-spread rule; both would
have lost had they been bet, reconfirming that veto rather than contradicting
it. econ-ppi forecasts now n=10, brier_delta -0.0062 (score.py) — beating
market, consistent with the bet leg's own good calibration.

## Category verdict

econ-ppi: n=1 settled bet (won, brier_delta -0.0228), n=10 settled
forecasts (brier_delta -0.0062). Both below the ~15-settlement threshold
for a category-wide verdict — this is one bracket-set event, not
independent repeated tests. Do not generalize to "econ-ppi is a proven
edge" or "the base-effect technique is validated" off a single event: the
playbook's own pre-registration was explicit that this needed grading
before reuse, and one win is exactly one data point, not a track record.

## Action

**Strategy edit**: update the PPI base-effect projection section in
`strategy/playbook.md` to record the graded outcome (won, +1.41, full
sibling set consistent) and soften the "do not reuse confidently" caveat
to "reuse with n=1 confidence — same small-n discipline as the SD
calibration note already in that section" rather than leaving it flagged
as untested. No change to `risk.json` or edge-class treatment: this bet
was already classified `other` (not a new edge class) and cleared the
existing 0.04 min_edge normally: the win doesn't argue for loosening
anything, it argues the existing floor and wide-book veto (which correctly
blocked the two higher-edge-looking siblings) both worked correctly this
time.
