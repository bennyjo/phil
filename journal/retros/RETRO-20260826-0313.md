# RETRO 2026-08-26 03:13Z (FULL cycle)

No ledger bets settled this cycle (0/21, 2 still open — de95e5168de3 AfD
Sachsen-Anhalt not due, c6f16acc55d9 not due). One forecast settled.

## Texas Rangers vs. Chicago White Sox: O/U 16.5 (forecast 78c695ce2ec5)

Recorded at 01:57:57Z on the `newmarket:3894923` TRIGGERED tick,
`skip_reason=architecture-mismatch`, est=0.99 vs mkt=0.61. Settled WON
(Over) at 03:13:38Z. Combined runs were already 17 (>= the 16.5 line) at
record time — the true probability genuinely was ~0.99, but the CLOB book
for the Over token had zero asks (bids only, up to 0.61), so there was no
way to actually buy the correct side. Declined correctly; nothing was lost
by not betting an unfillable market, and nothing would have been won
either since no fill was possible.

This is the second of what's now four same-session instances of
`new_market` firing on an already-live in-game total with a one-sided,
near-1.0-ask book (see `journal/proposals.md`, entries through the
2026-08-26 fourth instance on 3895121). The proposal to treat these fires
as a distinct live-game-state triage category is already open and not
mine to implement (`core/watch.py`/`config/protected.json`). This
settlement doesn't change that: the architecture-mismatch gate did exactly
what it's for here — recognized a genuine-but-unfillable edge and declined
rather than chasing it — and the outcome (Over) confirms the read was
right, not that the gate was too conservative.

**No edit.** Confirms the existing architecture-mismatch skip is
well-calibrated on this failure shape; the actionable fix (upstream
trigger timing) is already escalated in proposals.md and stands unchanged.
