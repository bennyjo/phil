# DEEP RETRO — 2026-09-06

Window audited: cb002fc (DEEP-2026-09-05, 04:48Z) → bd752ed (04:14Z
today). Sync: clone was shallow at start; `git fetch --unshallow`
succeeded cleanly, local main was strictly 37 behind origin/main, fast-
forwarded via `checkout -B main origin/main` — no divergence, no
manufactured lineage split. `resolve.py`: 0/28 ledger settled (4 open),
0 forecasts settled at run time.

## Headline: the runner lease has silently disabled cloud FULL cycles

The operator's lease (9d55035, live ~00:45Z) requires the cloud runner
to push `refs/phil/lease`. The cloud git credential returns HTTP 403 on
any custom-ref push (isolated by the hourly agent at 00:13Z: branch
push dry-run succeeds, lease-ref push fails every time, and the re-read
shows the ref genuinely absent — not a race). CYCLE.md step 0 reads
`acquired: false` as "run LIGHT", so **every scheduled cloud tick since
the lease landed has demoted to LIGHT** (00:13Z and 04:14Z lease-403;
01:54Z was TRIGGERED-exempt; 02:12Z was collision-guard). The
`min_full_cycles_per_day: 4` guardrail in schedule.json cannot fire
because the demotion happens at step 0, before pacing is consulted.
Net effect: zero scan/research/bet activity on the cloud runner until
the operator acts — a regression to worse-than-pre-lease behavior,
exactly as the hourly agent's proposal predicted. Escalated via
notification this run; proposal ENDORSED below. The hourly agent's
handling was exemplary: it followed CYCLE.md literally, did not touch
protected `core/lease.py`, isolated the cause by hand, and filed the
evidence.

## (a) Calibration and P&L (score.py, all 28 settled; nothing new settled in window)

Overall: 28 settled, 9W, P&L −$36.78, ROI −0.263, brier_agent 0.3335 vs
brier_market 0.2111 (delta +0.1223), luck-adjusted z = −4.24. Unchanged
from yesterday — no ledger rows settled in this window (the 3 pre-lease
FULL cycles placed one bet, still open; election-night settles are
pending resolution).

Where brier_delta is negative (beating the market): mlb-moneyline n=1
(−0.742), mlb-spreads n=2 (−0.057), econ-ppi n=1 (−0.023); by edge
class, book-devig n=1 (−0.073). Every one of these is n≤2 —
**insufficient data, no edge claim**. Everywhere with n≥4 the delta is
positive (losing to the market): news n=4 (+0.476), soccer n=8
(+0.003, essentially market-equal), unclassified n=18 (+0.126). The
shape is unchanged: the book loses where it disagrees with the market
on narrative/timing and roughly ties where it has a mechanical
benchmark.

Pre-registered carve-out bar (mechanical, quoted per the operator's
2026-09-06 00:30Z instruction, after refreshing the event map with
`screen_replay.py events --limit 200`, 52 new mappings):

> pre-registered carve-out bar for countable-metric: **not met** —
> rows 5 (need 5), independent events 1 (need 3), brier_delta −0.1367
> (need < 0), pnl +61.68 (need > 0), positive held-out folds 3 of 4
> (need 3).

Still one gamma event (the GTA VI family). The veto boundary stays.

Mechanical CF ledger cross-check: outside-view-veto 116 rows, 46W/62L
fillable, +$72.17, brier_delta +0.0279, held-out +$106.18 — the vetoed
beliefs remain worse-calibrated than the market they disagree with;
the veto is still load-bearing. wide-spread-veto: 4 rows, 2W/1L
fillable, −$2.82, brier_delta −0.0553 — directionally interesting,
far too small to act on.

Real ledger: 32/32 rows are settle sweeps reporting an empty
DepositWallet. No real fill has ever occurred; the paper→real funnel
has still produced zero twins. Informational.

## (b) Audit of hourly-agent edits since DEEP-2026-09-05

1. **3c18ae0 (06:16Z) — Miami 92-93F veto CF row, table to 103 trades.**
   KEEP. Arithmetic re-sums, correct same-commit table discipline.
2. **fbfb65c (14:19Z) — wide-spread-veto esports batch (bf608b923988,
   5b76118ad958), table to 105.** KEEP. Honest on the awkward part:
   both directional reads were right yet realizable edge was negative
   against the recorded ask — the veto's arithmetic justification held
   on a batch where the model was correct. Also correctly flagged (as
   n=1 watch, not a rule) the recorded `best_ask_at_record` 0.77 vs
   the research note's 0.39 snapshot — a recording-integrity gap to
   watch for recurrence.
3. **fc83ed9 (18:15Z) — Poisson re-grade unit corrected to independent
   matches.** KEEP; best edit of the window. Five Forest/Spurs
   derivative legs all beat the market's Brier (avg dBrier −0.080)
   because one fitted model and one realized outcome make every leg of
   the same match agree — the naive n=7 "bar met" was an artifact of
   counting legs. The agent applied the GTA-VI/Astra one-event lesson
   to its own method *before* being told to. Tally now n=3 independent
   matches, 2 favorable; bar is n≥6. Correct to keep forecast-only.
4. **2605e10 (20:16Z) — Guangzhou 35°C veto CF row, table to 106.**
   KEEP. Same-day-weather family honestly booked at 1W/3L realizable;
   ruling "the veto's job is avoiding correlated losses, not winning
   every row" is the right frame at n=4.
5. **schedule.json pacing edits (ab9bc9d, 82e4213 et al.).** KEEP.
   Deferral was used on genuinely empty windows (election results
   pending, no new polls), not to dodge research: 13 FULL cycles in
   the 24h before the lease landed, min-4 guardrail respected, watch
   items carried. The 18:20Z deferral to 21:20Z explicitly reasoned
   that no information could arrive before the election settles —
   legitimate. Post-00:13Z LIGHTs are the lease 403, not pacing.
6. **Sensing (discovery.py).** Unchanged since 2026-08-05; not
   audited as an edit. Pool health: scans returned 1046–1125
   candidates across 4 queries; the window surfaced politics, econ,
   CFB, soccer, esports, weather, news — no new structural blindness
   observed. The candidate-pool-as-query-artifact question stays open
   but nothing this window suggests a newly invisible category.
7. **One bet placed (7d2200f, 22:25Z): 0ed1d77858d7 Iran ceasefire No
   @0.54, edge 0.39, info-race.** Disciplined on every axis: verbatim
   resolution clause quoted (>$10k liquidity rule), fact-finality
   gate condition (i) met on the acting government's own confirmation
   (the exact distinction from the July d2dd24206542 adversary-media
   loss), deterministic calendar math, spread 0.01. Still open; grade
   on settlement, not here.

**No reverts this window.** Second consecutive clean audit.

## (c) Biggest estimation errors of the day

Nothing settled on the bet ledger, so the day's errors are
forecast-ledger only and small: Atalanta no-edge skip LOST with est
0.228 vs mkt 0.185 (market read it better; gate worked, n=1);
Schalke/Bayern BTTS unvalidated-method LOST; Munich 25°C LOST est 0.26
vs mkt 0.23; three CFB/MLS moneyline skips settled within a point or
two of market. No single error exceeds noise at these n's; variance,
not wrong reasoning or wrong benchmark. The standing z=−4.24 remains
the only estimation-error fact that matters, and nothing this window
moved it.

## (d) Discipline

No bets under min_edge (one bet, edge 0.39). No in-play bets (the
01:54Z TRIGGERED check on UNLV/Hawai'i was pre-kickoff and correctly
no-betted at devig-vs-mid delta 0.003). No spread breaches. Correlated
exposure: the 22:25Z cycle auto-declined the AfD seat bracket because
the two open Sachsen-Anhalt positions already sat at the $10 per-event
cap — the pre-registered rule fired exactly as written. Clean window.

## (e) Proposals pass

- **2026-09-06 00:13Z lease-403 (hourly agent): ENDORSED, urgent.**
  Status set in proposals.md. Evidence verified against cycles.log
  (00:13Z and 04:14Z both LIGHT on identical 403 isolation). Note for
  the operator: any fix must not require the cloud credential to write
  custom refs — the agent's own suggestions (lease file on main, or
  API-based lock) both work; a third option is lease.py treating
  "403 on lease push + ref absent on re-read" as *acquired with
  degraded confidence* (tip guard still protects), which keeps the
  operator machine's semantics intact. Operator call.
- **Collision-guard lease ask (2026-09-04 00:40Z): marked ACTIONED**
  per the operator's 2026-09-06 00:45Z note (with the irony recorded
  that the cure created the lease-403 regression on cloud).
- **Countable-metric trigger amendment (DEEP-2026-09-05 ask): marked
  ACTIONED** per the operator's 2026-09-06 00:30Z note; bar is now
  mechanical and quoted above.

Open operator asks after this pass: **1** (lease-403).

## Housekeeping

- The `.claude` spill trap was triggered twice this run (a `git show`
  and a `git diff` on schedule.json-containing ranges both exceeded
  the inline limit); both spill files were left untouched and the
  information re-obtained via scoped commands. The lesson holds:
  schedule.json's mtm-checkpoint churn makes any multi-commit diff
  that includes it spill — always diff it alone or read the live file.
- Real-mode funnel: still zero real twins ever (32/32 empty sweeps).

## Verdict

Clean window, one urgent operator escalation: the hourly agent's edits
were all kept (the Poisson unit correction is the system genuinely
learning — it applied the one-event lesson to itself unprompted), no
discipline violations, the carve-out bar honestly held shut at 1
independent event — but the new runner lease has silently demoted every
scheduled cloud cycle to LIGHT via a credential 403, and until the
operator reworks the lease mechanism the 24/7 learning engine is
settle-and-monitor only.
