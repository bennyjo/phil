{
  "_comment": "PROTECTED — the trading agent may not edit this file or anything in core/. Changes here are made by the human operator only.",
  "sim_bankroll_usd": 1000.0,
  "max_stake_usd": 10.0,
  "max_open_positions": 60,
  "max_new_positions_per_cycle": 15,
  "banned_question_patterns": [
    "Up or Down - .*[0-9]:[0-9]{2}[AP]M-[0-9]",
    "Up or Down.*ET$"
  ],
  "min_minutes_to_resolution": 20,
  "max_entry_price": 0.95,
  "min_entry_price": 0.02,
  "screener": {
    "_comment": "Tunables for the screening tier (core/screen.py). Screening judgment comes from Haiku subagents the cycle agent fans out with its Task tool, so the day's cost is counted in batches, not dollars - there is no API path and no model field. The max_batches_per_day and max_pool_after_strata hard ceilings live in core/validate.py and core/screen.py; raising either is a protected-core edit by design.",
    "batch_size": 20,
    "top_n": 15,
    "max_batches_per_day": 150,
    "max_pool_after_strata": 400
  },
  "real_trading_enabled": true,
  "real": {
    "_comment": "Caps for real execution via Pearl Connect (laptop only). Hard ceilings live in core/validate.py; raising them is a protected-core edit by design.",
    "max_stake_usd": 1.0,
    "max_open_positions": 10,
    "daily_stake_cap_usd": 5.0,
    "allowed_edge_classes": ["cross-market"]
  }
}
